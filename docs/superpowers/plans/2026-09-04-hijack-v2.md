# hijack-v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `hii form hijack` v2 — захват вопроса setup-страницы по полному hardware-доказанному рецепту (авто-unlock жертвы + строковые аппенды + IFR prompt/help + $SPF хелп-контрол).

**Architecture:** стадии внутри `hijack_form` (`crates/uefi-engine/src/hii/form_hijack.rs`); unlock — переиспользование gates-слоя (`find_gates`/`plan_gates_skip_unlocked`/`apply_flips`); $SPF — сигнатурный скан пул-контролов строк (`spf.rs`). Схема v2 без title/fs-opt; контракт `HiiFormHijackResponse` v2 (`unlock_flips` + `help_controls`, records удалён).

**Tech Stack:** Rust workspace (edition 2024), binrw/r-efi уже в движке, tonic/gRPC (uefi-proto), serde_json.

**Spec:** `docs/superpowers/specs/2026-09-04-hijack-v2-design.md`

## Global Constraints

- AGENTS.md: module-first (новых модулей нет — всё в существующих); TDD (тест → падение → реализация → проход); один коммит на задачу; **никаких комментариев в коде** (кроме `file:line`-ссылок); после каждой задачи `cargo test -p <crate>` и `cargo clippy -p <crate> -- -D warnings`.
- BIOS-образы никогда не коммитятся (`refs/fw/`, `refs/amibcp/` — вне трекинга).
- Длины IFR-пакета/списка/PE и $SPF-контейнера не меняются; правки same-length/slot-fit.
- Крейты для парсинга: свои IFR-структуры не определять (`r_efi::hii::*`); byte-offset парсинг только через существующие walker'ы движка.
- Сообщения коммитов — из задач; ветка `fix/cycle6-reimplent`, push только в remote `dsevosty`.

---

### Task 1: `plan_gates_skip_unlocked` — идемпотентный планировщик флипов

**Files:**
- Modify: `crates/uefi-engine/src/hii/gates.rs` (после `plan_gates`, ~строка 333)
- Test: там же, `mod tests`

**Interfaces:**
- Consumes: `plan_flip`, `Gate`, `PlannedFlip`, `GateExpr` (все уже в gates.rs).
- Produces: `pub fn plan_gates_skip_unlocked(body: &[u8], gates: &[Gate]) -> Result<Vec<PlannedFlip>, String>` — используют Task 4 и Task 7.

- [ ] **Step 1: Write the failing test** (в конец `mod tests`; хелперы `opcode/form/end/package/ref_op/one_of_op/eq_id_val/uint64/equal` уже есть в этом модуле)

```rust
    #[test]
    fn plan_gates_skip_unlocked_passes_already_unlocked() {
        let mut ifr = form_set(7);
        ifr.extend(form(10002, 20));
        ifr.extend(opcode(IFR_SUPPRESS_IF_OP, true, &[]));
        ifr.extend(uint64(1));
        ifr.extend(uint64(2));
        ifr.extend(equal());
        ifr.extend(ref_op(10029, 0x003A));
        ifr.extend(end());
        ifr.extend(end());
        ifr.extend(form(10029, 21));
        ifr.extend(opcode(IFR_GRAY_OUT_IF_OP, true, &[]));
        ifr.extend(eq_id_val(0x009A, 0xFFFF));
        ifr.extend(one_of_op(0x003B));
        ifr.extend(end());
        ifr.extend(end());
        ifr.extend(end());
        ifr.extend(end());
        let pkg = package(&ifr);

        let form_gates = find_gates(&pkg, &FORM_GATE_TARGET);
        assert!(!form_gates.is_empty());
        assert!(plan_gates_skip_unlocked(&pkg, &form_gates).unwrap().is_empty());

        let q_gates = find_gates(&pkg, &QUESTION_GATE_TARGET);
        assert!(!q_gates.is_empty());
        assert!(plan_gates_skip_unlocked(&pkg, &q_gates).unwrap().is_empty());
    }

    #[test]
    fn plan_gates_skip_unlocked_plans_only_locked() {
        let mut ifr = form_set(7);
        ifr.extend(form(10029, 21));
        ifr.extend(opcode(IFR_GRAY_OUT_IF_OP, true, &[]));
        ifr.extend(eq_id_val(0x009A, 1));
        ifr.extend(one_of_op(0x003B));
        ifr.extend(opcode(IFR_GRAY_OUT_IF_OP, true, &[]));
        ifr.extend(eq_id_val(0x009B, 0xFFFF));
        ifr.extend(one_of_op(0x003C));
        ifr.extend(end());
        ifr.extend(end());
        ifr.extend(end());
        let pkg = package(&ifr);
        let gates = find_gates(&pkg, &GateTarget { form_id: 10029, question_id: Some(0x003B) });
        let flips = plan_gates_skip_unlocked(&pkg, &gates).unwrap();
        assert_eq!(flips.len(), 1);
        assert_eq!(flips[0].to, 0xFFFFu16.to_le_bytes().to_vec());
    }

    #[test]
    fn plan_gates_skip_unlocked_errors_on_wide_storage() {
        let mut ifr = form_set(7);
        ifr.extend(form(10029, 21));
        ifr.extend(numeric_op(0x009A, 0x01));
        ifr.extend(opcode(IFR_GRAY_OUT_IF_OP, true, &[]));
        ifr.extend(eq_id_val(0x009A, 1));
        ifr.extend(one_of_op(0x003B));
        ifr.extend(end());
        ifr.extend(end());
        ifr.extend(end());
        let pkg = package(&ifr);
        let gates = find_gates(&pkg, &QUESTION_GATE_TARGET);
        assert!(plan_gates_skip_unlocked(&pkg, &gates).is_err());
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-engine plan_gates_skip_unlocked`
Expected: FAIL — `cannot find function plan_gates_skip_unlocked`

- [ ] **Step 3: Write minimal implementation** (после `plan_gates`)

```rust
fn is_unlocked_expr(expr: &GateExpr) -> bool {
    match expr {
        GateExpr::EqConst { a, b } => a != b,
        GateExpr::EqIdVal { value, .. } => *value == 0xFFFF,
        _ => false,
    }
}

pub fn plan_gates_skip_unlocked(body: &[u8], gates: &[Gate]) -> Result<Vec<PlannedFlip>, String> {
    let mut flips = Vec::new();
    for gate in gates {
        if is_unlocked_expr(&gate.expr) {
            continue;
        }
        match plan_flip(body, gate) {
            Some(flip) => flips.push(flip),
            None => {
                let region = &body[gate.expr_offset..gate.expr_end.min(body.len())];
                return Err(format!(
                    "{} gate at pkg+{:#x} wrapping {:?}: expression [{}] is not a hardware-validated flip class",
                    gate.kind.as_str(),
                    gate.scope_offset,
                    gate.wraps,
                    region
                        .iter()
                        .map(|b| format!("{b:02x}"))
                        .collect::<Vec<_>>()
                        .join(" ")
                ));
            }
        }
    }
    Ok(flips)
}
```

- [ ] **Step 4: Run tests + clippy**

Run: `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add crates/uefi-engine/src/hii/gates.rs
git commit -m "feat(uefi-engine): idempotent gate planner plan_gates_skip_unlocked"
```

---

### Task 2: `scan_string_controls` / `write_string_control` — $SPF пул-контролы строк

**Files:**
- Modify: `crates/uefi-engine/src/hii/spf.rs`
- Test: там же, `mod tests`

**Interfaces:**
- Produces (используют Task 5, Task 7):
  - `pub const SPF_STRING_CONTROL_STR_ID: usize = 2;`
  - `pub struct SpfStringControl { pub offset: usize, pub string_id: u16 }` — `offset` = адрес **поля str-id** (начало контроля + 2);
  - `pub fn scan_string_controls(body: &[u8]) -> Vec<SpfStringControl>`
  - `pub fn write_string_control(body: &mut [u8], offset: usize, string_id: u16)`

Сигнатура контроля (спека §6): `{u16 5 @+0, u16 str-id @+2, u16 0 @+4, u16 idxA @+6, u16 idxB @+8, u16 78 @+0xA, zeros, u32 57 @+0x14, u32 ptrs[]}`.

- [ ] **Step 1: Write the failing test**

```rust
    fn string_control(str_id: u16, idx_a: u16, idx_b: u16) -> Vec<u8> {
        let mut c = vec![0u8; 0x108];
        c[0..2].copy_from_slice(&5u16.to_le_bytes());
        c[2..4].copy_from_slice(&str_id.to_le_bytes());
        c[6..8].copy_from_slice(&idx_a.to_le_bytes());
        c[8..10].copy_from_slice(&idx_b.to_le_bytes());
        c[10..12].copy_from_slice(&78u16.to_le_bytes());
        c[0x14..0x18].copy_from_slice(&57u32.to_le_bytes());
        c
    }

    #[test]
    fn scan_string_controls_finds_ladder() {
        let mut body = vec![0u8; 0x500];
        let rec = question_record(9, 0x10, 0, 0);
        body[0x40..0x40 + SPF_RECORD_SIZE].copy_from_slice(&rec);
        let ctrl_a = string_control(10, 1199, 120);
        let ctrl_b = string_control(11, 1200, 121);
        let ctrl_c = string_control(12, 1201, 122);
        body[0x100..0x100 + 0x108].copy_from_slice(&ctrl_a);
        body[0x208..0x208 + 0x108].copy_from_slice(&ctrl_b);
        body[0x310..0x310 + 0x108].copy_from_slice(&ctrl_c);

        let found = scan_string_controls(&body);
        assert_eq!(found.len(), 3, "question record must not match control signature");
        assert_eq!(found[0].string_id, 10);
        assert_eq!(found[0].offset, 0x100 + SPF_STRING_CONTROL_STR_ID);
        assert_eq!(found[2].string_id, 12);

        assert!(scan_string_controls(&question_record(9, 0x10, 0, 0)).is_empty());
    }

    #[test]
    fn write_string_control_same_length() {
        let mut body = vec![0u8; 0x200];
        let ctrl = string_control(420, 1208, 129);
        body[0x80..0x80 + 0x108].copy_from_slice(&ctrl);
        let before = body.clone();
        let off = 0x80 + SPF_STRING_CONTROL_STR_ID;
        write_string_control(&mut body, off, 751);
        assert_eq!(&body[off..off + 2], &[0xEF, 0x02]);
        for i in 0..body.len() {
            if i != off && i != off + 1 {
                assert_eq!(body[i], before[i]);
            }
        }
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-engine scan_string_controls`
Expected: FAIL — `cannot find function`

- [ ] **Step 3: Write minimal implementation** (после `write_record_defaults`)

```rust
pub const SPF_STRING_CONTROL_STR_ID: usize = 2;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct SpfStringControl {
    pub offset: usize,
    pub string_id: u16,
}

pub fn scan_string_controls(body: &[u8]) -> Vec<SpfStringControl> {
    let mut out = Vec::new();
    if body.len() < 0xC {
        return out;
    }
    for p in 0..=body.len() - 0xC {
        if u16::from_le_bytes([body[p], body[p + 1]]) == 5
            && u16::from_le_bytes([body[p + 4], body[p + 5]]) == 0
            && u16::from_le_bytes([body[p + 0xA], body[p + 0xB]]) == 78
        {
            out.push(SpfStringControl {
                offset: p + SPF_STRING_CONTROL_STR_ID,
                string_id: u16::from_le_bytes([body[p + 2], body[p + 3]]),
            });
        }
    }
    out
}

pub fn write_string_control(body: &mut [u8], offset: usize, string_id: u16) {
    body[offset..offset + 2].copy_from_slice(&string_id.to_le_bytes());
}
```

- [ ] **Step 4: Run tests + clippy**

Run: `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add crates/uefi-engine/src/hii/spf.rs
git commit -m "feat(uefi-engine): \$SPF string-control scan/write (help channel)"
```

---

### Task 3: Схема v2 + чистка пайплайна (title/fs-opt долой, HijackResult v2-каркас)

**Files:**
- Modify: `crates/uefi-engine/src/hii/schema.rs:189-205`
- Modify: `crates/uefi-engine/src/hii/form_hijack.rs` (schema usage, `apply_hijack_ifr`, результат; удалить `rewrite_form_title`, `HijackRecordEdit`, запись fs/opt)
- Modify: `crates/uefi-engine/src/rpc/server.rs:843-850` (маппинг ответа)
- Test: schema.rs tests + form_hijack.rs tests

**Interfaces:**
- Produces:
  - `HijackQuestionSchema { question_id: u16, prompt: String, help: String }`, `HijackSchema { questions: Vec<HijackQuestionSchema> }` — оба `#[serde(deny_unknown_fields)]`; пустой `questions` → `HiiError::InvalidSchema`.
  - `pub struct HelpControlEdit { pub question_id: u16, pub offset: usize, pub old_string_id: u16, pub new_string_id: u16 }` (form_hijack.rs).
  - `pub struct HijackResult { pub string_ids: HashMap<String, u16>, pub unlock_flips: Vec<String>, pub help_controls: Vec<HelpControlEdit>, pub form_ifr_start: u32, pub form_ifr_end: u32 }` — `unlock_flips`/`help_controls` в этой задаче заполняются пустыми (`Vec::new()`), наполняются Task 4/5.
  - `rewrite_question_strings(pkg, q_off, prompt_id, help_id)` — остаётся как есть.

- [ ] **Step 1: Write the failing tests**

schema.rs tests (заменить `parse_hijack_schema_minimal` и соседние hijack-тесты):

```rust
    #[test]
    fn parse_hijack_schema_v2() {
        let s = parse_hijack_schema(
            r#"{"questions": [{"question_id": 59, "prompt": "P", "help": "H"}]}"#,
        )
        .unwrap();
        assert_eq!(s.questions.len(), 1);
        assert_eq!(s.questions[0].question_id, 59);
        assert_eq!(s.questions[0].prompt, "P");
        assert_eq!(s.questions[0].help, "H");
    }

    #[test]
    fn parse_hijack_schema_rejects_legacy_fields() {
        let e = parse_hijack_schema(
            r#"{"title": "T", "questions": [{"question_id": 1, "prompt": "P", "help": "H", "failsafe": 1, "optimal": 1}]}"#,
        )
        .unwrap_err();
        assert!(format!("{e:?}").contains("unknown field"));
    }

    #[test]
    fn parse_hijack_schema_requires_questions() {
        assert!(parse_hijack_schema(r#"{"questions": []}"#).is_err());
    }
```

form_hijack.rs tests — обновить:

```rust
    fn hijack_schema() -> crate::hii::schema::HijackSchema {
        crate::hii::schema::parse_hijack_schema(
            r#"{"questions": [{"question_id": 17, "prompt": "PQ", "help": "PH"}]}"#,
        )
        .unwrap()
    }
```

В `hijack_form_rewrites_ifr_and_spf_records` (переименовать в `hijack_form_rewrites_ifr_and_leaves_spf_defaults_untouched`):
- `res.string_ids.len()` → 2 (`PQ`, `PH`), `contains_key("PH")`;
- удалить ассерты `res.records…`;
- вместо `assert_eq!(form.title, "UEFIPATCHER")` — ассерт, что IFR title не тронут: байты `pkg[form_op+4..form_op+6]` в rebuilt-образе равны байтам до hijack (сравнить с `pkg_before`, см. фикстуру `hijack_flash_image().1`);
- `recs[0].failsafe == 0 && recs[0].optimal == 0` (fs/opt не пишутся);
- добавить ассерт `res.unlock_flips.is_empty()` (фикстура без гейтов — unlock-стадии нечего вскрывать).

Аналогично обновить `hijack_form_pe32_resource_channel_appends_strings_to_resource` (schema → v2, `records`-ассерты → fs/opt нет, title-ассерт → байты) и `hijack_form_survives_lzma_slot_fit` / `hijack_form_atomic_precheck_failures` (только сигнатура схемы).

- [ ] **Step 2: Run tests to verify they fail**

Run: `cargo test -p uefi-engine hijack`
Expected: FAIL — компиляция: `unknown field title`, отсутствие `HijackRecordEdit` после правки, и т.д. Сначала упадут новые schema-тесты (deny_unknown_fields ещё не добавлен — старая структура примет title), затем правка структур валит остальное.

- [ ] **Step 3: Implement**

schema.rs:

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct HijackQuestionSchema {
    pub question_id: u16,
    pub prompt: String,
    pub help: String,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct HijackSchema {
    pub questions: Vec<HijackQuestionSchema>,
}

pub fn parse_hijack_schema(json: &str) -> Result<HijackSchema, HiiError> {
    let s: HijackSchema =
        serde_json::from_str(json).map_err(|e| HiiError::InvalidSchema(e.to_string()))?;
    if s.questions.is_empty() {
        return Err(HiiError::InvalidSchema(
            "questions must not be empty".to_string(),
        ));
    }
    Ok(s)
}
```

form_hijack.rs:
- `let mut strings: Vec<String> = Vec::with_capacity(hijack.questions.len() * 2);` — только prompt+help каждого вопроса, `dedup()`;
- в `apply_hijack_ifr` удалить `title_id`/`rewrite_form_title`; удалить `fn rewrite_form_title`;
- удалить `HijackRecordEdit`; `HijackResult` заменить на v2-каркас (см. Interfaces; `unlock_flips: Vec::new()`, `help_controls: Vec::new()` в конструкторе);
- блок «запись fs/opt» (`spf::write_record_defaults` + цикл edits + второй `mark_rebuild_to_root_by_path`) удалить; precheck записей (`matched.len() != 1`) **оставить** как проверку регистрации вопроса;
- server.rs: убрать маппинг `records: result.records…` → `records: Vec::new()` (поле proto ещё существует — удаляется в Task 6).

- [ ] **Step 4: Run tests + clippy**

Run: `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add crates/uefi-engine/src/hii/schema.rs crates/uefi-engine/src/hii/form_hijack.rs crates/uefi-engine/src/rpc/server.rs
git commit -m "feat(uefi-engine): hijack schema v2, drop dead title/fs-opt pipeline"
```

---

### Task 4: Unlock-стадия в hijack (авто-вскрытие жертвы)

**Files:**
- Modify: `crates/uefi-engine/src/hii/mod.rs:240` — `fn flip_text` → `pub(crate) fn flip_text`
- Modify: `crates/uefi-engine/src/hii/form_hijack.rs`
- Test: form_hijack.rs tests

**Interfaces:**
- Consumes: `gates::{find_gates, plan_gates_skip_unlocked, apply_flips, GateTarget, PlannedFlip}` (Task 1), `crate::hii::flip_text`.
- Produces: заполненный `HijackResult.unlock_flips` (тексты в формате unlock-op).

- [ ] **Step 1: Write the failing tests** (в form_hijack tests; хелперы-эмиссары скопировать из gates.rs tests в этот модуль)

```rust
    fn opcode(op_code: u8, scope: bool, payload: &[u8]) -> Vec<u8> {
        let mut v = Vec::with_capacity(payload.len() + 2);
        v.push(op_code);
        v.push(((payload.len() + 2) as u8) | if scope { 0x80 } else { 0 });
        v.extend_from_slice(payload);
        v
    }

    fn gated_pkg() -> Vec<u8> {
        use r_efi::hii::{
            IFR_EQUAL_OP, IFR_EQ_ID_VAL_OP, IFR_FORM_OP, IFR_FORM_SET_OP, IFR_GRAY_OUT_IF_OP,
            IFR_ONE_OF_OP, IFR_SUPPRESS_IF_OP, IFR_UINT64_OP, PACKAGE_FORMS,
        };
        let form_set = {
            let mut p = Vec::new();
            p.extend_from_slice(&Guid::from_str(FORMSET_GUID).unwrap().to_bytes());
            p.extend_from_slice(&7u16.to_le_bytes());
            p.extend_from_slice(&0u16.to_le_bytes());
            p.push(0u8);
            opcode(IFR_FORM_SET_OP, true, &p)
        };
        let uint64 = |v: u64| {
            let mut b = vec![IFR_UINT64_OP, 0x0A];
            b.extend_from_slice(&v.to_le_bytes());
            b
        };
        let equal = || vec![IFR_EQUAL_OP, 0x02];
        let eq_id_val = |qid: u16, val: u16| {
            let mut b = vec![IFR_EQ_ID_VAL_OP, 0x06];
            b.extend_from_slice(&qid.to_le_bytes());
            b.extend_from_slice(&val.to_le_bytes());
            b
        };
        let ref_op = |form_id: u16| {
            let mut v = vec![r_efi::hii::IFR_REF_OP, 0x0F];
            v.extend_from_slice(&vec![0u8; 11]);
            v.extend_from_slice(&form_id.to_le_bytes());
            v
        };
        let mut ifr = form_set;
        ifr.extend(opcode(IFR_FORM_OP, true, &[6u16.to_le_bytes(), 1u16.to_le_bytes()].concat()));
        ifr.extend(opcode(IFR_SUPPRESS_IF_OP, true, &[]));
        ifr.extend(uint64(1));
        ifr.extend(uint64(1));
        ifr.extend(equal());
        ifr.extend(ref_op(7));
        ifr.extend(vec![r_efi::hii::IFR_END_OP, 0x02]);
        ifr.extend(vec![r_efi::hii::IFR_END_OP, 0x02]);
        ifr.extend(opcode(IFR_FORM_OP, true, &[7u16.to_le_bytes(), 1u16.to_le_bytes()].concat()));
        ifr.extend(opcode(IFR_GRAY_OUT_IF_OP, true, &[]));
        ifr.extend(eq_id_val(0x9A, 1));
        ifr.extend(opcode(IFR_ONE_OF_OP, true, &{
            let mut p = vec![0u8; 11];
            p[4..6].copy_from_slice(&0x11u16.to_le_bytes());
            p
        }));
        ifr.extend(vec![r_efi::hii::IFR_END_OP, 0x02]);
        ifr.extend(vec![r_efi::hii::IFR_END_OP, 0x02]);
        ifr.extend(vec![r_efi::hii::IFR_END_OP, 0x02]);
        ifr.extend(vec![r_efi::hii::IFR_END_OP, 0x02]);
        let total = 4 + ifr.len();
        let mut pkg = vec![
            (total & 0xFF) as u8,
            ((total >> 8) & 0xFF) as u8,
            ((total >> 16) & 0xFF) as u8,
            PACKAGE_FORMS,
        ];
        pkg.extend_from_slice(&ifr);
        pkg
    }

    fn gated_flash_image() -> (Vec<u8>, Vec<u8>, Vec<u8>) {
        let pkg = gated_pkg();
        let qs = locate_questions(&pkg, 7);
        let records: Vec<spf::SpfQuestionRecord> = qs
            .iter()
            .enumerate()
            .map(|(i, &(off, qid))| spf::SpfQuestionRecord {
                offset: 0x100 + i * spf::SPF_RECORD_SIZE,
                question_id: qid,
                ifr_offset: off as u32,
                failsafe: 0,
                optimal: 0,
            })
            .collect();
        let spf_body = spf_container(&records, 0x400);
        let setup = ffs_file_bytes(
            &Guid::from_str(FILE_GUID).unwrap(),
            &file_sections(&[
                section_bytes(EFI_SECTION_RAW, &pkg),
                section_bytes(EFI_SECTION_RAW, &string_package_bytes()),
            ]),
        );
        let sd = ffs_file_bytes(
            &Guid::from_str(SETUPDATA_GUID_STR).unwrap(),
            &file_sections(&[
                section_bytes(EFI_SECTION_UI, &ui_name("AMITSESetupData")),
                section_bytes(EFI_SECTION_FREEFORM_SUBTYPE_GUID, &spf_body),
            ]),
        );
        let flash = flash_with_files(vec![setup, sd]);
        (flash, pkg, spf_body)
    }

    const GATED_ITEM: &str = "5C60F367-A505-419A-859E-2A4FF6CA6FE5:0x19:0#7";

    #[test]
    fn hijack_auto_unlocks_victim_gates() {
        let (flash, pkg_before, _) = gated_flash_image();
        let mut img = parse_image(&flash, ImageMode::Write, "i", "s").unwrap();
        let res = hijack_form(&mut img, GATED_ITEM, &hijack_schema(), None).unwrap();
        assert_eq!(res.unlock_flips.len(), 2, "REF suppress + question grayout");

        let built = build_image(&img).unwrap();
        let re = parse_image(&built, ImageMode::Read, "i2", "s2").unwrap();
        let node = crate::parser::target::find_item(
            &re.root,
            &crate::parser::target::parse_target(&format!("{FILE_GUID}:0x19:0")).unwrap(),
        )
        .unwrap();
        assert_eq!(node.body.len(), pkg_before.len());
        let after = &node.body;
        let mut diffs: Vec<usize> = Vec::new();
        for i in 0..pkg_before.len() {
            if pkg_before[i] != after[i] {
                diffs.push(i);
            }
        }
        let suppressed_const = {
            let needle = {
                let mut n = vec![r_efi::hii::IFR_UINT64_OP, 0x0A];
                n.extend_from_slice(&1u64.to_le_bytes());
                n
            };
            let mut hits = pkg_before
                .windows(needle.len())
                .enumerate()
                .filter(|(_, w)| *w == &needle[..]);
            let first = hits.next().unwrap().0;
            let second = hits.next().unwrap().0;
            assert_eq!(second, first + 10, "two adjacent uint64(1) operands");
            second + 2
        };
        assert_eq!(pkg_before[suppressed_const], 1);
        assert_eq!(after[suppressed_const], 2);
        let grayout_const = after
            .windows(6)
            .position(|w| w[0] == 0x12 && w[1] == 0x06 && w[2..4] == [0x9A, 0] && w[4..6] == [0xFF, 0xFF])
            .unwrap();
        assert_eq!(pkg_before[grayout_const + 4], 1);
    }

    #[test]
    fn hijack_keeps_atomicity_on_schema_refusal() {
        let (flash, _, _) = gated_flash_image();
        let mut img = parse_image(&flash, ImageMode::Write, "i", "s").unwrap();
        let before = build_image(&img).unwrap();
        assert!(hijack_form(&mut img, GATED_ITEM, &{
            let mut sc = hijack_schema();
            sc.questions[0].question_id = 99;
            sc
        }, None)
            .is_err());
        assert_eq!(build_image(&img).unwrap(), before, "no mutation on precheck failure");
    }
```

(Atomicity на новых классах отказов Task 5 — `hijack_fails_when_help_control_missing_or_ambiguous`; unsupported-гейт покрыт юнит-уровнем gates.rs Task 1 и real-image сценарием.)

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-engine hijack_auto_unlocks`
Expected: FAIL — `unlock_flips` пуст (стадии нет)

- [ ] **Step 3: Implement** (в `hijack_form`)

Precheck (после блока «вопросы схемы есть в форме», до $SPF):

```rust
    let unlock_flips: Vec<gates::PlannedFlip> = {
        let pkg = package_of(&image.root, &target)?;
        let mut targets: Vec<gates::GateTarget> = vec![gates::GateTarget {
            form_id,
            question_id: None,
        }];
        targets.extend(hijack.questions.iter().map(|q| gates::GateTarget {
            form_id,
            question_id: Some(q.question_id),
        }));
        let mut merged: Vec<gates::PlannedFlip> = Vec::new();
        for gt in &targets {
            let found = gates::find_gates(pkg, gt);
            if found.is_empty() {
                continue;
            }
            let flips = gates::plan_gates_skip_unlocked(pkg, &found)
                .map_err(HiiError::GateExpressionUnsupported)?;
            for f in flips {
                if !merged.iter().any(|m| m.offset == f.offset) {
                    merged.push(f);
                }
            }
        }
        merged
    };
```

Применение — в существующем блоке IFR-мутации:

```rust
    {
        let root = &mut image.root;
        let pkg = package_of_mut(root, &target)?;
        apply_hijack_ifr(pkg, form_id, hijack, &string_ids)?;
        gates::apply_flips(pkg, &unlock_flips).map_err(HiiError::GateExpressionUnsupported)?;
    }
```

Результат: `unlock_flips: unlock_flips.iter().map(|f| crate::hii::flip_text(0, f)).collect()`, и после применения — `tracing::warn!(flips = unlock_flips.len(), "hijack auto-unlock applied");`

mod.rs: `fn flip_text` → `pub(crate) fn flip_text`.

- [ ] **Step 4: Run tests + clippy**

Run: `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add crates/uefi-engine/src/hii/form_hijack.rs crates/uefi-engine/src/hii/mod.rs
git commit -m "feat(uefi-engine): hijack auto-unlocks victim gates (gates layer, idempotent)"
```

---

### Task 5: Хелп-контрол-стадия ($SPF)

**Files:**
- Modify: `crates/uefi-engine/src/hii/form_hijack.rs`
- Modify: `crates/uefi-engine/tests/real_image.rs` (устаревшие asserts: `help_controls.is_empty()` и «$SPF file byte-identical» — стадия теперь пишет 2 байта в контрол)
- Test: form_hijack.rs tests

**Interfaces:**
- Consumes: `spf::{scan_string_controls, write_string_control}` (Task 2), `HelpControlEdit` (Task 3).
- Produces: заполненный `HijackResult.help_controls`.

- [ ] **Step 1: Write the failing tests**

Расширить фикстуру `spf_container` (добавить контрол; str-id = help-id фикстурного вопроса = 0 — `one_of` с payload по нулям, help@+4):

```rust
    pub(crate) fn spf_container(records: &[spf::SpfQuestionRecord], body_len: usize) -> Vec<u8> {
        let mut body = vec![0u8; body_len];
        body[16..20].copy_from_slice(b"$SPF");
        for r in records {
            // …без изменений…
        }
        let ctrl = 0x100 + records.len() * spf::SPF_RECORD_SIZE;
        body[ctrl..ctrl + 2].copy_from_slice(&5u16.to_le_bytes());
        body[ctrl + 4..ctrl + 6].copy_from_slice(&0u16.to_le_bytes());
        body[ctrl + 10..ctrl + 12].copy_from_slice(&78u16.to_le_bytes());
        body[ctrl + 0x14..ctrl + 0x18].copy_from_slice(&57u32.to_le_bytes());
        body
    }
```

(str-id поля @ctrl+2 остаётся 0 — соответствует help-id вопроса фикстуры.)

Дефект фикстуры: текущий `package_with_form` эмитит `emit_one_of(2, 3, 0x11, 1, 0, 0, 1)` — help-id вопроса = 3, а не 0 («payload по нулям» верно только для `gated_pkg`). Чтобы str-id контрола (0) совпал с текущим help-id вопроса, `emit_one_of` в `package_with_form` меняется на `emit_one_of(2, 0, 0x11, 1, 0, 0, 1)` (prompt не трогаем — его проверяет `rewrite_question_strings_same_length`).

Унаследованный дефект фикстуры (прошлый цикл): `lzma_guided_file_bytes` даёт LZMA-слоту нулевой запас (`stream.resize(stream.len().max(64), 0x00)` — слот = точный размер сжатого потока). Пока hijack v2 не трогал тело $SPF, рекомпрессия не запускалась; контрол-стадия Task 5 мутирует 2 байта, минимальный поток sweep-а вырастает (наблюдено 70 против бюджета 69), секция растёт на байт и ломает `hijack_form_survives_lzma_slot_fit`. Фикстуре добавляется реалистичный запас: `stream.resize(stream.len().max(64) + 32, 0x00)` — `compress_lzma_fit` паддингует поток до бюджета, размер слота сохраняется.

```rust
    #[test]
    fn hijack_rewrites_help_control() {
        let (flash, _, spf_before) = hijack_flash_image();
        let mut img = parse_image(&flash, ImageMode::Write, "i", "s").unwrap();
        let res = hijack_form(&mut img, ITEM, &hijack_schema(), None).unwrap();
        assert_eq!(res.help_controls.len(), 1);
        assert_eq!(res.help_controls[0].question_id, 17);
        assert_eq!(res.help_controls[0].old_string_id, 0);
        let new_help = *res.string_ids.get("PH").unwrap();
        assert_eq!(res.help_controls[0].new_string_id, new_help);

        let built = build_image(&img).unwrap();
        let re = parse_image(&built, ImageMode::Read, "i2", "s2").unwrap();
        let sd_file = re.root.children.iter().find_map(|v| {
            v.children
                .iter()
                .find(|f| f.guid == Some(Guid::from_str(SETUPDATA_GUID_STR).unwrap()))
        }).unwrap();
        let pfs_leaf = sd_file
            .children
            .iter()
            .find(|c| c.body.windows(4).any(|w| w == b"$SPF"))
            .unwrap();
        assert_eq!(pfs_leaf.body.len(), spf_before.len(), "$SPF length invariant");
        let ctrls = spf::scan_string_controls(&pfs_leaf.body);
        assert_eq!(ctrls.len(), 1);
        assert_eq!(ctrls[0].string_id, new_help);
        assert_eq!(ctrls[0].offset, res.help_controls[0].offset);
    }

    #[test]
    fn hijack_fails_when_help_control_missing_or_ambiguous() {
        for dup in [false, true] {
            let (flash, _, _) = hijack_flash_image();
            let mut img = parse_image(&flash, ImageMode::Write, "i", "s").unwrap();
            let sd_guid = Guid::from_str(SETUPDATA_GUID_STR).unwrap();
            let vol = img.root.children.iter_mut()
                .find(|v| v.children.iter().any(|f| f.guid == Some(sd_guid))).unwrap();
            let file = vol.children.iter_mut().find(|f| f.guid == Some(sd_guid)).unwrap();
            let leaf = file.children.iter_mut().find(|c| c.body.windows(4).any(|w| w == b"$SPF")).unwrap();
            let first = spf::scan_string_controls(&leaf.body)[0].offset;
            if dup {
                let second = 0x300;
                leaf.body[second..second + 2].copy_from_slice(&5u16.to_le_bytes());
                leaf.body[second + 4..second + 6].copy_from_slice(&0u16.to_le_bytes());
                leaf.body[second + 10..second + 12].copy_from_slice(&78u16.to_le_bytes());
            } else {
                leaf.body[first - 2..first].copy_from_slice(&7u16.to_le_bytes());
            }
            let before = build_image(&img).unwrap();
            assert!(
                hijack_form(&mut img, ITEM, &hijack_schema(), None).is_err(),
                "dup={dup}"
            );
            assert_eq!(build_image(&img).unwrap(), before, "atomic: no mutation beyond fixture corruption");
        }
    }

    #[test]
    fn hijack_idempotent_rerun() {
        let (flash, _, _) = hijack_flash_image();
        let mut img = parse_image(&flash, ImageMode::Write, "i", "s").unwrap();
        let res1 = hijack_form(&mut img, ITEM, &hijack_schema(), None).unwrap();
        let res2 = hijack_form(&mut img, ITEM, &hijack_schema(), None).unwrap();
        assert!(res2.unlock_flips.is_empty(), "gates already unlocked");
        assert_eq!(res2.help_controls.len(), 1);
        assert_eq!(res2.help_controls[0].old_string_id, res1.help_controls[0].new_string_id);
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-engine hijack_rewrites_help_control`
Expected: FAIL — `help_controls` пуст (стадии нет)

- [ ] **Step 3: Implement** (в `hijack_form`)

Precheck (после снятия IFR help-id; рядом с $SPF-записями):

```rust
    let help_ids: Vec<(u16, u16)> = {
        let pkg = package_of(&image.root, &target)?;
        let qs = locate_questions(pkg, form_id);
        let mut out = Vec::new();
        for q in &hijack.questions {
            let off = qs
                .iter()
                .find(|&&(_, qid)| qid == q.question_id)
                .map(|&(o, _)| o)
                .ok_or(HiiError::NotFound)?;
            out.push((
                q.question_id,
                u16::from_le_bytes([pkg[off + 4], pkg[off + 5]]),
            ));
        }
        out
    };
    let control_edits: Vec<(u16, usize, u16)> = {
        let controls = spf::scan_string_controls(&spf_records);
        let mut out = Vec::new();
        for &(qid, sid) in &help_ids {
            let matched: Vec<_> = controls.iter().filter(|c| c.string_id == sid).collect();
            if matched.len() != 1 {
                return Err(HiiError::NotFound);
            }
            out.push((qid, matched[0].offset, sid));
        }
        out
    };
```

Применение — после `apply_hijack_ifr` (в той же мутации, до `mark_rebuild`):

```rust
    {
        let node = node_at_mut(&mut image.root, &sd_path);
        for e in &control_edits {
            let new_id = *string_ids
                .get(&hijack.questions.iter().find(|q| q.question_id == e.0).unwrap().help)
                .ok_or(HiiError::InvalidIfr)?;
            spf::write_string_control(&mut node.body, e.1, new_id);
        }
    }
    ops::mark_rebuild_to_root_by_path(&mut image.root, &sd_path);
```

Результат: `help_controls: control_edits.iter().map(|&(qid, off, old)| HelpControlEdit { question_id: qid, offset: off, old_string_id: old, new_string_id: /* new_id как выше; сохранить из цикла применения в Vec<(qid, off, old, new)> */ }).collect()` — для этого цикл применения строит `Vec<HelpControlEdit>` напрямую и кладёт в результат.

- [ ] **Step 4: Run tests + clippy**

Run: `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS (в т.ч. `hijack_idempotent_rerun`)

- [ ] **Step 5: Commit**

```bash
git add crates/uefi-engine/src/hii/form_hijack.rs
git commit -m "feat(uefi-engine): hijack patches \$SPF help string-control (exactly-one precheck)"
```

---

### Task 6: Контракт v2 — proto/server/CLI/mocks

**Files:**
- Modify: `crates/uefi-proto/proto/engine.proto:195-214`
- Modify: `crates/uefi-engine/src/rpc/server.rs` (маппинг)
- Modify: `crates/uefi-cli/src/output.rs:359-405` (`print_form_hijack`)
- Modify: `crates/uefi-cli/tests/mock_server.rs`, `crates/uefi-tui/tests/mock_server.rs`, `crates/uefi-gateway/tests/mock_server.rs` (конструкторы ответа)
- Test: output.rs tests + server tests (`form_hijack_status`-смок)

**Interfaces:**
- Produces: proto-сообщения `HiiHelpControlEdit { question_id, offset, old_string_id, new_string_id }` и `HiiFormHijackResponse { string_ids, reserved 2, form_ifr_start, form_ifr_end, unlock_flips: repeated string, help_controls: repeated HiiHelpControlEdit }`; `HiiFormHijackRecord` удаляется.

- [ ] **Step 1: Update proto + regenerate**

```proto
message HiiHelpControlEdit {
  uint32 question_id = 1;
  uint32 offset = 2;
  uint32 old_string_id = 3;
  uint32 new_string_id = 4;
}

message HiiFormHijackRequest {
  string image_id = 1;
  string target = 2;
  string schema_json = 3;
  string setupdata_guid = 4;
}

message HiiFormHijackResponse {
  map<string, uint32> string_ids = 1;
  reserved 2;
  uint32 form_ifr_start = 3;
  uint32 form_ifr_end = 4;
  repeated string unlock_flips = 5;
  repeated HiiHelpControlEdit help_controls = 6;
}
```

(удалить `message HiiFormHijackRecord { … }`; `HiiFormHijackRequest` оставить как есть — приведён для контекста). Run: `cargo build -p uefi-proto` — упадёт на зависимых крейтах (records удалён) — это и есть failing-шаг.

- [ ] **Step 2: Update server mapping** (`hii_form_hijack`):

```rust
        Ok(Response::new(HiiFormHijackResponse {
            string_ids: result
                .string_ids
                .iter()
                .map(|(k, v)| (k.clone(), u32::from(*v)))
                .collect(),
            form_ifr_start: result.form_ifr_start,
            form_ifr_end: result.form_ifr_end,
            unlock_flips: result.unlock_flips.clone(),
            help_controls: result
                .help_controls
                .iter()
                .map(|c| HiiHelpControlEdit {
                    question_id: u32::from(c.question_id),
                    offset: c.offset as u32,
                    old_string_id: u32::from(c.old_string_id),
                    new_string_id: u32::from(c.new_string_id),
                })
                .collect(),
        }))
```

- [ ] **Step 3: Update CLI print** (`print_form_hijack`): text-режим —

```
hijack\tstring_ids\t{n}\tunlock_flips\t{m}\thelp_controls\t{k}
string_id\t{name}\t{sid}
unlock\t{flip_text}
help_control\tqid=0x{qid:X}\tstr@0x{offset:X}\t{old:X}→{new:X}
```

json-режим — `{"string_ids":…, "unlock_flips":[…], "help_controls":[{"question_id":…,"offset":…,"old_string_id":…,"new_string_id":…}], "form_ifr_start":…, "form_ifr_end":…}`. Обновить `form_hijack_print_smoke` под новый конструктор (заполнить `unlock_flips: vec!["pkg+0x10: 01→02".into()]`, `help_controls: vec![…]`).

- [ ] **Step 4: Update mocks** — во всех трёх mock_server.rs заменить конструкторы `HiiFormHijackResponse { records: … }` на v2-поля.

- [ ] **Step 5: Run all + clippy**

Run: `cargo test --all && cargo clippy --all -- -D warnings`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-proto/proto/engine.proto crates/uefi-engine/src/rpc/server.rs crates/uefi-cli/src/output.rs crates/uefi-cli/tests/mock_server.rs crates/uefi-tui/tests/mock_server.rs crates/uefi-gateway/tests/mock_server.rs
git commit -m "feat: hijack v2 contract - unlock_flips + help_controls, drop records"
```

---

### Task 7: Real-image gate — сценарии A и B

**Files:**
- Modify: `crates/uefi-engine/tests/real_image.rs` (рядом с `real_image_hii_form_hijack_built_bytes`, ~строка 1766)
- Test: два новых `#[ignore]`-теста

**Interfaces:**
- Consumes: всё предыдущие задачи; живой образ `../../../refs/fw/HNX99TF_200525_original_E5C88C6F.bin`; item `899407d7-99fe-43d8-9a21-79ec328cac21:0x10:0#10029`; SetupData GUID `FE612B72-203C-47B1-8560-A66D946EB371`.

- [ ] **Step 1: Write the failing tests**

Сценарий A (hijack без отдельного unlock — E24-класс):

```rust
#[test]
#[ignore = "needs real image at ../../../refs/fw/HNX99TF_200525_original_E5C88C6F.bin"]
fn real_image_hijack_v2_scenario_a_unlocks_victim_only() {
    // parse original, mode Write; collect_forms до — сохранить title-строку формы 10029
    // hijack_form(target#10029, schema {questions:[{59, "UEFIPatcher E27 A", "UEFIPatcher E27 A help"}]}, Some(sd_guid))
    // assert res.unlock_flips.len() == 2 (REF suppress EqConst + q59 grayout EqIdVal)
    // build; re-parse; формы: collect_forms форма 10029 title == сохранённый до-hijack (title не тронут)
    // IFR re-plan: find_gates(pkg, form- и q59-таргеты) → plan_gates_skip_unlocked == empty
    // $SPF: scan_string_controls — контроль с str_id == res.string_ids["… help"] существует;
    //       контроля со str_id == 420 больше нет
    // diff confinement: изменения только в слотах Setup-модуля и SetupData (по образцу
    //   существующего теста real_image_hii_form_hijack_built_bytes)
}
```

Сценарий B (полная страница — E26-эквивалент):

```rust
#[test]
#[ignore = "needs real image at ../../../refs/fw/HNX99TF_200525_original_E5C88C6F.bin"]
fn real_image_hijack_v2_scenario_b_full_page_matches_e26_content() {
    // 1) hii::unlock(img, "…#10029") → UnlockOutcome.applied (ожидание: 10 флипов формы/REF — E25)
    // 2) для qid в [54,55,56,57,58,60,61]: hii::unlock(img, "…#10029:{qid}")
    //    (каждый — EqIdVal→FFFF; суммарно с шагом 1 — все 8 вопросов вскрыты)
    // 3) hijack_form q59 → res.unlock_flips.is_empty() (всё уже вскрыто — идемпотентность
    //    plan_gates_skip_unlocked на живых данных)
    // 4) build; re-parse: IFR re-plan по всем 8 вопрос-таргетам и form-таргету → empty
    // 5) $SPF: help-контрол q59 → new id; q59 prompt/help в IFR → appended ids
    // 6) значения unlock-флипов в decompressed IFR равны ожиданиям E24/E25:
    //    REF-константы = 2, все EQ(0x9A,·) = FFFF (проверка через re-plan уже покрывает)
}
```

Точные ассерты диффов — по образцу `real_image_hii_form_hijack_built_bytes` (setup_slot/sd_slot вычисляются так же). Ожидания количества флипов: form-unlock = 10 (9× EqIdVal→FFFF + 1× EqConst→2, см. TODO E25), вопрос q59 = 1 (EqIdVal), вопрос-остальные = по 1. Если фактические числа отличаются — фиксировать фактическое в тесте с комментарием-ссылкой `TODO.md:…` и отдельным docs-коммитом до правки (правило 11 AGENTS.md).

- [ ] **Step 2: Run (fail first)**

Run: `cargo test -p uefi-engine --test real_image -- --ignored real_image_hijack_v2 2>/dev/null || cargo test -p uefi-engine real_image_hijack_v2 -- --ignored`
Expected: FAIL — тестов нет/не компилируются до написания; после написания — зелёные только при корректной реализации. Порядок TDD здесь: тесты пишутся против уже готового API (Tasks 1–6), поэтому «failing first» = момент до Tasks 1–5; допускается написание тестов и прогон сразу — при падении AssertionError чинить реализацию, не тест.

- [ ] **Step 3: Run gate against live image**

Run: `cargo test -p uefi-engine real_image_hijack_v2 -- --ignored`
Expected: PASS (при наличии файла образа)

- [ ] **Step 4: Commit**

```bash
git add crates/uefi-engine/tests/real_image.rs
git commit -m "test(uefi-engine): real-image hijack-v2 gates (victim-only + full-page E26-equivalent)"
```

---

### Task 8: E27 — движковый артефакт + закрытие цикла в docs

**Files:**
- Modify: `TODO.md` (секция 455), `docs/reports/` — новый отчёт `2026-09-04-hijack-v2-e27.md` после флеша (пока — сборка и чеклист)
- Артефакт: `refs/amibcp/e27-hijack-v2-engine.bin` (НЕ коммитить)

- [ ] **Step 1: Собрать E27 движком**

```bash
UEFIPATCHER_SOCK=/tmp/uefipatcher-e27.sock ./target/debug/engine &
uefi-cli session init --name e27 --force
uefi-cli image open refs/fw/HNX99TF_200525_original_E5C88C6F.bin --mode write
uefi-cli hii form unlock --target 899407d7-99fe-43d8-9a21-79ec328cac21:0x10:0#10029
for q in 54 55 56 57 58 60 61; do
  uefi-cli hii question unlock --target 899407d7-99fe-43d8-9a21-79ec328cac21:0x10:0#10029:$q
done
cat > /tmp/e27-schema.json <<'EOF'
{"questions": [{"question_id": 59, "prompt": "UEFIPatcher E27: engine hijack-v2", "help": "UEFIPatcher E27: engine-built help channel"}]}
EOF
uefi-cli hii form hijack \
  --target 899407d7-99fe-43d8-9a21-79ec328cac21:0x10:0#10029 \
  --file /tmp/e27-schema.json \
  --setupdata-guid FE612B72-203C-47B1-8560-A66D946EB371
uefi-cli image save refs/amibcp/e27-hijack-v2-engine.bin
sha256sum refs/amibcp/e27-hijack-v2-engine.bin
```

Вывод hijack должен показать: string_ids = 2, unlock_flips = 0 (всё вскрыто шагами unlock), help_controls = 1.

- [ ] **Step 2: Сверка артефакта**

Побайтово (по образцу E18-отчёта §1): дифф против оригинала confined в слоты Setup-модуля и SetupData; re-parse: форма 10029 на месте, IFR q59 prompt/help = аппенды, title-строка формы не тронута; $SPF-контрол хелпа = новый id. Любое расхождение с гейт-тестом сценария B — стоп, разбор.

- [ ] **Step 3: Чеклист флеша в TODO + commit**

TODO (секция 455): артефакт, sha256, команды, чеклист ожиданий (= состояние E26: полная страница; у Above 4G Decoding наш prompt, при выделении — наш help; опции Enabled/Disabled; смена значения сохраняется после Save&Exit+reboot).

```bash
git add TODO.md
git commit -m "docs: hijack-v2 cycle closed on engine side, E27 artifact awaiting flash"
git push dsevosty fix/cycle6-reimplent
```

- [ ] **Step 4 (после отчёта пользователя о прошивке):** отчёт `docs/reports/2026-09-04-hijack-v2-e27.md` по чеклисту + финальный статус цикла. Движок на `/tmp/uefipatcher-e27.sock` — не останавливать до слова пользователя.
