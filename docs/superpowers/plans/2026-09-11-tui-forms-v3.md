# TUI Forms View — ступень V3 (операции добавления, schema-файлы) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Пять ex-команд schema-добавления в Forms-view TUI (`:hii formset add` / `:hii form add` / `:hii question add` / `:hii page add` / `:hii hijack`), клавиша `a` с prefill, таб-комплит существительных/таргетов/путей — паритет с CLI (гейт: sha256).

**Architecture:** Engine-добавок нет — все пять RPC (`HiiFormSetAdd`/`HiiFormAdd`/`HiiQuestionAdd`/`HiiPageAdd`/`HiiFormHijack`) реализованы (дуги setup-new-page + hijack) и уже есть в TUI-моке (статические ответы). TUI-диспетчер `:hii` достраивается двухтокенной грамматикой `<существительное> add` (позиционные аргументы, как `set-value` из V2); schema-файл читает сам TUI-процесс (клиент), в RPC уходит строка `schema_json` — путь до движка не доходит (не как `:save`, там absolutize нужен потому что файл пишет engine). После мутации — `refresh_tree` (меняется дерево образа) + `reload_forms` + `refresh_form_details_if_needed` (как set-value в V2). Мок дописывает recorder schema-вызовов (`SchemaCall`), чтобы интеграционные тесты ассертили форму запроса, а не только ответ.

**Tech Stack:** Rust workspace (edition 2024), ratatui 0.28 + crossterm 0.28, tonic-клиент поверх unix-сокета. Без proto-изменений, без новых крейтов.

**Spec:** `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md` (§4 V3 — таблица команд + prefill + гейт; §6 конвенции дуги; решения D5 «без визардов», D6 «item_id из браузинга»). План закрывает ступень V3 — финальную ступень дуги V1–V3.

## Global Constraints

- Ветка `feat/tui-forms-v3` (создать от master до Task 1), PR по завершении — как в V2.
- TDD-порядок: тест падает (красным считается и compile-error на отсутствующей функции) → реализация → зелёный → commit (сообщения — из плана, один коммит на шаг с `git commit`).
- После каждой задачи: `cargo test -p uefi-tui` и `cargo clippy -p uefi-tui -- -D warnings`.
- Финальная проверка (обязательна, урок `9933f59`): `cargo test --all` + `cargo clippy --all -- -D warnings` + `cargo fmt --all -- --check`.
- Новых модулей нет — module-first rule не срабатывает; все правки в существующих файлах.
- item_id-контракт: form_id — ДЕСЯТИЧНОЕ (`parse_item_id` не примет `0x…` для form-части), question_id — hex. `<target>` — «голый» адрес `GUID:0xNN:<idx>` (= `FormInfo.form_id`), для `question add` — `<target>#<form_id>`.
- Грамматика TUI — позиционная (как `:hii set-value ITEM VALUE` из V2), НЕ flag-стиль CLI (`--target/--file`): `:hii form add TARGET FILE`. Исключение — опциональный `--ffs GUID` у `formset add` (паритет с CLI-опцией; выбор FFS-файла для вставки формсета).
- Комментарии в коде — нет (кроме коротких rustdoc-контрактов `///` на pub-функциях: что делает/чего не делает + ссылка на спеку).
- Rule-11 (AGENTS.md): расхождение плана с реальностью — отдельный docs-коммит `docs: fix Task N in tui-forms-v3 plan (…)` ДО реализации.
- Mock-ответы пяти RPC меняются с пустых на фикстуры — это видно только TUI-тестам (мок — test-support uefi-tui, других потребителей нет).

## File Structure

| Файл | Ответственность | Задачи |
|---|---|---|
| `crates/uefi-tui/src/commands.rs` | статус-хелперы schema-операций; `read_schema`; глаголы `formset`/`form`/`question`/`page`/`hijack` в `:hii`; `add_prefill`; `complete_path` + completion-контекст | 1, 3, 4, 5, 6 |
| `crates/uefi-tui/tests/mock_server.rs` | `SchemaCall` + recorder + фикстуры пяти RPC; `start_mock` возвращает handle recorder'а | 2 |
| `crates/uefi-tui/tests/tui_integration.rs` | интеграционные тести пяти глаголов против мока | 3, 4 |
| `crates/uefi-tui/src/main.rs` | клавиша `a` в `handle_normal_forms` | 5 |
| `crates/uefi-tui/src/ui/help.rs` | help: `a`-клавиша + пять `:hii`-команд | 7 |
| `crates/uefi-tui/src/ui/mod.rs` | hint Forms[List]: `a add` | 7 |
| `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md` | аддендум §9 — сценарий гейта V3 | 8 |
| `roadmap.md` | статус ступени V3 | 8 |

---

### Task 1: статус-хелперы schema-операций (чистые функции)

**Files:**
- Modify: `crates/uefi-tui/src/commands.rs` (новые pub-функции после `form_visibility_command`, ~строка 947; тесты — в существующий `mod tests`)

**Interfaces:**
- Consumes: типы `uefi_proto::{HiiQuestionAddOutcome, HiiPageAddResponse, HiiFormHijackResponse}` (уже импортированы глобальным `use uefi_proto::*;`).
- Produces (для Task 3/4, точные сигнатуры):
  - `pub fn formset_add_status(new_ffs_id: &str, form_ids: &[u32], string_ids: &std::collections::HashMap<String, u32>) -> String`
  - `pub fn form_add_status(form_ids: &[u32], string_ids: &std::collections::HashMap<String, u32>) -> String`
  - `pub fn question_add_status(questions: &[HiiQuestionAddOutcome], refs: &[HiiQuestionAddOutcome]) -> String`
  - `pub fn page_add_status(resp: &HiiPageAddResponse) -> String`
  - `pub fn hijack_status(resp: &HiiFormHijackResponse) -> String`

Форматы (однострочные, `·`-разделитель — как set-value в V2; string_ids сортируются по имени — HashMap-порядок недетерминирован):

- `formset added: ffs <id> · forms <ids,|> · strings <name=sid,|(none)>`
- `form added: forms <ids,|(none)> · strings <name=sid,|(none)>`
- `question add: questions <hex-qid,|(none)> · refs <hex-qid,|(none)> · strings <name=sid,|(none)>`
- `page added: form <form_id> · slot <slot> · offset <page_offset> · title sid <title_string_id>` (десятичные, как CLI `print_page_add`)
- `hijack: ifr <start:#x>..<end:#x> · flips <joined|"none"> · strings <len>` (hex для IFR-границ, flips join `" · "` — как unlock в V2)

- [ ] **Step 1: Write the failing test**

В `mod tests` файла `commands.rs` (после теста `form_visibility_command_only_unsuppresses`):

```rust
    #[test]
    fn schema_status_formats() {
        let mut sids = std::collections::HashMap::new();
        sids.insert("title".to_string(), 600);
        sids.insert("help".to_string(), 601);
        assert_eq!(
            formset_add_status("ffs-9", &[10101, 10102], &sids),
            "formset added: ffs ffs-9 · forms 10101,10102 · strings help=601,title=600"
        );
        assert_eq!(
            formset_add_status("ffs-9", &[], &std::collections::HashMap::new()),
            "formset added: ffs ffs-9 · forms (none) · strings (none)"
        );
        assert_eq!(
            form_add_status(&[10101], &sids),
            "form added: forms 10101 · strings help=601,title=600"
        );
        assert_eq!(
            form_add_status(&[], &std::collections::HashMap::new()),
            "form added: forms (none) · strings (none)"
        );
    }

    #[test]
    fn question_add_status_hex_qids_and_strings() {
        let mk = |qid: u32, sids: &[(&str, u32)]| HiiQuestionAddOutcome {
            question_id: qid,
            string_ids: sids
                .iter()
                .map(|(n, s)| (n.to_string(), *s))
                .collect(),
            spf_record_offset: 0x1C,
        };
        assert_eq!(
            question_add_status(&[mk(0x258, &[("prompt", 600)])], &[]),
            "question add: questions 0x258 · refs (none) · strings prompt=600"
        );
        assert_eq!(
            question_add_status(&[mk(0x258, &[])], &[mk(0x25A, &[("prompt", 600), ("help", 601)])]),
            "question add: questions 0x258 · refs 0x25a · strings help=601,prompt=600"
        );
        assert_eq!(
            question_add_status(&[], &[]),
            "question add: questions (none) · refs (none) · strings (none)"
        );
    }

    #[test]
    fn page_and_hijack_status() {
        let page = HiiPageAddResponse {
            form_id: 10019,
            slot: 1,
            page_offset: 42,
            title_string_id: 600,
        };
        assert_eq!(
            page_add_status(&page),
            "page added: form 10019 · slot 1 · offset 42 · title sid 600"
        );
        let mut sids = std::collections::HashMap::new();
        sids.insert("title".to_string(), 600);
        let hijack = HiiFormHijackResponse {
            string_ids: sids,
            form_ifr_start: 0x1000,
            form_ifr_end: 0x1100,
            unlock_flips: vec!["pkg+0x1c: 01 00 -> ff ff".into()],
            ..Default::default()
        };
        assert_eq!(
            hijack_status(&hijack),
            "hijack: ifr 0x1000..0x1100 · flips pkg+0x1c: 01 00 -> ff ff · strings 1"
        );
        let no_flips = HiiFormHijackResponse::default();
        assert_eq!(
            hijack_status(&no_flips),
            "hijack: ifr 0x0..0x0 · flips none · strings 0"
        );
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-tui --lib`
Expected: FAIL — `E0425 cannot find function formset_add_status in this module` (compile-error = красный).

- [ ] **Step 3: Write minimal implementation**

В `commands.rs`, после `form_visibility_command` (перед `refresh_forms`):

```rust
fn fmt_string_ids(ids: &std::collections::HashMap<String, u32>) -> String {
    if ids.is_empty() {
        return "(none)".into();
    }
    let mut v: Vec<(&String, &u32)> = ids.iter().collect();
    v.sort_by_key(|(name, _)| name.to_string());
    v.iter()
        .map(|(name, sid)| format!("{name}={sid}"))
        .collect::<Vec<_>>()
        .join(",")
}

/// Однострочный статус :hii formset add. Спека tui-forms-view §4 V3.
pub fn formset_add_status(
    new_ffs_id: &str,
    form_ids: &[u32],
    string_ids: &std::collections::HashMap<String, u32>,
) -> String {
    let forms = if form_ids.is_empty() {
        "(none)".into()
    } else {
        form_ids
            .iter()
            .map(|i| i.to_string())
            .collect::<Vec<_>>()
            .join(",")
    };
    format!("formset added: ffs {new_ffs_id} · forms {forms} · strings {}", fmt_string_ids(string_ids))
}

/// Однострочный статус :hii form add. Спека tui-forms-view §4 V3.
pub fn form_add_status(
    form_ids: &[u32],
    string_ids: &std::collections::HashMap<String, u32>,
) -> String {
    let forms = if form_ids.is_empty() {
        "(none)".into()
    } else {
        form_ids
            .iter()
            .map(|i| i.to_string())
            .collect::<Vec<_>>()
            .join(",")
    };
    format!("form added: forms {forms} · strings {}", fmt_string_ids(string_ids))
}

/// Однострочный статус :hii question add: qid в hex (конвенция q0xNNN),
/// string_ids всех исходов вперёд, сортировка по имени. Спека §4 V3.
pub fn question_add_status(
    questions: &[HiiQuestionAddOutcome],
    refs: &[HiiQuestionAddOutcome],
) -> String {
    let fmt_ids = |v: &[HiiQuestionAddOutcome]| {
        if v.is_empty() {
            "(none)".to_string()
        } else {
            v.iter()
                .map(|o| format!("{:#x}", o.question_id))
                .collect::<Vec<_>>()
                .join(",")
        }
    };
    let mut names: Vec<(String, u32)> = questions
        .iter()
        .chain(refs)
        .flat_map(|o| o.string_ids.iter().map(|(n, s)| (n.clone(), *s)))
        .collect();
    names.sort_by_key(|(n, _)| n.clone());
    let strings = if names.is_empty() {
        "(none)".into()
    } else {
        names
            .iter()
            .map(|(n, s)| format!("{n}={s}"))
            .collect::<Vec<_>>()
            .join(",")
    };
    format!(
        "question add: questions {} · refs {} · strings {strings}",
        fmt_ids(questions),
        fmt_ids(refs)
    )
}

/// Однострочный статус :hii page add (десятичные поля, как CLI). Спека §4 V3.
pub fn page_add_status(resp: &HiiPageAddResponse) -> String {
    format!(
        "page added: form {} · slot {} · offset {} · title sid {}",
        resp.form_id, resp.slot, resp.page_offset, resp.title_string_id
    )
}

/// Однострочный статус :hii hijack: flips join как unlock (V2), строки —
/// количеством (имён может быть много). Спека §4 V3.
pub fn hijack_status(resp: &HiiFormHijackResponse) -> String {
    let flips = if resp.unlock_flips.is_empty() {
        "none".to_string()
    } else {
        resp.unlock_flips.join(" · ")
    };
    format!(
        "hijack: ifr {:#x}..{:#x} · flips {flips} · strings {}",
        resp.form_ifr_start,
        resp.form_ifr_end,
        resp.string_ids.len()
    )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cargo test -p uefi-tui --lib`
Expected: PASS (3 новых теста).

- [ ] **Step 5: Lint**

Run: `cargo clippy -p uefi-tui -- -D warnings`
Expected: без замечаний.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/commands.rs
git commit -m "feat(uefi-tui): статус-хелперы schema-операций V3 (formset/form/question/page/hijack)"
```

---

### Task 2: мок — recorder schema-вызовов + фикстуры пяти RPC

**Files:**
- Modify: `crates/uefi-tui/tests/mock_server.rs` (структура + 5 хендлеров + `start_mock` + тест)

**Interfaces:**
- Consumes: хендлеры `hii_form_set_add`/`hii_form_add`/`hii_form_hijack`/`hii_question_add`/`hii_page_add` уже существуют (статические пустые ответы, строки 344–454).
- Produces (для Task 3/4):
  - `#[derive(Debug, Clone, PartialEq, Eq)] pub struct SchemaCall { pub rpc: &'static str, pub image_id: String, pub target: String, pub schema_json: String, pub extra: String }` — `target` = `target`/`item_id`/`target_ffs_guid` запроса по роли, `extra` = `setupdata_guid` hijack (иначе `""`).
  - `pub async fn start_mock(sock: &Path) -> (JoinHandle<()>, Arc<Mutex<Vec<SchemaCall>>>)` — второй элемент = клон `MockEngine::schema_calls` для ассертов.
  - Фикстуры ответов: formset add → `new_ffs_id "mock-ffs-1"`, `inserted_form_ids [10101]`, `string_ids {"title": 600}`; form add → `inserted_form_ids [10101]`, `string_ids {"title": 600}`; question add → `questions [HiiQuestionAddOutcome { question_id: 0x258, string_ids {"prompt": 600}, spf_record_offset: 0x1C }]`, `refs []`; page add → `HiiPageAddResponse { form_id: 10019, slot: 1, page_offset: 42, title_string_id: 600 }`; hijack → `string_ids {"title": 600}`, `form_ifr_start 0x1000`, `form_ifr_end 0x1100`, `unlock_flips ["pkg+0x1c: 01 00 -> ff ff"]`.

Важно: все существующие вызовы `let _handle = mock_server::start_mock(&sock).await;` остаются компилируемыми — `_handle` теперь привязывает кортеж, никто его не читает.

- [ ] **Step 1: Write the failing test**

В `mod tests` файла `mock_server.rs` (после `mock_roundtrip`):

```rust
    #[tokio::test]
    async fn schema_rpcs_record_calls_and_fixture_responses() {
        let td = TempDir::new().unwrap();
        let sock = td.path().join("mock.sock");
        let (_h, calls) = start_mock(&sock).await;
        let sock_str = sock.to_string_lossy().to_string();
        let channel = Endpoint::try_from("http://localhost")
            .unwrap()
            .connect_with_connector(service_fn(move |_: http::Uri| {
                let s = sock_str.clone();
                async move {
                    Ok::<_, std::io::Error>(TokioIo::new(tokio::net::UnixStream::connect(s).await?))
                }
            }))
            .await
            .unwrap();
        let mut client = EngineServiceClient::new(channel);
        let r = client
            .hii_form_set_add(HiiFormSetAddRequest {
                image_id: "img-1".into(),
                schema_json: "{\"forms\":[]}".into(),
                target_ffs_guid: "FFS-GUID".into(),
            })
            .await
            .unwrap()
            .into_inner();
        assert_eq!(r.new_ffs_id, "mock-ffs-1");
        assert_eq!(r.inserted_form_ids, vec![10101]);
        let calls = calls.lock().await;
        assert_eq!(calls.len(), 1);
        assert_eq!(
            calls[0],
            SchemaCall {
                rpc: "HiiFormSetAdd",
                image_id: "img-1".into(),
                target: "FFS-GUID".into(),
                schema_json: "{\"forms\":[]}".into(),
                extra: String::new(),
            }
        );
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-tui --test mock_server`
Expected: FAIL — `E0425 cannot find function SchemaCall` / несоответствие возвращаемого типа `start_mock` (compile-error = красный).

- [ ] **Step 3: Write minimal implementation**

3a. Структура + поле + recorder — после `pub struct MockEngine {...}`:

```rust
/// Записанный schema-вызов: rpc-имя, поля запроса по роли (target =
/// target/item_id/target_ffs_guid, extra = setupdata_guid hijack).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SchemaCall {
    pub rpc: &'static str,
    pub image_id: String,
    pub target: String,
    pub schema_json: String,
    pub extra: String,
}

#[derive(Default)]
pub struct MockEngine {
    pub sessions: Arc<Mutex<HashMap<String, String>>>,
    pub schema_calls: Arc<Mutex<Vec<SchemaCall>>>,
}

impl MockEngine {
    async fn record_schema(
        &self,
        rpc: &'static str,
        image_id: &str,
        target: &str,
        schema_json: &str,
        extra: &str,
    ) {
        self.schema_calls.lock().await.push(SchemaCall {
            rpc,
            image_id: image_id.into(),
            target: target.into(),
            schema_json: schema_json.into(),
            extra: extra.into(),
        });
    }
}
```

(старое объявление `pub struct MockEngine { pub sessions: ... }` заменить на новое выше.)

3b. Пять хендлеров — заменить тела:

```rust
    async fn hii_form_set_add(
        &self,
        req: Request<HiiFormSetAddRequest>,
    ) -> Result<Response<HiiFormSetAddResponse>, Status> {
        let r = req.into_inner();
        self.record_schema("HiiFormSetAdd", &r.image_id, &r.target_ffs_guid, &r.schema_json, "")
            .await;
        Ok(Response::new(HiiFormSetAddResponse {
            new_ffs_id: "mock-ffs-1".into(),
            inserted_form_ids: vec![10101],
            string_ids: [("title".to_string(), 600)].into(),
        }))
    }
    async fn hii_form_add(
        &self,
        req: Request<HiiFormAddRequest>,
    ) -> Result<Response<HiiFormAddResponse>, Status> {
        let r = req.into_inner();
        self.record_schema("HiiFormAdd", &r.image_id, &r.target, &r.schema_json, "").await;
        Ok(Response::new(HiiFormAddResponse {
            inserted_form_ids: vec![10101],
            string_ids: [("title".to_string(), 600)].into(),
        }))
    }
    async fn hii_form_hijack(
        &self,
        req: Request<HiiFormHijackRequest>,
    ) -> Result<Response<HiiFormHijackResponse>, Status> {
        let r = req.into_inner();
        self.record_schema(
            "HiiFormHijack",
            &r.image_id,
            &r.target,
            &r.schema_json,
            &r.setupdata_guid,
        )
        .await;
        Ok(Response::new(HiiFormHijackResponse {
            string_ids: [("title".to_string(), 600)].into(),
            form_ifr_start: 0x1000,
            form_ifr_end: 0x1100,
            unlock_flips: vec!["pkg+0x1c: 01 00 -> ff ff".into()],
            ..Default::default()
        }))
    }
    async fn hii_question_add(
        &self,
        req: Request<HiiQuestionAddRequest>,
    ) -> Result<Response<HiiQuestionAddResponse>, Status> {
        let r = req.into_inner();
        self.record_schema("HiiQuestionAdd", &r.image_id, &r.target, &r.schema_json, "").await;
        Ok(Response::new(HiiQuestionAddResponse {
            questions: vec![HiiQuestionAddOutcome {
                question_id: 0x258,
                string_ids: [("prompt".to_string(), 600)].into(),
                spf_record_offset: 0x1C,
            }],
            refs: vec![],
        }))
    }
    async fn hii_page_add(
        &self,
        req: Request<HiiPageAddRequest>,
    ) -> Result<Response<HiiPageAddResponse>, Status> {
        let r = req.into_inner();
        self.record_schema("HiiPageAdd", &r.image_id, &r.target, &r.schema_json, "").await;
        Ok(Response::new(HiiPageAddResponse {
            form_id: 10019,
            slot: 1,
            page_offset: 42,
            title_string_id: 600,
        }))
    }
```

3c. `start_mock` — сервис остаётся по значению (Arc через общие поля), наружу отдаём клон recorder'а:

```rust
pub async fn start_mock(sock: &Path) -> (JoinHandle<()>, Arc<Mutex<Vec<SchemaCall>>>) {
    let _ = std::fs::remove_file(sock);
    let listener = tokio::net::UnixListener::bind(sock).unwrap();
    let incoming = UnixListenerStream::new(listener);
    let mock = MockEngine::default();
    let schema_calls = mock.schema_calls.clone();
    let handle = tokio::spawn(async move {
        Server::builder()
            .add_service(EngineServiceServer::new(mock))
            .serve_with_incoming(incoming)
            .await
            .unwrap();
    });
    tokio::time::sleep(std::time::Duration::from_millis(100)).await;
    (handle, schema_calls)
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cargo test -p uefi-tui`
Expected: PASS — новый тест + все существующие (включая `tui_integration`, `let _handle = start_mock(...)` компилируется с кортежем).

- [ ] **Step 5: Lint**

Run: `cargo clippy -p uefi-tui -- -D warnings`
Expected: без замечаний.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/tests/mock_server.rs
git commit -m "test(uefi-tui): мок пишет schema-вызовы (SchemaCall) + фикстуры пяти RPC; start_mock отдаёт recorder"
```

---

### Task 3: глаголы `:hii formset add` и `:hii form add`

**Files:**
- Modify: `crates/uefi-tui/src/commands.rs` (ветки в match `sub` внутри `"hii"`-arm `execute_command`, строки ~558–670; `read_schema` рядом с `absolutize_path`)
- Test: `crates/uefi-tui/tests/tui_integration.rs`

**Interfaces:**
- Consumes: Task 1 (`formset_add_status`, `form_add_status`), Task 2 (`start_mock -> (_, Arc<Mutex<Vec<SchemaCall>>>)`); существующие `refresh_tree`/`reload_forms`/`refresh_form_details_if_needed`/`auth_req`.
- Produces: работающие `:hii formset add FILE [--ffs GUID]` и `:hii form add TARGET FILE`; `fn read_schema(file: &str) -> Result<String, String>` (private — Task 4 переиспользует); обновлённый usage-текст «hii» без подкоманды.

- [ ] **Step 1: Write the failing tests**

В `tui_integration.rs` (после `hii_set_value_keeps_question_cursor`):

```rust
fn schema_file(td: &tempfile::TempDir, name: &str, body: &str) -> String {
    let p = td.path().join(name);
    std::fs::write(&p, body).unwrap();
    p.display().to_string()
}

#[tokio::test(flavor = "multi_thread")]
async fn hii_formset_add_sends_schema_and_refreshes() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let (_h, calls) = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    uefi_tui::commands::execute_command(&mut app, "forms", &mut client)
        .await
        .unwrap();
    let body = r#"{"formset_guid":"NEW-GUID","forms":[]}"#;
    let file = schema_file(&td, "formset.json", body);

    app.forms_cursor_down();
    uefi_tui::commands::refresh_form_details_if_needed(&mut app, &mut client)
        .await
        .unwrap();
    assert!(!app.forms.questions.is_empty(), "кэш вопросов заполнен до add");
    app.forms.cursor = 0;
    app.tree.clear();

    let r = uefi_tui::commands::execute_command(
        &mut app,
        &format!("hii formset add {file}"),
        &mut client,
    )
    .await
    .unwrap();
    assert_eq!(r, "mock-ffs-1");
    let calls = calls.lock().await;
    assert_eq!(calls.len(), 1);
    assert_eq!(calls[0].rpc, "HiiFormSetAdd");
    assert_eq!(calls[0].schema_json, body);
    assert_eq!(calls[0].target, "", "без --ffs уходит пустой target_ffs_guid");
    drop(calls);
    assert_eq!(
        app.status_msg,
        "formset added: ffs mock-ffs-1 · forms 10101 · strings title=600"
    );
    assert!(
        app.forms.questions.is_empty(),
        "reload_forms сбросил кэш вопросов (курсор на FormSet — re-fetch не вернул)"
    );
    assert!(!app.tree.is_empty(), "дерево образа обновлено после вставки FFS");
}

#[tokio::test(flavor = "multi_thread")]
async fn hii_formset_add_ffs_flag() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let (_h, calls) = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    let file = schema_file(&td, "formset.json", "{}");
    uefi_tui::commands::execute_command(
        &mut app,
        &format!("hii formset add {file} --ffs ABC-GUID"),
        &mut client,
    )
    .await
    .unwrap();
    let calls = calls.lock().await;
    assert_eq!(calls[0].target, "ABC-GUID");
}

#[tokio::test(flavor = "multi_thread")]
async fn hii_form_add_sends_target_and_schema() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let (_h, calls) = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    let body = r#"{"forms":[{"id":10101}]}"#;
    let file = schema_file(&td, "form.json", body);

    let r = uefi_tui::commands::execute_command(
        &mut app,
        &format!("hii form add 11111111-2222-3333-4444-555555555555:0x19:0 {file}"),
        &mut client,
    )
    .await
    .unwrap();
    assert_eq!(r, "10101");
    let calls = calls.lock().await;
    assert_eq!(calls[0].rpc, "HiiFormAdd");
    assert_eq!(calls[0].target, "11111111-2222-3333-4444-555555555555:0x19:0");
    assert_eq!(calls[0].schema_json, body);
    assert_eq!(
        app.status_msg,
        "form added: forms 10101 · strings title=600"
    );
}

#[tokio::test(flavor = "multi_thread")]
async fn hii_add_usage_and_file_errors() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let (_h, _calls) = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    let e = uefi_tui::commands::execute_command(&mut app, "hii", &mut client)
        .await
        .unwrap_err();
    assert!(e.contains("formset add"), "usage перечисляет V3-глаголы: {e}");
    assert!(uefi_tui::commands::execute_command(&mut app, "hii formset", &mut client)
        .await
        .is_err());
    assert!(uefi_tui::commands::execute_command(
        &mut app,
        "hii form add 11111111-2222-3333-4444-555555555555:0x19:0",
        &mut client
    )
    .await
    .is_err());
    let e = uefi_tui::commands::execute_command(
        &mut app,
        "hii formset add /nonexistent-9f1/schema.json",
        &mut client,
    )
    .await
    .unwrap_err();
    assert!(e.contains("/nonexistent-9f1/schema.json"), "ошибка несёт путь: {e}");
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cargo test -p uefi-tui --test tui_integration`
Expected: FAIL — `unknown hii subcommand: formset` (первый тест), остальные по usage/ошибкам.

- [ ] **Step 3: Write minimal implementation**

3a. `read_schema` — в `commands.rs` после `absolutize_path`:

```rust
/// Читает schema-файл в TUI-процессе (клиент): в RPC уходит содержимое
/// строкой, путь до движка не доходит (не :save — там пишет engine).
fn read_schema(file: &str) -> Result<String, String> {
    std::fs::read_to_string(file).map_err(|e| format!("{file}: {e}"))
}
```

3b. Usage-текст «hii» без подкоманды — заменить `ok_or(...)` на входе в arm `"hii"`:

```rust
            let sub = parts.get(1).copied().ok_or(
                "usage: :hii set-value ITEM VALUE | :hii visibility ITEM on|off | :hii unlock ITEM | :hii formset add FILE [--ffs GUID] | :hii form add TARGET FILE | :hii question add TARGET#FORM FILE | :hii page add TARGET FILE | :hii hijack TARGET FILE [SETUPDATA-GUID]",
            )?;
```

3c. Две новые ветки в `match sub` (после `"unlock"`, до `other =>`):

```rust
                "formset" => {
                    if parts.get(2).copied() != Some("add") {
                        return Err("usage: :hii formset add FILE [--ffs GUID]".into());
                    }
                    let file = parts
                        .get(3)
                        .ok_or("usage: :hii formset add FILE [--ffs GUID]")?;
                    let ffs = parts
                        .iter()
                        .position(|p| *p == "--ffs")
                        .and_then(|i| parts.get(i + 1).copied())
                        .unwrap_or_default();
                    let schema_json = read_schema(file)?;
                    let r = client
                        .inner
                        .hii_form_set_add(auth_req(
                            &client.state,
                            HiiFormSetAddRequest {
                                image_id: iid,
                                schema_json,
                                target_ffs_guid: ffs.to_string(),
                            },
                        ))
                        .await
                        .map_err(|e| e.message().to_string())?
                        .into_inner();
                    refresh_tree(app, client).await?;
                    reload_forms(app, client).await?;
                    let _ = refresh_form_details_if_needed(app, client).await;
                    app.status_msg =
                        formset_add_status(&r.new_ffs_id, &r.inserted_form_ids, &r.string_ids);
                    Ok(r.new_ffs_id)
                }
                "form" => {
                    if parts.get(2).copied() != Some("add") {
                        return Err("usage: :hii form add TARGET FILE".into());
                    }
                    let target = parts
                        .get(3)
                        .ok_or("usage: :hii form add TARGET FILE")?;
                    let file = parts
                        .get(4)
                        .ok_or("usage: :hii form add TARGET FILE")?;
                    let schema_json = read_schema(file)?;
                    let r = client
                        .inner
                        .hii_form_add(
                            auth_req(
                                &client.state,
                                HiiFormAddRequest {
                                    image_id: iid,
                                    target: target.to_string(),
                                    schema_json,
                                },
                            ),
                        )
                        .await
                        .map_err(|e| e.message().to_string())?
                        .into_inner();
                    refresh_tree(app, client).await?;
                    reload_forms(app, client).await?;
                    let _ = refresh_form_details_if_needed(app, client).await;
                    app.status_msg = form_add_status(&r.inserted_form_ids, &r.string_ids);
                    Ok(r.inserted_form_ids
                        .iter()
                        .map(|i| i.to_string())
                        .collect::<Vec<_>>()
                        .join(","))
                }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cargo test -p uefi-tui`
Expected: PASS — 4 новых интеграционных теста + все существующие.

- [ ] **Step 5: Lint**

Run: `cargo clippy -p uefi-tui -- -D warnings`
Expected: без замечаний.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/commands.rs crates/uefi-tui/tests/tui_integration.rs
git commit -m "feat(uefi-tui): :hii formset add FILE [--ffs] и :hii form add TARGET FILE — schema-файлы, refresh tree+forms"
```

---

### Task 4: глаголы `:hii question add`, `:hii page add`, `:hii hijack`

**Files:**
- Modify: `crates/uefi-tui/src/commands.rs` (ещё три ветки в `match sub` arm'а `"hii"`)
- Test: `crates/uefi-tui/tests/tui_integration.rs`

**Interfaces:**
- Consumes: Task 1 (`question_add_status`, `page_add_status`, `hijack_status`), Task 3 (`read_schema`, dispatcher-паттерн), Task 2 (recorder).
- Produces: `:hii question add TARGET#FORM FILE`, `:hii page add TARGET FILE`, `:hii hijack TARGET FILE [SETUPDATA-GUID]` — грамматика спеки §4 V3 (hijack — одно слово, без `add`-посредника; setupdata-guid — опциональный 4-й позиционный).

- [ ] **Step 1: Write the failing tests**

В `tui_integration.rs` (после тестов Task 3; хелпер `schema_file` уже определён в Task 3 Step 1 в этом же файле — переиспользовать, не дублировать):

```rust
#[tokio::test(flavor = "multi_thread")]
async fn hii_question_add_item_id_and_status() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let (_h, calls) = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    let body = r#"{"questions":[{"form_id":10019,"question_id":512}]}"#;
    let file = schema_file(&td, "questions.json", body);

    let r = uefi_tui::commands::execute_command(
        &mut app,
        &format!("hii question add 11111111-2222-3333-4444-555555555555:0x19:0#10019 {file}"),
        &mut client,
    )
    .await
    .unwrap();
    assert_eq!(r, "0x258", "возврат — qid вставленных вопросов (hex)");
    let calls = calls.lock().await;
    assert_eq!(calls[0].rpc, "HiiQuestionAdd");
    assert_eq!(
        calls[0].target,
        "11111111-2222-3333-4444-555555555555:0x19:0#10019",
        "item_id: form_id десятичное (контракт parse_item_id)"
    );
    assert_eq!(calls[0].schema_json, body);
    assert_eq!(
        app.status_msg,
        "question add: questions 0x258 · refs (none) · strings prompt=600"
    );
}

#[tokio::test(flavor = "multi_thread")]
async fn hii_page_add_status() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let (_h, calls) = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    let file = schema_file(&td, "page.json", r#"{"title":"New"}"#);

    let r = uefi_tui::commands::execute_command(
        &mut app,
        &format!("hii page add 11111111-2222-3333-4444-555555555555:0x19:0 {file}"),
        &mut client,
    )
    .await
    .unwrap();
    assert_eq!(r, "10019");
    let calls = calls.lock().await;
    assert_eq!(calls[0].rpc, "HiiPageAdd");
    assert_eq!(
        app.status_msg,
        "page added: form 10019 · slot 1 · offset 42 · title sid 600"
    );
}

#[tokio::test(flavor = "multi_thread")]
async fn hii_hijack_with_and_without_setupdata_guid() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let (_h, calls) = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    let file = schema_file(&td, "hijack.json", r#"{"form":{"id":1}}"#);

    uefi_tui::commands::execute_command(
        &mut app,
        &format!("hii hijack 11111111-2222-3333-4444-555555555555:0x19:0 {file}"),
        &mut client,
    )
    .await
    .unwrap();
    {
        let calls = calls.lock().await;
        assert_eq!(calls[0].rpc, "HiiFormHijack");
        assert_eq!(calls[0].extra, "", "setupdata_guid опционален");
        assert_eq!(
            app.status_msg,
            "hijack: ifr 0x1000..0x1100 · flips pkg+0x1c: 01 00 -> ff ff · strings 1"
        );
    }

    uefi_tui::commands::execute_command(
        &mut app,
        &format!("hii hijack 11111111-2222-3333-4444-555555555555:0x19:0 {file} SETUP-GUID"),
        &mut client,
    )
    .await
    .unwrap();
    let calls = calls.lock().await;
    assert_eq!(calls[1].extra, "SETUP-GUID");
    assert!(
        uefi_tui::commands::execute_command(
            &mut app,
            "hii hijack 11111111-2222-3333-4444-555555555555:0x19:0",
            &mut client
        )
        .await
        .is_err(),
        "без FILE — usage-ошибка"
    );
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cargo test -p uefi-tui --test tui_integration`
Expected: FAIL — `unknown hii subcommand: question` / `page` / `hijack`.

- [ ] **Step 3: Write minimal implementation**

Три ветки в `match sub` (после `"form"` из Task 3, до `other =>`):

```rust
                "question" => {
                    if parts.get(2).copied() != Some("add") {
                        return Err("usage: :hii question add TARGET#FORM FILE".into());
                    }
                    let item = parts
                        .get(3)
                        .ok_or("usage: :hii question add TARGET#FORM FILE")?;
                    let file = parts
                        .get(4)
                        .ok_or("usage: :hii question add TARGET#FORM FILE")?;
                    let schema_json = read_schema(file)?;
                    let r = client
                        .inner
                        .hii_question_add(
                            auth_req(
                                &client.state,
                                HiiQuestionAddRequest {
                                    image_id: iid,
                                    target: item.to_string(),
                                    schema_json,
                                },
                            ),
                        )
                        .await
                        .map_err(|e| e.message().to_string())?
                        .into_inner();
                    refresh_tree(app, client).await?;
                    reload_forms(app, client).await?;
                    let _ = refresh_form_details_if_needed(app, client).await;
                    app.status_msg = question_add_status(&r.questions, &r.refs);
                    Ok(r.questions
                        .iter()
                        .map(|o| format!("{:#x}", o.question_id))
                        .collect::<Vec<_>>()
                        .join(","))
                }
                "page" => {
                    if parts.get(2).copied() != Some("add") {
                        return Err("usage: :hii page add TARGET FILE".into());
                    }
                    let target = parts
                        .get(3)
                        .ok_or("usage: :hii page add TARGET FILE")?;
                    let file = parts
                        .get(4)
                        .ok_or("usage: :hii page add TARGET FILE")?;
                    let schema_json = read_schema(file)?;
                    let r = client
                        .inner
                        .hii_page_add(
                            auth_req(
                                &client.state,
                                HiiPageAddRequest {
                                    image_id: iid,
                                    target: target.to_string(),
                                    schema_json,
                                },
                            ),
                        )
                        .await
                        .map_err(|e| e.message().to_string())?
                        .into_inner();
                    refresh_tree(app, client).await?;
                    reload_forms(app, client).await?;
                    let _ = refresh_form_details_if_needed(app, client).await;
                    app.status_msg = page_add_status(&r);
                    Ok(r.form_id.to_string())
                }
                "hijack" => {
                    let target = parts
                        .get(2)
                        .ok_or("usage: :hii hijack TARGET FILE [SETUPDATA-GUID]")?;
                    let file = parts
                        .get(3)
                        .ok_or("usage: :hii hijack TARGET FILE [SETUPDATA-GUID]")?;
                    let setupdata_guid = parts.get(4).copied().unwrap_or_default();
                    let schema_json = read_schema(file)?;
                    let r = client
                        .inner
                        .hii_form_hijack(
                            auth_req(
                                &client.state,
                                HiiFormHijackRequest {
                                    image_id: iid,
                                    target: target.to_string(),
                                    schema_json,
                                    setupdata_guid: setupdata_guid.to_string(),
                                },
                            ),
                        )
                        .await
                        .map_err(|e| e.message().to_string())?
                        .into_inner();
                    refresh_tree(app, client).await?;
                    reload_forms(app, client).await?;
                    let _ = refresh_form_details_if_needed(app, client).await;
                    app.status_msg = hijack_status(&r);
                    Ok(target.to_string())
                }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cargo test -p uefi-tui`
Expected: PASS — все интеграционные тести, включая Task 3.

- [ ] **Step 5: Lint**

Run: `cargo clippy -p uefi-tui -- -D warnings`
Expected: без замечаний.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/commands.rs crates/uefi-tui/tests/tui_integration.rs
git commit -m "feat(uefi-tui): :hii question add ITEM FILE · :hii page add TARGET FILE · :hii hijack TARGET FILE [SETUPDATA-GUID]"
```

---

### Task 5: клавиша `a` — Insert-prefill formset/form add

**Files:**
- Modify: `crates/uefi-tui/src/commands.rs` (`add_prefill` после `set_value_prefill`, ~строка 939; unit-тесты)
- Modify: `crates/uefi-tui/src/main.rs` (обработчик в `handle_normal_forms`)

**Interfaces:**
- Consumes: `App::forms_rows`/`forms.cursor`, `crate::forms::FormsRow`, `FormKey.target` (= `FormInfo.form_id`, голый target).
- Produces: `pub fn add_prefill(app: &App) -> Option<String>` — FormSet-строка → `Some("hii formset add ")`, Form-строка → `Some("hii form add {target} ")`, DanglingRef/пусто → `None`. Спека §4 V3: «на форме клавиша a → Insert mode с `hii form add <target> `; на формсете — `hii formset add `». Решение D6: target из выделения, не руками.

- [ ] **Step 1: Write the failing test**

В `mod tests` файла `commands.rs`:

```rust
    #[test]
    fn add_prefill_formset_form_and_dangling() {
        let mut app = crate::app::App::new();
        app.forms.forms = vec![uefi_proto::FormInfo {
            form_id: "11111111-2222-3333-4444-555555555555:0x19:0".into(),
            formset_guid: "SET-A".into(),
            form_id_ifr: 10001,
            title: "Main".into(),
            visible: true,
        }];
        app.forms.edges = vec![uefi_proto::FormEdge {
            formset_guid: "SET-A".into(),
            parent_form_id: 10001,
            form_id: 99,
        }];
        app.forms.expanded = crate::forms::all_row_keys(&app.forms.forms, &app.forms.edges);
        app.forms.cursor = 0;
        assert_eq!(
            add_prefill(&app).as_deref(),
            Some("hii formset add "),
            "FormSet-строка: formset add не требует target"
        );
        app.forms.cursor = 1;
        assert_eq!(
            add_prefill(&app).as_deref(),
            Some("hii form add 11111111-2222-3333-4444-555555555555:0x19:0 ")
        );
        app.forms.cursor = 2;
        assert_eq!(
            add_prefill(&app),
            None,
            "DanglingRef — не форма и не формсет, prefill нет"
        );
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-tui --lib commands::tests::add_prefill`
Expected: FAIL — `E0425 cannot find function add_prefill`.

- [ ] **Step 3: Write minimal implementation**

3a. В `commands.rs` после `set_value_prefill`:

```rust
/// Insert-prefill для клавиши `a` в Forms-view: FormSet-строка —
/// `hii formset add ` (target не нужен), Form-строка — `hii form add
/// <target> ` из выделения (решение D6); DanglingRef — None.
/// Спека tui-forms-view §4 V3.
pub fn add_prefill(app: &App) -> Option<String> {
    match app.forms_rows().get(app.forms.cursor) {
        Some(crate::forms::FormsRow::FormSet { .. }) => Some("hii formset add ".into()),
        Some(crate::forms::FormsRow::Form { key, .. }) => {
            Some(format!("hii form add {} ", key.target))
        }
        _ => None,
    }
}
```

3b. В `main.rs`, в `handle_normal_forms` — после ветки `AppEvent::Key('u')` (перед `AppEvent::Key('T')`):

```rust
        AppEvent::Key('a') if !app.forms.show_strings => {
            if let Some(pre) = commands::add_prefill(app) {
                app.enter_insert_mode("hii", pre);
            }
        }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cargo test -p uefi-tui`
Expected: PASS (unit + интеграция; main.rs — компиляция).

- [ ] **Step 5: Lint**

Run: `cargo clippy -p uefi-tui -- -D warnings`
Expected: без замечаний.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/commands.rs crates/uefi-tui/src/main.rs
git commit -m "feat(uefi-tui): клавиша a в Forms-view — prefill :hii formset add / :hii form add <target> (D6)"
```

---

### Task 6: таб-комплит — существительные hii, таргеты, пути, --ffs

**Files:**
- Modify: `crates/uefi-tui/src/commands.rs` (`context_candidates` hii-блок, `--ffs`-ветки, `complete_path` + дедуп-хелперы; обновление существующего теста `complete_hii_verbs_and_item_ids`)

**Interfaces:**
- Consumes: `complete(app, cmdline) -> (Option<String>, Vec<String>)` (публичный, не меняется), `app.forms.forms`.
- Produces:
  - `fn complete_path(token: &str) -> Vec<String>` (private) — кандидаты-пути: набранный каталог-префикс сохраняется как есть, каталоги получают `/`, скрытые файлы — только если префикс начинается с `.`.
  - `fn unique_form_targets(app: &App) -> Vec<String>` / `fn unique_formset_guids(app: &App) -> Vec<String>` (private) — дедуп по первому появлению.
  - Поведение: `hii <TAB>` → `["formset","form","question","page","hijack","set-value","visibility","unlock"]`; `hii formset|form|question|page <TAB>` → `["add"]`; позиция таргета: form/page/hijack → голые target'ы (дедуп), question → `target#form_id`; позиция файла → `complete_path`; `--ffs`-флаг у `formset add` → `["--ffs"]`, значение → гуиды формсетов. Спека §4 V3: «Табы A5 дополняют пути/таргеты».

- [ ] **Step 1: Write the failing tests**

В `mod tests` файла `commands.rs`. Сначала ОБНОВИТЬ существующий `complete_hii_verbs_and_item_ids` — заменить первый ассерт (глаголов стало восемь, порядок зафиксирован):

```rust
        let (_, opts) = complete(&app, "hii ");
        assert_eq!(
            opts,
            vec![
                "formset".to_string(),
                "form".to_string(),
                "question".to_string(),
                "page".to_string(),
                "hijack".to_string(),
                "set-value".to_string(),
                "visibility".to_string(),
                "unlock".to_string()
            ]
        );
```

(второй ассерт теста — про `hii set-value ` и item-кандидат — остаётся без изменений).

Новые тесты рядом:

```rust
    #[test]
    fn complete_hii_nouns_add_targets_items_and_paths() {
        let mut app = crate::app::App::new();
        app.forms.forms = vec![
            uefi_proto::FormInfo {
                form_id: "t:0x19:0".into(),
                formset_guid: "SET-A".into(),
                form_id_ifr: 10001,
                title: "Main".into(),
                visible: true,
            },
            uefi_proto::FormInfo {
                form_id: "t:0x19:0".into(),
                formset_guid: "SET-A".into(),
                form_id_ifr: 10019,
                title: "Serial".into(),
                visible: false,
            },
        ];
        let (_, opts) = complete(&app, "hii form ");
        assert_eq!(opts, vec!["add".to_string()]);
        let (_, opts) = complete(&app, "hii formset ");
        assert_eq!(opts, vec!["add".to_string()]);

        let (rep, _) = complete(&app, "hii form add ");
        assert_eq!(
            rep.as_deref(),
            Some("hii form add t:0x19:0"),
            "голые target-кандидаты, дедуп по двум формам одного target"
        );
        let (rep, _) = complete(&app, "hii hijack ");
        assert_eq!(rep.as_deref(), Some("hii hijack t:0x19:0"));
        let (_, opts) = complete(&app, "hii question add ");
        assert_eq!(
            opts,
            vec!["t:0x19:0#10001".to_string(), "t:0x19:0#10019".to_string()],
            "question add — item-кандидаты target#form_id (form_id десятичное)"
        );
    }

    #[test]
    fn complete_path_lists_dir_entries_with_slash_for_dirs() {
        let td = tempfile::TempDir::new().unwrap();
        std::fs::write(td.path().join("schema-a.json"), "{}").unwrap();
        std::fs::write(td.path().join("schema-b.json"), "{}").unwrap();
        std::fs::write(td.path().join(".hidden.json"), "{}").unwrap();
        std::fs::create_dir(td.path().join("fixtures")).unwrap();
        let base = td.path().display().to_string();
        assert_eq!(
            complete_path(&format!("{base}/schema-")),
            vec![
                format!("{base}/schema-a.json"),
                format!("{base}/schema-b.json")
            ]
        );
        assert_eq!(
            complete_path(&format!("{base}/fix")),
            vec![format!("{base}/fixtures/")],
            "каталог получает / — следующий Tab спускается внутрь"
        );
        assert_eq!(
            complete_path(&format!("{base}/.")),
            vec![format!("{base}/.hidden.json")],
            "скрытые — только по явному точечному префиксу"
        );
        assert!(complete_path(&format!("{base}/no-such-dir-9f1/x")).is_empty());
    }

    #[test]
    fn complete_hii_add_file_position_is_path_completion() {
        let td = tempfile::TempDir::new().unwrap();
        std::fs::write(td.path().join("schema-x.json"), "{}").unwrap();
        let base = td.path().display().to_string();
        let mut app = crate::app::App::new();
        app.forms.forms = vec![uefi_proto::FormInfo {
            form_id: "t:0x19:0".into(),
            formset_guid: "SET-A".into(),
            form_id_ifr: 10001,
            title: "Main".into(),
            visible: true,
        }];
        let (_, opts) = complete(&app, &format!("hii formset add {base}/schema-x"));
        assert_eq!(opts, vec![format!("{base}/schema-x.json")]);
        let (rep, _) = complete(&app, &format!("hii form add t:0x19:0 {base}/schema-x"));
        assert_eq!(
            rep.as_deref(),
            Some(format!("hii form add t:0x19:0 {base}/schema-x.json").as_str())
        );
        let (_, opts) = complete(&app, &format!("hii hijack t:0x19:0 {base}/schema-x"));
        assert_eq!(opts, vec![format!("{base}/schema-x.json")]);
    }

    #[test]
    fn complete_ffs_flag_and_formset_guid_value() {
        let mut app = crate::app::App::new();
        app.forms.forms = vec![uefi_proto::FormInfo {
            form_id: "t:0x19:0".into(),
            formset_guid: "SET-A".into(),
            form_id_ifr: 10001,
            title: "Main".into(),
            visible: true,
        }];
        let (_, opts) = complete(&app, "hii formset add f.json --");
        assert_eq!(opts, vec!["--ffs".to_string()]);
        let (rep, _) = complete(&app, "hii formset add f.json --ffs ");
        assert_eq!(rep.as_deref(), Some("hii formset add f.json --ffs SET-A"));
    }
```

Проверить в `crates/uefi-tui/Cargo.toml`, что `tempfile` есть в `[dev-dependencies]` (используется интеграционными тестами — если есть, unit-тестам он тоже доступен; если нет — добавить `tempfile = "…"` той же версией, что в dev-deps интеграционных, см. Cargo.toml).

- [ ] **Step 2: Run tests to verify they fail**

Run: `cargo test -p uefi-tui --lib`
Expected: FAIL — обновлённый `complete_hii_verbs_and_item_ids` (список глаголов), `E0425 cannot find function complete_path`, noun-тесты дают `["set-value",...]` вместо существительных.

- [ ] **Step 3: Write minimal implementation**

3a. Дедуп-хелперы и `complete_path` — перед `context_candidates`:

```rust
fn unique_form_targets(app: &App) -> Vec<String> {
    let mut seen = std::collections::HashSet::new();
    app.forms
        .forms
        .iter()
        .map(|f| f.form_id.clone())
        .filter(|t| seen.insert(t.clone()))
        .collect()
}

fn unique_formset_guids(app: &App) -> Vec<String> {
    let mut seen = std::collections::HashSet::new();
    app.forms
        .forms
        .iter()
        .map(|f| f.formset_guid.clone())
        .filter(|g| seen.insert(g.clone()))
        .collect()
}

/// Пути по префиксу для позиции schema-файла: набранный каталог-префикс
/// сохраняется как есть (абсолютный/относительный), каталоги получают
/// "/", скрытые файлы — только по точечному префиксу. Спека §4 V3.
fn complete_path(token: &str) -> Vec<String> {
    let (dir_part, prefix) = match token.rfind('/') {
        Some(i) => (&token[..=i], &token[i + 1..]),
        None => ("", token),
    };
    let dir = if dir_part.is_empty() { "." } else { dir_part };
    let Ok(rd) = std::fs::read_dir(dir) else {
        return vec![];
    };
    let mut out: Vec<String> = rd
        .flatten()
        .map(|e| {
            (
                e.file_name().to_string_lossy().to_string(),
                e.file_type().map(|t| t.is_dir()).unwrap_or(false),
            )
        })
        .filter(|(name, _)| name.starts_with(prefix))
        .filter(|(name, _)| !name.starts_with('.') || prefix.starts_with('.'))
        .map(|(name, is_dir)| format!("{dir_part}{name}{}", if is_dir { "/" } else { "" }))
        .collect();
    out.sort();
    out
}
```

3b. Значение `--ffs` — ветка сразу после `--artifact-id`-ветки в `context_candidates`:

```rust
    if head.last() == Some(&"--ffs") {
        return unique_formset_guids(app)
            .into_iter()
            .filter(|c| c.starts_with(token))
            .collect();
    }
```

3c. Флаги — расширить match в `token.starts_with("--")`-ветке:

```rust
    if token.starts_with("--") {
        let flags: &[&str] = match cmd {
            "insert" => &["--file", "--artifact-id", "--mode"],
            "replace" => &["--file", "--artifact-id", "--body-only"],
            "hii" if head.contains(&"formset") && head.contains(&"add") => &["--ffs"],
            _ => &[],
        };
        return flags
            .iter()
            .filter(|c| c.starts_with(token))
            .map(|s| s.to_string())
            .collect();
    }
```

3d. hii-блок — заменить целиком (существующий блок `if cmd == "hii" {...}`):

```rust
    if cmd == "hii" {
        if head.len() == 1 {
            return [
                "formset", "form", "question", "page", "hijack", "set-value", "visibility",
                "unlock",
            ]
            .iter()
            .filter(|c| c.starts_with(token))
            .map(|s| s.to_string())
            .collect();
        }
        let noun = head[1];
        if matches!(noun, "formset" | "form" | "question" | "page") {
            if head.len() == 2 {
                return ["add"]
                    .iter()
                    .filter(|c| c.starts_with(token))
                    .map(|s| s.to_string())
                    .collect();
            }
            if head.get(2) != Some(&"add") {
                return vec![];
            }
            let file_pos = if noun == "formset" { 3 } else { 4 };
            if head.len() == file_pos {
                return complete_path(token);
            }
            if head.len() == file_pos - 1 {
                if noun == "question" {
                    return app
                        .forms
                        .forms
                        .iter()
                        .map(|f| format!("{}#{}", f.form_id, f.form_id_ifr))
                        .filter(|c| c.starts_with(token))
                        .collect();
                }
                return unique_form_targets(app)
                    .into_iter()
                    .filter(|c| c.starts_with(token))
                    .collect();
            }
            return vec![];
        }
        if noun == "hijack" {
            if head.len() == 2 {
                return unique_form_targets(app)
                    .into_iter()
                    .filter(|c| c.starts_with(token))
                    .collect();
            }
            if head.len() == 3 {
                return complete_path(token);
            }
            return vec![];
        }
        if matches!(head[1], "set-value" | "visibility" | "unlock")
            && !head[2..].iter().any(|s| !s.starts_with("--"))
        {
            return app
                .forms
                .forms
                .iter()
                .map(|f| format!("{}#{}", f.form_id, f.form_id_ifr))
                .filter(|c| c.starts_with(token))
                .collect();
        }
        return vec![];
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cargo test -p uefi-tui`
Expected: PASS — обновлённый + 4 новых completion-теста, интеграция Task 3–5 зелёная.

- [ ] **Step 5: Lint**

Run: `cargo clippy -p uefi-tui -- -D warnings`
Expected: без замечаний.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/commands.rs crates/uefi-tui/Cargo.toml
git commit -m "feat(uefi-tui): completion V3 — существительные hii, target/item-кандидаты, пути schema-файлов, --ffs"
```

(Cargo.toml в `git add` — только если пришлось добавить dev-dependency `tempfile`.)

---

### Task 7: help и hint

**Files:**
- Modify: `crates/uefi-tui/src/ui/help.rs` (const `HELP` + новый `mod tests`)
- Modify: `crates/uefi-tui/src/ui/mod.rs` (hint-строка Forms[List])

**Interfaces:**
- Consumes: нет.
- Produces: документация `a`-клавиши и пяти команд; hint `a add`. Текст — часть UX-контракта ступени, фиксируется тестом.

- [ ] **Step 1: Write the failing test**

В конце `ui/help.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::HELP;

    #[test]
    fn help_documents_v3_add_commands_and_a_key() {
        for cmd in [
            ":hii formset add FILE [--ffs GUID]",
            ":hii form add TARGET FILE",
            ":hii question add TARGET#FORM FILE",
            ":hii page add TARGET FILE",
            ":hii hijack TARGET FILE [SETUPDATA-GUID]",
        ] {
            assert!(HELP.contains(cmd), "help должен документировать {cmd}");
        }
        assert!(HELP.contains("a              add"), "клавиша a в FORMS VIEW-секции");
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p uefi-tui --lib ui::help`
Expected: FAIL — `HELP` не содержит V3-команды (мод tests отсутствует → после добавления теста — красный ассерт).

- [ ] **Step 3: Write minimal implementation**

3a. `ui/help.rs`, блок FORMS VIEW — строка после `  u              unlock выбранной формы (:hii unlock)`:

```
  a              add: FormSet → :hii formset add · Form → :hii form add <target>
```

3b. `ui/help.rs`, секция EX-COMMANDS — после строки `  :hii unlock ITEM`:

```
  :hii formset add FILE [--ffs GUID]
  :hii form add TARGET FILE
  :hii question add TARGET#FORM FILE     (form_id — десятичное)
  :hii page add TARGET FILE
  :hii hijack TARGET FILE [SETUPDATA-GUID]
```

3c. `ui/mod.rs`, `render_hint` — Forms[List]-строка (ветка `else` после Details), вставить `a add · ` после `u unlock`:

```rust
                "NORMAL[Forms]: j/k move · h/l collapse/expand · v show hidden (unsuppress) · u unlock · a add · T tree/flat · S strings · Tab image-view · :cmd · ?help · q"
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cargo test -p uefi-tui`
Expected: PASS.

- [ ] **Step 5: Lint**

Run: `cargo clippy -p uefi-tui -- -D warnings`
Expected: без замечаний.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/ui/help.rs crates/uefi-tui/src/ui/mod.rs
git commit -m "feat(uefi-tui): help/hint V3 — клавиша a-add и пять :hii add/hijack команд"
```

---

### Task 8: гейт-сценарий §9, roadmap, финальная верификация workspace

**Files:**
- Modify: `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md` (новый §9 после §8)
- Modify: `roadmap.md` (секция «Дуга TUI Revival + Forms View» — статус V3)

**Interfaces:**
- Consumes: всё из Task 1–7.
- Produces: гейт-сценарий для владельца (вердикт добавится после прогона — конвенция §6 дуги), обновлённый roadmap.

- [ ] **Step 1: Add §9 to the spec**

После раздела «### Вердикт владельца по гейту V2 (2026-09-11) — ПОЗИТИВНЫЙ» (перед `## Решения (decisions log)`) вставить:

```markdown
## 9. Гейт V3 — сценарий для владельца (аддендум, 2026-09-11)

Гейт — паритет байт-в-байт (sha256) TUI/CLI на операциях добавления; спека-схемы —
fixtures дуги setup-new-page (`crates/uefi-engine/tests/data/serial/`). Живой движок,
образ HNX99TF, режим write.

1. `cargo run -p uefi-engine`; в другом терминале `cargo run -p uefi-tui`;
   `:open <HNX99TF> --mode write`; `Tab` → Forms-view.
2. Prefill: `a` на FormSet-строке → `hii formset add `; `Esc`; `a` на форме →
   `hii form add <target> ` (target подставлен из выделения, D6).
3. Formset-add: `:hii formset add crates/uefi-engine/tests/data/serial/np_form.json`
   → статус «formset added: ffs … · forms … · strings …»; список форм пополнился
   (reload); `Tab` → Image-view: новый FFS-файл в дереве.
4. Question-add паритет: `:snapshot`; `:hii question add <TARGET>#<FORM>
   <…>/s3_questions.json`; `:save /tmp/tui-qadd.bin`; `:restore <snap-id>`.
   Затем CLI: `uefi-cli image open <HNX99TF> --mode write` →
   `uefi-cli hii question add <ITEM> --file <…>/s3_questions.json` →
   `uefi-cli image save /tmp/cli-qadd.bin`. `sha256sum` обоих файлов совпадает.
5. Form-add паритет: тот же цикл с form-add fixture из `data/serial/` (`np_form*.json`)
   против живого формсета образа; sha256 совпадает.
6. Completion: `hii q<TAB>` → `question`; `hii question add <TAB>` → item-кандидаты;
   позиция файла — `<TAB>` по префиксу каталога (спуск в каталоги через `/`);
   `hii formset add f.json --<TAB>` → `--ffs`, значение — гуиды формсетов.

Вердикт (да/нет по пунктам + выводы sha256sum) — аддендумом сюда; успех = дуга
V1–V3 закрыта: полный редактор форм в TUI (просмотр → правки → добавление).
```

- [ ] **Step 2: Update roadmap**

В `roadmap.md`, секция «Дуга TUI Revival + Forms View (2026-09-11)»: дополнить строку
о ступени V3 — «план написан (`docs/superpowers/plans/2026-09-11-tui-forms-v3.md`),
исполняется» (формулировку согласовать с соседними строками V1/V2 в этом файле).

- [ ] **Step 3: Full-workspace verification**

```bash
cargo test --all
cargo clippy --all -- -D warnings
cargo fmt --all -- --check
```

Expected: все тесты зелёные (включая uefi-cli/uefi-gateway/uefi-engine — proto не
менялся, ломать нечему, но прогон обязателен по уроку `9933f59`), lint и fmt чистые.

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/specs/2026-09-11-tui-forms-view-design.md roadmap.md
git commit -m "docs(spec,roadmap): аддендум §9 — сценарий гейта V3 (sha256-паритет TUI/CLI); статус ступени"
```

- [ ] **Step 5: Push + PR**

```bash
git push -u origin feat/tui-forms-v3
```

и открыть PR `feat/tui-forms-v3` → `master` (как закрывали V2).

---

## Самопроверка (исполнителю — прочитать до старта)

- Пять команд спеки §4 V3 ↔ Task 3 (`formset`/`form`) + Task 4 (`question`/`page`/`hijack`): покрыты тестами на форму запроса (recorder), статус, reload/tree-refresh.
- Prefill `a` ↔ Task 5. Completion «пути/таргеты» ↔ Task 6. Гейт §4 V3 ↔ Task 8 (сценарий; вердикт — после прогона владельцем, по конвенции §6).
- Известная ловушка: `HashMap`-порядок недетерминирован — все статус-хелперы сортируют string_ids по имени (Task 1), ассерты в тестах полагаются на это.
- `start_mock` меняет возвращаемый тип — существующие `let _handle = …` НЕ ломаются (кортеж связывается и не читается). Если всё же что-то не компилируется — это расхождение плана с реальностью: rule-11 docs-коммит, не молчаливый fixup.
