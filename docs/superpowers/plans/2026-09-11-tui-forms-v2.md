# TUI Forms View — ступень V2 (правки + REF-дерево) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Правки форм на месте в Forms-view TUI — REF-дерево форм (`HiiFormTree`), `v` visibility / `u` unlock / `Enter` set-value с подсказкой из `HiiQuestionInfo`, блок гейтов в деталях.

**Architecture:** Единственная engine-добавка — RPC `HiiFormTree` (рёбра `parent → FormId@+13` из `IFR_REF_OP`; walker на выверенном `walk_statements`). TUI строит дерево клиент-side (чистые функции в `src/forms.rs`), правки идут через новое существительное `:hii` (`set-value`/`visibility`/`unlock`), клавиши `v`/`u`/`Enter` лишь конструируют item_id из выделения (решение D6) и вызывают те же ex-команды. После мутаций — `reload_forms` (re-fetch с сохранением состояния), после входа — `refresh_forms` (сброс, как V1).

**Tech Stack:** Rust workspace (edition 2024), ratatui 0.28 + crossterm 0.28, tonic (proto-генерация через build.rs), r-efi константы.

**Spec:** `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md` (ступень V2 §4 + §3.5 REF-дерево; решения D1–D6; риски §5).

## Global Constraints

- Module-first rule: `pub mod X;` в родительском модуле В ТОМ ЖЕ ШАГЕ, что создание файла, ДО `cargo test`.
- TDD: тест падает (в Rust красным считается и compile-error на отсутствующей функции) → реализация → зелёный → commit (сообщения коммитов — из плана).
- После каждой задачи: `cargo test -p <crate>` и `cargo clippy -p <crate> -- -D warnings`.
- Proto-добавка ломает чужие реализаторы трейта `EngineService` (3 тестовых мока: uefi-cli, uefi-tui, uefi-gateway) — ловится ТОЛЬКО `cargo test --all` (урок `9933f59`). Task 2 обязан закончиться `cargo test --all` + `cargo clippy --all -- -D warnings` + `cargo fmt --all -- --check`.
- Комментарии в коде — нет (кроме rustdoc-контрактов на pub-функциях: что делает/чего не делает + ссылка на спеку).
- IFR-константы — только `r_efi::hii` (`IFR_REF_OP`), никаких своих.
- item_id-синтаксис: `<target>#<form_id>:<question_id>`, где form_id — ДЕСЯТИЧНОЕ число (`parse_item_id` в `hii/mod.rs:173` делает `form_str.parse::<u16>()` — `0x…` для form-части НЕ пройдёт), question_id — loose (hex ок).
- Работа в ветке `feat/tui-forms-v2` (создать от master до Task 1, PR по завершении).
- Rule-11 (AGENTS.md): расхождение плана с реальностью — отдельный docs-коммит `docs: fix Task N in ...` ДО реализации, не молчаливый fixup.
- Проверка финальная: `cargo test --all && cargo clippy --all -- -D warnings && cargo fmt --all -- --check`.
- Реальный образ для гейта: `refs/fw/HNX99TF_200525_original_E5C88C6F.bin` (`#[ignore]`-тесты запускаются явно при наличии файла).

## File Structure

| Файл | Ответственность | Задачи |
|---|---|---|
| `crates/uefi-engine/src/hii/ref_tree.rs` | CREATE — walker REF-рёбер: `package_edges(pkg)` + обход образа `collect_edges(image)` | 1 |
| `crates/uefi-engine/src/hii/mod.rs` | `pub mod ref_tree;` | 1 |
| `crates/uefi-proto/proto/engine.proto` | RPC `HiiFormTree` + messages `FormEdge`/req/resp | 2 |
| `crates/uefi-engine/src/rpc/server.rs` | handler `hii_form_tree` (по образцу `hii_list_forms`) | 2 |
| `crates/uefi-cli/tests/mock_server.rs` | мок-метод `hii_form_tree` (пустой) | 2 |
| `crates/uefi-gateway/tests/mock_server.rs` | мок-метод `hii_form_tree` (пустой) | 2 |
| `crates/uefi-tui/tests/mock_server.rs` | мок `hii_form_tree` (fixture-рёбра) + fixture `hii_set_value`/`hii_unlock`/`hii_question_info`/`hii_gates_list` | 2, 6, 7 |
| `crates/uefi-engine/tests/real_image.rs` | `#[ignore]` real-image гейт `form_tree` | 3 |
| `crates/uefi-tui/src/app.rs` | `FormsData.edges/flat_mode/gates/question_*`, `App::forms_rows` dispatch, question-cursor, `selected_*` | 4, 5, 7 |
| `crates/uefi-tui/src/commands.rs` | `refresh_forms` (+edges), `reload_forms`, `:hii`-глаголы, `refresh_form_details_if_needed`, `refresh_question_info_if_needed`, prefill-хелперы, completion | 4, 6, 7 |
| `crates/uefi-tui/src/forms.rs` | display-модель: `build_tree_rows`, `all_row_keys`, `FormsRow::{Form(+depth/path/has_children), DanglingRef}`, `form_details_text` | 5, 7 |
| `crates/uefi-tui/src/ui/forms.rs` | рендер: отступы по depth, dangling-маркер, details через `form_details_text` | 5, 7 |
| `crates/uefi-tui/src/main.rs` | клавиши `T`/`v`/`u`, Details-focus `j`/`k`/`Enter` | 5, 7 |
| `crates/uefi-tui/src/ui/mod.rs`, `src/ui/help.rs` | hint и help для новых клавиш/команд | 5, 7 |
| `crates/uefi-tui/tests/tui_integration.rs` | integration-тесты против мока | 4, 5, 6, 7 |
| `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md` | аддендум §8 — сценарий гейта V2 | 8 |
| `TODO.md`, `roadmap.md` | статусы V2 | 8 |

---

### Task 1: engine — walker `hii/ref_tree.rs` (чистые функции + обход образа)

**Files:**
- Create: `crates/uefi-engine/src/hii/ref_tree.rs`
- Modify: `crates/uefi-engine/src/hii/mod.rs` (добавить `pub mod ref_tree;` между `pub mod questions;` и `pub mod schema;`, строки 13–14)

**Interfaces:**
- Consumes: `crate::hii::values::walk_statements(&[u8], impl FnMut(u8, usize, usize, Option<u16>))` (pub(crate), уже есть), `crate::hii::ifr::parse_form_package(&[u8]) -> Option<FormSetInfo>` (`FormSetInfo.guid: crate::types::Guid`, ifr.rs:403), `crate::hii::package_list::parse_package_list`, `crate::hii::pe_resource::{bare_form_packages, hii_resource_blobs, hii_resource_ranges}`, `crate::types::{FfsNode, FfsType, Image, guid_to_upper_string}`, `r_efi::hii::{IFR_REF_OP, PACKAGE_FORMS}`.
- Produces (для Task 2/3):

```rust
pub fn collect_edges(image: &Image) -> Vec<uefi_proto::FormEdge>;   // FormEdge появится в Task 2
pub(crate) fn package_edges(pkg: &[u8]) -> Vec<(u16, u16)>;         // (parent_form_id, ref-цель)
```

Пока `uefi_proto::FormEdge` нет (появится в Task 2), Task 1 реализует `collect_edges` возвращающим `Vec<(String, u32, u32)>` (formset_guid, parent, child), а Task 2 адаптирует сигнатуру в proto-тип. Это позволяет держать Task 1 зелёным без proto-правок.

Итоговые контракты:
- `package_edges`: в форме X каждый `IFR_REF_OP` с `len >= 15` даёт ребро `(X, u16@off+13)` — паттерн чтения ровно как `gates.rs:201` (ветка `Wraps::Ref`). REF вне формы (`current == None`) и короткие REF (`len < 15`) пропускаются. Дубликаты `(parent, child)` внутри пакета схлопываются (порядок первого появления).
- `collect_edges`: обходит образ тем же путём, что `forms.rs::collect_forms` (File-узлы рекурсивно; RAW form-пакет / PE32 resource package-list / bare_form_packages / COMPRESSION|GUIDED рекурсия), formset_guid = `guid_to_upper_string(&fs.guid)` — строково РАВНА `FormInfo.formset_guid` из `HiiListForms` (консистентность группировки в TUI). Висячие цели (формы нет в списке) НЕ отбрасываются. Дубликаты `(guid, parent, child)` между пакетами схлопываются.

- [ ] **Step 1: Создать файл с тестами + объявить модуль**

`crates/uefi-engine/src/hii/ref_tree.rs` создаётся с ЕДИНСТВЕННЫМ содержимым — тестовым блоком (реализация появится в Step 3; compile-error на отсутствующих `package_edges`/`collect_edges` — это красный тест). Use-строки реализации в Step 1 НЕ нужны — все тестовые use живут внутри `mod tests`:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::str::FromStr;

    use crate::types::{Action, Guid, ImageMode, ParsingData};
    use r_efi::hii::{IFR_END_OP, IFR_FORM_OP, IFR_FORM_SET_OP, IFR_REF_OP, PACKAGE_FORMS};

const FILE_GUID: &str = "899407d7-92a6-4174-968f-6f0b47f86a23";
const FORMSET_GUID: &str = "5C60F367-A505-419A-859E-2A4FF6CA6FE5";

fn opcode(op_code: u8, scope: bool, payload: &[u8]) -> Vec<u8> {
    let mut v = Vec::with_capacity(payload.len() + 2);
    v.push(op_code);
    v.push(((payload.len() + 2) as u8) | if scope { 0x80 } else { 0 });
    v.extend_from_slice(payload);
    v
}

fn form_set() -> Vec<u8> {
    let g = Guid::from_str(FORMSET_GUID).unwrap();
    let mut p = Vec::new();
    p.extend_from_slice(&g.to_bytes());
    p.extend_from_slice(&1u16.to_le_bytes());
    p.extend_from_slice(&0u16.to_le_bytes());
    p.push(0u8);
    opcode(IFR_FORM_SET_OP, true, &p)
}

fn form(id: u16) -> Vec<u8> {
    opcode(
        IFR_FORM_OP,
        true,
        &[id.to_le_bytes(), 0u16.to_le_bytes()].concat(),
    )
}

fn end() -> Vec<u8> {
    vec![IFR_END_OP, 0x02]
}

/// 11 байт question-header: prompt@0 help@2 qid@4 store@6 offset@8 flags@10
/// (абсолютно: +2..+13; REF FormId читается на +13 — gates.rs:201).
fn question_header(prompt: u16, qid: u16, store: u16, off: u16) -> Vec<u8> {
    let mut p = Vec::new();
    p.extend_from_slice(&prompt.to_le_bytes());
    p.extend_from_slice(&0x01A4u16.to_le_bytes());
    p.extend_from_slice(&qid.to_le_bytes());
    p.extend_from_slice(&store.to_le_bytes());
    p.extend_from_slice(&off.to_le_bytes());
    p.push(0x00);
    p
}

/// IFR_REF: header(11) + FormId u16 → length = 15 (минимум для чтения +13).
fn ref_op(qid: u16, target_form: u16) -> Vec<u8> {
    let mut p = question_header(0x10, qid, 0xFFFF, 0);
    p.extend_from_slice(&target_form.to_le_bytes());
    opcode(IFR_REF_OP, false, &p)
}

fn package(ifr: &[u8]) -> Vec<u8> {
    let total = 4 + ifr.len();
    let mut b = vec![
        (total & 0xFF) as u8,
        ((total >> 8) & 0xFF) as u8,
        ((total >> 16) & 0xFF) as u8,
        PACKAGE_FORMS,
    ];
    b.extend_from_slice(ifr);
    b
}

fn tree_pkg() -> Vec<u8> {
    package(
        &[
            form_set(),
            form(10001),
            ref_op(0x30, 10019),
            ref_op(0x31, 10019),
            end(),
            form(10019),
            ref_op(0x32, 10030),
            end(),
            end(),
            end(),
        ]
        .concat(),
    )
}

fn mk_node(
    guid: Option<Guid>,
    node_type: FfsType,
    subtype: u8,
    body: Vec<u8>,
    children: Vec<FfsNode>,
) -> FfsNode {
    FfsNode {
        guid,
        node_type,
        subtype,
        offset: 0,
        header: vec![],
        body,
        tail: vec![],
        children,
        action: Action::NoAction,
        parsing_data: ParsingData::None,
        fixed: false,
        compressed: false,
        alignment_bytes: vec![],
    }
}

fn mk_image(form_body: Vec<u8>) -> Image {
    let form_sec = mk_node(None, FfsType::Section, 0x19, form_body, vec![]);
    let file = mk_node(
        Some(Guid::from_str(FILE_GUID).unwrap()),
        FfsType::File,
        0x07,
        vec![],
        vec![form_sec],
    );
    let volume = mk_node(None, FfsType::Volume, 0, vec![], vec![file]);
    let root = mk_node(None, FfsType::Image, 0, vec![], vec![volume]);
    Image {
        image_id: "img".into(),
        session_id: "s".into(),
        root,
        mode: ImageMode::Read,
    }
}

#[test]
fn package_edges_dedup_and_order() {
    let pkg = tree_pkg();
    assert_eq!(
        package_edges(&pkg),
        vec![(10001, 10019), (10019, 10030)],
        "дубль (10001,10019) схлопнулся, порядок первого появления"
    );
}

#[test]
fn package_edges_skips_short_and_orphan_refs() {
    let short = opcode(IFR_REF_OP, false, &question_header(0x10, 0x30, 0xFFFF, 0)[..10]);
    let outside = ref_op(0x33, 10040);
    let pkg = package(
        &[
            form_set(),
            outside,
            short,
            form(10001),
            ref_op(0x30, 10019),
            end(),
            end(),
            end(),
        ]
        .concat(),
    );
    assert_eq!(
        package_edges(&pkg),
        vec![(10001, 10019)],
        "REF вне формы и REF с len<15 не дают рёбер"
    );
}

#[test]
fn collect_edges_walks_raw_section_and_keeps_dangling() {
    let edges = collect_edges(&mk_image(tree_pkg()));
    assert_eq!(edges.len(), 2);
    assert_eq!(edges[0], (FORMSET_GUID.to_string(), 10001, 10019));
    assert_eq!(edges[1], (FORMSET_GUID.to_string(), 10019, 10030));
}

#[test]
fn collect_edges_dangling_target_not_dropped() {
    let pkg = package(
        &[
            form_set(),
            form(10001),
            ref_op(0x30, 65535),
            end(),
            end(),
            end(),
        ]
        .concat(),
    );
    let edges = collect_edges(&mk_image(pkg));
    assert_eq!(edges, vec![(FORMSET_GUID.to_string(), 10001, 65535)]);
}

#[test]
fn collect_edges_empty_image() {
    let root = mk_node(None, FfsType::Image, 0, vec![], vec![]);
    let img = Image {
        image_id: "img".into(),
        session_id: "s".into(),
        root,
        mode: ImageMode::Read,
    };
    assert!(collect_edges(&img).is_empty());
}
}
```

(весь перечисленный выше код — тело `mod tests`; закрывающая скобка в конце; `FfsType`, `Image`, `FfsNode` приходят через `use super::*` после появления use-строк реализации в Step 3)

В `crates/uefi-engine/src/hii/mod.rs` между `pub mod questions;` (строка 13) и `pub mod schema;` (строка 14) добавить:

```rust
pub mod ref_tree;
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `cargo test -p uefi-engine ref_tree`
Expected: FAIL — compile error: cannot find function `package_edges` / `collect_edges` in this module.

- [ ] **Step 3: Реализовать**

В начало `ref_tree.rs` (ПЕРЕД `#[cfg(test)]`-блоком) добавить реализации и use-строки:

```rust
use crate::ffs::{
    EFI_SECTION_COMPRESSION, EFI_SECTION_GUID_DEFINED, EFI_SECTION_PE32, EFI_SECTION_RAW,
};
use crate::hii::ifr::parse_form_package;
use crate::hii::package_list::parse_package_list;
use crate::hii::pe_resource::{bare_form_packages, hii_resource_blobs, hii_resource_ranges};
use crate::hii::values::walk_statements;
use crate::types::{FfsNode, FfsType, Image, guid_to_upper_string};
use r_efi::hii::{IFR_REF_OP, PACKAGE_FORMS};

/// REF-рёбра одного form-пакета: в форме X каждый IFR_REF_OP с
/// length >= 15 даёт ребро X → FormId@+13 (паттерн чтения — gates.rs,
/// ветка Wraps::Ref). REF вне формы и короткие REF пропускаются;
/// дубликаты схлопываются. Спека tui-forms-view §3.5.
pub(crate) fn package_edges(pkg: &[u8]) -> Vec<(u16, u16)> {
    let mut out: Vec<(u16, u16)> = Vec::new();
    walk_statements(pkg, |op, off, len, current| {
        if op != IFR_REF_OP || len < 15 {
            return;
        }
        let Some(parent) = current else { return };
        let child = u16::from_le_bytes([pkg[off + 13], pkg[off + 14]]);
        if !out.contains(&(parent, child)) {
            out.push((parent, child));
        }
    });
    out
}

/// REF-рёбра всех form-пакетов образа (обход как у collect_forms:
/// RAW / PE-resource / bare, рекурсивно через compression). NOT
/// read-only-ограничен — работает в любом ImageMode; висячие цели
/// отдаются как есть. Спека tui-forms-view §3.5.
pub fn collect_edges(image: &Image) -> Vec<(String, u32, u32)> {
    let mut out = Vec::new();
    walk_files(&image.root, &mut out);
    out
}

fn walk_files(node: &FfsNode, out: &mut Vec<(String, u32, u32)>) {
    for child in &node.children {
        if child.node_type == FfsType::File {
            collect_file_edges(child, out);
        }
        walk_files(child, out);
    }
}

fn collect_file_edges(file: &FfsNode, out: &mut Vec<(String, u32, u32)>) {
    if file.guid.is_none() {
        return;
    }
    walk_sections(file, out);
}

fn walk_sections(node: &FfsNode, out: &mut Vec<(String, u32, u32)>) {
    for child in &node.children {
        if child.node_type != FfsType::Section {
            continue;
        }
        match child.subtype {
            EFI_SECTION_RAW => {
                if let Some(fs) = parse_form_package(&child.body) {
                    push_edges(&child.body, &fs.guid, out);
                }
            }
            EFI_SECTION_PE32 => {
                let ranges = hii_resource_ranges(&child.body);
                for blob in hii_resource_blobs(&child.body) {
                    let Some(list) = parse_package_list(blob) else {
                        continue;
                    };
                    for pkg in &list.packages {
                        if pkg.kind != PACKAGE_FORMS {
                            continue;
                        }
                        if let Some(fs) = parse_form_package(pkg.bytes) {
                            push_edges(pkg.bytes, &fs.guid, out);
                        }
                    }
                }
                for pkg in bare_form_packages(&child.body, &ranges) {
                    if let Some(fs) = parse_form_package(pkg) {
                        push_edges(pkg, &fs.guid, out);
                    }
                }
            }
            EFI_SECTION_COMPRESSION | EFI_SECTION_GUID_DEFINED => walk_sections(child, out),
            _ => {}
        }
    }
}

fn push_edges(pkg: &[u8], formset: &crate::types::Guid, out: &mut Vec<(String, u32, u32)>) {
    let guid = guid_to_upper_string(formset);
    for (parent, child) in package_edges(pkg) {
        let e = (guid.clone(), u32::from(parent), u32::from(child));
        if !out.contains(&e) {
            out.push(e);
        }
    }
}
```

Итоговая структура файла: use-строки реализации + реализации (выше) и `#[cfg(test)] mod tests` из Step 1 (ниже); типы `FfsNode`/`FfsType`/`Image` приходят в тесты через `use super::*`, `Guid`/`Action`/`ImageMode`/`ParsingData` — собственным use тестового блока.

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run: `cargo test -p uefi-engine ref_tree`
Expected: PASS (5 тестов).

- [ ] **Step 5: Clippy + полный прогон крейта**

Run: `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS, 0 warnings.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-engine/src/hii/ref_tree.rs crates/uefi-engine/src/hii/mod.rs
git commit -m "feat(uefi-engine): hii/ref_tree — REF-рёбра форм (IFR_REF FormId@+13 по паттерну gates.rs, walk_statements, дедуп; висячие цели как есть)"
```

---

### Task 2: proto `HiiFormTree` + engine handler + 3 мока — cargo test --all

**Files:**
- Modify: `crates/uefi-proto/proto/engine.proto` (rpc-строка после `HiiListForms` — строка 28; messages после `HiiListFormsResponse` — строка 174)
- Modify: `crates/uefi-engine/src/rpc/server.rs` (handler после `hii_list_forms`, строки 738–747)
- Modify: `crates/uefi-engine/src/hii/ref_tree.rs` (адаптация `collect_edges` → `Vec<uefi_proto::FormEdge>`)
- Modify: `crates/uefi-cli/tests/mock_server.rs` (мок после `hii_list_questions`, строки 311–318)
- Modify: `crates/uefi-gateway/tests/mock_server.rs` (мок после `hii_list_questions`, строка 247)
- Modify: `crates/uefi-tui/tests/mock_server.rs` (мок после `hii_list_questions`, строки 307–331)

**Interfaces:**
- Produces (для Task 4+):

```proto
rpc HiiFormTree(HiiFormTreeRequest) returns (HiiFormTreeResponse);
message FormEdge {
  string formset_guid = 1;
  uint32 parent_form_id = 2;
  uint32 form_id = 3;      // цель REF-вопроса
}
message HiiFormTreeRequest  { string image_id = 1; }
message HiiFormTreeResponse { repeated FormEdge edges = 1; }
```

- [ ] **Step 1: Proto — rpc-строка и messages**

В `crates/uefi-proto/proto/engine.proto`, в service-блоке сразу после строки 28 (`rpc HiiListForms(...)`) добавить с выравниванием колонок как у соседей:

```proto
  rpc HiiFormTree(HiiFormTreeRequest)                  returns (HiiFormTreeResponse);
```

Сразу после строки 174 (`message HiiListFormsResponse { repeated FormInfo forms = 1; }`) добавить блок из спеки §3.5 (дословно):

```proto
message FormEdge {
  string formset_guid = 1;
  uint32 parent_form_id = 2;
  uint32 form_id = 3;      // цель REF-вопроса
}
message HiiFormTreeRequest  { string image_id = 1; }
message HiiFormTreeResponse { repeated FormEdge edges = 1; }
```

- [ ] **Step 2: Адаптировать collect_edges под proto-тип**

В `crates/uefi-engine/src/hii/ref_tree.rs`: сигнатуру `pub fn collect_edges(image: &Image) -> Vec<(String, u32, u32)>` заменить на `pub fn collect_edges(image: &Image) -> Vec<uefi_proto::FormEdge>`; внутренние типы `Vec<(String, u32, u32)>` у `walk_files`/`collect_file_edges`/`walk_sections`/`push_edges` — на `Vec<uefi_proto::FormEdge>`; в `push_edges`:

```rust
fn push_edges(pkg: &[u8], formset: &crate::types::Guid, out: &mut Vec<uefi_proto::FormEdge>) {
    let guid = guid_to_upper_string(formset);
    for (parent, child) in package_edges(pkg) {
        let e = uefi_proto::FormEdge {
            formset_guid: guid.clone(),
            parent_form_id: u32::from(parent),
            form_id: u32::from(child),
        };
        if !out.contains(&e) {
            out.push(e);
        }
    }
}
```

Тесты: `assert_eq!(edges[0], (FORMSET_GUID.to_string(), 10001, 10019));` заменить на:

```rust
assert_eq!(
    edges[0],
    uefi_proto::FormEdge {
        formset_guid: FORMSET_GUID.to_string(),
        parent_form_id: 10001,
        form_id: 10019,
    }
);
```

(аналогично `edges[1]`, тест `collect_edges_dangling_target_not_dropped` — на `vec![uefi_proto::FormEdge { formset_guid: FORMSET_GUID.to_string(), parent_form_id: 10001, form_id: 65535 }]`).

- [ ] **Step 3: Handler в rpc/server.rs**

Сразу после `hii_list_forms` (строка 747, закрывающая `}`) добавить:

```rust
    #[tracing::instrument(skip(self, req), err)]
    async fn hii_form_tree(
        &self,
        req: Request<HiiFormTreeRequest>,
    ) -> RpcResult<HiiFormTreeResponse> {
        let r = req.into_inner();
        let img = self.get_or_load_image(&r.image_id).await?;
        let edges = crate::hii::ref_tree::collect_edges(&img);
        let _ = self.sm.touch(&img.session_id);
        tracing::info!(image_id = %r.image_id, count = edges.len(), "hii form tree");
        Ok(Response::new(HiiFormTreeResponse { edges }))
    }
```

Read-only RPC (без `flush_image`/`images.lock()`) — по образцу `hii_list_forms`/`hii_list_questions`.

- [ ] **Step 4: Прогон engine**

Run: `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS (uefi-proto перегенерируется build.rs автоматически).

- [ ] **Step 5: Моки — cli и gateway (пустые)**

В `crates/uefi-cli/tests/mock_server.rs` после метода `hii_list_questions` (строки 311–318) добавить:

```rust
    async fn hii_form_tree(
        &self,
        _req: Request<HiiFormTreeRequest>,
    ) -> Result<Response<HiiFormTreeResponse>, Status> {
        Ok(Response::new(HiiFormTreeResponse { edges: vec![] }))
    }
```

В `crates/uefi-gateway/tests/mock_server.rs` после метода `hii_list_questions` (строка 247) — тот же метод дословно.

- [ ] **Step 6: Мок — tui (fixture-рёбра под формы из мока)**

В `crates/uefi-tui/tests/mock_server.rs` после метода `hii_list_questions` (строки 307–331) добавить (ребро согласовано с fixture `hii_list_forms`: Main 10001 → Serial Port 1 Configuration 10019):

```rust
    async fn hii_form_tree(
        &self,
        _req: Request<HiiFormTreeRequest>,
    ) -> Result<Response<HiiFormTreeResponse>, Status> {
        Ok(Response::new(HiiFormTreeResponse {
            edges: vec![FormEdge {
                formset_guid: "11111111-2222-3333-4444-555555555555".into(),
                parent_form_id: 10001,
                form_id: 10019,
            }],
        }))
    }
```

- [ ] **Step 7: Полный workspace (урок 9933f59 — моки ловятся только здесь)**

Run: `cargo test --all && cargo clippy --all -- -D warnings && cargo fmt --all -- --check`
Expected: PASS везде; если fmt ругается на выравнивание proto/нового кода — поправить и повторить.

- [ ] **Step 8: Commit**

```bash
git add crates/uefi-proto/proto/engine.proto crates/uefi-engine/src/rpc/server.rs crates/uefi-engine/src/hii/ref_tree.rs crates/uefi-cli/tests/mock_server.rs crates/uefi-gateway/tests/mock_server.rs crates/uefi-tui/tests/mock_server.rs
git commit -m "feat(uefi-proto,uefi-engine): HiiFormTree RPC — рёбра REF-дерева форм; моки cli/tui/gateway (cargo test --all)"
```

---

### Task 3: engine — real-image гейт-тест form_tree

**Files:**
- Modify: `crates/uefi-engine/tests/real_image.rs` (новый `#[ignore]`-тест после `hii_list_questions_real_image_consistent_with_question_info`, строки 801–845)

**Interfaces:**
- Consumes: `uefi_engine::hii::forms::collect_forms`, `uefi_engine::hii::ref_tree::collect_edges`, хелперы файла `load_fw()`/`parse_image(...)` (уже есть в real_image.rs).

- [ ] **Step 1: Написать тест**

После теста `hii_list_questions_real_image_consistent_with_question_info` (строка 845) добавить:

```rust
#[test]
#[ignore = "requires external real BIOS image under refs/fw/ (gitignored)"]
fn form_tree_real_image_edges_consistent() {
    let data = load_fw();
    let img = parse_image(&data, ImageMode::Read, "img1", "s1").expect("parse_image");
    let forms = uefi_engine::hii::forms::collect_forms(&img);
    assert!(!forms.is_empty());

    let edges = uefi_engine::hii::ref_tree::collect_edges(&img);
    assert!(!edges.is_empty(), "HNX99TF Setup menu is REF-nested (V1 verdict)");

    let mut by_set: std::collections::HashMap<String, std::collections::HashSet<u32>> =
        std::collections::HashMap::new();
    for f in &forms {
        by_set
            .entry(f.formset_guid.clone())
            .or_default()
            .insert(f.form_id_ifr);
    }
    let mut dangling = 0usize;
    let mut live = 0usize;
    for e in &edges {
        let parent_ok = by_set
            .get(&e.formset_guid)
            .is_some_and(|ids| ids.contains(&e.parent_form_id));
        assert!(
            parent_ok,
            "edge parent {} must exist in formset {} (walker only walks existing forms)",
            e.parent_form_id, e.formset_guid
        );
        if by_set
            .get(&e.formset_guid)
            .is_some_and(|ids| ids.contains(&e.form_id))
        {
            live += 1;
        } else {
            dangling += 1;
        }
    }
    assert!(live > 0, "at least one non-dangling REF edge expected");
    println!(
        "form_tree real-image: {} edges ({} live, {} dangling), {} formsets with forms",
        edges.len(),
        live,
        dangling,
        by_set.len()
    );
}
```

- [ ] **Step 2: Прогон без образца (compiles) и с образом (если файл есть)**

Run: `cargo test -p uefi-engine --test real_image`
Expected: PASS (тест skipped как `#[ignore]`).
Если `refs/fw/HNX99TF_200525_original_E5C88C6F.bin` доступен: `cargo test -p uefi-engine --test real_image -- --ignored form_tree` — PASS.

- [ ] **Step 3: Clippy + commit**

Run: `cargo clippy -p uefi-engine -- -D warnings`
Expected: PASS.

```bash
git add crates/uefi-engine/tests/real_image.rs
git commit -m "test(uefi-engine): real-image гейт V2 — form_tree рёбра ненулевые, родители существуют, висячие цели допустимы"
```

---

### Task 4: TUI — fetch `HiiFormTree` + `reload_forms` (re-fetch с сохранением состояния)

**Files:**
- Modify: `crates/uefi-tui/src/app.rs` (`FormsData` + поле `edges`; import `FormEdge`)
- Modify: `crates/uefi-tui/src/commands.rs` (`refresh_forms` + edges; новая `reload_forms`)
- Test: `crates/uefi-tui/tests/tui_integration.rs`

**Interfaces:**
- Consumes: `client.inner.hii_form_tree(Request<HiiFormTreeRequest>)` (Task 2), `App::selected_form_key()`, `App::forms_sanitize_cursor()` (есть).
- Produces (для Task 6):

```rust
pub async fn reload_forms(app: &mut App, client: &mut Client) -> Result<(), String>;
```

Семантика: `refresh_forms` — вход (`:forms`, Tab, `:image switch`): полный сброс как V1 + edges; `reload_forms` — мутации: re-fetch forms+edges, СОХРАНЯЕТ `flat_mode`/`expanded`/`cursor` (по ключу выделенной формы)/`show_strings`/`strings*`, сбрасывает только per-form кэши (`questions`/`questions_key`; `question_cursor`/`gates`/`question_info` появятся в Task 7 и тоже сбрасываются там).

- [ ] **Step 1: FormsData.edges**

В `crates/uefi-tui/src/app.rs`: import `FormEdge` в строку `use uefi_proto::{...}` (строка 4); в `FormsData` после `pub forms: Vec<FormInfo>,` (строка 66) добавить:

```rust
    pub edges: Vec<FormEdge>,
```

(`Default`-derive даёт пустой Vec; поле заполняется только refresh/reload.)

- [ ] **Step 2: refresh_forms тянет edges**

В `crates/uefi-tui/src/commands.rs`, в `refresh_forms` (строки 763–785): в первом запросе (`HiiListFormsRequest`) заменить `image_id` на `image_id: image_id.clone()` (значение ещё понадобится второму запросу), а присваивание `app.forms.forms = resp.forms;` заменить блоком:

```rust
    let edges = client
        .inner
        .hii_form_tree(auth_req(
            &client.state,
            HiiFormTreeRequest { image_id },
        ))
        .await
        .map_err(|e| e.message().to_string())?
        .into_inner()
        .edges;
    app.forms.forms = resp.forms;
    app.forms.edges = edges;
```

(остальное тело — сбросы V1 — не меняется; в Task 5 Step 3 строка `app.forms.expanded = crate::forms::all_formset_guids(...)` сменит helper).

- [ ] **Step 3: reload_forms (красный путь — сначала тест)**

В `crates/uefi-tui/tests/tui_integration.rs` добавить:

```rust
#[tokio::test(flavor = "multi_thread")]
async fn forms_load_fetches_edges_and_reload_preserves_state() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let _handle = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    uefi_tui::commands::execute_command(&mut app, "forms", &mut client)
        .await
        .unwrap();

    assert_eq!(app.forms.edges.len(), 1, "mock fixture: Main -> Serial");
    assert_eq!(app.forms.edges[0].parent_form_id, 10001);
    assert_eq!(app.forms.edges[0].form_id, 10019);

    app.forms.cursor = 4;
    app.forms_set_expanded(false);
    assert_eq!(app.forms_rows().len(), 4, "SET-B collapsed hides 902");
    app.forms.cursor = 2;
    assert_eq!(app.selected_form_key().unwrap().form_id_ifr, 10019);
    uefi_tui::commands::refresh_questions_if_needed(&mut app, &mut client)
        .await
        .unwrap();
    assert_eq!(app.forms.questions.len(), 2, "кэш вопросов заполнен до reload");

    uefi_tui::commands::reload_forms(&mut app, &mut client)
        .await
        .unwrap();
    assert_eq!(app.forms.forms.len(), 3, "re-fetched");
    assert_eq!(app.forms.edges.len(), 1, "edges re-fetched");
    assert_eq!(app.forms_rows().len(), 4, "SET-B still collapsed");
    assert_eq!(
        app.selected_form_key().unwrap().form_id_ifr,
        10019,
        "cursor restored by form key"
    );
    assert!(app.forms.questions.is_empty(), "per-form cache reset");
}
```

Run: `cargo test -p uefi-tui --test tui_integration forms_load_fetches_edges`
Expected: FAIL — нет функции `reload_forms` (compile error).

- [ ] **Step 4: Реализовать reload_forms**

В `crates/uefi-tui/src/commands.rs` сразу после `refresh_forms` добавить:

```rust
/// Re-fetch форм И рёбер после мутации: сохраняет flat_mode,
/// развёрнутость, выделение (по formset+form_id), strings-браузер;
/// сбрасывает per-form кэши (questions). Вход во view —
/// refresh_forms (сброс), не эта функция. Спека tui-forms-view §3.3.
pub async fn reload_forms(app: &mut App, client: &mut Client) -> Result<(), String> {
    let image_id = app
        .active_image_id
        .clone()
        .or_else(|| client.state.active_image_id.clone())
        .ok_or("no active image")?;
    let sel = app
        .selected_form_key()
        .map(|k| (k.formset_guid, k.form_id_ifr));
    let forms = client
        .inner
        .hii_list_forms(auth_req(
            &client.state,
            HiiListFormsRequest {
                image_id: image_id.clone(),
            },
        ))
        .await
        .map_err(|e| e.message().to_string())?
        .into_inner()
        .forms;
    let edges = client
        .inner
        .hii_form_tree(auth_req(
            &client.state,
            HiiFormTreeRequest { image_id },
        ))
        .await
        .map_err(|e| e.message().to_string())?
        .into_inner()
        .edges;
    app.forms.forms = forms;
    app.forms.edges = edges;
    app.forms.questions.clear();
    app.forms.questions_key = None;
    match sel {
        Some((guid, fid)) => {
            let rows = app.forms_rows();
            let idx = rows.iter().position(|r| {
                matches!(
                    r,
                    crate::forms::FormsRow::Form { key, .. }
                        if key.formset_guid == guid && key.form_id_ifr == fid
                )
            });
            match idx {
                Some(i) => app.forms.cursor = i,
                None => app.forms_sanitize_cursor(),
            }
        }
        None => app.forms_sanitize_cursor(),
    }
    Ok(())
}
```

- [ ] **Step 5: Прогон + clippy**

Run: `cargo test -p uefi-tui && cargo clippy -p uefi-tui -- -D warnings`
Expected: PASS (новый тест зелёный, V1-тесты не сломаны).

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/app.rs crates/uefi-tui/src/commands.rs crates/uefi-tui/tests/tui_integration.rs
git commit -m "feat(uefi-tui): fetch HiiFormTree + reload_forms — re-fetch форм с сохранением состояния (курсор/развёрнутость/strings)"
```

---

### Task 5: TUI — REF-дерево строк (`build_tree_rows`) + рендер + `T`-переключатель

**Files:**
- Modify: `crates/uefi-tui/src/forms.rs` (display-модель: варианты `FormsRow`, `build_tree_rows`, `all_row_keys`)
- Modify: `crates/uefi-tui/src/app.rs` (`FormsData.flat_mode`, `forms_rows` dispatch, `forms_set_expanded`)
- Modify: `crates/uefi-tui/src/ui/forms.rs` (отступы по depth, dangling-маркер, Path в details)
- Modify: `crates/uefi-tui/src/main.rs` (клавиша `T`)
- Modify: `crates/uefi-tui/src/ui/mod.rs` (hint), `crates/uefi-tui/src/ui/help.rs` (help)
- Test: unit-тесты в `src/forms.rs`; integration в `tests/tui_integration.rs`

**Interfaces:**
- Consumes: `FormInfo`, `FormEdge` (uefi_proto), `HashSet<String>`-ключи развёрнутости: формсет — `"<guid>"`, форма — `"<guid>#<form_id_ifr>"`.
- Produces (для Task 7):

```rust
pub enum FormsRow {
    FormSet { guid: String, expanded: bool },
    Form { key: FormKey, visible: bool, depth: usize, path: String, has_children: bool },
    DanglingRef { form_id: u32, depth: usize },
}
pub fn build_tree_rows(forms: &[FormInfo], edges: &[FormEdge], expanded: &HashSet<String>) -> Vec<FormsRow>;
pub fn all_row_keys(forms: &[FormInfo], edges: &[FormEdge]) -> HashSet<String>;
```

Правила построения (спека §3.5): корни — формы формсета без входящих рёбер (в порядке появления — первая форма формсета естественно первой); после обхода корней непоказанные формы (участники циклов) добавляются корнями в порядке появления. Дети группируются по родителю (рёбра формсета, дедуп); форма с несколькими родителями рендерится у каждого; защита от циклов — множество «на пути» (повторный вход пропускается); цель без формы в формсете — строка `DanglingRef`. `path` — титулы от корня до самой формы включительно через " → " (спека §3.5: Details показывает полный путь `Main → Advanced → Serial Port 1 Configuration` для Serial).

- [ ] **Step 1: Красные unit-тесты display-модели**

В `crates/uefi-tui/src/forms.rs` в `mod tests` добавить (хелпер `fi` уже есть):

```rust
    fn edge(set: &str, parent: u32, child: u32) -> uefi_proto::FormEdge {
        uefi_proto::FormEdge {
            formset_guid: set.into(),
            parent_form_id: parent,
            form_id: child,
        }
    }

    #[test]
    fn tree_rows_nested_path_and_dangling() {
        let forms = vec![
            fi("t1", "S", 1, "Main", true),
            fi("t1", "S", 2, "Advanced", true),
            fi("t1", "S", 3, "Serial", false),
        ];
        let edges = vec![edge("S", 1, 2), edge("S", 2, 3), edge("S", 1, 99)];
        let ex = all_row_keys(&forms, &edges);
        let rows = build_tree_rows(&forms, &edges, &ex);
        assert_eq!(rows.len(), 5);
        assert!(matches!(&rows[1], FormsRow::Form { key, depth: 1, path, has_children: true, .. }
            if key.form_id_ifr == 1 && path == "Main"));
        assert!(matches!(&rows[2], FormsRow::Form { key, depth: 2, path, .. }
            if key.form_id_ifr == 2 && path == "Main → Advanced"));
        assert!(matches!(&rows[3], FormsRow::Form { key, depth: 3, path, visible: false, .. }
            if key.form_id_ifr == 3 && path == "Main → Advanced → Serial"));
        assert!(matches!(&rows[4], FormsRow::DanglingRef { form_id: 99, depth: 2 }));
    }

    #[test]
    fn tree_rows_cycle_does_not_loop_and_form_still_visible() {
        let forms = vec![
            fi("t1", "S", 1, "A", true),
            fi("t1", "S", 2, "B", true),
        ];
        let edges = vec![edge("S", 1, 2), edge("S", 2, 1)];
        let ex = all_row_keys(&forms, &edges);
        let rows = build_tree_rows(&forms, &edges, &ex);
        assert_eq!(
            rows.len(),
            3,
            "FormSet + A + B ровно один раз каждый (обратное ребро 2->1 пропущено)"
        );
        assert!(matches!(&rows[1], FormsRow::Form { key, .. } if key.form_id_ifr == 1));
        assert!(matches!(&rows[2], FormsRow::Form { key, depth: 2, .. } if key.form_id_ifr == 2));
    }

    #[test]
    fn tree_rows_multi_parent_rendered_under_each() {
        let forms = vec![
            fi("t1", "S", 1, "P1", true),
            fi("t1", "S", 2, "P2", true),
            fi("t1", "S", 3, "Kid", true),
        ];
        let edges = vec![edge("S", 1, 3), edge("S", 2, 3)];
        let ex = all_row_keys(&forms, &edges);
        let rows = build_tree_rows(&forms, &edges, &ex);
        let kid_rows = rows
            .iter()
            .filter(|r| matches!(r, FormsRow::Form { key, .. } if key.form_id_ifr == 3))
            .count();
        assert_eq!(kid_rows, 2, "форма с двумя родителями — у каждого");
    }

    #[test]
    fn tree_rows_collapsed_form_hides_children() {
        let forms = vec![
            fi("t1", "S", 1, "Main", true),
            fi("t1", "S", 2, "Adv", true),
        ];
        let edges = vec![edge("S", 1, 2)];
        let mut ex = all_row_keys(&forms, &edges);
        ex.remove("S#1");
        let rows = build_tree_rows(&forms, &edges, &ex);
        assert_eq!(rows.len(), 2, "FormSet + свёрнутая Main");
        assert!(matches!(&rows[1], FormsRow::Form { has_children: true, .. }));
    }

    #[test]
    fn tree_rows_edges_of_other_formset_ignored() {
        let forms = vec![
            fi("t1", "S", 1, "Main", true),
            fi("t2", "X", 9, "Other", true),
        ];
        let edges = vec![edge("X", 9, 1)];
        let ex = all_row_keys(&forms, &edges);
        let rows = build_tree_rows(&forms, &edges, &ex);
        assert_eq!(rows.len(), 5);
        assert!(matches!(&rows[1], FormsRow::Form { depth: 1, .. }), "ребро чужого формсета не вкладывает");
        assert!(
            matches!(&rows[4], FormsRow::DanglingRef { form_id: 1, depth: 2 }),
            "цель 1 — не форма формсета X: висячая строка под Other"
        );
    }

    #[test]
    fn all_row_keys_cover_formsets_and_parent_forms() {
        let forms = vec![
            fi("t1", "S", 1, "Main", true),
            fi("t1", "S", 2, "Adv", true),
            fi("t2", "X", 9, "Other", true),
        ];
        let edges = vec![edge("S", 1, 2)];
        let keys = all_row_keys(&forms, &edges);
        assert!(keys.contains("S"));
        assert!(keys.contains("X"));
        assert!(keys.contains("S#1"), "родительский узел развёрнут по умолчанию");
        assert!(!keys.contains("S#2"), "лист не нужен в наборе");
    }
```

Существующие V1-тесты `build_rows_*` продолжают строиться через `build_rows` (плоский режим) — их матчеры `FormsRow::Form { key, .. }` переживают добавление полей благодаря `..` (матчер `{ key, visible: false }` в `build_rows_groups_and_expands` дополняется `..`). NB: для компиляции крейта после замены `FormsRow` одновременно применяются arm `DanglingRef` в `ui/forms.rs::row_item` (полная замена — Step 4) и изменения `app.rs` из Step 3.1/3.3 (match в `forms_set_expanded` становится non-exhaustive); промежуточный зелёный прогон — после Step 3.

Run: `cargo test -p uefi-tui --lib forms`
Expected: FAIL — compile error: нет `build_tree_rows`/`all_row_keys`/полей/DanglingRef.

- [ ] **Step 2: Реализовать display-модель**

В `crates/uefi-tui/src/forms.rs`: import `FormEdge` (`use uefi_proto::{FormEdge, FormInfo};`), заменить определение `FormsRow` на:

```rust
#[derive(Debug, Clone)]
pub enum FormsRow {
    FormSet { guid: String, expanded: bool },
    Form {
        key: FormKey,
        visible: bool,
        depth: usize,
        path: String,
        has_children: bool,
    },
    DanglingRef { form_id: u32, depth: usize },
}
```

Добавить после `build_rows`:

```rust
/// Строки левой панели в режиме REF-дерева (спека §3.5): корни —
/// формы без входящих рёбер, затем непоказанные (циклы) в порядке
/// появления; кратные родители — у каждого; висячие REF-цели —
/// DanglingRef-строкой; циклы не зацикливают (множество на-пути).
pub fn build_tree_rows(
    forms: &[FormInfo],
    edges: &[FormEdge],
    expanded: &HashSet<String>,
) -> Vec<FormsRow> {
    let mut order: Vec<String> = Vec::new();
    let mut by_set: HashMap<String, Vec<&FormInfo>> = HashMap::new();
    for f in forms {
        if !by_set.contains_key(&f.formset_guid) {
            order.push(f.formset_guid.clone());
        }
        by_set.entry(f.formset_guid.clone()).or_default().push(f);
    }
    let mut rows = Vec::new();
    for guid in &order {
        rows.push(FormsRow::FormSet {
            guid: guid.clone(),
            expanded: expanded.contains(guid),
        });
        if !expanded.contains(guid) {
            continue;
        }
        let Some(list) = by_set.get(guid) else { continue };
        let mut children: HashMap<u32, Vec<u32>> = HashMap::new();
        let mut has_incoming: HashSet<u32> = HashSet::new();
        for e in edges {
            if e.formset_guid != *guid {
                continue;
            }
            let kids = children.entry(e.parent_form_id).or_default();
            if !kids.contains(&e.form_id) {
                kids.push(e.form_id);
            }
            has_incoming.insert(e.form_id);
        }
        let by_id: HashMap<u32, &FormInfo> =
            list.iter().map(|f| (f.form_id_ifr, *f)).collect();
        let mut emitted: HashSet<u32> = HashSet::new();
        let mut queue: Vec<u32> = list
            .iter()
            .filter(|f| !has_incoming.contains(&f.form_id_ifr))
            .map(|f| f.form_id_ifr)
            .collect();
        let mut reachable: HashSet<u32> = HashSet::new();
        let mut stack = queue.clone();
        while let Some(cur) = stack.pop() {
            if !reachable.insert(cur) {
                continue;
            }
            if let Some(kids) = children.get(&cur) {
                for child in kids {
                    if by_id.contains_key(child) {
                        stack.push(*child);
                    }
                }
            }
        }
        for f in list {
            let id = f.form_id_ifr;
            if !reachable.contains(&id) && !queue.contains(&id) {
                queue.push(id);
            }
        }
        for root in queue {
            if emitted.contains(&root) {
                continue;
            }
            let mut path: Vec<String> = Vec::new();
            let mut on_path: HashSet<u32> = HashSet::new();
            emit_form(
                &by_id,
                &children,
                root,
                guid,
                expanded,
                1,
                &mut path,
                &mut on_path,
                &mut emitted,
                &mut rows,
            );
        }
    }
    #[allow(clippy::too_many_arguments)]
    fn emit_form(
        by_id: &HashMap<u32, &FormInfo>,
        children: &HashMap<u32, Vec<u32>>,
        form_id: u32,
        guid: &str,
        expanded: &HashSet<String>,
        depth: usize,
        path: &mut Vec<String>,
        on_path: &mut HashSet<u32>,
        emitted: &mut HashSet<u32>,
        rows: &mut Vec<FormsRow>,
    ) {
        if on_path.contains(&form_id) {
            return;
        }
        let Some(f) = by_id.get(&form_id) else { return };
        let kids = children.get(&form_id).cloned().unwrap_or_default();
        path.push(f.title.clone());
        rows.push(FormsRow::Form {
            key: FormKey {
                target: f.form_id.clone(),
                formset_guid: guid.to_string(),
                form_id_ifr: f.form_id_ifr,
                title: f.title.clone(),
            },
            visible: f.visible,
            depth,
            path: path.join(" → "),
            has_children: !kids.is_empty(),
        });
        emitted.insert(form_id);
        if !kids.is_empty() && expanded.contains(&format!("{guid}#{form_id}")) {
            on_path.insert(form_id);
            for child in kids {
                if by_id.contains_key(&child) {
                    emit_form(
                        by_id, children, child, guid, expanded, depth + 1, path, on_path, emitted,
                        rows,
                    );
                } else {
                    rows.push(FormsRow::DanglingRef {
                        form_id: child,
                        depth: depth + 1,
                    });
                }
            }
            on_path.remove(&form_id);
        }
        path.pop();
    }
    rows
}

/// Полный набор ключей развёрнутости для входа во view: гуиды
/// формсетов + "guid#form_id" форм-родителей (полное расширение —
/// паритет с V1, где все формсеты были развёрнуты).
pub fn all_row_keys(forms: &[FormInfo], edges: &[FormEdge]) -> HashSet<String> {
    let mut set = all_formset_guids(forms);
    let parents: HashSet<(String, u32)> = edges
        .iter()
        .map(|e| (e.formset_guid.clone(), e.parent_form_id))
        .collect();
    for f in forms {
        if parents.contains(&(f.formset_guid.clone(), f.form_id_ifr)) {
            set.insert(format!("{}#{}", f.formset_guid, f.form_id_ifr));
        }
    }
    set
}
```

- [ ] **Step 3: App — flat_mode, dispatch, h/l на формах**

В `crates/uefi-tui/src/app.rs`:

1. В `FormsData` после `pub forms: Vec<FormInfo>,` добавить `pub flat_mode: bool,` (Default=false → режим дерева; соответствует решению спеки: дерево — основной, плоский — переключатель).
2. `forms_rows` (строки 289–291) заменить на:

```rust
    pub fn forms_rows(&self) -> Vec<crate::forms::FormsRow> {
        if self.forms.flat_mode {
            crate::forms::build_rows(&self.forms.forms, &self.forms.expanded)
        } else {
            crate::forms::build_tree_rows(&self.forms.forms, &self.forms.edges, &self.forms.expanded)
        }
    }
```

3. `forms_set_expanded` (строки 313–331): матч по строке заменить на:

```rust
        let key = match rows.get(self.forms.cursor) {
            Some(crate::forms::FormsRow::FormSet { guid, .. }) => Some(guid.clone()),
            Some(crate::forms::FormsRow::Form {
                key,
                has_children,
                ..
            }) => {
                if !self.forms.flat_mode && *has_children {
                    Some(format!("{}#{}", key.formset_guid, key.form_id_ifr))
                } else {
                    Some(key.formset_guid.clone())
                }
            }
            _ => None,
        };
```

(хвост функции — insert/remove + clamp — без изменений, кроме переименования биндинга: `if let Some(key) = key {`, т.к. новый матч биндит `key`).

4. В `commands.rs::refresh_forms` строку `app.forms.expanded = crate::forms::all_formset_guids(&app.forms.forms);` заменить на:

```rust
    app.forms.expanded = crate::forms::all_row_keys(&app.forms.forms, &app.forms.edges);
```

- [ ] **Step 4: Рендер — отступы, dangling, Path**

В `crates/uefi-tui/src/ui/forms.rs`:

1. `row_item` заменить на:

```rust
fn row_item(row: &FormsRow) -> ListItem<'static> {
    match row {
        FormsRow::FormSet { guid, expanded } => {
            let marker = if *expanded { "▾" } else { "▸" };
            ListItem::from(format!("{marker} FormSet {}", short_guid(guid)))
        }
        FormsRow::Form {
            key,
            visible,
            depth,
            ..
        } => {
            let vis = if *visible { "[V]" } else { "[H]" };
            ListItem::from(format!(
                "{}{:<6} {:<34} {vis}",
                " ".repeat(2 * depth),
                key.form_id_ifr,
                key.title
            ))
        }
        FormsRow::DanglingRef { form_id, depth } => ListItem::from(format!(
            "{}! {:<6} (dangling REF target)",
            " ".repeat(2 * depth),
            form_id
        )),
    }
}
```

2. В `details_text` после блока `Target:` добавить строку Path (данные берутся из строки, не из ключа — заменить первую часть функции):

```rust
fn details_text(app: &App) -> String {
    let rows = app.forms_rows();
    let Some(FormsRow::Form { key, path, .. }) = rows.get(app.forms.cursor) else {
        return "no form selected".into();
    };
    let mut s = format!(
        "Form:    {}\nForm ID: {}\nFormSet: {}\nTarget:  {}\n",
        key.title,
        key.form_id_ifr,
        short_guid(&key.formset_guid),
        key.target
    );
    if !path.is_empty() {
        s.push_str(&format!("Path:    {path}\n"));
    }
```

(хвост — блок Questions как в V1 — не меняется; в Task 7 details_text переезжает в `crate::forms::form_details_text`).

- [ ] **Step 5: Клавиша T + hint + help**

В `crates/uefi-tui/src/main.rs` в `handle_normal_forms` рядом с `AppEvent::Key('S')` добавить:

```rust
        AppEvent::Key('T') if !app.forms.show_strings => {
            app.forms.flat_mode = !app.forms.flat_mode;
            app.forms_sanitize_cursor();
        }
```

В `crates/uefi-tui/src/ui/mod.rs` hint-строку `NORMAL[Forms]: ...` заменить на:

```rust
                "NORMAL[Forms]: j/k move · h/l collapse/expand · v visibility · u unlock · T tree/flat · S strings · Tab image-view · :cmd · ?help · q"
```

(`v`/`u` появятся в Task 7 — hint обновляется опережающе, чтобы не править дважды; если ревьюер предпочтёт раздельно — вынести `v · u` из hint в Task 7 отдельным шагом).

В `crates/uefi-tui/src/ui/help.rs` блок `FORMS VIEW` заменить на:

```
FORMS VIEW (Tab / Shift-Tab, :forms / :image)
  j / k          move cursor (формы / строки)
  h / l          collapse / expand формсета (и формы в REF-дереве)
  T              плоский список <-> REF-дерево (путь в details)
  S              strings-браузер (повторно — закрыть; Esc тоже)
  /              открыть strings + промпт фильтра (:filter TEXT)
  Ctrl-hjkl      focus List <-> Details
  Tab            обратно в Image-view
```

- [ ] **Step 6: Integration-тест дерева на моке**

В `crates/uefi-tui/tests/tui_integration.rs` добавить:

```rust
#[tokio::test(flavor = "multi_thread")]
async fn forms_tree_mode_nested_path() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let _handle = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    uefi_tui::commands::execute_command(&mut app, "forms", &mut client)
        .await
        .unwrap();

    let rows = app.forms_rows();
    assert_eq!(rows.len(), 5, "SET1 + Main + Serial(вложенно) + SET2 + Platform");
    assert!(
        matches!(
            &rows[2],
            uefi_tui::forms::FormsRow::Form { key, depth: 2, path, .. }
                if key.form_id_ifr == 10019
                    && path == "Main → Serial Port 1 Configuration"
        ),
        "Serial вложена в Main, путь в details"
    );

    app.forms.flat_mode = true;
    let flat = app.forms_rows();
    assert!(
        matches!(&flat[2], uefi_tui::forms::FormsRow::Form { depth: 1, path, .. } if path.is_empty()),
        "плоский режим: без вложенности и пути"
    );
}
```

- [ ] **Step 7: Прогон + clippy + fmt**

Run: `cargo test -p uefi-tui && cargo clippy -p uefi-tui -- -D warnings && cargo fmt -p uefi-tui -- --check`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add crates/uefi-tui/src/forms.rs crates/uefi-tui/src/app.rs crates/uefi-tui/src/ui/forms.rs crates/uefi-tui/src/main.rs crates/uefi-tui/src/ui/mod.rs crates/uefi-tui/src/ui/help.rs crates/uefi-tui/src/commands.rs crates/uefi-tui/tests/tui_integration.rs
git commit -m "feat(uefi-tui): REF-дерево в Forms-view — build_tree_rows (вложенность, путь, висячие цели, циклы, кратные родители), T — плоский режим"
```

---

### Task 6: TUI — существительное `:hii`: глаголы `set-value` / `visibility` / `unlock`

**Files:**
- Modify: `crates/uefi-tui/src/commands.rs` (ветка `"hii"` в `execute_command`, `parse_u64_loose`, `COMMANDS`, `context_candidates`, unit-тесты)
- Modify: `crates/uefi-tui/tests/mock_server.rs` (fixture `hii_set_value`/`hii_unlock`)
- Test: `crates/uefi-tui/tests/tui_integration.rs`

**Interfaces:**
- Consumes: `reload_forms` (Task 4), `refresh_questions_if_needed` (переименовывается в Task 7 — здесь зовётся под текущим именем), RPC `HiiSetValueRequest{image_id, item_id, value: u64}`, `HiiSetFormVisibilityRequest{image_id, item_id, visible}`, `HiiUnlockResponse{gates, applied_flips}`.
- Produces (для Task 7 и V3):

```
:hii set-value <item_id> <value>    — value: 0x-hex или десятичное
:hii visibility <item_id> on|off
:hii unlock <item_id>
```

- [ ] **Step 1: Красные тесты — unit (парсер, completion)**

В `crates/uefi-tui/src/commands.rs` в `mod tests` добавить:

```rust
    #[test]
    fn parse_u64_loose_hex_and_dec() {
        assert_eq!(parse_u64_loose("0x1").unwrap(), 1);
        assert_eq!(parse_u64_loose("0xFF").unwrap(), 255);
        assert_eq!(parse_u64_loose("42").unwrap(), 42);
        assert!(parse_u64_loose("0xG").is_err());
        assert!(parse_u64_loose("").is_err());
    }

    #[test]
    fn complete_hii_verbs_and_item_ids() {
        let mut app = crate::app::App::new();
        app.forms.forms = vec![uefi_proto::FormInfo {
            form_id: "t:0x19:0".into(),
            formset_guid: "S".into(),
            form_id_ifr: 10001,
            title: "Main".into(),
            visible: true,
        }];
        let (_, opts) = complete(&app, "hii ");
        assert_eq!(
            opts,
            vec![
                "set-value".to_string(),
                "visibility".to_string(),
                "unlock".to_string()
            ]
        );
        let (rep, opts) = complete(&app, "hii set-value ");
        assert_eq!(rep.as_deref(), Some("hii set-value t:0x19:0#10001"));
        assert!(opts.is_empty(), "unique candidate completes directly, no menu");
    }
```

Run: `cargo test -p uefi-tui --lib commands`
Expected: FAIL — нет `parse_u64_loose`, `"hii"` не в COMMANDS.

- [ ] **Step 2: Реализовать — парсер, ветку hii, COMMANDS, completion**

В `crates/uefi-tui/src/commands.rs`:

1. Рядом с `mode_to_i32` (строка 123) добавить:

```rust
fn parse_u64_loose(s: &str) -> Result<u64, String> {
    if let Some(hex) = s.strip_prefix("0x") {
        u64::from_str_radix(hex, 16)
    } else {
        s.parse::<u64>()
    }
    .map_err(|e| format!("invalid value '{s}': {e}"))
}
```

2. В `execute_command` перед веткой `"filter"` (строка 534) добавить ветку:

```rust
        "hii" => {
            let sub = parts
                .get(1)
                .copied()
                .ok_or("usage: :hii set-value ITEM VALUE | :hii visibility ITEM on|off | :hii unlock ITEM")?;
            let iid = client
                .state
                .active_image_id
                .clone()
                .ok_or("no active image")?;
            match sub {
                "set-value" => {
                    let item = parts
                        .get(2)
                        .ok_or("usage: :hii set-value ITEM VALUE")?
                        .to_string();
                    let value = parse_u64_loose(
                        parts
                            .get(3)
                            .ok_or("usage: :hii set-value ITEM VALUE")?,
                    )?;
                    let r = client
                        .inner
                        .hii_set_value(auth_req(
                            &client.state,
                            HiiSetValueRequest {
                                image_id: iid,
                                item_id: item.clone(),
                                value,
                            },
                        ))
                        .await
                        .map_err(|e| e.message().to_string())?
                        .into_inner();
                    reload_forms(app, client).await?;
                    let _ = refresh_questions_if_needed(app, client).await;
                    let flips = if r.applied_flips.is_empty() {
                        "none".to_string()
                    } else {
                        r.applied_flips.join(" · ")
                    };
                    let stores = if r.stores.is_empty() {
                        "none".to_string()
                    } else {
                        r.stores.join(" · ")
                    };
                    app.status_msg =
                        format!("set {item}={value}: flips {flips} · stores {stores}");
                    Ok(item)
                }
                "visibility" => {
                    let item = parts
                        .get(2)
                        .ok_or("usage: :hii visibility ITEM on|off")?
                        .to_string();
                    let visible = match parts.get(3) {
                        Some(&"on") => true,
                        Some(&"off") => false,
                        _ => return Err("usage: :hii visibility ITEM on|off".into()),
                    };
                    client
                        .inner
                        .hii_set_form_visibility(auth_req(
                            &client.state,
                            HiiSetFormVisibilityRequest {
                                image_id: iid,
                                item_id: item.clone(),
                                visible,
                            },
                        ))
                        .await
                        .map_err(|e| e.message().to_string())?;
                    reload_forms(app, client).await?;
                    let _ = refresh_questions_if_needed(app, client).await;
                    app.status_msg = format!("visibility {item}: {}", if visible { "on" } else { "off" });
                    Ok(item)
                }
                "unlock" => {
                    let item = parts
                        .get(2)
                        .ok_or("usage: :hii unlock ITEM")?
                        .to_string();
                    let r = client
                        .inner
                        .hii_unlock(auth_req(
                            &client.state,
                            HiiUnlockRequest {
                                image_id: iid,
                                item_id: item.clone(),
                            },
                        ))
                        .await
                        .map_err(|e| e.message().to_string())?
                        .into_inner();
                    reload_forms(app, client).await?;
                    let _ = refresh_questions_if_needed(app, client).await;
                    app.status_msg = if r.applied_flips.is_empty() {
                        format!("unlock {item}: no flippable gates ({} gates)", r.gates.len())
                    } else {
                        format!("unlock {item}: {}", r.applied_flips.join(" · "))
                    };
                    Ok(item)
                }
                other => Err(format!("unknown hii subcommand: {other}")),
            }
        }
```

3. В `COMMANDS` (строка 568) добавить `"hii",` (после `"goto", "g",`).

4. В `context_candidates` (строка 641) перед блоком `let has_positional = ...` добавить:

```rust
    if cmd == "hii" {
        if head.len() == 1 {
            return ["set-value", "visibility", "unlock"]
                .iter()
                .filter(|c| c.starts_with(token))
                .map(|s| s.to_string())
                .collect();
        }
        if matches!(head[1], "set-value" | "visibility" | "unlock")
            && !head[2..].iter().any(|s| !s.starts_with("--"))
        {
            return app
                .forms
                .forms
                .iter()
                .map(|f| format!("{}#{}", f.form_id, f.form_id_ifr))
                .filter(|c| c.starts_with(token))
                .collect();
        }
        return vec![];
    }
```

- [ ] **Step 3: Мок-fixture ответов мутаций**

В `crates/uefi-tui/tests/mock_server.rs` заменить методы `hii_set_form_visibility` (оставить как есть — `Empty`), `hii_unlock` (строки 363–371) и `hii_set_value` (строки 378–383) на:

```rust
    async fn hii_unlock(
        &self,
        _req: Request<HiiUnlockRequest>,
    ) -> Result<Response<HiiUnlockResponse>, Status> {
        Ok(Response::new(HiiUnlockResponse {
            gates: vec![],
            applied_flips: vec!["pkg+0x1c: 01 00 -> ff ff".into()],
        }))
    }
```

```rust
    async fn hii_set_value(
        &self,
        _req: Request<HiiSetValueRequest>,
    ) -> Result<Response<HiiSetValueResponse>, Status> {
        Ok(Response::new(HiiSetValueResponse {
            question: Some(QuestionInfo {
                question_id: 0x210,
                kind: "one_of".into(),
                ..Default::default()
            }),
            applied_flips: vec!["pkg+0x3e: 01 -> 00".into()],
            stores: vec!["Setup".into()],
        }))
    }
```

- [ ] **Step 4: Integration-тест глаголов**

В `crates/uefi-tui/tests/tui_integration.rs` добавить:

```rust
#[tokio::test(flavor = "multi_thread")]
async fn hii_verbs_visibility_setvalue_unlock() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let _handle = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    uefi_tui::commands::execute_command(&mut app, "forms", &mut client)
        .await
        .unwrap();
    let item = "11111111-2222-3333-4444-555555555555:0x19:0#10001";

    uefi_tui::commands::execute_command(&mut app, &format!("hii visibility {item} off"), &mut client)
        .await
        .unwrap();
    assert!(app.status_msg.contains("visibility"));
    assert_eq!(app.forms.forms.len(), 3, "forms reloaded after mutation");

    uefi_tui::commands::execute_command(&mut app, &format!("hii set-value {item}:0x210 1"), &mut client)
        .await
        .unwrap();
    assert!(app.status_msg.contains("flips pkg+0x3e: 01 -> 00"));

    uefi_tui::commands::execute_command(&mut app, &format!("hii unlock {item}"), &mut client)
        .await
        .unwrap();
    assert!(app.status_msg.contains("unlock"));

    assert!(
        uefi_tui::commands::execute_command(&mut app, &format!("hii set-value {item} abc"), &mut client)
            .await
            .is_err(),
        "нечисловой value — ошибка"
    );
    assert!(
        uefi_tui::commands::execute_command(&mut app, "hii bogus x", &mut client)
            .await
            .is_err(),
        "неизвестный глагол — ошибка"
    );
}
```

- [ ] **Step 5: Прогон + clippy**

Run: `cargo test -p uefi-tui && cargo clippy -p uefi-tui -- -D warnings`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add crates/uefi-tui/src/commands.rs crates/uefi-tui/tests/mock_server.rs crates/uefi-tui/tests/tui_integration.rs
git commit -m "feat(uefi-tui): :hii set-value/visibility/unlock — мутации форм из TUI (reload_forms + статусы applied_flips/stores)"
```

---

### Task 7: TUI — интерактивные правки: `v`/`u` клавиши, Details-focus выбор вопроса, `question_info`+`gates` блоки, `Enter` → set-value

**Files:**
- Modify: `crates/uefi-tui/src/app.rs` (`FormsData`: `gates`, `question_cursor`, `question_info`, `question_info_key`; методы `forms_question_cursor_down/up`, `selected_question_id`, `selected_form_visible`)
- Modify: `crates/uefi-tui/src/commands.rs` (переименование `refresh_questions_if_needed` → `refresh_form_details_if_needed` + gates-fetch; новая `refresh_question_info_if_needed`; хелперы `selected_form_item_id`, `set_value_prefill`)
- Modify: `crates/uefi-tui/src/forms.rs` (переезд `details_text` → `pub fn form_details_text(...)` с блоками Question/Gates)
- Modify: `crates/uefi-tui/src/ui/forms.rs` (вызов `form_details_text`)
- Modify: `crates/uefi-tui/src/main.rs` (клавиши `v`, `u`; Details `j`/`k`/`Enter`)
- Modify: `crates/uefi-tui/src/ui/mod.rs` (hint Details), `src/ui/help.rs` (help)
- Modify: `crates/uefi-tui/tests/mock_server.rs` (fixture `hii_question_info` с echo item_id, `hii_gates_list`)
- Test: unit в `src/forms.rs`/`src/app.rs`; integration в `tests/tui_integration.rs`

**Interfaces:**
- Consumes: RPC `HiiGatesListRequest{image_id, item_id}` → `GateInfo{gate_kind, expression, flippable, ...}`; `HiiQuestionInfoRequest` → `QuestionInfo{question_id, kind, var_store_id, var_offset, width, min, max, step, options}`; `:hii`-глаголы (Task 6).
- Produces (для main.rs и тестов):

```rust
// commands.rs
pub fn selected_form_item_id(app: &App) -> Option<String>;      // "<target>#<form_id>"
pub fn set_value_prefill(app: &App) -> Option<String>;           // "hii set-value <target>#<form>:<qid-hex> "
pub async fn refresh_form_details_if_needed(app, client) -> Result<(), String>;  // questions + gates
pub async fn refresh_question_info_if_needed(app, client) -> Result<(), String>;
// app.rs
pub fn forms_question_cursor_down(&mut self);
pub fn forms_question_cursor_up(&mut self);
pub fn selected_question_id(&self) -> Option<u32>;
pub fn selected_form_visible(&self) -> Option<bool>;
// forms.rs
pub fn form_details_text(forms: &FormsData, rows: &[FormsRow], cursor: usize) -> String;
pub(crate) fn short_guid(guid: &str) -> &str;   // переезд из ui/forms.rs
```

- [ ] **Step 1: Красные unit-тесты (app + forms)**

В `crates/uefi-tui/src/app.rs` `mod tests` добавить:

```rust
    #[test]
    fn question_cursor_clamps_and_selected_question() {
        let mut app = App::new();
        app.forms.forms = vec![form_info("S", 1)];
        app.forms.expanded = ["S".into()].into();
        app.forms.questions_key = Some(crate::forms::FormKey {
            target: "t:0x19:0".into(),
            formset_guid: "S".into(),
            form_id_ifr: 1,
            title: "f1".into(),
        });
        app.forms.questions = vec![
            uefi_proto::QuestionSummary {
                question_id: 0x210,
                ..Default::default()
            },
            uefi_proto::QuestionSummary {
                question_id: 0x211,
                ..Default::default()
            },
        ];
        assert_eq!(app.selected_question_id(), Some(0x210));
        app.forms_question_cursor_down();
        assert_eq!(app.selected_question_id(), Some(0x211));
        app.forms_question_cursor_down();
        assert_eq!(app.selected_question_id(), Some(0x211), "clamp на последнем");
        app.forms_question_cursor_up();
        assert_eq!(app.selected_question_id(), Some(0x210));
    }

    #[test]
    fn selected_form_visible_reads_row() {
        let mut app = App::new();
        app.forms.forms = vec![form_info("S", 1), form_info("S", 2)];
        app.forms.expanded = ["S".into()].into();
        app.forms.cursor = 2;
        assert_eq!(app.selected_form_visible(), Some(true));
        app.forms.forms[1].visible = false;
        assert_eq!(app.selected_form_visible(), Some(false));
        app.forms.cursor = 0;
        assert_eq!(app.selected_form_visible(), None, "FormSet-строка не форма");
    }
```

В `crates/uefi-tui/src/forms.rs` `mod tests` добавить:

```rust
    fn gi(kind: &str, expr: &str, flippable: bool) -> uefi_proto::GateInfo {
        uefi_proto::GateInfo {
            gate_kind: kind.into(),
            expression: expr.into(),
            flippable,
            ..Default::default()
        }
    }

    #[test]
    fn form_details_text_path_marker_question_and_gates() {
        let forms = vec![
            fi("t1", "S", 1, "Main", true),
            fi("t1", "S", 2, "Serial", false),
        ];
        let edges = vec![edge("S", 1, 2)];
        let ex = all_row_keys(&forms, &edges);
        let rows = build_tree_rows(&forms, &edges, &ex);
        let mut fd = FormsData {
            questions: vec![
                uefi_proto::QuestionSummary {
                    question_id: 0x210,
                    kind: "one_of".into(),
                    prompt: "Serial Port".into(),
                    ..Default::default()
                },
                uefi_proto::QuestionSummary {
                    question_id: 0x211,
                    kind: "numeric".into(),
                    prompt: "Baud".into(),
                    ..Default::default()
                },
            ],
            questions_key: Some(FormKey {
                target: "t1".into(),
                formset_guid: "S".into(),
                form_id_ifr: 2,
                title: "Serial".into(),
            }),
            question_cursor: 1,
            question_info: Some(uefi_proto::QuestionInfo {
                question_id: 0x211,
                kind: "numeric".into(),
                var_store_id: 1,
                var_offset: 0x60,
                width: 1,
                min: 0,
                max: 255,
                step: 1,
                ..Default::default()
            }),
            gates: vec![gi("suppress", "eq(1, 1)", true)],
            ..Default::default()
        };
        let t = form_details_text(&fd, &rows, 2);
        assert!(t.contains("Path:    Main → Serial"));
        assert!(t.contains("> q0x211  numeric  Baud"), "курсор вопроса маркерится");
        assert!(t.contains("  q0x210  one_of  Serial Port"), "не выбранный — без маркера");
        assert!(t.contains("Question q0x211 (numeric):"));
        assert!(t.contains("range 0..=255 step 1"));
        assert!(t.contains("Gates (1):"));
        let gates_line = t
            .lines()
            .find(|l| l.contains("eq(1, 1)"))
            .expect("gates line present");
        assert!(gates_line.contains("suppress") && gates_line.contains("flippable"));

        fd.question_info = Some(uefi_proto::QuestionInfo {
            question_id: 0x210,
            kind: "one_of".into(),
            options: vec![
                uefi_proto::OptionEntry { string_id: 18, value: 0, flags: 0 },
                uefi_proto::OptionEntry { string_id: 17, value: 1, flags: 0 },
            ],
            ..Default::default()
        });
        let t = form_details_text(&fd, &rows, 2);
        assert!(t.contains("options: 0x0(sid 18) · 0x1(sid 17)"));
    }
```

(`FormsData` уже `Default`; `QuestionSummary`/`QuestionInfo`/`OptionEntry`/`GateInfo` — prost-типы с `Default`.)

Run: `cargo test -p uefi-tui --lib`
Expected: FAIL — нет методов/полей/`form_details_text`.

- [ ] **Step 2: App — поля и методы**

В `crates/uefi-tui/src/app.rs`:

1. В `FormsData` после `pub questions_key: ...` добавить:

```rust
    pub gates: Vec<GateInfo>,
    pub question_cursor: usize,
    pub question_info: Option<QuestionInfo>,
    pub question_info_key: Option<(crate::forms::FormKey, u32)>,
```

и дополнить import: `use uefi_proto::{ArtifactInfo, FormEdge, FormInfo, GateInfo, ImageInfo, QuestionInfo, QuestionSummary, StringInfo};`.

2. Рядом с `forms_cursor_*` добавить:

```rust
    pub fn forms_question_cursor_down(&mut self) {
        let n = self.forms.questions.len();
        if n > 0 && self.forms.question_cursor + 1 < n {
            self.forms.question_cursor += 1;
        }
    }

    pub fn forms_question_cursor_up(&mut self) {
        if self.forms.question_cursor > 0 {
            self.forms.question_cursor -= 1;
        }
    }

    pub fn selected_question_id(&self) -> Option<u32> {
        if self.forms.questions_key.is_none() {
            return None;
        }
        self.forms
            .questions
            .get(self.forms.question_cursor)
            .map(|q| q.question_id)
    }

    pub fn selected_form_visible(&self) -> Option<bool> {
        match self.forms_rows().get(self.forms.cursor) {
            Some(crate::forms::FormsRow::Form { visible, .. }) => Some(*visible),
            _ => None,
        }
    }
```

- [ ] **Step 3: commands — refresh с gates, question_info, prefill-хелперы**

В `crates/uefi-tui/src/commands.rs`:

1. `refresh_questions_if_needed` переименовать в `refresh_form_details_if_needed` (rustdoc-комментарий: «Вопросы и гейты выделенной формы: лениво, по смене FormKey; gates-ошибка не валит вопросы — в status_msg»). Тело расширить (полностью новая версия):

```rust
/// Вопросы И гейты выделенной формы — лениво, по смене FormKey
/// (кэш на одну форму). Ошибка gates-запроса не валит вопросы:
/// текст в status_msg, список гейтов пуст. Спека tui-forms-view §4 V2.
pub async fn refresh_form_details_if_needed(
    app: &mut App,
    client: &mut Client,
) -> Result<(), String> {
    let Some(key) = app.selected_form_key() else {
        return Ok(());
    };
    if app.forms.questions_key.as_ref() == Some(&key) {
        return Ok(());
    }
    let image_id = app
        .active_image_id
        .clone()
        .or_else(|| client.state.active_image_id.clone())
        .ok_or("no active image")?;
    let resp = client
        .inner
        .hii_list_questions(auth_req(
            &client.state,
            HiiListQuestionsRequest {
                image_id: image_id.clone(),
                target: key.target.clone(),
                form_id: key.form_id_ifr,
            },
        ))
        .await
        .map_err(|e| e.message().to_string())?
        .into_inner();
    app.forms.questions = resp.questions;
    app.forms.questions_key = Some(key.clone());
    app.forms.question_cursor = 0;
    app.forms.question_info = None;
    app.forms.question_info_key = None;
    let item_id = format!("{}#{}", key.target, key.form_id_ifr);
    match client
        .inner
        .hii_gates_list(auth_req(
            &client.state,
            HiiGatesListRequest {
                image_id,
                item_id,
            },
        ))
        .await
    {
        Ok(r) => app.forms.gates = r.into_inner().gates,
        Err(e) => {
            app.forms.gates.clear();
            app.status_msg = format!("gates: {}", e.message());
        }
    }
    Ok(())
}
```

2. Добавить новую:

```rust
/// Диапазон/options выбранного вопроса — лениво, по смене
/// (FormKey, question_id); кэш на один вопрос. Спека tui-forms-view
/// §4 V2 (подсказка при вводе set-value).
pub async fn refresh_question_info_if_needed(
    app: &mut App,
    client: &mut Client,
) -> Result<(), String> {
    let Some(key) = app.selected_form_key() else {
        return Ok(());
    };
    let Some(qid) = app.selected_question_id() else {
        app.forms.question_info = None;
        app.forms.question_info_key = None;
        return Ok(());
    };
    if app.forms.question_info_key.as_ref() == Some(&(key.clone(), qid)) {
        return Ok(());
    }
    let image_id = app
        .active_image_id
        .clone()
        .or_else(|| client.state.active_image_id.clone())
        .ok_or("no active image")?;
    let item_id = format!("{}#{}:{:#x}", key.target, key.form_id_ifr, qid);
    let resp = client
        .inner
        .hii_question_info(auth_req(
            &client.state,
            HiiQuestionInfoRequest { image_id, item_id },
        ))
        .await
        .map_err(|e| e.message().to_string())?
        .into_inner();
    app.forms.question_info = resp.question;
    app.forms.question_info_key = Some((key, qid));
    Ok(())
}
```

3. Добавить хелперы (рядом с `refresh_forms`):

```rust
/// item_id выделенной формы: "<target>#<form_id>" (form_id —
/// десятичное, контракт parse_item_id). None — если строка не форма.
pub fn selected_form_item_id(app: &App) -> Option<String> {
    let key = app.selected_form_key()?;
    Some(format!("{}#{}", key.target, key.form_id_ifr))
}

/// Insert-prefill для Enter на вопросе: вопрос из question_cursor.
/// item_id-контракт: form_id — ДЕСЯТИЧНОЕ (parse_item_id), qid — hex.
/// Спека tui-forms-view §4 V2, решение D6.
pub fn set_value_prefill(app: &App) -> Option<String> {
    let key = app.selected_form_key()?;
    let qid = app.selected_question_id()?;
    Some(format!(
        "hii set-value {}#{}:{:#x} ",
        key.target, key.form_id_ifr, qid
    ))
}
```

4. Заменить вызовы `refresh_questions_if_needed` на `refresh_form_details_if_needed` во ВСЕХ местах: 3 вызова в ветках `"hii"` (Task 6), 2 вызова в `main.rs::handle_normal_forms` (arms `j`/`k` при `FormsFocus::List`) и 2 вызова в существующих тестах `tests/tui_integration.rs` (`forms_command_switches_view_and_loads`, `forms_load_fetches_edges_and_reload_preserves_state`) — иначе test-crate не скомпилируется.
5. В `reload_forms` (Task 4) дополнить блок сброса per-form кэшей полями из Step 2 (`question_cursor` не существовал на момент Task 4):

```rust
    app.forms.gates.clear();
    app.forms.question_cursor = 0;
    app.forms.question_info = None;
    app.forms.question_info_key = None;
```

- [ ] **Step 4: forms.rs — form_details_text (переезд из ui)**

В `crates/uefi-tui/src/forms.rs`:

1. Перенести `fn short_guid` из `ui/forms.rs` (в `crate::forms`, `pub(crate)`); в `ui/forms.rs` — использовать `crate::forms::short_guid`.
2. Import `crate::app::FormsData`; добавить:

```rust
/// Текст правой панели Forms-view: заголовок формы, путь по
/// REF-дереву, вопросы с курсором, вопрос-подсказка (диапазон/
/// options), блок гейтов. Чистая функция от state — рендерит
/// ui/forms.rs. Спека tui-forms-view §3.2, §4 V2.
pub fn form_details_text(forms: &FormsData, rows: &[FormsRow], cursor: usize) -> String {
    let Some(FormsRow::Form { key, path, .. }) = rows.get(cursor) else {
        return "no form selected".into();
    };
    let mut s = format!(
        "Form:    {}\nForm ID: {}\nFormSet: {}\nTarget:  {}\n",
        key.title,
        key.form_id_ifr,
        short_guid(&key.formset_guid),
        key.target
    );
    if !path.is_empty() {
        s.push_str(&format!("Path:    {path}\n"));
    }
    if forms.questions_key.as_ref() == Some(key) {
        s.push_str(&format!(
            "\nQuestions ({}) — qid · kind · prompt:\n",
            forms.questions.len()
        ));
        for (i, q) in forms.questions.iter().enumerate() {
            let marker = if i == forms.question_cursor { ">" } else { " " };
            let prompt = if q.prompt.is_empty() { "-" } else { &q.prompt };
            s.push_str(&format!("{marker} q{:#x}  {}  {}\n", q.question_id, q.kind, prompt));
        }
        if let Some(qi) = &forms.question_info {
            s.push_str(&format!("\nQuestion q{:#x} ({}):\n", qi.question_id, qi.kind));
            s.push_str(&format!(
                "  store {} · offset {:#x} · width {}\n",
                qi.var_store_id, qi.var_offset, qi.width
            ));
            match qi.kind.as_str() {
                "numeric" => {
                    s.push_str(&format!("  range {}..={} step {}\n", qi.min, qi.max, qi.step));
                }
                "one_of" => {
                    let opts: Vec<String> = qi
                        .options
                        .iter()
                        .map(|o| format!("{:#x}(sid {})", o.value, o.string_id))
                        .collect();
                    s.push_str(&format!("  options: {}\n", opts.join(" · ")));
                }
                _ => {}
            }
        }
        if !forms.gates.is_empty() {
            s.push_str(&format!("\nGates ({}):\n", forms.gates.len()));
            for g in &forms.gates {
                let flip = if g.flippable { "flippable" } else { "-" };
                s.push_str(&format!("  {:<8} {:<24} {}\n", g.gate_kind, g.expression, flip));
            }
        }
    } else {
        s.push_str("\nQuestions: loading…\n");
    }
    s
}
```

3. В `ui/forms.rs` удалить локальный `details_text` и `short_guid`; вызов в `render` заменить на:

```rust
    let text = crate::forms::form_details_text(&app.forms, &rows, app.forms.cursor);
```

(строка `let text = details_text(app);` — заменить; `rows` уже вычислен выше в `render`).

- [ ] **Step 5: main.rs — клавиши v/u, Details j/k/Enter**

В `crates/uefi-tui/src/main.rs` в `handle_normal_forms`:

1. Добавить рядом с `AppEvent::Key('S')`:

```rust
        AppEvent::Key('v') if !app.forms.show_strings => {
            if let (Some(item), Some(vis)) =
                (commands::selected_form_item_id(app), app.selected_form_visible())
            {
                let cmd = format!("hii visibility {item} {}", if vis { "off" } else { "on" });
                if let Some(c) = client.as_mut()
                    && let Err(e) = commands::execute_command(app, &cmd, c).await
                {
                    app.status_msg = format!("error: {e}");
                }
            }
        }
        AppEvent::Key('u') if !app.forms.show_strings => {
            if let Some(item) = commands::selected_form_item_id(app) {
                let cmd = format!("hii unlock {item}");
                if let Some(c) = client.as_mut()
                    && let Err(e) = commands::execute_command(app, &cmd, c).await
                {
                    app.status_msg = format!("error: {e}");
                }
            }
        }
```

2. Добавить Details-навигацию (после существующих List-веток `j`/`k`):

```rust
        AppEvent::Key('j') | AppEvent::Down
            if app.forms.focus == FormsFocus::Details && !app.forms.show_strings =>
        {
            app.forms_question_cursor_down();
            if let Some(c) = client.as_mut() {
                let _ = commands::refresh_question_info_if_needed(app, c).await;
            }
        }
        AppEvent::Key('k') | AppEvent::Up
            if app.forms.focus == FormsFocus::Details && !app.forms.show_strings =>
        {
            app.forms_question_cursor_up();
            if let Some(c) = client.as_mut() {
                let _ = commands::refresh_question_info_if_needed(app, c).await;
            }
        }
        AppEvent::Enter
            if app.forms.focus == FormsFocus::Details && !app.forms.show_strings =>
        {
            if let Some(pre) = commands::set_value_prefill(app) {
                app.enter_insert_mode("hii", pre);
            }
        }
```

- [ ] **Step 6: hint + help**

В `crates/uefi-tui/src/ui/mod.rs`: дополнить импорт `use crate::app::App;` до `use crate::app::{App, FormsFocus};`, после чего ветку `Mode::Normal if app.view == View::Forms && !show_strings` (строка из Task 5) заменить на:

```rust
            crate::app::Mode::Normal if app.view == crate::app::View::Forms => {
                if app.forms.show_strings {
                    "NORMAL[Forms/Strings]: j/k move · /filter · S/Esc close · Ctrl-hjkl focus · :cmd · ?help · q"
                } else if app.forms.focus == FormsFocus::Details {
                    "NORMAL[Forms/Details]: j/k вопрос · Enter set-value · Tab image-view · :cmd · ?help · q"
                } else {
                    "NORMAL[Forms]: j/k move · h/l collapse/expand · v visibility · u unlock · T tree/flat · S strings · Tab image-view · :cmd · ?help · q"
                }
            }
```

В `crates/uefi-tui/src/ui/help.rs` блок `FORMS VIEW` дополнить строками:

```
  v              toggle visibility выбранной формы (:hii visibility)
  u              unlock выбранной формы (:hii unlock)
  Details focus: j/k выбор вопроса · Enter → :hii set-value <item> <value>
```

и в `EX-COMMANDS` добавить:

```
  :hii set-value ITEM VALUE   (ITEM = target#form[:qid], form — десятичное)
  :hii visibility ITEM on|off
  :hii unlock ITEM
```

- [ ] **Step 7: Мок-fixture question_info (echo) и gates**

В `crates/uefi-tui/tests/mock_server.rs` заменить `hii_question_info` (строки 372–377) и `hii_gates_list` (строки 357–362):

```rust
    async fn hii_gates_list(
        &self,
        _req: Request<HiiGatesListRequest>,
    ) -> Result<Response<HiiGatesListResponse>, Status> {
        Ok(Response::new(HiiGatesListResponse {
            gates: vec![GateInfo {
                gate_kind: "suppress".into(),
                wraps: "form".into(),
                form_id: 10001,
                expression: "eq(1, 1)".into(),
                flippable: true,
                ..Default::default()
            }],
        }))
    }
```

```rust
    async fn hii_question_info(
        &self,
        req: Request<HiiQuestionInfoRequest>,
    ) -> Result<Response<HiiQuestionInfoResponse>, Status> {
        let r = req.into_inner();
        let qid = r
            .item_id
            .rsplit(':')
            .next()
            .and_then(|s| u32::from_str_radix(s.trim_start_matches("0x"), 16).ok())
            .unwrap_or(0x210);
        Ok(Response::new(HiiQuestionInfoResponse {
            question: Some(QuestionInfo {
                question_id: qid,
                kind: "one_of".into(),
                var_store_id: 1,
                var_offset: 0x5F,
                width: 1,
                options: vec![
                    OptionEntry { string_id: 18, value: 0, flags: 0 },
                    OptionEntry { string_id: 17, value: 1, flags: 0 },
                ],
                ..Default::default()
            }),
        }))
    }
```

- [ ] **Step 8: Integration-тест**

В `crates/uefi-tui/tests/tui_integration.rs` добавить:

```rust
#[tokio::test(flavor = "multi_thread")]
async fn forms_details_question_info_gates_and_prefill() {
    let td = tempfile::TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    let _handle = mock_server::start_mock(&sock).await;
    let state = uefi_common::State {
        session_id: Some("s1".into()),
        token: Some("t1".into()),
        active_image_id: None,
        sock_path: Some(sock.display().to_string()),
    };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client)
        .await
        .unwrap();
    uefi_tui::commands::execute_command(&mut app, "forms", &mut client)
        .await
        .unwrap();

    app.forms_cursor_down();
    assert_eq!(
        uefi_tui::commands::selected_form_item_id(&app).unwrap(),
        "11111111-2222-3333-4444-555555555555:0x19:0#10001"
    );
    uefi_tui::commands::refresh_form_details_if_needed(&mut app, &mut client)
        .await
        .unwrap();
    assert_eq!(app.forms.questions.len(), 2);
    assert_eq!(app.forms.gates.len(), 1, "gates пришли вместе с вопросами");

    app.forms.focus = uefi_tui::app::FormsFocus::Details;
    app.forms_question_cursor_down();
    uefi_tui::commands::refresh_question_info_if_needed(&mut app, &mut client)
        .await
        .unwrap();
    let qi = app.forms.question_info.as_ref().unwrap();
    assert_eq!(qi.question_id, 0x211, "мок эхом возвращает qid из item_id");
    assert_eq!(app.selected_question_id(), Some(0x211));

    assert_eq!(
        uefi_tui::commands::set_value_prefill(&app).unwrap(),
        "hii set-value 11111111-2222-3333-4444-555555555555:0x19:0#10001:0x211 "
    );
}
```

- [ ] **Step 9: Прогон + clippy + fmt**

Run: `cargo test -p uefi-tui && cargo clippy -p uefi-tui -- -D warnings && cargo fmt -p uefi-tui -- --check`
Expected: PASS.

- [ ] **Step 10: Commit**

```bash
git add crates/uefi-tui/src/app.rs crates/uefi-tui/src/commands.rs crates/uefi-tui/src/forms.rs crates/uefi-tui/src/ui/forms.rs crates/uefi-tui/src/main.rs crates/uefi-tui/src/ui/mod.rs crates/uefi-tui/src/ui/help.rs crates/uefi-tui/tests/mock_server.rs crates/uefi-tui/tests/tui_integration.rs
git commit -m "feat(uefi-tui): интерактивные правки — v/u клавиши, Details-focus выбор вопроса, question_info/gates в деталях, Enter → set-value prefill"
```

---

### Task 8: финал — полный workspace + аддендум гейта V2 + статусы docs

**Files:**
- Modify: `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md` (новый §8 — сценарий гейта V2)
- Modify: `TODO.md` (закрыть backlog-пункты вердикта V1: REF-дерево, дизамбигуация титулов путём)
- Modify: `roadmap.md` (дуга TUI: V2 исполнена, гейт pending)

**Interfaces:**
- Consumes: всё из Task 1–7.

- [ ] **Step 1: Полный workspace**

Run: `cargo test --all && cargo clippy --all -- -D warnings && cargo fmt --all -- --check`
Expected: PASS (это же — условие PR).

- [ ] **Step 2: Спека — аддендум §8 (сценарий гейта для владельца)**

В `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md` после §7 (вердикт V1) добавить:

```markdown
## 8. Гейт V2 — сценарий для владельца (аддендум, 2026-09-11)

Engine-часть автоматизирована (`real_image.rs::form_tree_real_image_edges_consistent`,
#[ignore]). Ручная TUI-часть (на живом движке и HNX99TF; байт-в-байт сравнения —
sha256sum):

1. `cargo run -p uefi-engine`, в другом терминале `cargo run -p uefi-tui`;
   `:open <HNX99TF> --mode write`; `Tab` → Forms-view.
2. REF-дерево: «Serial Port 1 Configuration» вложена в «Advanced» (отступ), в
   details — путь «… → Advanced → Serial Port 1 Configuration»; сверка parent
   с `uefi-cli hii form list`.
3. Две «Boot» в формсете 7B59104A-C00D различимы путём в details.
4. `T` — плоский режим (V1-вид) и обратно; сумма Form-строк дерева ≥
   `hii form list` по формсету (кратные родители учитываются дважды).
5. Висячие REF-цели помечены «! … (dangling REF target)»; навигация не
   зацикливается на циклических рёбрах.
6. `v` на видимой форме → маркер `[H]`, статус «visibility …: off»; сверка с
   `uefi-cli hii form list` после правки.
7. Unlock-паритет: `:snapshot`, `u` на форме, `:save /tmp/tui-unlock.bin`;
   `:restore`, из CLI `uefi-cli hii form unlock <item>` + сохранение образа;
   `sha256sum` обоих файлов совпадает.
8. Set-value-паритет: `:snapshot`, Details-focus → `j` до вопроса → `Enter`
   (prefill `hii set-value …`), ввести значение, `:save /tmp/tui-setval.bin`;
   `:restore`, CLI `uefi-cli hii question set-value <item> <value>` +
   сохранение; sha256 совпадает. Перед вводом details показывает диапазон/
   options (подсказка).

Вердикт (все пункты да/нет + скриншоты) — аддендумом сюда; при негативе —
бисект по задаче плана. Успех = переход к плану V3 (операции добавления).
```

- [ ] **Step 3: TODO.md и roadmap.md**

`TODO.md`: в секции находок вердикта V1 (пункты 1–2 о плоских формах и одинаковых титулах) пометить «закрыто в V2 (REF-дерево + путь)» со ссылкой на коммит Task 5; если пункты лежат в backlog-списке — убрать с пометкой в истории коммита.

`roadmap.md`: в секции «Дуга TUI Revival + Forms View» у цикла B отметить «V2 исполнена (REF-дерево + правки на месте), гейт V2 pending» со ссылкой на план `2026-09-11-tui-forms-v2.md`.

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/specs/2026-09-11-tui-forms-view-design.md TODO.md roadmap.md
git commit -m "docs(spec,todo,roadmap): сценарий гейта V2 (аддендум §8) — REF-дерево/правки исполнены, гейт pending"
```

- [ ] **Step 5: Финальная проверка ветки**

Run: `cargo test --all && cargo clippy --all -- -D warnings && cargo fmt --all -- --check && git log --oneline master..feat/tui-forms-v2`
Expected: всё зелёное; история — 8 коммитов задач + docs-fixes (если были rule-11).

После этого — `superpowers:finishing-a-development-branch` (PR `feat/tui-forms-v2` → master), затем гейт V2 у владельца на живом образе.

---

## Self-Review (выполнен при написании плана)

1. **Покрытие спеки §4 V2 + §3.5:** REF-дерево — engine (Tasks 1–3: walker, RPC, real-image гейт) + TUI (Tasks 4–5: edges fetch, `build_tree_rows` с путём/висячими/циклами/кратными родителями, `T`-переключатель); `v` visibility (Task 7), `u` unlock (Task 7); applied_flips в status_msg + re-fetch (Task 6 `reload_forms`); блок гейтов из `HiiGatesList` (Task 7); `Enter` → Insert-промпт `hii set-value` (Task 7); диапазон/options из `HiiQuestionInfo` перед вводом (Task 7); существительное `:hii` с глаголом `set-value` (Task 6). Гейт V2: sha256-паритет unlock/set-value — сценарий §8 (Task 8); visible-маркер — Task 6 + §8; «Serial видит родителя»/«две Boot путём»/сумма узлов/висячие помечены/циклы — Task 5 (+ unit-тесты) и §8.
2. **Плейсхолдеры:** отсутствуют; все шаги содержат полный код. Найденные при self-review дефекты исправлены в тексте плана: `set_value_prefill` — item_id с десятичным form_id и hex-qid (контракт `parse_item_id`, `hii/mod.rs:173`); из integration-теста Task 5 убрана ошибочная строка; assert гейтов в Task 7 не зависит от точных пробелов; `refresh_forms` клонирует `image_id` для второго запроса; переименование `refresh_form_details_if_needed` покрывает все 5 call-sites (3 в `:hii` + 2 в main.rs).
3. **Типы:** `collect_edges` — переходный кортеж `(String, u32, u32)` в Task 1 → `uefi_proto::FormEdge` в Task 2 (адаптация прописана); `FormsRow::Form` расширяется полями `depth/path/has_children` — V1-тесты переживают через `..`; `FormsData` новые поля с `Default`-derive; mock-типы `QuestionInfo`/`OptionEntry`/`GateInfo`/`QuestionSummary` — prost-`Default`.

---

## Аддендум исполнения (post-plan, по запросу владельца)

После завершения Tasks 1–8 и финального ревью ветки владелец запросил две полировочные правки UI (спеку не затрагивают, вне объёма §4):

1. **Треугольник вложенности у форм с чилдами** — `FormsRow::Form` получил поле `expanded` (из lookup `guid#form_id` в `build_tree_rows`; flat-режим — `false`), рендер показывает `▾`/`▸` как у формсетов, висячие цели выровнены по колонке id. Коммит `be899dc`.
2. **Пиктограммы типов вопросов в Details** — `theme::question_icon(kind)` (Nerd Font, аналог `type_icon` из Image View): one_of `\u{F0CA}`, checkbox `\u{F14A}`, numeric `\u{F1EC}`, фолбэк — глиф нераспознанной секции `\u{F016}`; иконка в строках вопросов и в заголовке Question info. Коммит `983bc7b`.

Плюс fix-волна финального ревью: `c8f2d39` — гейт `selected_question_id` по равенству `questions_key` выделенной форме (кросс-форменный prefill), `5f1f357` — TODO-бэклог (закрыт `forms_sanitize_cursor`, записаны отложенные находки).

Итог ветки: 18 коммитов, `cargo test --all` 732 passed / 0 failed / 44 ignored, clippy `-D warnings` и fmt чистые.

### Правки по итогам гейта V2 (владелец, живой образ)

Гейт §8 вскрыл два блокера и одну читабельность-правку (спеку не меняют, план-уровень):

1. **Тексты опций one_of** — Details показывал `0x0(sid 5) · 0x1(sid 253)…`, выбрать значение было невозможно. Движок теперь резолвит тексты опций тем же механизмом, что промпты (`prompt_texts` по string-пакетам файла формсета), proto `OptionEntry` получил `string text = 4`; TUI рендерит `0x0 "Disabled" · 0x1 "Enabled"` (sid — фолбэк), CLI печатает тексты (TSV-колонка + text-формат). Коммит `7154bc4` + `f5cf0f7`.
2. **`v` на видимой форме** — слал `hii visibility … off` и получал ошибку движка («hiding is not implemented: only unsuppress exists»). Семантика `v` закреплена как unsuppress: на `[V]` — статус-подсказка без RPC, на `[H]` — `on` как раньше; hint/help обновлены. Решение вынесено в тестируемый хелпер `commands::form_visibility_command`. Коммит `f5cf0f7`.
3. **Человекочитаемый порядок в списке вопросов** — строка `q0x22  checkbox  Serial Port` → `Serial Port (q0x22) (checkbox)` (prompt вперёд), заголовок `Questions (N) — qid · kind · prompt:` → `Prompt · Questions (N) — qid:`. Коммит `88e15c0`.

Итог после правок: `cargo test --all` 734 passed / 0 failed / 44 ignored, clippy/fmt чистые.

### Правки по итогам гейта V2, раунд 2 (владелец, живой образ)

1. **Опции one_of — по одной на строку** (`options: 0x0 "Auto"` / перенос `0x1 "IO=3F8h; IRQ=4;"` …, выравнивание 11 пробелов) + `Wrap { trim: false }` на Details-параграф — длинные строки пер переносятся, а не режутся. Коммит `d43ddb9`.
2. **Статус-бар показывает id активного образа** (`image:815ae862`) вместо безымянного `loaded`; `:image switch` теперь ставит `image_loaded` (раньше после выбора образа из реестра статус писал `image:none`). Коммит `7955475`.
3. **`h`/`l` в tree-режиме** — сворачивают ближайшего складного предка (родительская форма через REF-ребро, не формсет напрямую) и пересаживают курсор на выжившую строку выделения/предка вместо прыжка на тот же индекс в чужом формсете. Коммит `acdea76`.

Итог: `cargo test --all` 738 passed / 0 failed / 44 ignored.

### Правки по итогам гейта V2, раунд 3 (владелец, живой образ)

1. **engine: повторный unlock образа с уже применённым флипом** — `unlock()` звал строгий `plan_gates`, который трактует гейт `eq(q, 0xFFFF)` (уже разблокированный класс) как ошибку «not a hardware-validated flip class». Переведён на `plan_gates_skip_unlocked` (как в `form_hijack`): уже разблокированные гейты пропускаются, байты не трогаются, гейт остаётся в infos.
2. **TUI: h/l из Details-focus больше не рулят левым списком** — гейт `focus == List`; после h/l/T вызывается `refresh_form_details_if_needed` (уходит зависшее «Questions: loading…» при смене выделенной формы сворачиванием/переключением режима).
3. **TUI: set-value сохраняет курсор вопроса** — после reload+refresh курсор восстанавливается на редактированном qid (+ refresh question_info).
4. **TUI: каретка ▌ в командной строке** — видно, куда вводится текст в Insert/Command.

### Правки по итогам гейта V2, раунд 4 (владелец, живой образ)

Верификация сохранённого файла пробником через сам движок: повторный
`set_value(…:0x23, 0x5)` — no-op (значение на месте в обоих StdDefaults-сторах,
`store+0x63: 05 -> 04` при записи другого значения); `unlock` на сохранённом
образе — 1 гейт, applied пусто (skip-unlocked из раунда 3); ~2.88 млн прочих
байтовых отличий от оригинала — перекомпрессия LZMA-секций при rebuild
(контент-эквивалентно).

1. **`:save` с относительным путём** — путь абсолютизируется от CWD клиента
   (`absolutize_path`), т.к. файл физически пишет engine-процесс и путь
   резолвился от каталога его запуска. 
2. **TODO**: read-back текущих значений вопросов (нет RPC get-value) — V3-кандидат.

### Правки по итогам гейта V2, раунд 5 (владелец, CLI-часть §8)

Владелец готовил CLI-сторону паритета и обнаружил: вопросы формы в CLI
перечислить было нельзя (RPC `HiiListQuestions` не экспонирован), а грамматика
item_id нигде не описана.

1. **`hii question list <target>#<form_id>`** — новый глагол CLI: TSV с header
   `item_id question_id kind prompt var_store_id var_offset width` и text-режим,
   где каждая строка — готовый item_id для копипаста в `question info/set-value/
   gates/unlock`. Мок CLI получил фикстуру вопросов, интеграционный тест
   проверяет оба формата.
2. Грамматика item_id задокументирована в `--help` глагола и rustdoc
   `question_list`.

### Правки по итогам гейта V2, раунд 6 (владелец)

**hii-легенда в stderr** (по образцу `format_legend` из `node list --tree`):
`hii form list` и `hii question list` в text-режиме печатают в stderr
грамматику item_id, расшифровку типов секций, встречающихся в выводе
(динамически, отсортировано), и для вопросов — подсказки kind/set-value.
TSV/JSON не тронуты (скрипты). Коммит легенды — в истории ветки.

Раунд 6 (продолжение): легенда form list дополнена блоком Columns
(form_id = target-часть item_id, formset_guid, form_id_ifr — десятичный IFR id,
title, visible = suppression state); question list — подсказка про prompt "-".
