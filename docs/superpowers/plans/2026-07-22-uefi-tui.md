# UEFI TUI (цикл 3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать TUI-фронтенд `uefi-tui` для навигации и редактирования UEFI-образов: vim-like режимы, 3-панельный layout, Nerd Font иконки + цвета, `:`-command-line для операций движка. Извлечь общий код в `uefi-common`.

**Architecture:** Новый крейт `uefi-common` (state.rs + error.rs из uefi-cli), refactor uefi-cli на него. Новый крейт `uefi-tui` (ratatui + crossterm): `app.rs` (state machine Mode/Normal/Command/Insert), `input.rs` (crossterm events), `commands.rs` (`:`-команды → gRPC), `theme.rs` (иконки/цвета), `ui/` (tree/details/cmdline/status/help). TUI — gRPC-клиент к EngineService (как CLI).

**Tech Stack:** Rust, ratatui (latest), crossterm (latest), tonic, uefi-proto, uefi-common, tokio, clap, anyhow.

## Global Constraints

- Спека: `docs/superpowers/specs/2026-07-22-uefi-tui-design.md` — источник истины.
- gRPC-контракт: `uefi-proto` (из цикла 1).
- Общий код: `uefi-common` (state `.uefipatcher`, error/exit-codes) — извлекается из `uefi-cli` цикла 2.
- Sock-приоритет: `--sock` > env `UEFIPATCHER_SOCK` > state `sock_path` > `${XDG_STATE_HOME}/uefipatcher/uefipatcher.sock`.
- Режимы: Normal (j/k/gg/G/Enter/Space/:/i/q/?), Command (`:`-команды, Enter/Esc), Insert (ввод, Esc).
- Layout: 3 панели (дерево слева | детали центр | command-line внизу) + статус-бар сверху + hint-бар внизу.
- Иконки: Nerd Font по FfsType (Image=, Volume=, File=, Section PE32=, GUIDed=, compressed=, UI=, raw=, Padding=, FreeSpace=). Action маркеры: NoAction=` `, Insert=`+`, Remove=`-`, Rebuild=`~`, Replace=`*`. Цвета по Action.
- Псевдографика: `├──`, `└──`, `│` (Unicode box-drawing). Expand/collapse: `` / `` (Nerd Folder).
- Команды: `:open`, `:dump`, `:find`, `:insert`, `:remove`, `:replace`, `:rebuild`, `:set-visibility`, `:save`, `:session init|destroy|list`, `:help`/`:h`, `:quit`/`:q`.
- Тесты: unit (app/commands/theme), integration (TestBackend + mock gRPC), E2E (round-trip).
- Кодстайл: `cargo fmt`, `cargo clippy -- -D warnings`. Без комментариев в коде.
- Mock-сервер: `tests/mock_server.rs` (как в цикле 2, переиспользовать).

---

## Файлы плана

| Файл | Назначение |
|---|---|
| `crates/uefi-common/Cargo.toml` | общий крейт |
| `crates/uefi-common/src/lib.rs` | re-export state + error |
| `crates/uefi-common/src/state.rs` | .uefipatcher TOML, sock-приоритет (из uefi-cli) |
| `crates/uefi-common/src/error.rs` | ExitCode, ErrKind, AppError (из uefi-cli) |
| `crates/uefi-cli/Cargo.toml` | refactor: зависимость от uefi-common |
| `crates/uefi-cli/src/state.rs` | удалить (→ uefi-common) |
| `crates/uefi-cli/src/error.rs` | удалить (→ uefi-common) |
| `crates/uefi-cli/src/main.rs` | update imports → uefi_common |
| `crates/uefi-cli/src/client.rs` | update imports → uefi_common |
| `crates/uefi-cli/src/commands/*.rs` | update imports → uefi_common |
| `crates/uefi-tui/Cargo.toml` | TUI-крейт |
| `crates/uefi-tui/src/main.rs` | entry, ratatui/crossterm setup, event loop |
| `crates/uefi-tui/src/app.rs` | App state machine, Mode, TreeNode |
| `crates/uefi-tui/src/input.rs` | crossterm events → AppEvent |
| `crates/uefi-tui/src/commands.rs` | `:`-команды → парсинг → gRPC |
| `crates/uefi-tui/src/theme.rs` | Nerd Font иконки, цвета |
| `crates/uefi-tui/src/ui/mod.rs` | layout |
| `crates/uefi-tui/src/ui/tree.rs` | рендер дерева |
| `crates/uefi-tui/src/ui/details.rs` | детали узла |
| `crates/uefi-tui/src/ui/cmdline.rs` | command-line |
| `crates/uefi-tui/src/ui/status.rs` | статус-бар |
| `crates/uefi-tui/src/ui/help.rs` | help popup |
| `crates/uefi-tui/tests/mock_server.rs` | mock EngineService (из цикла 2) |
| `crates/uefi-tui/tests/tui_integration.rs` | integration-тесты |
| `Cargo.toml` | workspace: добавить uefi-common, uefi-tui |

---

### Task 1: uefi-common — извлечение state.rs и error.rs из uefi-cli

**Files:**
- Modify: `Cargo.toml` (workspace)
- Create: `crates/uefi-common/Cargo.toml`
- Create: `crates/uefi-common/src/lib.rs`
- Create: `crates/uefi-common/src/state.rs` (переместить из uefi-cli)
- Create: `crates/uefi-common/src/error.rs` (переместить из uefi-cli)
- Modify: `crates/uefi-cli/Cargo.toml` (зависимость uefi-common)
- Modify: `crates/uefi-cli/src/main.rs`, `client.rs`, `commands/*.rs` (imports)

**Interfaces:**
- Consumes: `serde`, `toml`, `directories` (для state)
- Produces: `uefi_common::State`, `uefi_common::state_path`, `uefi_common::read_state`, `uefi_common::require_state`, `uefi_common::write_state`, `uefi_common::resolve_sock`, `uefi_common::default_sock`, `uefi_common::ExitCode`, `uefi_common::ErrKind`, `uefi_common::AppError`, `uefi_common::print_error`

- [ ] **Step 1: Обновить workspace Cargo.toml**

`Cargo.toml` (workspace members):
```toml
[workspace]
members = ["crates/uefi-proto", "crates/uefi-engine", "crates/uefi-cli", "crates/uefi-common", "crates/uefi-tui"]
resolver = "2"
```

- [ ] **Step 2: Создать uefi-common/Cargo.toml**

`crates/uefi-common/Cargo.toml`:
```toml
[package]
name = "uefi-common"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }
directories = "5"
thiserror.workspace = true
```

- [ ] **Step 3: Создать lib.rs**

`crates/uefi-common/src/lib.rs`:
```rust
pub mod state;
pub mod error;

pub use state::*;
pub use error::*;
```

- [ ] **Step 4: Переместить state.rs и error.rs**

Скопировать содержимое `crates/uefi-cli/src/state.rs` → `crates/uefi-common/src/state.rs` (заменить `crate::error::` → `crate::error::` без изменений, т.к. теперь в том же крейте).

Скопировать содержимое `crates/uefi-cli/src/error.rs` → `crates/uefi-common/src/error.rs` (без изменений).

- [ ] **Step 5: Удалить state.rs и error.rs из uefi-cli**

```bash
rm crates/uefi-cli/src/state.rs crates/uefi-cli/src/error.rs
```

- [ ] **Step 6: Обновить uefi-cli/Cargo.toml**

Добавить в `[dependencies]`:
```toml
uefi-common = { path = "../uefi-common" }
```

- [ ] **Step 7: Обновить imports в uefi-cli**

В `crates/uefi-cli/src/main.rs`: заменить `mod state; mod error;` на `use uefi_common;`. В `client.rs`, `commands/*.rs`: заменить `crate::state::` → `uefi_common::state::` и `crate::error::` → `uefi_common::error::`.

- [ ] **Step 8: Запустить тесты uefi-cli**

Run: `cargo test -p uefi-cli`
Expected: PASS (тесты state/error теперь в uefi-common, но uefi-cli использует их)

- [ ] **Step 9: Запустить тесты uefi-common**

Run: `cargo test -p uefi-common`
Expected: PASS

- [ ] **Step 10: Коммит**

```bash
git add Cargo.toml crates/uefi-common/ crates/uefi-cli/
git commit -m "refactor: extract state.rs and error.rs into uefi-common crate"
```

---

### Task 2: uefi-tui скелет — Cargo.toml, main.rs, app.rs

**Files:**
- Create: `crates/uefi-tui/Cargo.toml`
- Create: `crates/uefi-tui/src/main.rs`
- Create: `crates/uefi-tui/src/app.rs`

**Interfaces:**
- Consumes: `uefi-proto`, `uefi-common`, `ratatui`, `crossterm`, `tonic`, `tokio`, `clap`
- Produces:
  - `pub enum Mode { Normal, Command, Insert }`
  - `pub struct App { mode, tree, cursor, selected, cmdline, status_msg, image_loaded, quit }`
  - `pub struct TreeNode { path, depth, node_type, subtype, guid, name, action, expanded, has_children }`
  - `pub enum AppEvent { Key(KeyEvent), Quit }`

- [ ] **Step 1: Создать uefi-tui/Cargo.toml**

`crates/uefi-tui/Cargo.toml`:
```toml
[package]
name = "uefi-tui"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
uefi-proto = { path = "../uefi-proto" }
uefi-common = { path = "../uefi-common" }
ratatui = "0.28"
crossterm = "0.28"
tonic.workspace = true
tokio.workspace = true
clap = { version = "4", features = ["derive", "env"] }
anyhow.workspace = true

[dev-dependencies]
tempfile = "3"
assert_cmd = "2"
```

- [ ] **Step 2: Написать failing test для App**

`crates/uefi-tui/src/app.rs`:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Mode { Normal, Command, Insert }

#[derive(Debug, Clone)]
pub struct TreeNode {
    pub path: String,
    pub depth: usize,
    pub node_type: u8,
    pub subtype: u8,
    pub guid: Option<String>,
    pub name: String,
    pub action: u8,
    pub expanded: bool,
    pub has_children: bool,
}

pub struct App {
    pub mode: Mode,
    pub tree: Vec<TreeNode>,
    pub cursor: usize,
    pub selected: Option<String>,
    pub cmdline: String,
    pub status_msg: String,
    pub image_loaded: bool,
    pub quit: bool,
}

impl App {
    pub fn new() -> Self {
        Self {
            mode: Mode::Normal,
            tree: vec![],
            cursor: 0,
            selected: None,
            cmdline: String::new(),
            status_msg: "Welcome. Press : for commands, ? for help".into(),
            image_loaded: false,
            quit: false,
        }
    }

    pub fn cursor_down(&mut self) {
        if self.cursor + 1 < self.tree.len() { self.cursor += 1; }
    }

    pub fn cursor_up(&mut self) {
        if self.cursor > 0 { self.cursor -= 1; }
    }

    pub fn enter_command_mode(&mut self) {
        self.mode = Mode::Command;
        self.cmdline.clear();
    }

    pub fn exit_to_normal(&mut self) {
        self.mode = Mode::Normal;
        self.cmdline.clear();
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn app_new_starts_normal() {
        let app = App::new();
        assert_eq!(app.mode, Mode::Normal);
        assert!(!app.quit);
    }

    #[test]
    fn cursor_down_increments() {
        let mut app = App::new();
        app.tree = vec![
            TreeNode { path: "0".into(), depth: 0, node_type: 62, subtype: 0, guid: None, name: "Image".into(), action: 50, expanded: true, has_children: true },
            TreeNode { path: "0/0".into(), depth: 1, node_type: 65, subtype: 0, guid: None, name: "Volume".into(), action: 50, expanded: false, has_children: true },
        ];
        app.cursor_down();
        assert_eq!(app.cursor, 1);
    }

    #[test]
    fn cursor_down_clamps() {
        let mut app = App::new();
        app.tree = vec![TreeNode { path: "0".into(), depth: 0, node_type: 62, subtype: 0, guid: None, name: "X".into(), action: 50, expanded: false, has_children: false }];
        app.cursor_down();
        assert_eq!(app.cursor, 0);
    }

    #[test]
    fn mode_transitions() {
        let mut app = App::new();
        app.enter_command_mode();
        assert_eq!(app.mode, Mode::Command);
        app.exit_to_normal();
        assert_eq!(app.mode, Mode::Normal);
    }
}
```

- [ ] **Step 3: Создать main.rs (минимальный event loop)**

`crates/uefi-tui/src/main.rs`:
```rust
mod app;

use clap::Parser;
use crossterm::event::{self, Event, KeyCode, KeyEventKind};
use crossterm::terminal::{enable_raw_mode, disable_raw_mode, EnterAlternateScreen, LeaveAlternateScreen};
use crossterm::execute;
use ratatui::backend::CrosstermBackend;
use ratatui::Terminal;
use std::io::stdout;

#[derive(Parser)]
#[command(name = "uefi-tui", version, about = "UEFIPatcher TUI")]
struct Cli {
    #[arg(long, env = "UEFIPATCHER_SOCK")]
    sock: Option<String>,
}

fn main() -> anyhow::Result<()> {
    let _cli = Cli::parse();
    enable_raw_mode()?;
    execute!(stdout(), EnterAlternateScreen)?;
    let backend = CrosstermBackend::new(stdout());
    let mut terminal = Terminal::new(backend)?;
    let mut app = app::App::new();
    loop {
        terminal.draw(|f| render_placeholder(f, &app))?;
        if event::poll(std::time::Duration::from_millis(100))? {
            if let Event::Key(k) = event::read()? {
                if k.kind != KeyEventKind::Press { continue; }
                match k.code {
                    KeyCode::Char('q') => { app.quit = true; }
                    KeyCode::Char('j') => app.cursor_down(),
                    KeyCode::Char('k') => app.cursor_up(),
                    _ => {}
                }
            }
        }
        if app.quit { break; }
    }
    disable_raw_mode()?;
    execute!(stdout(), LeaveAlternateScreen)?;
    Ok(())
}

fn render_placeholder(f: &mut ratatui::Frame, app: &app::App) {
    use ratatui::widgets::{Block, Borders, Paragraph};
    let area = f.area();
    let p = Paragraph::new(format!("UEFI TUI — mode={:?} cursor={} tree={}", app.mode, app.cursor, app.tree.len()))
        .block(Block::default().borders(Borders::ALL).title("Placeholder"));
    f.render_widget(p, area);
}
```

- [ ] **Step 4: Запустить тесты app**

Run: `cargo test -p uefi-tui app::tests`
Expected: PASS

- [ ] **Step 5: Проверить сборку**

Run: `cargo build -p uefi-tui`
Expected: компиляция без ошибок

- [ ] **Step 6: Коммит**

```bash
git add crates/uefi-tui/ Cargo.toml
git commit -m "feat(tui): scaffold uefi-tui (ratatui + crossterm, App state machine, event loop)"
```

---

### Task 3: theme.rs — Nerd Font иконки и цвета

**Files:**
- Create: `crates/uefi-tui/src/theme.rs`
- Modify: `crates/uefi-tui/src/main.rs` (подключить)

**Interfaces:**
- Consumes: `ratatui::style::{Color, Style}`
- Produces:
  - `pub fn type_icon(node_type: u8, subtype: u8) -> &'static str`
  - `pub fn type_color(node_type: u8) -> Color`
  - `pub fn action_marker(action: u8) -> &'static str`
  - `pub fn action_color(action: u8) -> Color`
  - Константы FfsType: `TYPE_IMAGE=62`, `TYPE_VOLUME=65`, `TYPE_FILE=66`, `TYPE_SECTION=67`, `TYPE_PADDING=64`, `TYPE_FREESPACE=68`
  - Константы Action: `ACTION_NO=50`, `ACTION_INSERT=52`, `ACTION_REMOVE=54`, `ACTION_REBUILD=55`, `ACTION_REPLACE=53`

- [ ] **Step 1: Написать failing tests**

`crates/uefi-tui/src/theme.rs`:
```rust
use ratatui::style::Color;

pub const TYPE_IMAGE: u8 = 62;
pub const TYPE_VOLUME: u8 = 65;
pub const TYPE_FILE: u8 = 66;
pub const TYPE_SECTION: u8 = 67;
pub const TYPE_PADDING: u8 = 64;
pub const TYPE_FREESPACE: u8 = 68;

pub const ACTION_NO: u8 = 50;
pub const ACTION_INSERT: u8 = 52;
pub const ACTION_REPLACE: u8 = 53;
pub const ACTION_REMOVE: u8 = 54;
pub const ACTION_REBUILD: u8 = 55;

pub const SECTION_PE32: u8 = 0x10;
pub const SECTION_GUID_DEFINED: u8 = 0x02;
pub const SECTION_COMPRESSION: u8 = 0x01;
pub const SECTION_UI: u8 = 0x15;
pub const SECTION_RAW: u8 = 0x19;

pub fn type_icon(node_type: u8, subtype: u8) -> &'static str {
    match node_type {
        TYPE_IMAGE => "",
        TYPE_VOLUME => "",
        TYPE_FILE => "",
        TYPE_SECTION => match subtype {
            SECTION_PE32 => "",
            SECTION_GUID_DEFINED => "",
            SECTION_COMPRESSION => "",
            SECTION_UI => "",
            SECTION_RAW => "",
            _ => "",
        },
        TYPE_PADDING => "",
        TYPE_FREESPACE => "",
        _ => "?",
    }
}

pub fn type_color(node_type: u8) -> Color {
    match node_type {
        TYPE_IMAGE => Color::Cyan,
        TYPE_VOLUME => Color::Blue,
        TYPE_FILE => Color::Green,
        TYPE_SECTION => Color::Yellow,
        TYPE_PADDING | TYPE_FREESPACE => Color::DarkGray,
        _ => Color::Gray,
    }
}

pub fn action_marker(action: u8) -> &'static str {
    match action {
        ACTION_INSERT => "+",
        ACTION_REMOVE => "-",
        ACTION_REBUILD => "~",
        ACTION_REPLACE => "*",
        _ => " ",
    }
}

pub fn action_color(action: u8) -> Color {
    match action {
        ACTION_INSERT => Color::Green,
        ACTION_REMOVE => Color::Red,
        ACTION_REBUILD => Color::Yellow,
        ACTION_REPLACE => Color::Blue,
        _ => Color::Reset,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn icon_for_volume() {
        assert_eq!(type_icon(TYPE_VOLUME, 0), "");
    }

    #[test]
    fn icon_for_section_pe32() {
        assert_eq!(type_icon(TYPE_SECTION, SECTION_PE32), "");
    }

    #[test]
    fn icon_for_unknown_type() {
        assert_eq!(type_icon(99, 0), "?");
    }

    #[test]
    fn action_marker_insert() {
        assert_eq!(action_marker(ACTION_INSERT), "+");
    }

    #[test]
    fn action_marker_no_action() {
        assert_eq!(action_marker(ACTION_NO), " ");
    }

    #[test]
    fn color_for_remove() {
        assert_eq!(action_color(ACTION_REMOVE), Color::Red);
    }
}
```

- [ ] **Step 2: Подключить в main.rs**

`crates/uefi-tui/src/main.rs`: добавить `mod theme;`

- [ ] **Step 3: Запустить тесты**

Run: `cargo test -p uefi-tui theme::tests`
Expected: PASS

- [ ] **Step 4: Коммит**

```bash
git add crates/uefi-tui/src/theme.rs crates/uefi-tui/src/main.rs
git commit -m "feat(tui): add theme module (Nerd Font icons + colors by FfsType/Action)"
```

---

### Task 4: input.rs — crossterm events → AppEvent

**Files:**
- Create: `crates/uefi-tui/src/input.rs`
- Modify: `crates/uefi-tui/src/main.rs`

**Interfaces:**
- Consumes: `crossterm::event`
- Produces:
  - `pub enum AppEvent { Key(char), Enter, Esc, Backspace, Up, Down, Quit, Tick }`
  - `pub fn poll_event(timeout: Duration) -> Option<AppEvent>`

- [ ] **Step 1: Реализовать input.rs**

`crates/uefi-tui/src/input.rs`:
```rust
use crossterm::event::{self, Event, KeyCode, KeyEventKind};
use std::time::Duration;

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum AppEvent {
    Key(char),
    Enter,
    Esc,
    Backspace,
    Up,
    Down,
    Quit,
    Tick,
}

pub fn poll_event(timeout: Duration) -> Option<AppEvent> {
    if !event::poll(timeout).ok()? { return Some(AppEvent::Tick); }
    if let Event::Key(k) = event::read().ok()? {
        if k.kind != KeyEventKind::Press { return None; }
        return Some(match k.code {
            KeyCode::Char('q') => AppEvent::Quit,
            KeyCode::Char(c) => AppEvent::Key(c),
            KeyCode::Enter => AppEvent::Enter,
            KeyCode::Esc => AppEvent::Esc,
            KeyCode::Backspace => AppEvent::Backspace,
            KeyCode::Up => AppEvent::Up,
            KeyCode::Down => AppEvent::Down,
            _ => return None,
        });
    }
    None
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn tick_on_timeout() {
        let e = poll_event(Duration::from_millis(0));
        assert_eq!(e, Some(AppEvent::Tick));
    }
}
```

- [ ] **Step 2: Подключить в main.rs и заменить event-polling**

`crates/uefi-tui/src/main.rs`: добавить `mod input;`, заменить блок `event::poll`/`event::read` на `input::poll_event`.

- [ ] **Step 3: Запустить тесты**

Run: `cargo test -p uefi-tui input::tests`
Expected: PASS

- [ ] **Step 4: Коммит**

```bash
git add crates/uefi-tui/src/input.rs crates/uefi-tui/src/main.rs
git commit -m "feat(tui): add input module (crossterm events → AppEvent)"
```

---

### Task 5: ui/ — layout и рендер панелей

**Files:**
- Create: `crates/uefi-tui/src/ui/mod.rs`
- Create: `crates/uefi-tui/src/ui/tree.rs`
- Create: `crates/uefi-tui/src/ui/details.rs`
- Create: `crates/uefi-tui/src/ui/cmdline.rs`
- Create: `crates/uefi-tui/src/ui/status.rs`
- Create: `crates/uefi-tui/src/ui/help.rs`
- Modify: `crates/uefi-tui/src/main.rs`

**Interfaces:**
- Consumes: `app::App`, `theme::*`, `ratatui`
- Produces:
  - `pub fn render(f: &mut Frame, app: &App)` — главный layout
  - `pub fn render_tree(f: &mut Frame, area: Rect, app: &App)`
  - `pub fn render_details(f: &mut Frame, area: Rect, app: &App)`
  - `pub fn render_cmdline(f: &mut Frame, area: Rect, app: &App)`
  - `pub fn render_status(f: &mut Frame, area: Rect, app: &App)`
  - `pub fn render_help(f: &mut Frame, app: &App)`

- [ ] **Step 1: Создать ui/mod.rs с layout**

`crates/uefi-tui/src/ui/mod.rs`:
```rust
pub mod tree;
pub mod details;
pub mod cmdline;
pub mod status;
pub mod help;

use ratatui::layout::{Constraint, Direction, Layout, Rect};
use ratatui::Frame;
use crate::app::App;

pub fn render(f: &mut Frame, app: &App) {
    let full = f.area();
    let vertical = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(1), Constraint::Min(5), Constraint::Length(3), Constraint::Length(1)])
        .split(full);
    status::render(f, vertical[0], app);
    let horizontal = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(vertical[1]);
    tree::render(f, horizontal[0], app);
    details::render(f, horizontal[1], app);
    cmdline::render(f, vertical[2], app);
    render_hint(f, vertical[3], app);
}

fn render_hint(f: &mut Frame, area: Rect, app: &App) {
    use ratatui::widgets::{Block, Borders, Paragraph};
    let hint = match app.mode {
        crate::app::Mode::Normal => "NORMAL: j/k move · Space expand · :commands · ?help · q quit",
        crate::app::Mode::Command => "COMMAND: Enter execute · Esc cancel",
        crate::app::Mode::Insert => "INSERT: type text · Esc back to Normal",
    };
    let p = Paragraph::new(hint).block(Block::default().borders(Borders::NONE));
    f.render_widget(p, area);
}
```

- [ ] **Step 2: Создать ui/tree.rs**

`crates/uefi-tui/src/ui/tree.rs`:
```rust
use ratatui::layout::Rect;
use ratatui::style::{Style, Color};
use ratatui::text::{Line, Span};
use ratatui::widgets::{Block, Borders, List, ListItem};
use ratatui::Frame;
use crate::app::App;
use crate::theme::*;

pub fn render(f: &mut Frame, area: Rect, app: &App) {
    let items: Vec<ListItem> = app.tree.iter().enumerate().map(|(i, node)| {
        let indent = "  ".repeat(node.depth);
        let icon = type_icon(node.node_type, node.subtype);
        let expand = if node.has_children { if node.expanded { "" } else { "" } else { " " } };
        let marker = action_marker(node.action);
        let color = action_color(node.action);
        let line = Line::from(vec![
            Span::styled(format!("{indent}{expand} {icon} "), Style::default().fg(type_color(node.node_type))),
            Span::styled(format!("{} ", node.name), Style::default().fg(color)),
            Span::raw(format!("{} {marker}", node.guid.as_deref().unwrap_or(""))),
        ]);
        if i == app.cursor {
            ListItem::new(line).style(Style::default().bg(Color::DarkGray))
        } else {
            ListItem::new(line)
        }
    }).collect();
    let list = List::new(items).block(Block::default().borders(Borders::ALL).title("Tree"));
    f.render_widget(list, area);
}
```

- [ ] **Step 3: Создать ui/details.rs, cmdline.rs, status.rs, help.rs**

`crates/uefi-tui/src/ui/details.rs`:
```rust
use ratatui::layout::Rect;
use ratatui::widgets::{Block, Borders, Paragraph};
use ratatui::Frame;
use crate::app::App;

pub fn render(f: &mut Frame, area: Rect, app: &App) {
    let content = if let Some(node) = app.tree.get(app.cursor) {
        format!(
            "Path:    {}\nType:    {}\nSubtype: {:#04X}\nGUID:    {}\nName:    {}\nAction:  {}\nExpanded: {}",
            node.path, node.node_type, node.subtype,
            node.guid.as_deref().unwrap_or("(none)"),
            node.name, node.action, node.expanded
        )
    } else {
        "No node selected".into()
    };
    let p = Paragraph::new(content).block(Block::default().borders(Borders::ALL).title("Details"));
    f.render_widget(p, area);
}
```

`crates/uefi-tui/src/ui/cmdline.rs`:
```rust
use ratatui::layout::Rect;
use ratatui::widgets::{Block, Borders, Paragraph};
use ratatui::Frame;
use crate::app::{App, Mode};

pub fn render(f: &mut Frame, area: Rect, app: &App) {
    let content = match app.mode {
        Mode::Command => format!(":{}", app.cmdline),
        _ => String::from("Press : for commands"),
    };
    let p = Paragraph::new(content).block(Block::default().borders(Borders::ALL).title("Command"));
    f.render_widget(p, area);
}
```

`crates/uefi-tui/src/ui/status.rs`:
```rust
use ratatui::layout::Rect;
use ratatui::style::{Style, Color};
use ratatui::widgets::Paragraph;
use ratatui::Frame;
use crate::app::App;

pub fn render(f: &mut Frame, area: Rect, app: &App) {
    let mode_str = match app.mode { crate::app::Mode::Normal => "NORMAL", crate::app::Mode::Command => "COMMAND", crate::app::Mode::Insert => "INSERT" };
    let img = if app.image_loaded { "image:loaded" } else { "image:none" };
    let text = format!("[{mode_str}] {img} | {}", app.status_msg);
    let p = Paragraph::new(text).style(Style::default().fg(Color::White).bg(Color::Blue));
    f.render_widget(p, area);
}
```

`crates/uefi-tui/src/ui/help.rs`:
```rust
use ratatui::layout::{Rect, Constraint, Direction, Layout};
use ratatui::widgets::{Block, Borders, Clear, Paragraph};
use ratatui::Frame;

pub fn render(f: &mut Frame, app: &crate::app::App) {
    if !app.show_help { return; }
    let area = centered(60, 20, f.area());
    f.render_widget(Clear, area);
    let help_text = "UEFI TUI Help\n\
        \nNormal mode:\n\
        j/k      — move cursor\n\
        Space    — expand/collapse\n\
        Enter    — select node\n\
        :        — enter command mode\n\
        ?        — toggle this help\n\
        q        — quit\n\
        \nCommands:\n\
        :open PATH [--mode read|write]\n\
        :dump [--format text|tsv]\n\
        :find TARGET\n\
        :insert TARGET FFS [--mode into|before|after]\n\
        :remove TARGET\n\
        :replace TARGET DATA [--body-only]\n\
        :rebuild TARGET\n\
        :set-visibility ITEM [--visible|--hidden]\n\
        :save OUTPUT\n\
        :session init|destroy|list\n\
        :help / :h\n\
        :quit / :q";
    let p = Paragraph::new(help_text).block(Block::default().borders(Borders::ALL).title("Help"));
    f.render_widget(p, area);
}

fn centered(percent_w: u16, percent_h: u16, area: Rect) -> Rect {
    let popup = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage((100 - percent_h) / 2), Constraint::Percentage(percent_h), Constraint::Percentage((100 - percent_h) / 2)])
        .split(area);
    Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage((100 - percent_w) / 2), Constraint::Percentage(percent_w), Constraint::Percentage((100 - percent_w) / 2)])
        .split(popup[1])[1]
}
```

- [ ] **Step 4: Добавить show_help в App**

В `crates/uefi-tui/src/app.rs` добавить поле `pub show_help: bool` в `App` и инициализировать `false` в `new()`.

- [ ] **Step 5: Обновить main.rs — использовать ui::render**

`crates/uefi-tui/src/main.rs`: заменить `render_placeholder` на:
```rust
mod app;
mod theme;
mod input;
mod ui;

fn render(f: &mut ratatui::Frame, app: &app::App) {
    ui::render(f, app);
    ui::help::render(f, app);
}
```

- [ ] **Step 6: Проверить сборку**

Run: `cargo build -p uefi-tui`
Expected: компиляция без ошибок

- [ ] **Step 7: Коммит**

```bash
git add crates/uefi-tui/src/ui/ crates/uefi-tui/src/app.rs crates/uefi-tui/src/main.rs
git commit -m "feat(tui): add UI panels (tree/details/cmdline/status/help) with layout"
```

---

### Task 6: commands.rs — парсинг :команд и gRPC-вызовы

**Files:**
- Create: `crates/uefi-tui/src/commands.rs`
- Modify: `crates/uefi-tui/src/main.rs`, `app.rs`

**Interfaces:**
- Consumes: `uefi-proto` (gRPC-клиент), `uefi-common::state`, `app::App`
- Produces:
  - `pub async fn execute_command(app: &mut App, cmdline: &str) -> Result<String, String>` — парсит `:cmdline`, выполняет gRPC-вызов, возвращает статус-сообщение
  - `pub async fn connect(state: &State, sock: Option<&str>) -> Result<Client, String>` — подключение к движку

- [ ] **Step 1: Реализовать commands.rs**

`crates/uefi-tui/src/commands.rs`:
```rust
use tonic::transport::{Channel, Endpoint};
use uefi_proto::engine_service_client::EngineServiceClient;
use uefi_proto::*;
use uefi_common::state::{self, State};
use crate::app::App;

pub struct Client {
    inner: EngineServiceClient<Channel>,
    pub state: State,
}

pub async fn connect(cli_sock: Option<&str>, state: State) -> Result<Client, String> {
    let sock = state::resolve_sock(cli_sock, &state);
    let url = format!("unix://{}", sock.display());
    let ch = Channel::from_shared(url).map_err(|e| e.to_string())?
        .connect().await.map_err(|e| e.to_string())?;
    Ok(Client { inner: EngineServiceClient::new(ch), state })
}

pub async fn execute_command(app: &mut App, cmdline: &str, client: &mut Client) -> Result<String, String> {
    let parts: Vec<&str> = cmdline.split_whitespace().collect();
    if parts.is_empty() { return Err("empty command".into()); }
    let cmd = parts[0];
    match cmd {
        "open" | "o" => {
            let path = parts.get(1).ok_or("usage: :open PATH [--mode read|write]")?;
            let mode = if parts.iter().any(|&p| p == "write") { 1 } else { 0 };
            let sid = client.state.session_id.clone().ok_or("no session")?;
            let req = OpenImageRequest { session_id: sid, image_path: path.to_string(), mode };
            let r = client.inner.open_image(req).await.map_err(|e| e.message().to_string())?.into_inner();
            app.image_loaded = true;
            app.status_msg = format!("opened image {}", r.image_id);
            let dump_req = DumpTreeRequest { image_id: r.image_id.clone(), format: 0 };
            let dump = client.inner.dump_tree(dump_req).await.map_err(|e| e.message().to_string())?.into_inner();
            app.tree = parse_tree_dump(&dump.text);
            app.cursor = 0;
            Ok(r.image_id)
        }
        "save" | "s" => {
            let path = parts.get(1).ok_or("usage: :save OUTPUT")?;
            let iid = client.state.active_image_id.clone().ok_or("no active image")?;
            let req = SaveImageRequest { image_id: iid, output_path: path.to_string() };
            client.inner.save_image(req).await.map_err(|e| e.message().to_string())?;
            app.status_msg = format!("saved to {path}");
            Ok(path.into())
        }
        "quit" | "q" => { app.quit = true; Ok("quitting".into()) }
        "help" | "h" => { app.show_help = !app.show_help; Ok("help toggled".into()) }
        _ => Err(format!("unknown command: :{cmd}, try :help")),
    }
}

fn parse_tree_dump(text: &str) -> Vec<crate::app::TreeNode> {
    text.lines().filter_map(|line| {
        let trimmed = line.trim_start();
        let depth = line.len() - trimmed.len();
        let parts: Vec<&str> = trimmed.split_whitespace().collect();
        if parts.len() < 2 { return None; }
        Some(crate::app::TreeNode {
            path: parts[0].into(),
            depth: depth / 2,
            node_type: parts.get(1).and_then(|s| s.parse().ok()).unwrap_or(0),
            subtype: 0,
            guid: None,
            name: parts.get(2).unwrap_or(&"").to_string(),
            action: 50,
            expanded: true,
            has_children: false,
        })
    }).collect()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parse_tree_simple() {
        let text = "0  Image\n  0/0  Volume\n  0/0/0  File\n";
        let nodes = parse_tree_dump(text);
        assert_eq!(nodes.len(), 3);
        assert_eq!(nodes[0].depth, 0);
        assert_eq!(nodes[1].depth, 1);
        assert_eq!(nodes[2].depth, 2);
    }
}
```

- [ ] **Step 2: Подключить в main.rs и обновить event loop**

`crates/uefi-tui/src/main.rs`: добавить `mod commands;`. Обновить event loop для обработки Command mode (накопление cmdline, Enter → execute_command).

- [ ] **Step 3: Запустить тесты**

Run: `cargo test -p uefi-tui commands::tests`
Expected: PASS

- [ ] **Step 4: Коммит**

```bash
git add crates/uefi-tui/src/commands.rs crates/uefi-tui/src/main.rs
git commit -m "feat(tui): add commands module (:open/:save/:quit/:help + tree parser)"
```

---

### Task 7: Полный event loop — режимы, навигация, command-line

**Files:**
- Modify: `crates/uefi-tui/src/main.rs`
- Modify: `crates/uefi-tui/src/app.rs`

- [ ] **Step 1: Обновить main.rs с полным event loop**

`crates/uefi-tui/src/main.rs`:
```rust
mod app;
mod theme;
mod input;
mod ui;
mod commands;

use clap::Parser;
use crossterm::terminal::{enable_raw_mode, disable_raw_mode, EnterAlternateScreen, LeaveAlternateScreen};
use crossterm::execute;
use ratatui::backend::CrosstermBackend;
use ratatui::Terminal;
use std::io::stdout;
use std::time::Duration;
use app::{App, Mode};
use input::AppEvent;

#[derive(Parser)]
#[command(name = "uefi-tui", version, about = "UEFIPatcher TUI")]
struct Cli {
    #[arg(long, env = "UEFIPATCHER_SOCK")]
    sock: Option<String>,
}

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    let cli = Cli::parse();
    let state = uefi_common::read_state().unwrap_or_default();
    let mut client = match commands::connect(cli.sock.as_deref(), state.clone()).await {
        Ok(c) => Some(c),
        Err(e) => { eprintln!("warning: cannot connect to engine: {e}"); None }
    };
    enable_raw_mode()?;
    execute!(stdout(), EnterAlternateScreen)?;
    let backend = CrosstermBackend::new(stdout());
    let mut terminal = Terminal::new(backend)?;
    let mut app = App::new();
    loop {
        terminal.draw(|f| { ui::render(f, &app); ui::help::render(f, &app); })?;
        let event = input::poll_event(Duration::from_millis(100));
        let Some(ev) = event else { continue; };
        match app.mode {
            Mode::Normal => handle_normal(&mut app, &ev),
            Mode::Command => handle_command(&mut app, &ev, &mut client).await,
            Mode::Insert => handle_insert(&mut app, &ev),
        }
        if app.quit { break; }
    }
    disable_raw_mode()?;
    execute!(stdout(), LeaveAlternateScreen)?;
    Ok(())
}

fn handle_normal(app: &mut App, ev: &AppEvent) {
    match ev {
        AppEvent::Key('j') | AppEvent::Down => app.cursor_down(),
        AppEvent::Key('k') | AppEvent::Up => app.cursor_up(),
        AppEvent::Key(':') => app.enter_command_mode(),
        AppEvent::Key('?') => app.show_help = !app.show_help,
        AppEvent::Key('q') => app.quit = true,
        AppEvent::Quit => app.quit = true,
        _ => {}
    }
}

async fn handle_command(app: &mut App, ev: &AppEvent, client: &mut Option<commands::Client>) {
    match ev {
        AppEvent::Key(c) => app.cmdline.push(*c),
        AppEvent::Enter => {
            let cmd = app.cmdline.clone();
            if let Some(c) = client {
                match commands::execute_command(app, &cmd, c).await {
                    Ok(_) => {}
                    Err(e) => app.status_msg = format!("error: {e}"),
                }
            } else {
                app.status_msg = "no engine connection".into();
            }
            app.exit_to_normal();
        }
        AppEvent::Esc => app.exit_to_normal(),
        AppEvent::Backspace => { app.cmdline.pop(); }
        _ => {}
    }
}

fn handle_insert(app: &mut App, ev: &AppEvent) {
    match ev {
        AppEvent::Esc => app.exit_to_normal(),
        _ => {}
    }
}
```

- [ ] **Step 2: Проверить сборку**

Run: `cargo build -p uefi-tui`
Expected: компиляция без ошибок

- [ ] **Step 3: Коммит**

```bash
git add crates/uefi-tui/src/main.rs crates/uefi-tui/src/app.rs
git commit -m "feat(tui): full event loop with Normal/Command/Insert modes"
```

---

### Task 8: Mock-сервер и integration-тесты

**Files:**
- Create: `crates/uefi-tui/tests/mock_server.rs`
- Create: `crates/uefi-tui/tests/tui_integration.rs`

- [ ] **Step 1: Создать mock_server.rs (копия из uefi-cli цикла 2)**

`crates/uefi-tui/tests/mock_server.rs` — копировать `crates/uefi-cli/tests/mock_server.rs` (MockEngine с заглушками EngineService).

- [ ] **Step 2: Написать integration-тест**

`crates/uefi-tui/tests/tui_integration.rs`:
```rust
mod mock_server;

use crate::mock_server::start_mock;
use std::path::Path;
use tempfile::TempDir;

#[tokio::test]
async fn app_open_and_quit() {
    let td = TempDir::new().unwrap();
    let sock = td.path().join("test.sock");
    start_mock(&sock).await;
    let state = uefi_common::State { session_id: Some("s1".into()), token: Some("t1".into()), active_image_id: None, sock_path: Some(sock.display().to_string()) };
    let mut client = uefi_tui::commands::connect(None, state).await.unwrap();
    let mut app = uefi_tui::app::App::new();
    let result = uefi_tui::commands::execute_command(&mut app, "open /dev/null", &mut client).await;
    assert!(result.is_ok());
    assert!(app.image_loaded);
    assert!(!app.tree.is_empty());
    app.quit = true;
    assert!(app.quit);
}
```

Добавить в `crates/uefi-tui/Cargo.toml` [dependencies] публичные модули: `app` и `commands` должны быть `pub` (проверить в main.rs: `mod app;` → `pub mod app;`).

- [ ] **Step 3: Запустить integration-тесты**

Run: `cargo test -p uefi-tui --test tui_integration`
Expected: PASS

- [ ] **Step 4: Коммит**

```bash
git add crates/uefi-tui/tests/ crates/uefi-tui/src/main.rs crates/uefi-tui/Cargo.toml
git commit -m "test(tui): add mock server and integration tests (open + tree parse)"
```

---

### Task 9: Финальные проверки — clippy, fmt, все тесты

- [ ] **Step 1: Запустить все тесты**

Run: `cargo test --all`
Expected: PASS

- [ ] **Step 2: Запустить clippy**

Run: `cargo clippy --all -- -D warnings`
Expected: без warnings

- [ ] **Step 3: Запустить fmt check**

Run: `cargo fmt --all -- --check`
Expected: без diff

- [ ] **Step 4: Коммит финальных правок**

```bash
git add -A
git commit -m "chore(tui): final checks — all tests pass, clippy clean"
```

---

## Само-проверка плана (после написания)

**Спека-покрытие:**
- uefi-common (state + error extraction): Task 1 ✓
- uefi-tui скелет (Cargo/main/app): Task 2 ✓
- theme (Nerd Font + цвета): Task 3 ✓
- input (crossterm → AppEvent): Task 4 ✓
- ui (3 панели + статус + hint + help): Task 5 ✓
- commands (:команды → gRPC): Task 6 ✓
- event loop (Normal/Command/Insert): Task 7 ✓
- integration тесты (mock + TestBackend): Task 8 ✓
- финальные проверки: Task 9 ✓
- Vim-like режимы: Tasks 2, 7 ✓
- 3 панели layout: Task 5 ✓
- Nerd Font иконки: Task 3 ✓
- :command-line операции: Task 6 ✓
- hint-бар + :help: Task 5 ✓
- gRPC-клиент (как CLI): Tasks 6, 8 ✓

**Placeholder scan:** TBD/TODO нет; все шаги содержат код. ✓

**Type consistency:**
- `App`, `Mode`, `TreeNode` — единые в app.rs, ui/*, commands.rs, main.rs ✓
- `AppEvent` — единое в input.rs, main.rs ✓
- `Client` — в commands.rs, main.rs ✓
- `type_icon`/`action_marker` — theme.rs, ui/tree.rs ✓