# Бифуркация Port 2 — цикл A: разведка (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Развед-цикл бифуркации Port 2 на родном билде `refs/amibcp/450x — копия.bin`: RE-таблица MMR UncoreInitPeim (дизасм + дифф native↔HNX + Purley-сверка) и EPA-проба записываемости из DXE (пробник `BifEpaProbe`, 2 владельческие прошивки), с гейтом выбора варианта 1–4 для цикла B.

**Architecture:** Два трека по спеке §3: R (извлечение PEIM движковым `node extract`, дизасм-стенд `hack/uncore_disasm.py` на capstone в контейнере edk2-builder, калибровка на розетте `IIO-pei32.bin`, дифф native↔HNX → таблица MMR в JSON) и B (EDK2-пакет `UefiPatcherBifPkg` по образцу `UefiPatcherSerialPkg` серийного цикла; генератор `EpaConfig.h` печёт режим/таблицу из JSON; инжект — существующий `node insert` в DXE-FV). One-shot маркер `BifEpaStage` защищает от ресет-лупа, следующий бут докладывает, пережила ли запись ресет.

**Tech Stack:** podman-remote (fedora:44, образ `uefipatcher-edk2-builder`), edk2 против немодифицированного чекаута `refs/edk2` (паттерн `build_serial.sh`), python3 + capstone (RE-стенд, генератор), Rust-крейт uefi-engine (только тест над артефактом).

**Spec:** `docs/superpowers/specs/2026-09-14-bif-driver-recon-design.md` (спека путешествует с планом; исполнители читают обе). Первоисточник решения: `TODO.md` §450x, отчёт-сага `docs/reports/2026-09-14-450x-bifurcation-serial-saga.md` §7.

## Global Constraints

- **Никакой прошивки железа агентом.** Прошивки EPA-1/EPA-2 — только владелец (Tasks 8–9 заканчиваются инструкцией владельцу и ждут его вердикта; сбор результатов — следующий шаг после ответа владельца).
- **BIOS-образы и извлечённые PEIM не коммитируются** (`/refs` в .gitignore). Коммитим: hack-скрипты, пакет BifPkg, генерированный `EpaConfig.h` (diff показывает, что испечено), `.ffs`-артефакт, отчёт, JSON-таблицу.
- **`refs/edk2` и `refs/edk2-platforms` не модифицируются**; сборка — в git-archive-копии `/work/edk2` (паттерн `build_serial.sh:23-29`).
- **Движок — ноль изменений** (capability не трогаем; `node extract/insert` уже есть).
- **Движок/сокет-гигиена** (AGENTS.md): перед запуском `rm -f "$UEFIPATCHER_SOCK"`; глушить только `pkill -x engine`; при `RPC_INTERNAL` — перезапуск движка, не диагностика RPC-кода.
- **Правило-11**: расхождение плана с реальностью — отдельный `docs:`-коммит с правкой ЭТОГО плана ДО реализации шага.
- **Комментарии в коде — нет** (кроме docstring-шапок hack/-скриптов и ссылок на спеку/референс).
- После задач, меняющих Rust/тесты (Task 7): `cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings && cargo fmt --all -- --check`.
- Один коммит на задачу, сообщение — из задачи. Работа в текущей ветке `master`, без push (push — по команде владельца).
- **Container runtime** (`/run/.containerenv`): podman-remote; после правки `edk2-builder.containerfile` — пересборка образа (Task 2 Step 1).
- Изделия EPA: `refs/amibcp/450x-kopiya-bif-epa1.bin`, `…-epa2.bin` (локально, sha256 в отчёте).

## Решения плана (закрывают развилки спеки)

### R-A.1 Таблица MMR = JSON-контракт + генератор (Task 4 → 5 → 6)

Схема `docs/reports/2026-09-14-uncore-bif-mmr.json` фиксируется ДО анализа (Task 4 пишет генератор-валидатор), поэтому Task 5 (дифф-анализ) имеет исполняемый критерий приёмки: JSON проходит `gen_mmr_table.py --check`. Запись:

```json
{
  "entries": [
    {
      "tag": "IOU0-Port2-BifCtrl",
      "purpose": "IOU0 Port2 bifurcation control",
      "access": { "kind": "mmio32", "addr": "0xFED15004" },
      "width": 4,
      "and_mask": "0xFFFFFFF8",
      "or_value": "0x00000000",
      "sequence": 1,
      "notes": "haswell-ep UPG §…; native=0x40(x16) hnx=0x00(x4x4x4x4)"
    }
  ],
  "reset": "warm"
}
```

`access.kind` — `mmio32` (поле `addr`, десятичная строка не допускается, только `0x…`) либо `pci-cfg32` (поля `bus`, `dev`, `fun`, `off`; `off < 0x100` — CF8-legacy путь, extended-space регистры R2 обязан перекодировать в `mmio32` c уже разрешённой базой). Генератор печёт `EpaConfig.h`; комментарий-шапка генерированного файла — ссылка на спеку §5 (разрешено правилом комментариев).

### R-A.2 Пробник печёт всё на входе драйвера, а не на Ready-To-Boot

Спека §5 оставляла «entry point (ранний DXE)». Решение: entry point сразу (DEPEX `gEfiVariableWriteArchProtocolGuid` — ждём готовности SetVariable, всё ещё до BDS). Причины: (а) чем раньше пишем MMR, тем больше шансов до потребителей; (б) ресет из раннего DXE — та же фаза, где сам RC делает warm-ресеты; (в) UART уже доступен (инициализация своя, паттерн SerialDxe).

### R-A.3 Артефакт-инварианты и воспроизводимость — по серийному образцу

`hack/edk2_bif_check.py` — slim-вариант `hack/edk2_build_check.py`: GUID/тип/PE machine/subsystem + побайтовая воспроизводимость двух сборок (допустимое расхождение — только COFF TimeDateStamp). Коммита .ffs без зелёного валидатора не бывает.

### R-A.4 R2-фейл-путь

Если дифф native↔HNX не локализует полиси (нет строк/нет расходящихся веток): Task 5 всё равно закрывается — таблица строится по розетте `IIO-pei32.bin` (валидатор бифуркации), отчёт фиксирует «полиси не локализована → кандидат варианта 3», гейт Task 10 это учитывает.

---

### Task 1: R0 — извлечение UncoreInitPeim ×3 и каркас отчёта

**Files:**
- Create: `docs/reports/2026-09-14-uncore-bif-recon.md`
- Create (локально, не в git): `refs/extracted/bif/{IIO-pei32-native.pei, IIO-pei32-hnx.pei, IIO-pei32-sm.pei}`

**Interfaces:**
- Consumes: движок `target/debug/engine`, CLI `target/debug/uefi-cli` (`session init --force`, `image open --mode write`, `node search`, `node extract`, `artifact export`), образы `refs/amibcp/450x — копия.bin`, `refs/fw/HNX99TF_200525_original_E5C88C6F.bin`, `refs/fw/超微450.rom`.
- Produces: PEIM-файлы для Tasks 2/5; отчёт-каркас (секции R0/R1/R2/B1/B2/B3/G) для всех последующих задач.

- [ ] **Step 1: Запустить движок с отдельным сокетом**

```bash
cd /var/home/dsevosty/git/IMPLEMENTATION/UEFIPatcher
mkdir -p /tmp/bif refs/extracted/bif
export UEFIPATCHER_SOCK=/tmp/bif/engine.sock
rm -f "$UEFIPATCHER_SOCK"
nohup target/debug/engine > /tmp/bif/engine.log 2>&1 &
sleep 1; pgrep -x engine
```

Expected: `engine.log` содержит `Starting engine: sock=/tmp/bif/engine.sock`; pgrep печатает PID.

- [ ] **Step 2: Извлечь нативный UncoreInitPeim из «450x — копия.bin»**

```bash
export UEFIPATCHER_SOCK=/tmp/bif/engine.sock
target/debug/uefi-cli session init --force
target/debug/uefi-cli image open --mode write "refs/amibcp/450x — копия.bin"
target/debug/uefi-cli node search UncoreInitPeim --limit 20
```

Expected: найден FFS с UI-именем `UncoreInitPeim`, канонический GUID `D71C8BA4-4AF2-4D0D-B1BA-F2409F0C20D3` (байтово начинается с `A48B1CD7`; CLI показывает каноническую форму — GUID-поиск по `A48B1CD7` пуст, name-поиск надёжен), тип PEIM (`File(PEIM)`); записать путь вида `N/M`.

```bash
target/debug/uefi-cli node extract <N/M/1> --body-only
target/debug/uefi-cli artifact list
target/debug/uefi-cli artifact export <ID-из-list> refs/extracted/bif/IIO-pei32-native.pei
```

Expected: чистый PE32-образ (R0: тела 798880 натив / 606112 HNX / 628224 超微; саговские 606322/628434 — размеры FFS-файлов, 627410 в этих трёх не наблюдается). Извлекать PE32-секцию-потомка `…/1`: `--body-only` на самом FFS даёт секционный суп, не образ.

- [ ] **Step 3: Повторить для HNX и 超微450**

Тот же флоу на `refs/fw/HNX99TF_200525_original_E5C88C6F.bin` → `refs/extracted/bif/IIO-pei32-hnx.pei`; на `refs/fw/超微450.rom` → `refs/extracted/bif/IIO-pei32-sm.pei`. Каждый раз `session init --force` + `image open` (одна открытая сессия).

- [ ] **Step 4: Быстрая сверка сигнатур**

```bash
for f in refs/extracted/bif/*.pei refs/fw/IIO-pei32.bin; do
  printf '%s ' "$f"; head -c2 "$f" | od -An -c | tr -d ' \n'; echo
done
```

Expected: `MZ` (PE32) либо `VZ` (TE) у каждого; записать пары файл→сигнатура. Одинаковые сигнатуры native/hnx/sm упрощают дифф в Task 5.

- [ ] **Step 5: Каркас отчёта разведки**

Создать `docs/reports/2026-09-14-uncore-bif-recon.md`:

```markdown
# UncoreInitPeim бифуркации — разведка цикла A (розетта-первый)

Спека `docs/superpowers/specs/2026-09-14-bif-driver-recon-design.md`;
план `docs/superpowers/plans/2026-09-14-bif-recon.md`. Прошивки —
только владелец; таблица MMR — `2026-09-14-uncore-bif-mmr.json`.

## R0. Извлечение (2026-09-…)

| Билд | Путь в дереве | GUID | Размер | Сигнатура |
|---|---|---|---|---|
| натив (450x — копия) | N/M | A48B1CD7-… | … | MZ/VZ |
| HNX99TF | … | … | … | … |
| 超微450 | … | … | … | … |

Контроль-розетта: `refs/fw/IIO-pei32.bin` (798880, PE32, строки
«Invalid IOUx Bifurcation»/«busUncore»/«busIio», сага §5).

## R1. Калибровка дизасм-стенда
(заполняет Task 2)

## R2. Purley-карта / дифф native↔HNX / таблица MMR
(заполняют Tasks 3, 5)

## B1. Пробник и сборка
(заполняет Task 6)

## B2. EPA-1 (read-only) — вердикт владельца
(заполняет Task 8)

## B3. EPA-2 (write-ladder) — вердикт владельца
(заполняет Task 9)

## G. Гейт выбора варианта
(заполняет Task 10)
```

- [ ] **Step 6: Заглушить движок и закоммитить**

```bash
pkill -x engine
git add docs/reports/2026-09-14-uncore-bif-recon.md
git commit -m "docs(report): цикл A R0 — извлечены UncoreInitPeim натив/HNX/超微 (GUID, размеры, сигнатуры), каркас отчёта разведки"
```

---

### Task 2: R1 — дизасм-стенд + capstone в builder + калибровка на розетте

**Files:**
- Modify: `docker/edk2-builder.containerfile`
- Create: `hack/uncore_disasm.py`, `hack/disasm.sh`
- Modify: `docs/reports/2026-09-14-uncore-bif-recon.md` (секция R1)

**Interfaces:**
- Consumes: `refs/extracted/bif/*.pei` (Task 1), `refs/fw/IIO-pei32.bin`, podman-remote + образ `uefipatcher-edk2-builder:latest`.
- Produces: `hack/disasm.sh <cmd> <args>` — обёртка, запускающая `uncore_disasm.py` в контейнере; пути внутри контейнера: `/refs/extracted/bif/…`, `/refs/fw/IIO-pei32.bin`. Подкоманды: `info <file>`, `strings <file> [min-len]`, `xref <file> <0xVA | substring>`, `dis <file> <0xVA> <len>`, `mmio <file> <0xLO> <0xHI>`. Нужны Tasks 3/5.

- [ ] **Step 1: Добавить capstone в образ билдера и пересобрать**

`docker/edk2-builder.containerfile` после строки `RUN dnf install…` (строка 2):

```
FROM uefipatcher-runtime-base
RUN dnf install -y git nasm gcc make python3 libuuid-devel python3-capstone && dnf clean all
ENV EDK2_BUILDER_CONTAINER=1
WORKDIR /work
```

(строка 2 дополняется пакетом `python3-capstone`, остальное без изменений). Пересборка:

```bash
podman-remote build -t uefipatcher-edk2-builder:latest \
  -f docker/edk2-builder.containerfile docker/
podman-remote run --rm uefipatcher-edk2-builder:latest python3 -c "import capstone; print(capstone.__version__)"
```

Expected: версия capstone ≥ 4.

- [ ] **Step 2: hack/disasm.sh**

```bash
#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/.." && pwd)"
REFS="$(readlink -f "$REPO_ROOT/refs")"
IMAGE="uefipatcher-edk2-builder:latest"

if [ $# -lt 2 ]; then
    echo "usage: disasm.sh <info|strings|xref|dis|mmio> <file-in-/refs> [args...]" >&2
    exit 2
fi

exec podman-remote run --rm --security-opt label=disable \
    -v "$REPO_ROOT/hack":/hack:ro \
    -v "$REFS":/refs:ro \
    "$IMAGE" python3 /hack/uncore_disasm.py "$@"
```

`chmod +x hack/disasm.sh`. Все пути к файлам в аргументах — контейнерные (`/refs/…`).

- [ ] **Step 3: hack/uncore_disasm.py**

```python
#!/usr/bin/env python3
"""uncore_disasm.py — RE-стенд PEIM'ов Grantley (цикл A бифуркации).

Спека docs/superpowers/specs/2026-09-14-bif-driver-recon-design.md §4
(R1), план docs/superpowers/plans/2026-09-14-bif-recon.md Task 2.
Парсит PE32 (MZ/PE) и TE (VZ) образы: заголовки, ASCII-строки с VA,
xref-ы (байт-скан little-endian адреса), таргетный дизасм, скан
MMIO/PCI-CF8 доступов. Требует capstone (контейнер edk2-builder);
запуск через hack/disasm.sh. Не часть движка; запуск вручную:

    hack/disasm.sh info    /refs/fw/IIO-pei32.bin
    hack/disasm.sh strings /refs/fw/IIO-pei32.bin 8
    hack/disasm.sh xref    /refs/fw/IIO-pei32.bin 'Invalid IOUx'
    hack/disasm.sh dis     /refs/fw/IIO-pei32.bin 0x1000 0x80
    hack/disasm.sh mmio    /refs/fw/IIO-pei32.bin 0xF0000000 0xFFFFFFFF
"""
import struct
import sys

from capstone import Cs, CS_ARCH_X86, CS_MODE_32, CS_MODE_64

TE_SIG = b"VZ"
MACHINE_MODES = {0x14C: CS_MODE_32, 0x8664: CS_MODE_64}
MACHINE_NAMES = {0x14C: "I386", 0x8664: "AMD64"}


class Image:
    def __init__(self, path):
        self.path = path
        self.data = open(path, "rb").read()
        d = self.data
        self.te = d[:2] == TE_SIG
        if self.te:
            self.machine, nsec, self.subsystem, stripped = struct.unpack_from("<HHHH", d, 2)
            self.entry, self.base_of_code = struct.unpack_from("<II", d, 10)
            (self.image_base,) = struct.unpack_from("<Q", d, 18)
            sec_off, hdr_delta = 42, stripped
        else:
            assert d[:2] == b"MZ", f"{path}: no MZ"
            lfa = struct.unpack_from("<I", d, 0x3C)[0]
            assert d[lfa:lfa + 4] == b"PE\x00\x00", f"{path}: no PE"
            self.machine, nsec = struct.unpack_from("<HH", d, lfa + 4)
            self.subsystem = struct.unpack_from("<H", d, lfa + 92)[0]
            self.entry = struct.unpack_from("<I", d, lfa + 40)[0]
            if struct.unpack_from("<H", d, lfa + 24)[0] == 0x20B:
                (self.image_base,) = struct.unpack_from("<Q", d, lfa + 48)
            else:
                (self.image_base,) = struct.unpack_from("<I", d, lfa + 52)
            sec_off = lfa + 24 + struct.unpack_from("<H", d, lfa + 20)[0]
            hdr_delta = 0
        self.sections = []
        for i in range(nsec):
            s = sec_off + i * 40
            name = d[s:s + 8].rstrip(b"\x00").decode("ascii", "replace")
            vsize, va, rsize, roff = struct.unpack_from("<IIII", d, s + 8)
            self.sections.append({"name": name, "vsize": vsize, "va": va, "rsize": rsize, "roff": roff})
        self.hdr_delta = hdr_delta

    def file_off_to_va(self, off):
        for s in self.sections:
            if s["roff"] <= off < s["roff"] + s["rsize"]:
                return self.image_base + s["va"] + (off - s["roff"])
        return self.image_base + off - self.hdr_delta if off < self.hdr_delta + 0x400 else None

    def va_to_file(self, va):
        rva = va - self.image_base
        for s in self.sections:
            if s["va"] <= rva < s["va"] + max(s["vsize"], s["rsize"]):
                off = s["roff"] + (rva - s["va"])
                return off if off < len(self.data) else None
        return None

    def code_ranges(self):
        out = []
        for s in self.sections:
            if s["rsize"] and (s["name"].startswith(".text") or "X" in s["name"] or s["name"] in ("UPX0", ".cod")):
                out.append((s["roff"], s["roff"] + s["rsize"]))
        return out or [(self.sections and (self.sections[0]["roff"], self.sections[0]["roff"] + self.sections[0]["rsize"])) or (0, 0)]

    def disassembler(self):
        md = Cs(CS_ARCH_X86, MACHINE_MODES.get(self.machine, CS_MODE_32))
        md.detail = True
        return md


def parse_int(s):
    return int(s, 16) if s.lower().startswith("0x") else int(s, 0)


def cmd_info(img):
    kind = "TE" if img.te else "PE32"
    print(f"{img.path}: {kind} machine={MACHINE_NAMES.get(img.machine, hex(img.machine))} "
          f"subsystem={img.subsystem} entry={hex(img.image_base + img.entry)} base={hex(img.image_base)}")
    for s in img.sections:
        print(f"  {s['name']:<8} va={hex(img.image_base + s['va']):<12} vsize={s['vsize']:#x} raw={s['roff']:#x}+{s['rsize']:#x}")


def cmd_strings(img, min_len=4):
    min_len = int(min_len)
    runs, cur, start = [], b"", 0
    for i, b in enumerate(img.data):
        if 0x20 <= b < 0x7F:
            if not cur:
                start = i
            cur += bytes([b])
        else:
            if len(cur) >= min_len:
                runs.append((start, cur))
            cur = b""
    for off, s in runs:
        va = img.file_off_to_va(off)
        if va:
            print(f"{va:#010x} {s.decode('ascii')}")


def find_string_va(img, needle):
    target = needle.encode("ascii", "replace")
    idx = img.data.find(target)
    while idx != -1:
        va = img.file_off_to_va(idx)
        if va:
            return va
        idx = img.data.find(target, idx + 1)
    return None


def cmd_xref(img, target):
    va = parse_int(target) if target.lower().startswith("0x") else find_string_va(img, target)
    if va is None:
        sys.exit(f"xref: {target!r} not found / not section-backed")
    print(f"; target {target!r} va={va:#x}")
    refs = [va] if target.lower().startswith("0x") else range(va - 8, va + 1)
    for ref in refs:
        for width in (4, 8):
            pat = ref.to_bytes(width, "little")
            idx = img.data.find(pat)
            while idx != -1:
                site = img.file_off_to_va(idx)
                if site:
                    print(f"hit  file={idx:#x} site~{site:#x} width={width} ref={ref:#x} -{va - ref}")
                idx = img.data.find(pat, idx + 1)


def cmd_dis(img, va, length):
    off = img.va_to_file(va)
    if off is None:
        sys.exit(f"dis: {va:#x} not mapped")
    md = img.disassembler()
    for insn in md.disasm(img.data[off:off + length], va):
        print(f"{insn.address:#010x}  {insn.bytes.hex():<20} {insn.mnemonic} {insn.op_str}")


def cmd_mmio(img, lo, hi):
    md = img.disassembler()
    md.skipdata = True
    for roff, end in img.code_ranges():
        for insn in md.disasm(img.data[roff:end], img.file_off_to_va(roff) or img.image_base):
            if insn.id == 0:
                continue
            hit = None
            for op in insn.operands:
                if op.type == 3 and lo <= op.mem.disp <= hi:  # X86_OP_MEM, absolute/RIP-rel disp
                    hit = f"mem={op.mem.disp:#x}"
                elif op.type == 2 and insn.mnemonic.startswith("mov") and lo <= op.imm <= hi:  # X86_OP_IMM
                    hit = f"imm={op.imm:#x}"
            if 0xCF8 <= (insn.operands[1].imm if len(insn.operands) > 1 and insn.operands[1].type == 2 else 0) <= 0xCFC:
                hit = hit or "cf8/cfc"
            if hit:
                print(f"{insn.address:#010x}  {insn.bytes.hex():<20} {insn.mnemonic} {insn.op_str}  ; {hit}")


def main():
    cmds = {"info": (cmd_info, 0), "strings": (cmd_strings, 1), "xref": (cmd_xref, 1),
            "dis": (cmd_dis, 2), "mmio": (cmd_mmio, 2)}
    if len(sys.argv) < 3 or sys.argv[1] not in cmds:
        sys.exit(__doc__)
    name, path = sys.argv[1], sys.argv[2]
    fn, _ = cmds[name]
    fn(Image(path), *[parse_int(a) if a.lower().startswith("0x") else a for a in sys.argv[3:]])


if __name__ == "__main__":
    main()
```

Примечание к реализации: `mmio` идёт с `md.skipdata = True` и guard-ом `if insn.id == 0: continue` — линейный скан обязан переживать встроенные данные/таблицы переходов, а у skipdata-псевдоинструкций `.byte` (id==0) доступ к `insn.operands` в capstone 5.x кидает `CS_ERR_SKIPDATA` (контроль R1-ревью: без skipdata — остановка на первом невалидом байте, 81 инструкция из ~0xBFA48 байт .text; с skipdata+guard — 282127 инструкций, 318 hits в диапазоне 0xFFE00000+ на розетте). `dis` — без skipdata (остановка на мусоре в целевом окне информативна, аналитик ре-анкорится). `mmio` фильтрует `disp`-доступы в диапазоне (в 32-битном PEI disp абсолютный — MMR-адреса видны сразу; в 64-бит — RIP-относительные, диапазон их почти не ловит, тогда ищем через `xref` адреса-константы из `info`-вывода секций/BAR). Офсеты PE-заголовка: subsystem @ lfa+92 (сверено с `hack/edk2_build_check.py:pe_fields`); секционная таблица @ `lfa+24+SizeOfOptionalHeader` (u16 @ lfa+20); ImageBase — по magic @ lfa+24: PE32 (0x10B) DWORD @ lfa+52, PE32+ (0x20B) QWORD @ lfa+48 (контроль R1: все 4 образа — PE32 0x10B). `xref` для строкового таргета сканирует окно префикса `va-8..va`: строки в образах несут префиксы («ERROR: », «\n») и код ссылается на начало полной строки, не на начало иглы (контроль R1: «Invalid IOUx Bifurcation» — hit при ref=va-7; «busUncore»/«busIio» — при ref=va-1). TE-структура — по EFI_IMAGE_TE_HEADER (секционная таблица @42).

- [ ] **Step 4: Калибровка на розетте**

```bash
hack/disasm.sh info /refs/fw/IIO-pei32.bin
hack/disasm.sh strings /refs/fw/IIO-pei32.bin 8 | grep -Ei 'IOU|bif|uncore|iio'
hack/disasm.sh xref /refs/fw/IIO-pei32.bin 'Invalid IOUx Bifurcation'
```

Expected: `info` печатает machine=I386, subsystem=11, base=0xFFE64B08 и две секции (`.text`/`.reloc`); `strings` находит «Invalid IOUx Bifurcation», «busUncore», «busIio» с VA (база 0xFFE64B08, напр. `Invalid IIO PCIE DMI Box Instance` ≈ 0xFFE90Bxx); `xref` даёт ≥1 hit в кодовой секции со сдвигом префикса (розетта: «Invalid IOUx Bifurcation» → ref=va-7 «ERROR: », hit в .text @ file≈0x32FDF; «busUncore»/«busIio» → ref=va-1 «\n», hit @ file≈0x7439D/0x74371). R0: розетта байт-идентична нативному UncoreInitPeim (sha256 совпадает) — калибровка = локализация сразу в нативе; HNX/超微 строк-якорей не имеют (R0, ASCII и UTF-16). Зафиксировать вывод (по 2-3 строки на команду) в отчёт, секция R1.

- [ ] **Step 5: Прогнать info на трёх PEIM'ах Task 1**

```bash
for f in /refs/extracted/bif/IIO-pei32-native.pei /refs/extracted/bif/IIO-pei32-hnx.pei /refs/extracted/bif/IIO-pei32-sm.pei; do
  hack/disasm.sh info "$f"
done
```

Expected: все три парсятся (TE или PE32, machine I386 — PEI Grantley 32-битный); база/секции в отчёт R1. Если PEIM в секции-обёртке и `assert MZ/VZ` падает — `node extract` без `--body-only` и сместить на размер обёртки по выводу `node inspect` (зафиксировать по правилу-11).

- [ ] **Step 6: Commit**

```bash
git add hack/uncore_disasm.py hack/disasm.sh docker/edk2-builder.containerfile docs/reports/2026-09-14-uncore-bif-recon.md
git commit -m "feat(hack): RE-стенд uncore_disasm.py (PE/TE parse, strings+xref+dis+mmio, capstone в edk2-builder) — калибровка на розетте IIO-pei32"
```

---

### Task 3: R2a — Purley-референс (edk2-platforms) в отчёт

**Files:**
- Create (локально, не в git): `refs/edk2-platforms/` (shallow clone)
- Modify: `docs/reports/2026-09-14-uncore-bif-recon.md` (секция R2, подраздел Purley)

**Interfaces:**
- Consumes: сеть (git clone / WebFetch).
- Produces: карта «что/в каком порядке» для интерпретации дизасма в Task 5; файлы-цитаты в отчёте с путями `refs/edk2-platforms/…:NNN`.

- [ ] **Step 1: Shallow clone**

```bash
git clone --depth 1 https://github.com/tianocore/edk2-platforms refs/edk2-platforms
```

Fallback при отсутствии сети: WebFetch файлов PurleyOpenBoardPkg по списку из Step 2 (raw.githubusercontent.com), цитаты в отчёт с URL вместо путей.

- [ ] **Step 2: Найти Purley IIO-код**

```bash
find refs/edk2-platforms -ipath '*Purley*' -iname '*Iio*' | head -20
grep -rln 'IioBifurcation' refs/edk2-platforms/Platform/Intel/ | head -10
```

Ожидаемые зоны: `Platform/Intel/PurleyOpenBoardPkg/…` (конфиг-структуры, порядок портов), `Silicon/Intel/PurleySiliconBinPkg/Include/…` (заголовки структур IIO-полиси; реализация — закрытый бинарь, спека §8). Если `IioBifurcation` не находится — искать `Bifurcation`, `IOU`, `Lane` в тех же зонах.

- [ ] **Step 3: Карта в отчёт**

В секцию R2 отчёта добавить подраздел: (а) путь до структуры бифуркации и её полей (цитата 10–20 строк с `file:line`); (б) список шагов инициализации IIO из открытых файлов (порядок: порт-енаблы → bifurcation → lane-мап → ресет/тренинг); (в) вердикт «что переносимо на Grantley, что нет» (поколения разные — карта, не блюпринт). Явно пометить: регистровые АДРЕСА из Purley не переносятся — адреса даёт только дизасм Task 5.

- [ ] **Step 4: Commit**

```bash
git add docs/reports/2026-09-14-uncore-bif-recon.md
git commit -m "docs(report): цикл A R2a — Purley-карта IIO-инициализации (edk2-platforms): структура бифуркации, порядок шагов, границы переносимости"
```

---

### Task 4: Генератор EpaConfig.h (схема-контракт MMR-таблицы)

**Files:**
- Create: `hack/gen_mmr_table.py`
- Create: `docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h` (дефолт: read-режим, пустая таблица)

**Interfaces:**
- Consumes: `docs/reports/2026-09-14-uncore-bif-mmr.json` (создаётся Task 5; для этого таска — дефолт-JSON из Step 1).
- Produces: `hack/gen_mmr_table.py --mmr <json> --mode read|write [--reset warm|cold] [--pause-ms N] [--out <header>] [--check]` — exit 0 = схема валидна; печёт `EpaConfig.h` с макросами `EPA_MODE_WRITE/EPA_RESET_COLD/EPA_PAUSE_MS/EPA_ENTRY_COUNT`, типом `MMR_ENTRY` и массивом `mMmrEntries`. Нужен Tasks 5 (валидация), 6/8/9 (сборки).

- [ ] **Step 1: Дефолтная таблица**

`docs/reports/2026-09-14-uncore-bif-mmr.json` (создаётся здесь, наполняется Task 5):

```json
{
  "entries": [],
  "reset": "warm"
}
```

- [ ] **Step 2: Скрипт-генератор**

```python
#!/usr/bin/env python3
"""gen_mmr_table.py — печь EpaConfig.h пробника из MMR-таблицы (цикл A).

Спека docs/superpowers/specs/2026-09-14-bif-driver-recon-design.md §4-5
(таблица MMR = JSON-контракт, пробник без runtime-фроззинга). Валидирует
схему (--check), сортирует по sequence, генерирует заголовок для
BifEpaProbe.c. Не часть движка; запуск вручную или из build_bif_epa.sh:

    hack/gen_mmr_table.py --mmr docs/reports/2026-09-14-uncore-bif-mmr.json \
        --mode write --reset warm --pause-ms 3000 \
        --out docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h
"""
import argparse
import json
import sys

SCHEMA_ERR = "schema error: "
HEX_FIELDS = ("and_mask", "or_value")


def parse_hex(v, what):
    if not isinstance(v, str) or not v.lower().startswith("0x"):
        sys.exit(SCHEMA_ERR + f"{what}: {v!r} must be a 0x-hex string")
    n = int(v, 16)
    if not 0 <= n <= 0xFFFFFFFF:
        sys.exit(SCHEMA_ERR + f"{what}: {v!r} out of u32")
    return n


def validate(doc):
    if not isinstance(doc, dict) or "entries" not in doc:
        sys.exit(SCHEMA_ERR + "top-level object with 'entries' required")
    if doc.get("reset") not in ("warm", "cold"):
        sys.exit(SCHEMA_ERR + "reset: 'warm'|'cold'")
    seqs = []
    for i, e in enumerate(doc["entries"]):
        w = f"entries[{i}]"
        for f in ("tag", "purpose", "access", "width", "and_mask", "or_value", "sequence"):
            if f not in e:
                sys.exit(SCHEMA_ERR + f"{w}: missing {f}")
        if e["width"] != 4:
            sys.exit(SCHEMA_ERR + f"{w}.width: only 4 supported")
        parse_hex(e["and_mask"], f"{w}.and_mask")
        parse_hex(e["or_value"], f"{w}.or_value")
        a = e["access"]
        if a.get("kind") == "mmio32":
            addr = a.get("addr")
            if not isinstance(addr, str) or not addr.lower().startswith("0x"):
                sys.exit(SCHEMA_ERR + f"{w}.access.addr: 0x-hex string required")
            int(addr, 16)
        elif a.get("kind") == "pci-cfg32":
            for f in ("bus", "dev", "fun", "off"):
                v = a.get(f)
                if not isinstance(v, int):
                    sys.exit(SCHEMA_ERR + f"{w}.access.{f}: int required")
            if not 0 <= a["off"] < 0x100:
                sys.exit(SCHEMA_ERR + f"{w}.access.off: CF8 path requires off<0x100")
        else:
            sys.exit(SCHEMA_ERR + f"{w}.access.kind: 'mmio32'|'pci-cfg32'")
        seqs.append(e["sequence"])
    if len(seqs) != len(set(seqs)):
        sys.exit(SCHEMA_ERR + "sequence values must be unique")


def entry_addr(e):
    a = e["access"]
    if a["kind"] == "mmio32":
        return int(a["addr"], 16)
    return (a["bus"] << 16) | (a["dev"] << 11) | (a["fun"] << 8) | a["off"]


def gen(doc, mode_write, cold, pause_ms):
    entries = sorted(doc["entries"], key=lambda e: e["sequence"])
    lines = [
        "// Generated by hack/gen_mmr_table.py from docs/reports/2026-09-14-uncore-bif-mmr.json",
        "// Spec: docs/superpowers/specs/2026-09-14-bif-driver-recon-design.md 5 (no runtime fuzzing)",
        "#ifndef EPA_CONFIG_H_",
        "#define EPA_CONFIG_H_",
        "",
        f"#define EPA_MODE_WRITE {1 if mode_write else 0}",
        f"#define EPA_RESET_COLD {1 if cold else 0}",
        f"#define EPA_PAUSE_MS {pause_ms}",
        f"#define EPA_ENTRY_COUNT {len(entries)}",
        "",
        "typedef struct {",
        "  CONST CHAR8  *Tag;",
        "  UINT8         Kind;  // 0 = MMIO32, 1 = PCI CF832",
        "  UINT64        Addr;",
        "  UINT32        AndMask;",
        "  UINT32        OrValue;",
        "} MMR_ENTRY;",
        "",
        f"STATIC CONST MMR_ENTRY  mMmrEntries[EPA_ENTRY_COUNT] = {{",
    ]
    for e in entries:
        kind = 0 if e["access"]["kind"] == "mmio32" else 1
        lines.append(f'  {{ "{e["tag"]}", {kind}, 0x{entry_addr(e):X}, '
                     f'0x{parse_hex(e["and_mask"], "and_mask"):08X}, 0x{parse_hex(e["or_value"], "or_value"):08X} }},')
    lines += ["};", "", "#endif", ""]
    return "\n".join(lines)


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--mmr", required=True)
    ap.add_argument("--mode", choices=("read", "write"), default="read")
    ap.add_argument("--reset", choices=("warm", "cold"), default=None)
    ap.add_argument("--pause-ms", type=int, default=3000)
    ap.add_argument("--out", default=None)
    ap.add_argument("--check", action="store_true")
    args = ap.parse_args()
    doc = json.load(open(args.mmr))
    validate(doc)
    if args.check:
        print(f"ok: {len(doc['entries'])} entries")
        return
    text = gen(doc, args.mode == "write", (args.reset or doc["reset"]) == "cold", args.pause_ms)
    if args.out:
        open(args.out, "w").write(text)
        print(f"wrote {args.out}: mode={args.mode} entries={len(doc['entries'])}")
    else:
        sys.stdout.write(text)


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: Проверить генератор на дефолте и на позитивном кейсе**

```bash
hack/gen_mmr_table.py --mmr docs/reports/2026-09-14-uncore-bif-mmr.json --check
hack/gen_mmr_table.py --mmr docs/reports/2026-09-14-uncore-bif-mmr.json \
  --out docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h
grep -c 'EPA_ENTRY_COUNT 0' docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h
```

Expected: `ok: 0 entries`; заголовок создан, `EPA_ENTRY_COUNT 0`. Затем временный позитив-кейс (в /tmp, НЕ в отчётный JSON):

```bash
cat > /tmp/bif/mmr-pos.json <<'EOF'
{ "entries": [ { "tag": "T", "purpose": "t", "access": { "kind": "pci-cfg32", "bus": 0, "dev": 22, "fun": 4, "off": 64 },
  "width": 4, "and_mask": "0xFFFFFFF8", "or_value": "0x0", "sequence": 1 } ], "reset": "warm" }
EOF
hack/gen_mmr_table.py --mmr /tmp/bif/mmr-pos.json --mode write | grep -E 'EPA_MODE_WRITE 1|Kind|0xB440'
```

Expected: режим 1 и адрес `0xB440` (bus0 dev0x16 fun4 off0x40 упакованы: `22<<11 | 4<<8 | 64`). Негатив-кейсы (width 8, off 0x100, and_mask без 0x) — по одному, ожидаем `schema error` и exit≠0:

```bash
sed 's/"width": 4/"width": 8/' /tmp/bif/mmr-pos.json > /tmp/bif/mmr-neg.json
hack/gen_mmr_table.py --mmr /tmp/bif/mmr-neg.json --check; echo "exit=$?"
```

Expected: `schema error: entries[0].width…`, `exit=1`.

- [ ] **Step 4: Commit**

```bash
git add hack/gen_mmr_table.py docs/reports/2026-09-14-uncore-bif-mmr.json docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h
git commit -m "feat(hack): gen_mmr_table.py — схема-контракт MMR-таблицы и генератор EpaConfig.h (валидация, sequence-сортировка, RMW-записи)"
```

---

### Task 5: R2b — дифф native↔HNX и таблица MMR

**Files:**
- Modify: `docs/reports/2026-09-14-uncore-bif-mmr.json` (наполнить)
- Modify: `docs/reports/2026-09-14-uncore-bif-recon.md` (секция R2: дифф-вердикт + таблица)

**Interfaces:**
- Consumes: `hack/disasm.sh` (Task 2), `refs/extracted/bif/*.pei`, Purley-карта (Task 3), розетта `/refs/fw/IIO-pei32.bin`.
- Produces: валидный по схеме JSON с ≥1 записью бифуркации Port 2 (значение `or_value` = known-good HNX для x4x4x4x4); отчётный дифф-вердикт «где натив захардкодил» (offset в PEIM — вход варианта 2) — потребляют Tasks 6/8/9/10.

- [ ] **Step 1: Локализовать код бифуркации на розетте**

На розетте (строки есть; R0: розетта ≡ натив, sha256 совпадает — локализация сразу для натива) найти валидатор: `strings` → VA строки «Invalid IOUx Bifurcation» → `xref` → `dis` вокруг хитов (±0x100). Зафиксировать в отчёте: VA функции-валидатора, характерные константы (значения-энумы бифуркации: 0=x4x4x4x4 … 4=x16, 0xFF=Auto — те же, что в ручке, спека formset-unlock §6).

- [ ] **Step 2: Перенести якоря на native и HNX**

HNX/超微 строк-якорей не имеют (R0) — структурный перенос: байт-скан констант/imm-медиатов из Step 1 (`xref 0x…` на адреса-иммедиаты, найденные в коде валидатора) ЛИБО `mmio`-скан по диапазонам из `info` (BAR-диапазоны uncore: искать паттерны MMR-адресов как imm32). Модули натив/HNX разных ревизий (PE32 798880 vs 606112) — грубый байт-дифф бесполезен, матчить по валидаторным константам и структуре вызовов. Цель: на обоих образах найти кодовую область настройки IIO-бифуркации.

- [ ] **Step 3: Дифф полиси native↔HNX**

`dis` обеих областей; diff-подход: native ожидаемо пишет константу x16-политики (imm 0x4/0x40-подобные в RMW), HNX — читает значение из стора/таблицы. Зафиксировать в отчёте: (а) VA/offset расхождения в native (точный file-offset инструкции — это вход варианта 2); (б) конкретные MMR-адреса и значения x4x4x4x4 из HNX.

- [ ] **Step 4: Собрать таблицу MMR**

Каждый найденный регистр Port 2 → запись JSON (схема Task 4): tag `IOU0-Port2-<назначение>`, access по факту кода (CF8-паттерн → `pci-cfg32`; абсолютный адрес → `mmio32`), and_mask/or_value из RMW-инструкций HNX, sequence по порядку кода. Проверка:

```bash
hack/gen_mmr_table.py --mmr docs/reports/2026-09-14-uncore-bif-mmr.json --check
```

Expected: `ok: N entries`, N ≥ 1, среди tags есть `IOU0-Port2-*`. Если известен лок-регистр (виден в коде) — отдельная запись с `notes`, но БЕЗ неё в or_value (пробник локи не снимает — только фиксирует).

- [ ] **Step 5: Дифф-вердикт в отчёт + UART-база**

Секция R2 отчёта дополняется: таблица MMR (человекочитаемая копия JSON: purpose/access/native/hnx/sequence), вердикт «натив захардкодил <что> по <offset>», fallback-статус по R-A.4, и — для Task 8 — базовый адрес UART натива (из R0-заголовков/ServerSetup: ожидание COM1 0x3F8; подтвердить нечем без железа — отметить как допущение, EPA-1 проверит телеметрией).

- [ ] **Step 6: Commit**

```bash
git add docs/reports/2026-09-14-uncore-bif-mmr.json docs/reports/2026-09-14-uncore-bif-recon.md
git commit -m "docs(report): цикл A R2b — таблица MMR Port 2 (JSON, валидна по схеме) + дифф-вердикт native↔HNX: локализация захардкоженной полиси"
```

---

### Task 6: B1 — пакет UefiPatcherBifPkg, пробник, сборка, валидатор

**Files:**
- Create: `docker/edk2/UefiPatcherBifPkg/UefiPatcherBif.dsc`, `docker/edk2/UefiPatcherBifPkg/UefiPatcherBif.fdf`
- Create: `docker/edk2/UefiPatcherBifPkg/BifEpaProbe/BifEpaProbe.inf`, `docker/edk2/UefiPatcherBifPkg/BifEpaProbe/BifEpaProbe.c`
- Create: `docker/edk2/build_bif_epa.sh`, `hack/edk2_bif_check.py`
- Create (артефакт): `/tmp/bif/epa-build-1/BifEpaProbe.ffs`

**Interfaces:**
- Consumes: `EpaConfig.h` (Task 4/5), `docs/reports/2026-09-14-uncore-bif-mmr.json`, образец `docker/edk2/build_serial.sh`, `UefiPatcherSerial.dsc` (библиотечная карта), `hack/edk2_build_check.py` (паттерн валидатора).
- Produces: `docker/edk2/build_bif_epa.sh <out-dir> [--epa-mode=read|write] [--reset=warm|cold] [--pause-ms N]` → `BifEpaProbe.ffs` (FILE_GUID `B7E4A2C1-58D6-4E3F-B9A2-7C1D0E6F5A84`); FV `BIF_EPA_FV`; валидатор `hack/edk2_bif_check.py <out1> [<out2>]`. Потребляют Tasks 7/8/9.

- [ ] **Step 1: INF**

`docker/edk2/UefiPatcherBifPkg/BifEpaProbe/BifEpaProbe.inf`:

```
[Defines]
  INF_VERSION       = 0x00010005
  BASE_NAME         = BifEpaProbe
  FILE_GUID         = B7E4A2C1-58D6-4E3F-B9A2-7C1D0E6F5A84
  MODULE_TYPE       = DXE_DRIVER
  VERSION_STRING    = 0.1
  ENTRY_POINT       = BifEpaProbeEntry

[Sources]
  BifEpaProbe.c
  EpaConfig.h

[Packages]
  MdePkg/MdePkg.dec
  MdeModulePkg/MdeModulePkg.dec

[LibraryClasses]
  UefiDriverEntryPoint
  UefiBootServicesTableLib
  UefiRuntimeServicesTableLib
  BaseLib
  PrintLib
  IoLib
  SerialPortLib

[Protocols]
  gEfiVariableWriteArchProtocolGuid

[Depex]
  gEfiVariableWriteArchProtocolGuid
```

- [ ] **Step 2: Драйвер**

`docker/edk2/UefiPatcherBifPkg/BifEpaProbe/BifEpaProbe.c`:

```c
// Спека docs/superpowers/specs/2026-09-14-bif-driver-recon-design.md 5:
// EPA-лестница, one-shot BifEpaStage против ресет-лупа, post-reset отчёт.
#include <Uefi.h>
#include <Library/BaseLib.h>
#include <Library/IoLib.h>
#include <Library/PrintLib.h>
#include <Library/SerialPortLib.h>
#include <Library/UefiBootServicesTableLib.h>
#include <Library/UefiDriverEntryPoint.h>
#include <Library/UefiRuntimeServicesTableLib.h>
#include "EpaConfig.h"

#define EPA_VAR_NAME  L"BifEpaStage"

#define EPA_STAGE_IDLE     0
#define EPA_STAGE_WRITTEN  1

#define EPA_ACCESS_MMIO32  0
#define EPA_ACCESS_PCI_CF8 1

STATIC EFI_GUID  mEpaVarGuid = { 0x6F3B2C9D, 0x4A1E, 0x4D8F, { 0xB5, 0xC7, 0x2E, 0x9A, 0x0D, 0x8F, 0x1C, 0x34 } };

STATIC
VOID
ProbPrint (
  IN CONST CHAR8  *Fmt,
  ...
  )
{
  CHAR8    Buf[192];
  VA_LIST  Args;
  UINTN    Len;

  VA_START (Args, Fmt);
  AsciiVSPrint (Buf, sizeof (Buf), Fmt, Args);
  VA_END (Args);
  Len = AsciiStrLen (Buf);
  if (Len > 0) {
    SerialPortWrite ((UINT8 *)Buf, Len);
  }
}

STATIC
UINT32
EpaRead32 (
  IN CONST MMR_ENTRY  *Entry
  )
{
  if (Entry->Kind == EPA_ACCESS_MMIO32) {
    return MmioRead32 ((UINTN)Entry->Addr);
  }
  IoWrite32 (0xCF8, 0x80000000u | (UINT32)Entry->Addr);
  return IoRead32 (0xCFC);
}

STATIC
VOID
EpaWrite32 (
  IN CONST MMR_ENTRY  *Entry,
  IN UINT32            Value
  )
{
  if (Entry->Kind == EPA_ACCESS_MMIO32) {
    MmioWrite32 ((UINTN)Entry->Addr, Value);
    return;
  }
  IoWrite32 (0xCF8, 0x80000000u | (UINT32)Entry->Addr);
  IoWrite32 (0xCFC, Value);
}

STATIC
UINT32
EpaGetStage (
  VOID
  )
{
  EFI_STATUS  Status;
  UINTN       Size;
  UINT32      Stage;

  Size = sizeof (Stage);
  Stage = EPA_STAGE_IDLE;
  Status = gRT->GetVariable (EPA_VAR_NAME, &mEpaVarGuid, NULL, &Size, &Stage);
  if (EFI_ERROR (Status)) {
    ProbPrint ("BIF-EPA: prev stage: none (%r)\n", Status);
    return EPA_STAGE_IDLE;
  }
  return Stage;
}

STATIC
EFI_STATUS
EpaSetStage (
  IN UINT32  Stage
  )
{
  return gRT->SetVariable (
           EPA_VAR_NAME,
           &mEpaVarGuid,
           EFI_VARIABLE_NON_VOLATILE | EFI_VARIABLE_BOOTSERVICE_ACCESS,
           sizeof (Stage),
           &Stage
           );
}

STATIC
VOID
EpaDumpAll (
  IN CONST CHAR8  *Prefix
  )
{
  UINTN  Index;

  for (Index = 0; Index < EPA_ENTRY_COUNT; Index++) {
    ProbPrint ("BIF-EPA: %a [%u] %a raw=0x%08x\n",
               Prefix, (UINT32)Index, mMmrEntries[Index].Tag, EpaRead32 (&mMmrEntries[Index]));
  }
}

EFI_STATUS
EFIAPI
BifEpaProbeEntry (
  IN EFI_HANDLE        ImageHandle,
  IN EFI_SYSTEM_TABLE  *SystemTable
  )
{
  UINT32  Stage;
  UINTN   Index;
  UINT32  Mismatch;
  UINT32  Raw;
  UINT32  New;
  UINT32  Readback;

  SerialPortInitialize ();
  ProbPrint ("BIF-EPA: probe mode=%a entries=%u reset=%a\n",
             EPA_MODE_WRITE ? "write" : "read",
             (UINT32)EPA_ENTRY_COUNT,
             EPA_RESET_COLD ? "cold" : "warm");
  Stage = EpaGetStage ();
  ProbPrint ("BIF-EPA: prev stage=%u\n", Stage);

  if ((EPA_MODE_WRITE == 0) || (EPA_ENTRY_COUNT == 0)) {
    EpaDumpAll ("read");
    EpaSetStage (EPA_STAGE_IDLE);
    return EFI_SUCCESS;
  }

  if (Stage == EPA_STAGE_WRITTEN) {
    ProbPrint ("BIF-EPA: post-reset report (PEI overwrite check)\n");
    for (Index = 0; Index < EPA_ENTRY_COUNT; Index++) {
      Raw = EpaRead32 (&mMmrEntries[Index]);
      if ((Raw & ~mMmrEntries[Index].AndMask) == (mMmrEntries[Index].OrValue & ~mMmrEntries[Index].AndMask)) {
        ProbPrint ("BIF-EPA: [%u] %a raw=0x%08x KEPT\n", (UINT32)Index, mMmrEntries[Index].Tag, Raw);
      } else {
        ProbPrint ("BIF-EPA: [%u] %a raw=0x%08x LOST (PEI reprogrammed?)\n", (UINT32)Index, mMmrEntries[Index].Tag, Raw);
      }
    }
    EpaSetStage (EPA_STAGE_IDLE);
    return EFI_SUCCESS;
  }

  Mismatch = 0;
  for (Index = 0; Index < EPA_ENTRY_COUNT; Index++) {
    Raw = EpaRead32 (&mMmrEntries[Index]);
    New = (Raw & mMmrEntries[Index].AndMask) | mMmrEntries[Index].OrValue;
    EpaWrite32 (&mMmrEntries[Index], New);
    Readback = EpaRead32 (&mMmrEntries[Index]);
    ProbPrint ("BIF-EPA: write [%u] %a raw=0x%08x new=0x%08x rb=0x%08x %a\n",
               (UINT32)Index, mMmrEntries[Index].Tag, Raw, New, Readback,
               Readback == New ? "OK" : "MISMATCH");
    if (Readback != New) {
      Mismatch++;
    }
  }

  if (Mismatch != 0) {
    ProbPrint ("BIF-EPA: %u mismatch - NO reset\n", Mismatch);
    EpaSetStage (EPA_STAGE_IDLE);
    return EFI_SUCCESS;
  }

  if (EFI_ERROR (EpaSetStage (EPA_STAGE_WRITTEN))) {
    ProbPrint ("BIF-EPA: stage marker failed - NO reset\n");
    return EFI_SUCCESS;
  }

  ProbPrint ("BIF-EPA: readback OK - %a reset in %u ms\n",
             EPA_RESET_COLD ? "cold" : "warm", (UINT32)EPA_PAUSE_MS);
  gBS->Stall (EPA_PAUSE_MS * 1000);
  gRT->ResetSystem (EPA_RESET_COLD ? EfiResetCold : EfiResetWarm, EFI_SUCCESS, 0, NULL);
  ProbPrint ("BIF-EPA: ResetSystem returned!\n");
  return EFI_SUCCESS;
}
```

- [ ] **Step 3: DSC и FDF**

`docker/edk2/UefiPatcherBifPkg/UefiPatcherBif.dsc` (библиотечная карта и серийные PCD — копия `UefiPatcherSerial.dsc`, кроме PLATFORM/Components):

```
[Defines]
  PLATFORM_NAME           = UefiPatcherBif
  PLATFORM_GUID           = 8F1D4B6A-2C3E-4A5D-9B7F-6E2C1D4A5B8E
  PLATFORM_VERSION        = 0.1
  DSC_SPECIFICATION       = 0x00010005
  OUTPUT_DIRECTORY        = Build/UefiPatcherBif
  SUPPORTED_ARCHITECTURES = X64
  BUILD_TARGETS           = RELEASE
  SKUID_IDENTIFIER        = DEFAULT
  FLASH_DEFINITION        = UefiPatcherBifPkg/UefiPatcherBif.fdf

[LibraryClasses]
  BaseLib|MdePkg/Library/BaseLib/BaseLib.inf
  BaseMemoryLib|MdePkg/Library/BaseMemoryLib/BaseMemoryLib.inf
  DebugLib|MdePkg/Library/BaseDebugLibNull/BaseDebugLibNull.inf
  PrintLib|MdePkg/Library/BasePrintLib/BasePrintLib.inf
  PcdLib|MdePkg/Library/DxePcdLib/DxePcdLib.inf
  MemoryAllocationLib|MdePkg/Library/UefiMemoryAllocationLib/UefiMemoryAllocationLib.inf
  UefiBootServicesTableLib|MdePkg/Library/UefiBootServicesTableLib/UefiBootServicesTableLib.inf
  UefiRuntimeServicesTableLib|MdePkg/Library/UefiRuntimeServicesTableLib/UefiRuntimeServicesTableLib.inf
  UefiDriverEntryPoint|MdePkg/Library/UefiDriverEntryPoint/UefiDriverEntryPoint.inf
  UefiLib|MdePkg/Library/UefiLib/UefiLib.inf
  IoLib|MdePkg/Library/BaseIoLibIntrinsic/BaseIoLibIntrinsic.inf
  PlatformHookLib|MdeModulePkg/Library/BasePlatformHookLibNull/BasePlatformHookLibNull.inf
  PciLib|MdePkg/Library/BasePciLibCf8/BasePciLibCf8.inf
  PciCf8Lib|MdePkg/Library/BasePciCf8Lib/BasePciCf8Lib.inf
  StackCheckLib|MdePkg/Library/StackCheckLibNull/StackCheckLibNull.inf
  RegisterFilterLib|MdePkg/Library/RegisterFilterLibNull/RegisterFilterLibNull.inf
  ReportStatusCodeLib|MdePkg/Library/BaseReportStatusCodeLibNull/BaseReportStatusCodeLibNull.inf
  SerialPortLib|MdeModulePkg/Library/BaseSerialPortLib16550/BaseSerialPortLib16550.inf

[Components]
  UefiPatcherBifPkg/BifEpaProbe/BifEpaProbe.inf

[PcdsFixedAtBuild]
  gEfiMdePkgTokenSpaceGuid.PcdUartDefaultBaudRate|115200
  gEfiMdePkgTokenSpaceGuid.PcdUartDefaultDataBits|8
  gEfiMdePkgTokenSpaceGuid.PcdUartDefaultParity|1
  gEfiMdePkgTokenSpaceGuid.PcdUartDefaultStopBits|1
  gEfiMdePkgTokenSpaceGuid.PcdUartDefaultReceiveFifoDepth|1
  gEfiMdeModulePkgTokenSpaceGuid.PcdErrorCodeSetVariable|0x03058002
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialRegisterBase|0x3F8
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialUseMmio|FALSE
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialBaudRate|115200
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialLineControl|0x03
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialFifoControl|0x07
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialClockRate|1843200
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialRegisterStride|1
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialRegisterAccessWidth|8
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialUseHardwareFlowControl|FALSE
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialDetectCable|FALSE
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialExtendedTxFifoSize|64
  gEfiMdeModulePkgTokenSpaceGuid.PcdSerialPciDeviceInfo|{0xFF}
```

`docker/edk2/UefiPatcherBifPkg/UefiPatcherBif.fdf`:

```
[FV.BIF_EPA_FV]
FvNameGuid           = 4D2E7A1C-9F3B-4C6D-8E5A-1B7C3D9F2E64
INF UefiPatcherBifPkg/BifEpaProbe/BifEpaProbe.inf

[Rule.Common.DXE_DRIVER]
  FILE DRIVER = $(NAMED_GUID) {
    DXE_DEPEX    DXE_DEPEX               Optional   |.depex
    PE32     PE32                    |.efi
    UI       STRING="$(MODULE_NAME)"
  }
```

- [ ] **Step 4: Сборочный скрипт**

`docker/edk2/build_bif_epa.sh` (паттерн `docker/edk2/build_serial.sh`):

```bash
#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/../.." && pwd)"
PKG_DIR="$REPO_ROOT/docker/edk2"
EDK2_SRC="$REPO_ROOT/refs/edk2"
IMAGE="uefipatcher-edk2-builder:latest"
MMR_JSON="$REPO_ROOT/docs/reports/2026-09-14-uncore-bif-mmr.json"
HDR_OUT="$PKG_DIR/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h"

OUT_DIR=""
EPA_MODE="read"
EPA_RESET="warm"
EPA_PAUSE_MS=3000
while [ $# -gt 0 ]; do
    case "$1" in
        --epa-mode=*) EPA_MODE="${1#*=}" ;;
        --reset=*) EPA_RESET="${1#*=}" ;;
        --pause-ms=*) EPA_PAUSE_MS="${1#*=}" ;;
        *) OUT_DIR="$1" ;;
    esac
    shift
done
[ -n "$OUT_DIR" ] || { echo "usage: build_bif_epa.sh <out-dir> [--epa-mode=read|write] [--reset=warm|cold] [--pause-ms=N]" >&2; exit 2; }
case "$EPA_MODE" in read|write) ;; *) echo "--epa-mode: read|write" >&2; exit 2 ;; esac
case "$EPA_RESET" in warm|cold) ;; *) echo "--reset: warm|cold" >&2; exit 2 ;; esac

if [ "${EDK2_BUILDER_CONTAINER:-0}" != "1" ]; then
    python3 "$REPO_ROOT/hack/gen_mmr_table.py" --mmr "$MMR_JSON" \
        --mode "$EPA_MODE" --reset "$EPA_RESET" --pause-ms "$EPA_PAUSE_MS" --out "$HDR_OUT"
    mkdir -p "$OUT_DIR"
    podman-remote run --rm --security-opt label=disable \
        -v "$EDK2_SRC":/src/edk2:ro \
        -v "$PKG_DIR":/pkg:ro \
        -v "$OUT_DIR":/out \
        "$IMAGE" /pkg/build_bif_epa.sh /out
    echo "artifacts: $OUT_DIR"
    ls -la "$OUT_DIR"
    exit 0
fi

: "${OUT_DIR:?}"
rm -rf /work/edk2
mkdir -p /work/edk2
git -C /src/edk2 archive --format=tar HEAD | tar -xf - -C /work/edk2
mkdir -p /work/edk2/MdePkg/Library/MipiSysTLib/mipisyst/library/include \
         /work/edk2/MdeModulePkg/Library/BrotliCustomDecompressLib/brotli/c/include
cd /work/edk2
make -C BaseTools -j"$(nproc)" APPLICATIONS='GenFfs GenFv GenSec GenFw'
export WORKSPACE="$PWD"
export PACKAGES_PATH="$PWD:/pkg"
set +u
. ./edksetup.sh BaseTools
set -u
build -p UefiPatcherBifPkg/UefiPatcherBif.dsc -a X64 -t GCC -b RELEASE -n "$(nproc)"
BUILD_ROOT="$WORKSPACE/Build/UefiPatcherBif/RELEASE_GCC"
cp "$BUILD_ROOT/FV/Ffs/B7E4A2C1-58D6-4E3F-B9A2-7C1D0E6F5A84BifEpaProbe/B7E4A2C1-58D6-4E3F-B9A2-7C1D0E6F5A84.ffs" "$OUT_DIR/BifEpaProbe.ffs"
ls -la "$OUT_DIR"
```

Контейнерный режим получает уже спечённый `EpaConfig.h` через `/pkg:ro` (генератор работает только на хосте — в контейнере `/pkg` read-only).

- [ ] **Step 5: Валидатор артефактов**

`hack/edk2_bif_check.py` (slim-вариант `hack/edk2_build_check.py`, общий ход — копия оттуда):

```python
#!/usr/bin/env python3
"""edk2_bif_check.py — инварианты артефакта BifEpaProbe (цикл A).

Спека docs/superpowers/specs/2026-09-14-bif-driver-recon-design.md 5/9;
паттерн hack/edk2_build_check.py (S1). Со вторым аргументом-каталогом
сравнивает две сборки побайтово (допустимое расхождение — только COFF
TimeDateStamp). Не часть движка; запуск вручную:

    python3 hack/edk2_bif_check.py <out-dir> [<out-dir2>]
"""
import struct
import sys
import uuid

EXPECTED = {
    "BifEpaProbe.ffs": ("B7E4A2C1-58D6-4E3F-B9A2-7C1D0E6F5A84", 11),
}
FFS_TYPE_DRIVER = 0x07
SECTION_PE32 = 0x10
SECTION_UI = 0x15
PE_MACHINE_AMD64 = 0x8664


def u24(b, o):
    return b[o] | (b[o + 1] << 8) | (b[o + 2] << 16)


def walk_sections(d):
    off, end, pe32s, ui = 24, len(d), [], None
    while off + 4 <= end:
        size, stype = u24(d, off), d[off + 3]
        if size < 4:
            break
        if stype == SECTION_PE32:
            pe32s.append((off + 4, d[off + 4:off + size]))
        elif stype == SECTION_UI:
            ui = d[off + 4:off + size].decode("utf-16-le", "ignore").rstrip("\x00")
        off += (size + 3) & ~3
    return pe32s, ui


def pe_fields(pe):
    assert pe[:2] == b"MZ", "no MZ"
    lfa = struct.unpack_from("<I", pe, 0x3C)[0]
    assert pe[lfa:lfa + 4] == b"PE\x00\x00", "no PE signature"
    machine = struct.unpack_from("<H", pe, lfa + 4)[0]
    subsystem = struct.unpack_from("<H", pe, lfa + 92)[0]
    return machine, subsystem, lfa + 8


def zero_coff_tds(b):
    pe32s, _ = walk_sections(b)
    for pe_off, pe32 in pe32s:
        machine, subsystem, tds_off = pe_fields(pe32)
        struct.pack_into("<I", b, pe_off + tds_off, 0)


def check_ffs(path, expect_guid, expect_subsys):
    d = open(path, "rb").read()
    guid = str(uuid.UUID(bytes_le=d[:16])).upper()
    assert guid == expect_guid, f"{path}: guid {guid}"
    assert d[0x12] == FFS_TYPE_DRIVER, f"{path}: type {d[0x12]:#04x}"
    hdr = bytearray(d[:24])
    hdr[17] = hdr[23] = 0
    s = 0
    for x in hdr:
        s = (s + x) & 0xFF
    assert s == 0, f"{path}: header checksum"
    assert u24(d, 0x14) == len(d), f"{path}: size24 {u24(d, 0x14)} != {len(d)}"
    pe32s, ui = walk_sections(d)
    assert pe32s, f"{path}: no PE32"
    machine, subsystem, _ = pe_fields(pe32s[0][1])
    assert machine == PE_MACHINE_AMD64, f"{path}: machine {machine:#06x}"
    assert subsystem == expect_subsys, f"{path}: subsystem {subsystem}"
    assert ui == "BifEpaProbe", f"{path}: ui {ui!r}"
    assert b"BIF-EPA:" in pe32s[0][1], f"{path}: probe markers missing"
    return d


def main():
    if len(sys.argv) not in (2, 3):
        sys.exit(__doc__)
    out1 = sys.argv[1]
    blobs = {name: check_ffs(f"{out1}/{name}", guid, subsys) for name, (guid, subsys) in EXPECTED.items()}
    if len(sys.argv) == 3:
        out2 = sys.argv[2]
        for name, (guid, subsys) in EXPECTED.items():
            b2 = bytearray(check_ffs(f"{out2}/{name}", guid, subsys))
            b1 = bytearray(blobs[name])
            zero_coff_tds(b1)
            zero_coff_tds(b2)
            assert bytes(b1) == bytes(b2), f"{name}: builds differ beyond COFF TimeDateStamp"
    print(f"ok: {len(EXPECTED)} artifact(s)" + (", reproducible" if len(sys.argv) == 3 else ""))


if __name__ == "__main__":
    main()
```

`chmod +x docker/edk2/build_bif_epa.sh`.

- [ ] **Step 6: Собрать read-режим дважды и провалидировать**

```bash
docker/edk2/build_bif_epa.sh /tmp/bif/epa-build-1 --epa-mode=read
docker/edk2/build_bif_epa.sh /tmp/bif/epa-build-2 --epa-mode=read
python3 hack/edk2_bif_check.py /tmp/bif/epa-build-1 /tmp/bif/epa-build-2
```

Expected: обе сборки проходят; `ok: 1 artifact(s), reproducible`. При ошибках сборки — читать лог контейнера, чинить по правилу-11 (расхождение с планом — docs-коммит правки плана сначала). Артефакт и EpaConfig.h (read-режим, таблица Task 5) зафиксировать в отчёте B1 (sha256, размер, `EPA_ENTRY_COUNT`).

- [ ] **Step 7: Commit**

```bash
git add docker/edk2/UefiPatcherBifPkg docker/edk2/build_bif_epa.sh hack/edk2_bif_check.py docs/reports/2026-09-14-uncore-bif-recon.md
git commit -m "feat(edk2): UefiPatcherBifPkg + BifEpaProbe (EPA-лестница, one-shot BifEpaStage, post-reset отчёт) + сборка build_bif_epa.sh + валидатор edk2_bif_check.py"
```

---

### Task 7: Движковый тест над артефактом (bif_ffs.rs)

**Files:**
- Create: `crates/uefi-engine/tests/bif_ffs.rs`
- Create: `crates/uefi-engine/tests/data/bif/BifEpaProbe.ffs` (копия `/tmp/bif/epa-build-1/BifEpaProbe.ffs`)

**Interfaces:**
- Consumes: `uefi_engine::ffs::{calculate_checksum8, uint24_to_u32}`, `uefi_engine::parser::file::guid_from_bytes`, артефакт Task 6.
- Produces: гейт `cargo test -p uefi-engine` для всех последующих пересборок артефакта (Tasks 8–9 обязаны оставлять его зелёным).

- [ ] **Step 1: Скопировать артефакт и написать падающий тест**

```bash
mkdir -p crates/uefi-engine/tests/data/bif
cp /tmp/bif/epa-build-1/BifEpaProbe.ffs crates/uefi-engine/tests/data/bif/
```

`crates/uefi-engine/tests/bif_ffs.rs`:

```rust
use std::path::PathBuf;
use uefi_engine::ffs::{calculate_checksum8, uint24_to_u32};
use uefi_engine::parser::file::guid_from_bytes;

const FFS_TYPE_DRIVER: u8 = 0x07;
const SECTION_PE32: u8 = 0x10;
const SECTION_UI: u8 = 0x15;
const PE_MACHINE_AMD64: u16 = 0x8664;
const PE_SUBSYSTEM_BOOT_DRIVER: u16 = 11;

#[test]
fn bif_epa_probe_artifact_invariants() {
    let bytes = std::fs::read(
        PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/data/bif/BifEpaProbe.ffs"),
    )
    .expect("artifact missing: run docker/edk2/build_bif_epa.sh (план Task 6)");
    assert!(bytes.len() >= 24);

    let guid = guid_from_bytes(&bytes[..16]).unwrap().to_string().to_ascii_uppercase();
    assert_eq!(guid, "B7E4A2C1-58D6-4E3F-B9A2-7C1D0E6F5A84");
    assert_eq!(bytes[0x12], FFS_TYPE_DRIVER);

    let mut hdr = bytes[..24].to_vec();
    hdr[17] = 0;
    hdr[23] = 0;
    assert_eq!(calculate_checksum8(&hdr), 0, "FFS header checksum");

    let size24 = uint24_to_u32([bytes[0x14], bytes[0x15], bytes[0x16]]) as usize;
    assert_eq!(size24, bytes.len(), "FFS size24 == file size");

    let mut off = 24usize;
    let mut pe32: Option<&[u8]> = None;
    let mut ui_name = String::new();
    while off + 4 <= bytes.len() {
        let sec_size = uint24_to_u32([bytes[off], bytes[off + 1], bytes[off + 2]]) as usize;
        if sec_size < 4 {
            break;
        }
        match bytes[off + 3] {
            SECTION_PE32 => pe32 = Some(&bytes[off + 4..off + sec_size]),
            SECTION_UI => {
                let raw = &bytes[off + 4..off + sec_size];
                let units: Vec<u16> = raw.chunks_exact(2).map(|c| u16::from_le_bytes([c[0], c[1]])).collect();
                ui_name = String::from_utf16_lossy(&units).trim_end_matches('\0').to_string();
            }
            _ => {}
        }
        off += (sec_size + 3) & !3;
    }

    let pe = pe32.expect("no PE32 section");
    assert_eq!(&pe[..2], b"MZ");
    let lfanew = u32::from_le_bytes(pe[0x3C..0x40].try_into().unwrap()) as usize;
    assert_eq!(&pe[lfanew..lfanew + 4], b"PE\x00\x00");
    let pe_machine = u16::from_le_bytes(pe[lfanew + 4..lfanew + 6].try_into().unwrap());
    let pe_subsystem = u16::from_le_bytes(pe[lfanew + 92..lfanew + 94].try_into().unwrap());
    assert_eq!(pe_machine, PE_MACHINE_AMD64);
    assert_eq!(pe_subsystem, PE_SUBSYSTEM_BOOT_DRIVER);
    assert_eq!(ui_name, "BifEpaProbe");
    let marker = b"BIF-EPA:".as_slice();
    assert!(
        pe.windows(marker.len()).any(|w| w == marker),
        "probe markers missing in PE32"
    );
}
```

- [ ] **Step 2: Прогнать тест**

```bash
cargo test -p uefi-engine --test bif_ffs
```

Expected: PASS (артефакт уже скопирован в Step 1; падение = артефакт/инварианты битые → чинить Task 6, не тест).

- [ ] **Step 3: Полный прогон и commit**

```bash
cargo test -p uefi-engine && cargo clippy -p uefi-engine -- -D warnings && cargo fmt --all -- --check
git add crates/uefi-engine/tests/bif_ffs.rs crates/uefi-engine/tests/data/bif/BifEpaProbe.ffs
git commit -m "test(engine): bif_ffs — инварианты артефакта BifEpaProbe (GUID/тип/чексуммы/PE64/UI-имя/маркеры BIF-EPA)"
```

---

### Task 8: B2 — образ EPA-1 (read-only) и владельческий гейт

**Files:**
- Create (локально, не в git): `refs/amibcp/450x-kopiya-bif-epa1.bin`
- Modify: `docs/reports/2026-09-14-uncore-bif-recon.md` (секция B2: артефакт + инструкция + вердикт-плейсхолдер «ждёт владельца»)
- Modify (если артефакт/конфиг менялись): `crates/uefi-engine/tests/data/bif/BifEpaProbe.ffs`, `docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h`

**Interfaces:**
- Consumes: `build_bif_epa.sh` (Task 6), таблица Task 5, движок + `node insert` (паттерн E30: `image open` → хвостовой файл DXE-FV → `--mode after`).
- Produces: флеш-кандидат EPA-1 + инструкция владельцу; вердикт владельца (UART-телеметрия жива? raw-значения) — вход Tasks 9/10.

- [ ] **Step 1: Собрать read-режим с боевой таблицей и провалидировать**

```bash
docker/edk2/build_bif_epa.sh /tmp/bif/epa1 --epa-mode=read
python3 hack/edk2_bif_check.py /tmp/bif/epa1
cp /tmp/bif/epa1/BifEpaProbe.ffs crates/uefi-engine/tests/data/bif/
cargo test -p uefi-engine --test bif_ffs
```

Expected: валидатор зелёный, тест зелёный (артефакт обновлён под боевую таблицу).

- [ ] **Step 2: Собрать образ EPA-1**

```bash
export UEFIPATCHER_SOCK=/tmp/bif/engine.sock
rm -f "$UEFIPATCHER_SOCK"; nohup target/debug/engine > /tmp/bif/engine.log 2>&1 & sleep 1
target/debug/uefi-cli session init --force
target/debug/uefi-cli image open --mode write "refs/amibcp/450x — копия.bin"
target/debug/uefi-cli node list | tail -5
```

Найти хвостовой файл DXE-тома (аналог E30: FV1, последний `File(DXE driver)` — путь вида `3/2NN`).

```bash
target/debug/uefi-cli node insert <путь-хвоста> --file /tmp/bif/epa1/BifEpaProbe.ffs --mode after
target/debug/uefi-cli image save refs/amibcp/450x-kopiya-bif-epa1.bin
sha256sum "refs/amibcp/450x — копия.bin" refs/amibcp/450x-kopiya-bif-epa1.bin
pkill -x engine
```

Expected: размер = базе (16 МиБ); дифф только вставка в DXE-FV (сверка по engine.log `artifact inserted … image built size=16777216`).

- [ ] **Step 3: Секция B2 в отчёте: артефакт + инструкция владельцу**

Таблица: путь, sha256, размер, хвост-таргет вставки, `EPA_ENTRY_COUNT`/режим из EpaConfig.h. Инструкция владельцу (дословно в отчёт): прошить `450x-kopiya-bif-epa1.bin` через TMM; при буте смотреть SOL: ожидать строки `BIF-EPA: probe mode=read entries=N`, `prev stage=…`, `read [i] <tag> raw=0x…`. Телеметрия значений — только UART (NVRAM-переменная хранит лишь маркер стадии для one-shot логики); **молчание UART = STOP-условие**: без телеметрии EPA-2 не прошивать, возврат к Task 5 за уточнением UART-базы. Вердикт-строка: `ждёт владельца (дата)`.

- [ ] **Step 4: Commit (без вердикта)**

```bash
git add docs/reports/2026-09-14-uncore-bif-recon.md crates/uefi-engine/tests/data/bif/BifEpaProbe.ffs docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h
git commit -m "docs(report): цикл A B2 — образ EPA-1 read-only (sha256, вставка в DXE-FV копии) + инструкция владельцу; ждём вердикта"
```

- [ ] **Step 5: Владельческий гейт (ждать ответа владельца)**

Вердикт владельца (SOL-лог / «молчание» + значения raw) дописать в секцию B2 и пере-коммитить:

```bash
git add docs/reports/2026-09-14-uncore-bif-recon.md
git commit -m "docs(report): цикл A B2 — вердикт владельца EPA-1 (телеметрия/raw-значения)"
```

Далее Task 9. Если телеметрия молчит и NVRAM-канал недоступен для чтения владельцем — STOP: вернуться к Task 5 (уточнить UART-базу) по правилу-11; без телеметрии EPA-2 не прошивать.

---

### Task 9: B3 — образ EPA-2 (write-ladder) и владельческий гейт

**Files:**
- Create (локально, не в git): `refs/amibcp/450x-kopiya-bif-epa2.bin`
- Modify: `docs/reports/2026-09-14-uncore-bif-recon.md` (секция B3)
- Modify: `crates/uefi-engine/tests/data/bif/BifEpaProbe.ffs`, `docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h` (write-режим)

**Interfaces:**
- Consumes: вердикт EPA-1 (Task 8 Step 5), `build_bif_epa.sh --epa-mode=write`.
- Produces: флеш-кандидат EPA-2 + инструкция; результаты write-ladder (readback, пост-ресет отчёт, топология ОС) — вход Task 10.

- [ ] **Step 1: Собрать write-режим**

```bash
docker/edk2/build_bif_epa.sh /tmp/bif/epa2 --epa-mode=write
python3 hack/edk2_bif_check.py /tmp/bif/epa2
cp /tmp/bif/epa2/BifEpaProbe.ffs crates/uefi-engine/tests/data/bif/
cargo test -p uefi-engine --test bif_ffs
```

- [ ] **Step 2: Собрать образ EPA-2**

```bash
export UEFIPATCHER_SOCK=/tmp/bif/engine.sock
rm -f "$UEFIPATCHER_SOCK"; nohup target/debug/engine > /tmp/bif/engine.log 2>&1 & sleep 1
target/debug/uefi-cli session init --force
target/debug/uefi-cli image open --mode write "refs/amibcp/450x — копия.bin"
target/debug/uefi-cli node list | tail -5
target/debug/uefi-cli node insert <путь-хвоста-DXE-FV> --file /tmp/bif/epa2/BifEpaProbe.ffs --mode after
target/debug/uefi-cli image save refs/amibcp/450x-kopiya-bif-epa2.bin
sha256sum "refs/amibcp/450x — копия.bin" refs/amibcp/450x-kopiya-bif-epa2.bin
pkill -x engine
```

Expected: размер = базе (16 МиБ), вставка в DXE-FV. Прописать в отчёте бит-карту исходов (спека §6): какая комбинация «rb OK/LOST/KEPT + топология» какой вариант выбирает.

- [ ] **Step 3: Инструкция владельцу**

Дословно в отчёт: прошить EPA-2; при первом буте на SOL ожидать лестницу `write [i] … rb=… OK`, затем `readback OK - warm reset in 3000 ms` и респект-цикл; **после ресета** — второй бут печатает `post-reset report` с KEPT/LOST по каждой записи; затем зайти в ОС и проверить топологию Port 2 (`lspci -nn | grep -i nvme` / `nvme list`: две NVMe на 2A/2B = PASS-топология). При `MISMATCH … NO reset` — машина остаётся живой, доклад по строкам лога. При зависании на write — последняя напечатанная строка = виновник (записать и остановиться: TMM-recovery, результат в Task 10).

- [ ] **Step 4: Commit (без вердикта) + владельческий гейт**

```bash
git add docs/reports/2026-09-14-uncore-bif-recon.md crates/uefi-engine/tests/data/bif/BifEpaProbe.ffs docker/edk2/UefiPatcherBifPkg/BifEpaProbe/EpaConfig.h
git commit -m "docs(report): цикл A B3 — образ EPA-2 write-ladder (sha256) + инструкция владельцу; ждём вердикта"
```

Вердикт владельца (строки SOL-лога обоих бутов + топология ОС) дописать и закоммитить:

```bash
git add docs/reports/2026-09-14-uncore-bif-recon.md
git commit -m "docs(report): цикл A B3 — вердикт владельца EPA-2 (readback/пост-ресет/топология)"
```

---

### Task 10: G — гейт выбора варианта и закрытие цикла A

**Files:**
- Modify: `docs/reports/2026-09-14-uncore-bif-recon.md` (секция G)
- Modify: `TODO.md` (пункт саги «следующий цикл — свой бифуркационный драйвер…» — отметить выбор варианта и вход цикла B)

**Interfaces:**
- Consumes: все вердикты Tasks 5/8/9.
- Produces: выбранный вариант 1–4 с железным обоснованием; вход цикла B (для варианта 1 — готовый MVP; для 2 — offset патча; для 3 — HOB-заметки диффа).

- [ ] **Step 1: Заполнить матрицу решения**

Секция G отчёта — таблица по спеке §6 (все пять строк) с фактическими исходами и одной выделенной строкой-вердиктом. Плюс: список того, что уходит в цикл B (вариант, артефакты, открытые хвосты — например, UART-допущение 0x3F8, если не подтвердилось).

- [ ] **Step 2: Обновить TODO.md**

В пункте «следующий цикл — свой бифуркационный драйвер вместо борьбы…» дополнить: `[x] цикл A (разведка) закрыт <дата>: вариант N (обоснование — отчёт 2026-09-14-uncore-bif-recon.md §G); цикл B = <одно предложение скоупа>`. Новый пункт цикла B НЕ создавать (его откроет brainstorm цикла B).

- [ ] **Step 3: Commit**

```bash
git add docs/reports/2026-09-14-uncore-bif-recon.md TODO.md
git commit -m "docs(report,todo): цикл A закрыт — гейт G выбрал вариант N (EPA-вердикты, обоснование); вход цикла B зафиксирован"
```

---

## Самопроверка соответствия спеке (исполнителю — сверяться, не исполнять)

| Спека | План |
|---|---|
| §3 R0/R1/R2/B1/B2/B3/G | Tasks 1/2/3+5/4+6+7/8/9/10 |
| §4 дифф native↔HNX, розетта, Purley, fallback R-A.4 | Tasks 2 (розетта), 3 (Purley), 5 (дифф+fallback) |
| §5 пробник: two-channel телеметрия, one-shot, лестница, пауза, no-fuzzing | Task 6 Step 2 (C-код), Task 4 (таблица-контракт) |
| §5 инжект node insert, TMM-recovery | Tasks 8–9 (образы, инструкции) |
| §6 матрица гейта | Task 10 Step 1 |
| §7 движок ноль изменений; refs/* не коммитятся и не модифицируются | Global Constraints |
| §9 артефакты (коммитимое/локальное) | Interfaces задач + Constraints |
