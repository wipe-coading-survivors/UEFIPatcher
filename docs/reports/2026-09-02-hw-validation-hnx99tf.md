# Отчёт: аппаратная валидация unhide на Huananzhi X99-TF (HNX99TF) — 2026-09-02

Сессия живой прошивки внешним программатором. Плата — та же, что в
июльских экспериментах `~/git/BIOS` (перенос Console Redirection,
донор Machinist MNX99MR9A). Образ базы —
`refs/fw/HNX99TF_200525_original_E5C88C6F.bin` (16 МиБ, sha256
`7e9d2544…a1c5d`).

Плейсхолдеры: E-образы и пробники — `refs/amibcp/` (вне репозитория);
рабочие файлы сессии — `/tmp/uefipatcher-smoke/` (volatile).

## 1. Исходная точка

Первый smoke движка: `hii form set-visibility 'abbce13d-…:0x10:0#901'
--visible` + `image save` (CLI → RPC → LZMA-рекомпрессия → билдер).
Форма 901 «CPU HWPM State Control» (формсет EC87D643 «IntelRCSetup»,
файл FFS ABBCE13D, PE32 → .rsrc 'HII') — единственная suppressed-форма
образа. Результат на железе: POST идёт, **видеописк «нет видеокарты»**.
Оригинал, прошитый тем же программатором, — видео есть (аппаратная
часть исключена).

## 2. Матрица экспериментов

| Образ | Изменения относительно оригинала | Поток LZMA | Итог на железе |
|---|---|---|---|
| оригинал | — | AMI (props 0x5D, dict 16 МиБ) | грузится, видео есть |
| `hnx-patched.bin` (= движок) | IFR-правка 901 + рекомпрессия + рост файла 0x16279→0x42424 + сдвиг ~100 файлов +180КБ | **lzma-rs** (0x5D, 8 МиБ) | видеописк |
| `e1-recompress-only.bin` | та же рекомпрессия/сдвиг, **без** IFR-правки | **lzma-rs** | видеописк |
| `e3-minimal.bin` | IFR-правка, поток вписан в слот секции (файл/раскладка байт-в-байт, изменены только 90 160 байт потока) | liblzma (props 0x00=lc0/lp0/pb0, dict 16 МиБ, nice 273, size в заголовке, хвост добит нулями) | **грузится, видео есть** |
| `e5-identity-repack.bin` | только замена потока, контент без правки | liblzma (0x00) | не шился (не понадобился) |
| `e6-pure-shift.bin` | **ни одного изменённого байта контента**: pad-файл 0x2C0A8 перед ABBCE13D, все файлы FV2 после него сдвинуты ровно на дельту E1 (сдвиг поглощён свободным местом тома, за FV2 всё на местах) | родной AMI | **грузится** |
| `e7-liblzma-grown.bin` | движковый контент (IFR-правка + 2 хвостовых байта билдера), файл 0x1611B (меньше оригинала!), освободённое место закрыто pad-файлом 0x2C308, соседние файлы не тронуты | liblzma (0x00) | **грузится, видео, форма 901 открывается** |
| июльский `HUANAN-mod-CR-full-v3-final.rom` | файл Setup 899407D7 сжат на месте (0x6521→0x5b90) + pad-файл в освобождённом месте + 2 новых FFS (TerminalSrc/SerialIo) в свободном хвосте; ни один родной файл не сдвинут | liblzma (props 0x02=lc2/pb0, dict 16 МиБ) | грузился |

## 3. Корневые причины (две, обе закрыты диффами)

### 3.1 Видеописк = LZMA-поток lzma-rs

Дифференциальная цепочка: E1 (lzma-rs + сдвиг) мёртв; E6 (чистый сдвиг,
тот же объём смещения) жив; E3/E7 (liblzma, разные раскладки, в т.ч.
движковая с ростом) живы. Все остальные байты E1 проверены: 215 из 216
файлов FV2 байт-в-байт (с учётом сдвига), заголовки/чексуммы/выравнивание
всех файлов валидны, FIT-таблица не задета (её ссылки ≥0xd70000, сдвиг
кончается на 0xd60000), DEPEX идентичен, GUIDed-заголовок идентичен.
Остаточные отличия E1 от оригинала, кроме потока: +2 нулевых байта в
хвосте декомпрессированного потока, +1 байт 0x00 в хвосте файла (оба
были и в живом E7 — невиновны), словарь 8 МиБ против 16 (кандидат в
со-факторы, но класс потока lzma-rs покрывает всё).

Фикс: параметризуемый кодировщик liblzma-класса (`compress.rs`),
LZMA1 alone + size в заголовке; свип pb/lc — pb0/lc0 сжимает этот
модуль **лучше AMI** (90 180 байт против 90 516 при том же контенте) и
позволяет вписываться в исходный слот секции (E3-стиль: минимальный дифф).

### 3.2 Пустые подписи раскрытой формы = отсутствие PRC-токенов

E7: пункт меню «CPU HWPM State Control» появился, страница открывается,
но экран пустой — два виджета `[]` без подписей (скрин пользователя;
vision-анализ скрина галлюцинировал «5 вопросов» — данные IFR ниже
истинны). Внутри формы 901 (IFR-факты):

```
FORM 901 title=4894
  SUBTITLE 4894 'CPU HWPM State Control'
  SUBTITLE 5071 ''                    (пустая by design)
  ONE_OF 4965 'Enable CPU HWPM'           qid 0x0D5F, опции: 2639 'Disable', 4967 'HWPM NATIVE MODE', 4968 'HWPM OOB MODE'
  ONE_OF 4969 'Enable CPU Autonomous Cstate' qid 0x0D60, опции: 2639 'Disable', 2638 'Enable'
```

Все строки есть в пакете `en-US`. У модуля второй string-пакет
`x-UEFI-AMI` с AMI-токенами вида `PRC0003`/`PRC11B`, и корреляция
полная: у каждой отрендеренной строки видимых форм PRC-запись есть, у
строк формы 901 (4894/4965/4969) — нет. Вендор не создавал токи для
скрытой формы. AMITSESetupData (RAW внутри файла FE612B72, дерево
`3/208/0/0`, 108-байтные записи qid@0) содержит записи 0x0D5F/0x0D60,
структурно идентичные видимым — не причина. Июльский симптом «unnamed
rows» — та же причина (строки добавлялись только в en-US).

Фикс: (a) `string_pack` — вставка SIBT-записи на конкретный id
(разрезание SKIP-пропусков: пакет с дырами); (b) `set_item_visibility` —
PRC-патч для всех строк раскрытой формы (титул/субтитлы/вопросы/опции).

Предсказания для проверки на живом E7: заголовок страницы — пустой;
внутри скобок значения `[Disable]`-типа видны (опции 2638/2639 — общие,
с токенами).

## 4. Техника дифференциальных образов (переиспользуемая)

- Вписывание в слот: свип `pb×lc×lp` (ограничение lc+lp≤4) + `MF_BT4`,
  `nice_len 273`, dict 16 МиБ; 13-байтовый заголовок alone-потока с
  ручной записью точного size (python `FORMAT_ALONE` пишет unknown-size
  + end-marker); хвост добивается нулями — декодер останавливается по
  заявленному размеру.
- Чистый сдвиг: pad-файл (type 0xF0, GUID FF…FF, 0xFF-тело, hdr-чексумма
  0x100−Σ) перед целевым файлом; сдвиг только внутри FV, за пределы
  тома не выходить (ошибка первой сборки E6 съедала хвост FV3 с
  микрокодами).
- Побайтовая верификация: карты FV (offset/size/attr/state/hdr_ck всех
  216 файлов), диф декодированных потоков, идентичность сдвинутых
  файлов, FIT-парсер (`_FIT_   ` @0xd70000, phys = off + 0xFF000000).
- Пробники сохранены: `refs/amibcp/probes/` (suppress_probe.py,
  fvmap.py, abb_probe.py, e-билдеры, извлечённые AMI-данные).

## 5. Июльская история (майнинг `/tmp/long-analyze.json`, 146 user-сообщений)

Ключевые точки: [07-19 02:23] прошитый образ с одной вкладкой
IntelRCSetup (битый Setup-файл → вкладка исчезает, мать грузится —
платформа переживает битый модуль мягко); [07-19 11:00] вставленная
форма появилась в живом Setup без строк, опции переключались; [07-20
13:55] один образ не грузится — зависание на посткоде 4F (кривая сборка
FFS); [07-20 14:13] «help-строки можно удалить все» — компактфикация;
[07-21 12:11] «нам нужно средство, которое соберёт FFS с формами» —
момент рождения UEFIPatcher. Июльские грузившиеся образы —
position-preserving (сжатие на месте + pad-файл), что сначала было
принято за обязательное условие; E6 показал, что сдвиг сам по себе
платформу не валит.

## 6. Артефакты

- `refs/amibcp/`: hnx.bin, hnx-patched.bin, e1, e3, e5, e6,
  e7-liblzma-grown.bin, скриншоты AMIBCP и живого Setup, probes/.
- `/tmp/uefipatcher-smoke/`: рабочие копии, fvmap-отчёты, forms.tsv,
  извлечённые PE/AMI-данные, engine-логи (volatile — при перезагрузке
  потерять не жалко, всё ценное продублировано).

## 7. Открытые пункты

1. Подтверждение предсказаний §3.2 на живом экране (заголовок/скобки).
2. Мини-цикл фиксов: кодировщик → string_pack insert-at-id →
   PRC-патч в set_item_visibility (+ миноры из TODO).
3. Дальнейшая аппаратная валидация: formset add / form add на этой
   плате (R2 смягчён, но PRC и AMITSE-вставка для новых форм не
   проверялись живой прошивкой).

## 8. Финальная валидация цикла фиксов (E8) — pending

- Кандидат: `refs/amibcp/e8-unhide-fix-candidate.bin`
  (sha256: `5bfb87466456654fa83e90e41edba8c3a1b4baa5344a8bc518e6fc498010bbd4`, длина 16 МиБ).
- Путь сборки: чистый движковый (`image open` Write → `hii form set-visibility
  'abbce13d-e25a-4d9f-a1f9-2f7710786892:0x10:0#901' --visible` → `image save`);
  кодировщик liblzma raw-LZMA1 (slot-fit), PRC-патч `x-UEFI-AMI` (UPG-токены).
- Движковые самопроверки: real-image (a)–(e) зелёные
  (`cargo test -p uefi-engine --test real_image -- --ignored`).
- Чеклист прошивки (внешним программатором):
  - [ ] POST проходит;
  - [ ] видео есть (нет звукового кода «нет видеокарты»);
  - [ ] Setup → «CPU HWPM State Control»: заголовок формы непустой;
  - [ ] подписи обоих вопросов и опций непустые (PRC-каузальность);
  - [ ] опции переключаются, значения сохраняются.
- Опционально (до прошивки E8): подтверждение предсказания §3.2 на уже-прошитом
  E7 — пустой заголовок формы 901 при видимых значениях в скобках.
- Статус: ожидает прошивки пользователем; результат — аддендум §9.

## 9. Аддендум: E8 прошит — подписи по-прежнему пустые (2026-09-03)

Прошивка: flashrom -p ch347_spi, `Erase/write done`, `VERIFY: VERIFIED`.
Скрин: `refs/amibcp/Снимок экрана от 2026-09-03 01-25-29.png`.

Чеклист §8: POST ✓; видео ✓; пункт меню есть, активен, с help ✓; вход ✓;
**подписи вопросов и опций — пустые** (два ряда `[]`, скобки без значений —
предсказание «[Disable] видны» §3.2 опровергнуто). Заголовок страницы —
«IntelRCSetup» (титул формсета, id 4984), не титул формы 4894. Help-панель
рендерит настоящий текст вопроса: «Enable CPU HWPM for CPU for better Energy
performance» (id 4966). Итог: help рендерится, prompt/значения — нет;
E7 ≡ E8, вставка токенов ничего не изменила.

### 9.1 Разбор (оффлайн-улики, всё проверено на байтах)

- **PRC-теория фальсифицирована.** Help-строки (4966 и все 4891/4893/4899…)
  токенов не имеют никогда — и рендерятся. «Enable»/«Disable» (2638/2639,
  самые частые опции образа) токенов не имеют. Токены x-UEFI-AMI —
  регистровые имена классов Intel (IIO/PCH/PRC/QPI/MEM/ME0/EVL/OC0/CMRF),
  id1 пакета = «AmiMappingLanguage»: артефакт AMI-тулинга, не рантайм.
- **E8-артефакт корректен**: UPG0001-9 на нужных id в образе (независимая
  проверка) — рантайму этого мало.
- **Строки в порядке**: все тексты формы 901 в единственном en-US пакете
  ABBCE13D; коллизий id по всем 6 package-спискам образа нет; блоб .rsrc HII
  инвентаризирован целиком (FORM + en-US + x-UEFI-AMI + END, без скрытых
  списков). «Двойные копии» из побайтового скана — префиксы соседних id
  (4895 содержит титул как префикс).
- **AMITSE SetupData (FE612B72, 3 таблицы)**: записи обоих qid присутствуют
  со всеми полями; в Table-3 help id @+20 и prompt id @+48 совпадают с IFR;
  байт access @+16 = 0x09 — как у рабочих вопросов (0x0D63…). Эталон
  сообщества (UEFI-Editor) патчит ровно это: access@+16 (байт), failsafe@+52,
  optimal@+53 (байты; в hex-чарах — 32/104/106) + снятие suppress — всё у нас
  уже покрыто или совпадает с рабочими.
- **Varstore**: «IntelSetup» size 0x17CB покрывает смещения вопросов
  0x177E/0x177F. **REF→901** байт-в-байт той же структуры, что REF→896
  (оба из хаб-формы 12). Таблиц страниц (form_id+title_id) в AMITSE-данных
  нет ни для рабочих, ни для скрытых.
- IFR-смещения откалиброваны по r-efi: prompt@2/help@4/qid@6/varstore@8/
  offset@10/flags@12 — вопросы формы 901 структурно идентичны рабочим.

### 9.2 Дискриминирующий эксперимент E9 — перенос вопросов

Гипотезы после §9.1: (а) пустота привязана к форме 901 (её рендер-путь);
(б) к самим вопросам (qid/SDP-уровень). Дискриминатор: **перенос двух
ONE_OF байт-в-байт (qid 0x0D5F/0x0D60, те же строки и SDP-записи) из
формы 901 в рабочую форму 896 «CPU P State Control»**. Длина form-пакета и
PE не меняется — раскладка образа байт-в-байт оригинальная, меняется только
LZMA-поток (E3-style вписывание в слот, lc0/lp0/pb0, запас 0x120).

- Кандидат: `refs/amibcp/e9-relocate-test.bin`, 16 МиБ,
  sha256 `ec26dbce863a992caa80dbe1e4a0b83fb509f95060a9992dd55e6dabbfebc5dd`.
- Самопроверки: изменения только внутри потока секции ABBCE13D;
  диф декодированного = зона переноса (404 байта); string-пакеты байт-в-байт;
  вопросы 0x0D5F/0x0D60 подтверждены в форме 896 (prompt 4965/4969);
  в 901 вопросов нет; FIT не тронут.
- Чеклист прошивки:
  - [ ] POST проходит, видео есть;
  - [ ] IntelRC → «CPU P State Control»: внизу два новых ряда
        («Enable CPU HWPM», «Enable CPU Autonomous Cstate»);
  - [ ] **подписи новых рядов**: с текстом → форма-специфичная причина
        (фикс-путь = relocate-op движка); пустые → уровень вопросов
        (следующий шаг: SDP-глубокий разбор/дизасм браузера B1DA0ADF);
  - [ ] опции новых рядов переключаются (значения в скобках видны?).
- Примечание: форма 901 остаётся скрытой как в оригинале (E9 меняет одну
  переменную — только перенос).

## 10. Аддендум: E9 прошит — «пустота» следует за вопросами; E10 = Above 4G Decoding (2026-09-03)

### 10.1 Результат E9 (перенос вопросов в рабочую форму 896)

На странице «CPU P State Control» два перенесённых ряда появились;
**подписи по-прежнему пустые**, help меняется при переключении рядов
(оба help-id рендерятся). Вывод: причина не в форме 901 и не в её
регистрации — «пустота» следует за самими вопросами (qid 0x0D5F/0x0D60
с их строками), в любой форме. Форм-специфичная гипотеза исключена.

### 10.2 Смена цели (предложение пользователя)

Вместо глубокого копания вендорски-аномальных HWPM-вопросов — проверить
конвейер на «обычной» вендорской блокировке: **Above 4G Decoding**.

Анализ: модуль Setup (899407D7), форма 10029 «PCI Subsystem Settings»
(доступна из меню), вопрос qid 0x003B (prompt 419 / help 420, varstore
offset 0x003A). Вся страница загейчена вендором: каждый вопрос обёрнут
личным `GRAYOUT_IF(EQ_ID_VAL(qid 0x009A == 1))`, где 0x009A —
**невидимый NUMERIC без строк** (prompt=0/help=0) — мастер-выключатель.
Токен-пакет Setup: 419 = `PCIS006` (ещё один тулинг-неймспейс, PCIS###).

### 10.3 E10: снят grayout вокруг 0x003B

Чистое удаление враппера `[19 82][12 06 9a00 0100] … [29 02]` (−10 байт,
без хвостовых опкодов, грамматика IFR идеальна). Движковый shrink-паттерн:
длина PE/dec сохранена (10 нулей в хвост ресурса), u24-length form-пакета
и u32 total списка обновлены (0x2762→0x2758, 0xd7e2→0xd7d8). Рекомпрессия
lc0/lp0/pb0, в слот (0x63C9 ≤ 0x6491).

Верификация: диф образа только внутри LZMA-секции Setup; список пакетов
парсится; en-US нетронут (419/420 на месте); вопрос 0x003B на depth 1,
соседи 0x3A/0x3C на depth 2 (их гейт цел); хвост dec (reloc) байт-в-байт;
движковый CLI-smoke: 10 195 строк, форма 10029 видима.

- Кандидат: `refs/amibcp/e10-above4g-unlock.bin` (16 МиБ, sha256
  `b4d4e763f80032d251b3bb791aa64fe2333254ffe0ded3c48d0a9a51f84e3cfd`).
- Чеклист прошивки:
  - [ ] POST, видео;
  - [ ] Advanced → «PCI Subsystem Settings»: пункт «Above 4G Decoding»
        с текстом, доступен для изменения (не серый), опции
        [Disabled]/[Enabled] переключаются и сохраняются;
  - [ ] соседние пункты страницы — по-прежнему загейчены (контроль
        однопеременности).
- Интерпретация: текст+переключение → конвейер работает, HWPM-вопросы —
  вендорская аномалия уровня вопроса (дальше: дизасм B1DA0ADF или
  SDP-разбор, по желанию); пусто/серый → системная проблема нашего
  подхода к разблокировке — копаем SDP.

### 10.4 Инженерные находки сборки E10

- При изменении длины IFR-пакета обязательны: u24-length самого пакета,
  u32 total списка, длина PE не меняется (паддинг внутри .rsrc) — иначе
  сдвигается .reloc и ресурс перестаёт парситься. Движок так уже делает
  (shrink-путь fc0977c); E10 повторил паттерн вручную.
- Для движка не хватает операции «снять grayout/suppress с конкретного
  вопроса» (сейчас только форма целиком) — кандидат в цикл 6 вместе с
  relocate-op.

## 11. Аддендум: результаты E9/E10 на железе; E11 (2026-09-03)

### 11.1 E10: страница не видна — второй слой гейта

«PCI Subsystem Settings» не появилась в Advanced: REF→10029 (prompt 395,
@pkg+0x686, живёт в форме 10002 «Advanced») сам обёрнут
`SUPPRESS_IF([45 8a …][45 0a …][2f 02])` — выражением на строковых
опкодах (0x45, board-SKU-подобная проверка), всегда-истинном на этой
плате. Вендор прячет страницу двумя слоями: suppress пункта меню +
grayout каждого вопроса невидимым 0x009A. E10 снял только внутренний
слой → пункт меню остался скрыт. E10 нельзя считать проверенным
(страница не открывалась) и он несёт −10-сдвиг (риск §11.2) — заменён
E11, больше не прошивать.

### 11.2 E9: зависание Setup при входе в форму 896 — SDP-указатели

Уточнение показаний: подтверждение «help меняется» относилось к E8;
при реальном входе в модифицированную «CPU P State Control» на E9 Setup
зависает. Гипотеза, объясняющая всё сразу: **в Table-3 SDP setupdata
зашиты абсолютные смещения IFR-опкодов вопросов** (поле `d54e0200`
q1 = 0x024ED5 — ровно позиция его ONE_OF в form-пакете; q2/рабочие —
аналогично). E9 перенёс вопросы на +0x180 байт → указатели их записей
(и записей родных вопросов 896, попавших в сдвиг) устарели → браузер
падает при разборе страницы. E8 не висел: движковый unsuppress —
length-preserving, максимальный сдвиг +2 внутри зоны, и SDP-поле,
видимо, толерантно к ±2 (страница 901 открывалась). Выводы:
- E9 считать опасным (вход в 896 вешает UI), больше не прошивать;
- любые IFR-правки в этих образах — только length-preserving
  (класс E7/E8), relocate без патча SDP запрещён.

### 11.3 E11: length-preserving разблокировка страницы + 4G

Два движковых transform (shift+2 / END на старте / поглотить close) на
ORIG Setup-модуля, оба железно-проверенного класса:
1. `SUPPRESS_IF` вокруг REF→10029 опустошён → пункт «PCI Subsystem
   Settings» появляется в Advanced (сам REF стал прямым ребёнком формы
   10002; строковые опкоды выражения остались stray-стилеями — паттерн
   E7/E8, рантаймом прощается);
2. `GRAYOUT_IF` вокруг qid 0x003B (Above 4G Decoding) опустошён →
   вопрос доступен для изменения.

Длины form-пакета/PE/dec не изменились; весь контент после зон
(REF@0x697, GRAYOUT@0xdf6, все последующие вопросы) стоит на исходных
абсолютных смещениях — SDP-указатели валидны. Диф образа: только
LZMA-поток секции Setup (0x63BB ≤ слот 0x6491, lc2/pb0). Верифицировано
побайтово, структурно и движковым CLI-smoke (183 формы, 10029 видима,
10 195 строк).

- Кандидат: `refs/amibcp/e11-pci-page-above4g.bin`, 16 МиБ, sha256
  `92fa4ebb9fd53ccd40e69c8476570087e79be9848a1382884802623d7427f24a`.
- Чеклист:
  - [ ] POST, видео;
  - [ ] Advanced → «PCI Subsystem Settings» — пункт появился;
  - [ ] «Above 4G Decoding»: текст, не серый, [Disabled]/[Enabled]
        переключаются и сохраняются;
  - [ ] соседние пункты страницы серые (гейт 0x009A у них цел);
  - [ ] прочие страницы Setup без изменений (контроль побочных).
- Интерпретация: 4G с текстом и переключением → конвейер валиден на
  «нормальном» случае, HWPM-вопросы — вендорская аномалия (дизасм по
  желанию); пусто/серым → системная проблема → SDP-разбор.

## 12. Аддендум: E11 виснет на входе в Setup; E12 = чистые флипы данных (2026-09-03)

### 12.1 E11: зависание на входе в Setup

E11 грузится, но вход в Setup виснет. Диф с E10 (который в Setup
заходил): E10 не трогал suppress вокруг REF и имел чистое −10-удаление;
E11 добавил два непроверенных железом паттерна: пустые скоупы
supress/grayout + stray-опкоды выражений (UINT64/EQUAL) на уровне
операторов. Вывод: класс «опустошить скоуп + оставить хвосты» для
setup-модуля этого AMITSE непригоден (в ABBCE13D прощался — E7/E8 —
но переносить паттерн нельзя). E11 не прошивать.

### 12.2 E12: только байтовые флипы данных внутри выражений

Декодировка выражений (r-efi): UINT64=0x45, EQUAL=0x2F. Скрывающий
suppress страницы = литерал `UINT64(1)==UINT64(1)` («прятать всегда»).
Гейт 4G = `EQ_ID_VAL(0x009A == 1)`.

E12 меняет ровно **3 байта** декодированного контента, ноль
структурных изменений (длины/смещения/опкоды нетронуты):
- pkg+0x67A: второй операнд `1 → 2` → `1 == 2` = false → REF
  «PCI Subsystem Settings» виден в Advanced;
- pkg+0xDD1..0xDD2: литерал `1 → 0xFFFF` (недостижим для
  1-байтового значения) → grayout 4G не срабатывает.

Проверки: диф dec = ровно 3 байта; слот 0x63BF ≤ 0x6491 (lc2/pb0);
движковый CLI-smoke: 194 строки form list, 10029 видима.

- Кандидат: `refs/amibcp/e12-above4g-dataflip.bin`, sha256
  `506f068b973be83a30c6608f045c2a47215b38e1f774ebb8ae7cb252a73befab`.
- Чеклист: как у E11 (пункт меню + 4G текст/активность/переключение,
  соседи серые, прочие страницы целы).
- Интерпретация: E12 использует только существующие рантайм-пути
  (выражения вендора всегда вычислялись) — если и это виснет, дело не
  в правках, а в самой странице 10029 (отравлена, как HWPM-вопросы) →
  направление «раскрыть скрытую страницу» на этом образе закрыто.

### 12.3 ИТОГ E12: УСПЕХ — конвейер валидирован на железе (2026-09-03 02:43)

E12 загружается, Setup работает. Скрин
`refs/amibcp/Снимок экрана от 2026-09-03 02-43-56.png`: страница PCI
рендерится полностью («PCI Bus Driver Version A5.01.05», Latency
Timer/PERR#/SERR# с значениями, «Above 4G Decoding **[Enabled]**»,
SR-IOV, BME DMA, ссылки на PCIe-подстраницы). Протокол пользователя:
изменение → Save → перезагрузка → значение сохранилось (NVRAM-запись
работает).

Что доказано end-to-end на живой плате:
- парсинг образа → адресная правка (3 байта в литералах гейтящих
  выражений) → рекомпрессия liblzma (lc2/pb0) → вписывание в слот →
  раскладка байт-в-байт → прошивка → работающий Setup с живыми
  значениями;
- вендорская блокировка «страница + вопрос» снимается флипами
  констант: `UINT64(1)==UINT64(1)` → `1==2` (suppress=false) и
  `EQ_ID_VAL(0x009A==1)` → `==0xFFFF` (grayout=false).

Классы правок по итогам железа: (✓) флипы данных в выражениях —
валидированы; (✓) length-preserving unsuppress формсет-уровня —
валидирован (E7/E8); (✗) перенос вопросов — вешает (SDP-смещения);
(✗) пустые скоупы + stray-опкоды на setup-модуле — вешает (E11).

Открытым остаётся только аномалия HWPM-вопросов (пустые промпты при
живом help — E7/E8/E9), зафиксирована как вендор-специфика; на
«нормальных» настройках не воспроизводится.
