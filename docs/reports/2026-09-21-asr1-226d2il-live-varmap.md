# Живая сессия 226D2IL: финальная карта Setup-байтов + хауту (2026-09-21)

Продолжение разведки 2026-09-20 (docs/reports/2026-09-20-asr1-226d2il-setup-recon.md).
Атрибуция байтов в прошлый раз была снята с перепутанным тумблером — эта сессия
закрывает её живыми пробами: одно изменение в BIOS за ребут, полный слепок
efivars до/после, дифф, откат через efivar с проверкой. Стенд: asr1 (E3C226D2I,
P3.50), SOL-мост `hack/solrig.py` (лог-транскрипт навигации), ssh `root-asr1`.

## Хауту: как маппить поля конкретного BIOS

1. **Baseline — полный слепок efivars, не только Setup**: метод окупился сразу —
   Setup Prompt Timeout оказался в стандартной `Timeout`, угадать её по Setup-блобу
   невозможно.
   `hack/asr1-efivars-snap.sh s0-baseline` (все 69 переменных + плоская `Setup.bin`).
2. **Одно изменение за ребут** в Setup (F10). Навигация — под SOL-мостом: лог
   `/tmp/asr1-sol.log` пишет экраны и значения, можно сверять какой экран/блок.
3. ОС поднялась → `hack/asr1-efivars-snap.sh sN` →
   `hack/efivars-diff.py refs/amibcp/asr1-map/s0-baseline refs/amibcp/asr1-map/sN`
   (дифф печатает переменную, офсет по файлу и по данным с учётом 4B attrs).
4. **Откат**: `cat s0-…/Setup.bin > /sys/firmware/efi/efivars/…` (`chattr -i`),
   sha256-readback, `reboot` → проверка экраном в BIOS. Для SOL — self-verify:
   тумблер глушит UART, откат пишется вслепую по ssh, возврат POST в лог моста
   доказывает, что байт переключён верно.
5. Шум исключается списком известных переменных (ниже).

## Карта переменных (все значения подтверждены живыми flips)

| Меню BIOS | Переменная | Байт | IFR (899407D7…:0x18:0) | Значение |
|-----------|------------|------|------------------------|----------|
| Boot → Setup Prompt Timeout | **`Timeout`** 8be4df61-… | data[0], u16 LE | форма **1031** «Boot», qid **0x4** (numeric, w2, EFI-varstore→Timeout; IFR-дефолт 5) | секунды; вендор-запёк 1, у нас 5 |
| Advanced → North Bridge → **Above 4G Decoding** | Setup EC87D643 | **data[1]** (обр. 0x500089) | форма **1215** «North Bridge Configuration», qid **0x13E**, off 0x1, w1 | 1=Enabled |
| Serial Port Console Redirection → **COM1** | Setup | **data[1140]** (0x5004FC) | форма 1158, qid **0x10B**, off 0x474 | 1=Enabled |
| → **SOL** | Setup | **data[1141]** (0x5004FD) | форма 1158, qid **0x10D**, off 0x475 | 1=Enabled |
| → **EMS** (Out-of-Band Mgmt/Windows) | Setup | **data[1156]** (0x50050C) | форма 1158, qid **0x10F**, off 0x484 | 1=Enabled; IFR-дефолт 1, NVRAM-дефолт 0 |

Маппинг: image-офсет = 0x500088 + data[N] (raw-тело StdDefaults, live↔image 1:1);
файл efivars = 4 байта attrs + данные (live[i+4] = std[i]).

## Поправки к разведке 2026-09-20

- **Неверно** было «SOL=data[1], 4G=data[1141]» — снятие с неправильным тумблером.
  Верно: **4G=data[1], SOL=data[1141]** (подтверждено независимыми flips).
- Формы **1025** (пустой заголовок, формсет 985EEE91) и **1035** «PCI Subsystem
  Settings» с prompt «Above 4G Decoding» — призраки, разделяющие строку с живым
  вопросом; live-экран = форма **1215**, qid **0x13E**. Тест-константа
  ASR1_4G_ITEM переведена на 1215:0x13E.
- Timeout живёт в стандартной переменной — не в Setup-блобе.

## Шум для диффов (не настройки)

- `MonotonicCounter-01368881` — тикает от любой активности прошивки.
- `SetUpdateCountVar-81c76078` — +1 на каждое сохранение Setup (F10); удобный
  маркер «сейв был».
- `HiiDB-1b838190` — внутреннее состояние HII-базы.
- `ConIn/ConInDev/ConOut/ConOutDev/ErrOut/ErrOutDev` — производные списки
  консолей: COM1→on добавляет COM1-UART (~+97Б на три переменные), SOL→off
  выкидывает SOL-UART; **EMS не влияет**.

## Наблюдения стенда

- **SOL=0**: мост «жив, но нем» — ipmitool-сессия не рвётся, лог замолкает на
  выходе из BIOS (ни POST, ни ОС). Откат вслепую по ssh + ребут → POST возвращается
  в лог (self-verify: замолк на 17:17, ожил на 17:18, +25КБ).
- **EMS=1**: мост работает всю загрузку (login в логе), Con* не меняются — EMS
  не претендует на BMC UART.
- **4G=0**: ssh/network выжили (сеть переведена с mlx4) — проба прошла без
  запасного пути.
- Полный efivar-откат Setup (1221Б) + ребут прошивка подхватывает на каждой пробе.

## Путь запекания дефолтов

`hii set-value` (StdDefaults, ин-плейс; чек-суммы пересоберёт движок):

```
899407D7-99FE-43D8-9A21-79EC328CAC21:0x18:0#1215:0x13E = 1   # Above 4G Decoding on
899407D7-99FE-43D8-9A21-79EC328CAC21:0x18:0#1158:0x10D = 1   # SOL on
899407D7-99FE-43D8-9A21-79EC328CAC21:0x18:0#1158:0x10B = 0   # COM1 off
899407D7-99FE-43D8-9A21-79EC328CAC21:0x18:0#1158:0x10F = 0   # EMS off
899407D7-99FE-43D8-9A21-79EC328CAC21:0x18:0#1031:0x4   = 5   # Setup Prompt Timeout, сек
```

Timeout — полноценный HII-вопрос (форма 1031 «Boot», qid 0x4, numeric u16,
EFI-varstore `Timeout`): set-value печёт вложенную копию переменной в
StdDefaults (@0x50057A), проверено живьём (правка 01→05, ровно 1 байт).
Альтернатива — `uefi-cli nvar set Timeout --guid 8BE4DF61-… --offset 0
--value 5 --width 2` (тот же байт). Замечание: вендор запёк EMS=1 (IFR
seed=1) — печать 0 обязательна, иначе сброс NVRAM поднимет EMS.

Контроль испечённого (юзер-сборка `~/225d2i-3_50-ds1.rom`, 2026-09-21):
дифф от стока — ровно 4 байта (0x500089→1, 0x5004FD→1, 0x50050C→0,
0x50057A→5), значения совпадают с живым known-good состоянием рига
(s0-baseline) по всем пяти полям.

## Прошивка и активация (2026-09-21, вечер) — ЦИКЛ ЗАКРЫТ

Файл: юзер-сборка `~/225d2i-3_50-ds1.rom` (сток + 4 байта, собрана в TUI
пятью Enter: 1215:0x13E=1, 1158:0x10D=1, 1158:0x10F=0, 1031:0x4=5).
COM1 не трогали (вендор 0 = нужное).

Путь прошивки (хост CoreOS/bootc остался нетронутым):

1. **flashrom из toolbox t-adm** (v1.6; хостовой dnf заблокирован bootc,
   layering не использовали — «не радикально»). Хост виден в контейнере
   через `/run/host`.
2. **Стена 1 — iomem=strict (ядро)**: `/dev/mem mmap` RCRB = EPERM, контейнер
   не лечит. Обход без персистентных изменений: одноразовый кварг через
   правку BLS-записей `/boot/loader/entries/ostree-{2,3}.conf`
   (append `iomem=relaxed` к options, revert после прошивки).
3. **Стена 2 — регионы дескриптора**: полный чип не читается. FREG: descriptor
   0x0-0xFFFF (ro), GbE 0x10000-0x1FFFF (rw), ME 0x20000-0x4FFFFF (locked),
   **BIOS 0x500000-0x7FFFFF (rw)**. Layout `0x00500000:0x007fffff bios`,
   запись `-i bios` — ME/дескриптор/GbE не тронуты вовсе.
   Чип: `-c "W25Q64JV-.Q"` (старое определение BV/CV/FV даёт transaction
   error). Для layout-записи обязателен `--noverify-all` (без него flashrom
   читает весь чип под буфер и падает на защищённых регионах).
4. Запись: `Erase/write done 500000-7fffff`, `Verifying flash... VERIFIED`.
   Живой стор (NVAR-записи с 0x500819) стёрт вместе с регионом — это и есть
   активация.
5. **Первый бут: пересев из новых дефолтов сработал САМ, без F9** (в отличие
   от rd450x). Снапшот `s7-flash`: Setup-EC87D643 и Timeout отсутствуют в
   диффе от baseline (4G=1, SOL=1, EMS=0, COM1=0, Timeout=5), живая
   переменная StdDefaults перечитана из флеша (ровно 4 байта: data[18]→1,
   [1158]→1, [1173]→0, [1283]→5 — маппинг с базой image 0x500077).
   Прочий дифф — доброкачественные следы стёртого стора (Boot-записи,
   DefaultBootOrder, обнулившиеся счётчики, временно ушедшие AMI-кэши
   DmiArray/DmiVar/StageChk/BstLogVar, BACKUP_BMC_MAC обнулён — BMC жив).

Батарейка на плате высаживается за дни: любое снятие питания = пересев из
StdDefaults = теперь всегда НАШИ значения (4G/SOL вкл, EMS выкл, таймаут 5),
без клавиатуры/VGA. **Финальный физический тест проведён в тот же вечер**:
полное снятие питания (худший случай — RTC мёртв) → `s8-powerpull`: Setup
и Timeout отсутствуют в диффе от baseline (все пять значений на месте),
StdDefaults-копия несёт те же 4 байта, POST виден через SOL с холодного
старта, сеть/mlx4 поднялась. Сценарий «выдёрнул из розетки — а дефолты
правильные» закрыт полностью.

Бэкапы: регион до прошивки — `refs/amibcp/asr1-flash-bios-pre.bin` и
`/root/bios-pre.bin` на риге; исходник прошивки — `/root/225d2i-3_50-ds1.rom`.

## Хвосты

- TUI: подпись EMS-блока в форме 1158 — два text-опкода (двухстрочный заголовок),
  движок экспортирует оба; TUI отрисовал подписи только COM1/SOL — кандидат на
  правку рендера.
- Проверка «третьего варианта» в TUI закрыта: третий блок = EMS, структура
  (вопрос + подформа 1159) на месте.
- Форма 1025 (пустой заголовок, формсет 985EEE91) — служебная свалка AMI на
  1017 вопросов, включая не представленные больше нигде («Wirless device» —
  опечатка вендора; wireless на плате нет). Живых экранов не представляет —
  источник «призрачных» дублей промптов.
- Заголовки форм 1220-1225 (Storage Configuration) в строковом пакете —
  буквальный «%s»: плейсхолдер, живой браузер подставляет имя устройства.
  Не баг экстрактора (UEFITool/AMIBCP показывают так же).
- TUI: зависание после set-value (4G, форма 1215) — лечится перезапуском,
  правки на месте; нужен репро для разбора.

## Артефакты (refs/amibcp/asr1-map/, gitignored)

- `s0-baseline … s6-final` — снапшоты всех 69 efivars по пробам
  (s0==стартовое «SOL=1, 4G=1, COM1=0, EMS=0, Timeout=5»).
- `/tmp/asr1-sol.log` — полный SOL-транскрипт сессии.
