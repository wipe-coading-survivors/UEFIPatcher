# UEFIPatcher — Roadmap

Декомпозиция проекта на циклы разработки по приоритетам из `user-input.md`.
Каждый цикл — отдельный brainstorm → spec → plan → implementation.
Зависимости указывают, какой цикл должен быть завершён перед стартом данного.

## Приоритеты (из user-input.md)

1. Основной компонент бизнес-логики, п.4.1 — движок
2. CLI, п.2.1
3. TUI, п.2.2
4. Менеджер сессий, п.4.3
5. WebUI, п.2.3

## Технологический стек (общий)

| Крейт | Версия | Назначение |
|-------|--------|-----------|
| `uguid` | 2.2.1 (serde) | `Guid` с Display/FromStr/serde. UEFI mixed-endian. UPPERCASE wrapper для индустриального формата. |
| `r-efi` | 7.0 | UEFI спецификация: `base::Guid`, `hii::*` (IFR-структуры, opcode-константы, package types). no_std. |
| `binrw` | 0.15 (std) | Declarative binary parsing/writing через `#[brw]`-макросы. |
| `object` | 0.39 (read_core, pe) | PE32 parsing для PEI/DXE модулей. |
| `lzma-rs` | 0.3 | LZMA декомпрессия. |
| `tonic` | 0.12 | gRPC over unix-сокет. |
| `rusqlite` | 0.31 (bundled) | SQLite хранилище. |
| `clap` | 4 (derive, env) | CLI. |
| Rust edition | 2024 | |

## Циклы

### Цикл 1 — UEFI Engine (план готов, к реализации)

- **Scope**: серверный движок (п.4.1) + setup-visibility (часть п.1.2, только видимость) + крейт `uefi-common` (state/error) + engine binary с clap CLI + CLI-минимум для smoke-теста RPC.
- **Зависимости**: нет.
- **Spec**: `docs/superpowers/specs/2026-07-22-uefi-engine-design.md`
- **Plan**: `docs/superpowers/plans/2026-07-22-uefi-engine.md`
- **Статус**: план реализации готов (19 задач TDD), к исполнению.
- **Стек**: Rust edition 2024, uguid + r-efi + binrw + object, tonic gRPC over unix-сокет, SQLite.
- **Что включено**: крейт `uefi-common` (state.rs, error.rs — скелет, наполняется в цикле 2); парсер UEFI-образа → дерево `FfsNode` (через binrw); builder (сборка обратно); модификации (insert/remove/replace/rebuild, с поддержкой artifact_id); экстракция/импорт/экспорт артефактов; SetSetupItemVisibility (IFR через `r_efi::hii`); именованные сессии (name = CWD без symlink resolution); хранилище сессий+артефактов (TTL 10 дней, GC с `--purge-artifacts` по умолчанию false); gRPC-сервер `EngineService`; engine binary (`src/bin/engine.rs`, clap CLI); токены авторизации.
- **Что НЕ включено**: полный CLI, TUI, WebUI, новые пункты Setup, NVRAM, grpc-шлюз.

### Цикл 2 — Полный CLI (план готов, к реализации)

- **Scope**: полный CLI (п.2.1) для скриптования, аналог UEFIEdit, но свой rust-idiomatic синтаксис.
- **Зависимости**: цикл 1 (gRPC-контракт `uefi-proto`, `uefi-common`).
- **Spec**: `docs/superpowers/specs/2026-07-22-uefi-cli-design.md`
- **Plan**: `docs/superpowers/plans/2026-07-22-uefi-cli.md`
- **Статус**: план реализации готов (12 задач TDD), к исполнению.
- **Что включено**: наполнение `uefi-common` (state.rs, error.rs — полная реализация); команды session (init/list/destroy, name=CWD), image (open/switch/close/dump/list/find/save, extract/export/import/artifacts), edit (insert/remove/replace/rebuild, --from-artifact), setup (set-visibility/list-items); клиентская сессия в `.uefipatcher` (TOML) в CWD; JSON/text/tsv вывод.
- **Вопросы для brainstorm**: разрешены — синтаксис, state, вывод, lifecycle согласованы.

### Цикл 3 — TUI (реализован)

- **Scope**: TUI (п.2.2) с ANSI + UTF-8 (иконки, псевдографика), аналог yazi/nvim.
- **Зависимости**: цикл 1 (gRPC-контракт, `uefi-common`), цикл 2 (переиспользование commands).
- **Spec**: `docs/superpowers/specs/2026-07-22-uefi-tui-design.md`
- **Plan**: `docs/superpowers/plans/2026-07-22-uefi-tui.md`
- **Статус**: реализован (2026-08); пост-миграционный багфикс-цикл —
  `2026-08-12-tui-migration-bugfix-design.md`. Дальнейшее развитие — дуга TUI (ниже).

### Цикл 4 — Менеджер сессий (ОТМЕНЁН)

- **Scope**: менеджер сессий (п.4.3) как отдельный процесс.
- **Зависимости**: цикл 1.
- **Статус**: отменён. В цикле 1 менеджер сессий встроен в движок (TTL 10 дней, фоновый GC с --purge-artifacts, SQLite). Встроенного достаточно.

### Цикл 5+7 — WebUI + gRPC-шлюз (план готов, к реализации)

- **Scope**: WebUI (п.2.3, SvelteKit/TypeScript) + gRPC-шлюз (п.4.2, Rust/axum REST+WS прокси). Объединены — шлюз нужен для WebUI.
- **Зависимости**: цикл 1 (gRPC-контракт), цикл 6 (add-formset, опционально).
- **Spec**: `docs/superpowers/specs/2026-07-22-uefi-webui-design.md`
- **Plan**: `docs/superpowers/plans/2026-07-22-uefi-webui.md`
- **Статус**: план реализации готов (12 задач TDD), к исполнению.
- **Что включено**: крейт `uefi-gateway` (axum REST+WS, cookie→gRPC metadata, upload/download, artifact endpoints); `webui/` (SvelteKit SPA, tree-view, details, операции, setup add-formset); контейнеризация (`<component>.containerfile`, `registry.fedoraproject.org/fedora:44`, `rust-builder.containerfile`); тесты.

### Цикл 6 — Расширенный Setup (план готов, к реализации)

- **Scope**: доработка п.1.2 — добавление новых пунктов и разделов меню, управление NVRAM-переменными.
- **Зависимости**: цикл 1 (парсер IFR, SetSetupItemVisibility, ops::insert).
- **Spec**: `docs/superpowers/specs/2026-07-22-uefi-setup-advanced-design.md`
- **Plan**: `docs/superpowers/plans/2026-07-22-uefi-setup-advanced.md`
- **Статус**: план реализации готов (8 задач TDD), к исполнению.
- **Что включено**: JSON-схема; генерация IFR (FormSet/Form/VarStore/OneOf/CheckBox/Numeric/Ref/Text/Default) через `r_efi::hii` структуры; авто-добавление строк в HII String-пакет; сборка отдельного FFS; AMI-патчинг; gRPC-метод AddSetupFormSet. Использует `uguid::Guid` и `binrw`.

### Цикл 7 — gRPC-шлюз (объединён с циклом 5)

- **Статус**: объединён с циклом 5. Шлюз `uefi-gateway` реализован как часть цикла 5+7.

## Дуга Serial Console (S0–S5) — основная задача проекта

Полный AMI-функционал serial-консоли на HNX99TF (boot-вывод UEFI на
COM + redirect Setup-экрана, настройка из Setup) — флагманская цель из
`user-input.md`. Подход — «лестница доказательств»: каждая ступень
заканчивается железным гейтом (для S0/S1 — детерминированным
артефактом).

- **Spec (живой документ)**: `docs/superpowers/specs/2026-09-05-serial-console-ladder-design.md`
- **S0** — закрыта 2026-09-05: отчёт
  `docs/reports/2026-09-05-serial-s0-recon.md` (решения §8; гипотезы
  молчания (а)–(д) закрыты; целевой FV = FV1 DXE @0x890000; конфиг
  edk2-пары и развилка glue — вход S1). Аддендум спеки — §7.
- **S1** — закрыта 2026-09-06: отчёт
  `docs/reports/2026-09-06-serial-s1-build.md` (стенд
  `edk2-builder.containerfile` + `UefiPatcherSerialPkg`; SerialDxe +
  TerminalDxe + SerialConsoleGlue воспроизводимы (identical) и
  закоммичены как тест-данные; glue — развилка (а) с маркерами SC-S1,
  вход S2). Аддендум спеки — §7.
- **S2** — закрыта 2026-09-06 (E30 пройден с превышением: Setup-экран
  Aptio целиком на COM1 — навигация и сохранение работают, OS-вывод на
  serial; маркеры SC-S1 не наблюдаемы — гипотезы в аддендуме §7;
  0xB2-quirk входа в настройки из загрузчика — открытый вопрос, A/B на
  базовом образе паркуется): отчёт
  `docs/reports/2026-09-06-serial-s2-e30-pack.md` (вердикт — §6).
  Аддендум спеки — §7.
- **S3** — настройки serial в Setup (hijack/append + varstore; эталон
  геометрии $SPF — MNX99MR9A). **Закрыта (вердикт E31, 2026-09-06)**:
  гейт пройден — пункты видимы, смена baud действует на COM
  (значения сохраняются, «мусор» на старой скорости = смена скорости);
  Terminal Type применяется без видимых отличий. Различие F10-
  поведения и механизм COM-переезд-клина — непонятые поведения
  (отложено, гипотезы владельцем не приняты); COM-переезд — вход в
  S4. Отчёты:
  `docs/reports/2026-09-06-serial-s3-e31-pack.md` (кандидат sha256
  `d30cca1c…`), `docs/reports/2026-09-06-serial-s3-e31-verdict.md`
  (вердикт + разбор).
- **S4** — AMI-стек TermSrc+SerialIo с починенной привязкой (основной
  путь — своя сборка (б); патч донора (а) — резерв). Доноры после S1/S2
  не 100%-обязательны (своя сборка воспроизводима) — остаются
  источником готовых .ffs для пользователей без сборочной
  инфраструктуры (решение владельца 2026-09-06, аддендум спеки §7).
  - Пре-чек закрыт (2026-09-06, аддендум §10): донорский SerialIo
    resource-aware (база только из SIO-ресурсов) и не требует патчей;
    SIO-цепочка LIVE driver-model → glue v2 с нотификацией-fallback;
    потребитель PNP0501_0_NV статически не найден (дискриминатор —
    E32). Кандидат E32 = гибрид §8.3(в): донорский SerialIo + наш
    TerminalDxe + glue v2 + вопросы S3 — план
    `docs/superpowers/plans/2026-09-06-serial-s4-serialio-swap.md`.
  - Закрыта (вердикт E32, 2026-09-06): бут с первого раза, baud
    действует (мусор на старой скорости = смена скорости железом);
    переезд порта — **позитив доказан пост-проверкой владельца**: ОС
    видит `ttyS1 @ 0x2f8 (irq=3)` (платформа применяет PNP0501_0_NV[1]),
    BIOS-консоль следует за портом, ОС-вывод ушёл с ttyS0 (статическая
    консоль ОС); Terminal Type/цвета инертны (косметика,
    rd450x-референс; параллель OPi5+ + стоковый EDK2).
  - Отчёты: `docs/reports/2026-09-06-serial-s4-e32-pack.md` (кандидат
    sha256 `8def2850…`),
    `docs/reports/2026-09-06-serial-s4-e32-verdict.md` (вердикт + §6
    пост-проверки — дизъюнктура §10-4 закрыта, гипотеза A).
- **S4a** — резерв (а): полный AMI TermSrc (цвет; решение владельца
  2026-09-06 «идём на полный AMI»).
  - Пре-чек закрыт (аддендум §11): минимальный вариант БЕЗ патчей
    бинаря — все три Setup-чтения при несовпадении размера (0x94 vs
    LIVE 0x72) уходят в дефолты (порты 0/1 enabled, 115200 8N1, тип
    PcAnsi = сырой ANSI — оптимально для цели «цвет»); NVRAM-переменные
    самоинициализируются (инъекций нет); дефолтный тег TermSrc-чайлда
    PcAnsi ≠ VT-UTF8 glue v2 → glue v3 (search-all 4 терминальных
    GUID). Полный патч-вариант (19 байт, побайтно верифицирован) —
    фоллбэк.
  - Исполнено (2026-09-07): кандидат E33 собран живым движком (цепочка
    [SerialIo-донор, непатченный TermSrc 54891A9E, glue v3], FV1
    216→219, TermSrc заменяет TerminalDxe 9E863906; sha256
    `969aa67f…`), тройная валидация зелёная (гейт `5f2bfb0` с первого
    прохода, fv_audit, зонный дифф — вне зон 0) — отчёт
    `docs/reports/2026-09-06-serial-s4a-e33-pack.md`.
  - Вердикт E33 (2026-09-07) — НЕГАТИВ, откат на E32: serial глухая
    (единственная строка — RAW-статускод SOFTWARE/DXE_BS_DRIVER,
    печатается мимо SimpleTextOut), Setup виснет на «Entering
    Setup...» на обеих консолях; гейты (a)/(b)/(c) не пройдены.
    Разбор вердикта: контингенси (ii) опровергнуто статикой (Start не
    читает NVRAM-переменные), Supported принимает PcAnsi, ReadKeyStroke
    неблокирующая; гипотезы A/B/C и дискриминаторы — S4b (план
    отдельно) — отчёт
    `docs/reports/2026-09-07-serial-s4a-e33-verdict.md`. Дуга на
    E32-базе.
- **S4b** — дискриминация виса E33 (решение владельца 2026-09-07
  «шмаляй план, хост оживим следующим рефлешем»).
  - Новая гипотеза **D**: ConOut/ConIn — накопительные NV-переменные;
    к буту E33 там лежал мёртвый VT-UTF8-путь эры E32, который
    TermSrc.Supported клеймит (все 4 GUID) → повторный Start на том
    же порту (dedup скипает только конфиг-чтение) — кандидат на
    ERROR/вис; E30–E32 дубли обслуживались TerminalDxe молча.
  - Кандидаты **E34** (`65b18780…` = SerialIo+TermSrc без glue, только
    CMOS-clear + первый бут: Setup-вход и ERROR-строка) и **E35**
    (`031df12f…` = E32-тройка с glue v2→v3) — план
    `docs/superpowers/plans/2026-09-07-serial-s4b-term-discrimination.md`,
    pack `docs/reports/2026-09-07-serial-s4b-e34-e35-pack.md`
    (гейты s4b/s4c зелёные с первого прохода; E34: 20 945 Б только в
    окне; E35: 186 417 Б, вне зон 0).
  - Закрыта (вердикт E34, 2026-09-07): **D и B отвергнуты** (CMOS-clear
    + отсутствие glue в образе — симптомы идентичны: Setup-вис,
    ERROR-строка дважды = общий путь двух COM-инстансов),
    **подтверждено A/C — TermSrc токсичен сам по себе** в гибриде
    (чайлд создаётся ConnectAll независимо от ConOut; TSE/ConSplitter
    при входе в Setup роняет машину на первом вызове). **Резерв (а)
    закрыт** (аддендум §12): AMI TermSrc жизнеспособен только в полном
    AMI-окружении. E35 не потребовался. Дуга на **E32-базе**; цвет —
    рамка «свойство TSE-вывода» (AMITSE-рендер или путь б', отдельная
    спека).
  - Отчёты: `docs/reports/2026-09-07-serial-s4b-e34-verdict.md`
    (вердикт), `2026-09-07-serial-s4b-e34-e35-pack.md` (pack).
- **S5** — redirect Setup-экрана: гейт фактически продемонстрирован в
  E30 (Setup на COM1 + управление с терминала — AMITSE рендерит через
  gST->ConOut, отдельного redirect-переключателя в AMI-стеке нет);
  остаток — CSM/легаси-ветка (LEGACYREDIR, INT10), актуальна только
  при CSM-буте (аддендум спеки §7).

Зависимости: построена на циклах 1–6 (движок, CLI, hijack-механика).
Ступени строго последовательны; каждая может уточняться аддендумом к
спеке (прецедент: hijack-v2 → v2.1).

## Дуга Setup New Page — своя подстраница Setup (ветка setup-new-page)

Своя страница Setup БЕЗ жертвы-hijack: подстраница там же, где
serial-настройки — Advanced → Super IO → Serial Port 1 Configuration
(форма 10019), вход — goto-пункт с живой страницы (решение владельца
2026-09-07: страница не верхнего уровня). База кандидатов —
E32-конвейер (дуга serial на E32-базе, вердикт E34).

- **Spec (живой документ)**:
  `docs/superpowers/specs/2026-09-07-setup-new-page-design.md`
- **NP-R** — разведка с журналом, без прошивок (прецедент hijack
  v1/v2: tse-bridge-журнал): сравнительная геометрия $SPF LIVE ↔
  MNX99MR9A (подстраницы референса, тип $SPF-записи goto) + дизасм
  AMITSE (MPDT-блоб @0xA780C0) — закрыть 4 неизвестных tse-bridge §4
  (count-vs-zero-scan, таблица 0x180–0x354, marker/seq/off-by-4,
  записи 2-го класса) и главный вопрос: рендерит ли TSE goto-цель без
  страницы в $SPF.
  - Закрыта (2026-09-07, аддендум спеки §10): (3) «вторая таблица» —
    артефакт (одна таблица валидных страниц); (4) закрыто — marker=1
    (Setup-формсет), seq=свой слот, **B@+0x12 = слот родителя**
    (дерево; эталон MNX 10028→10029/10030 — сток-прецедент нашей
    подстраницы), u18=константа образа; (1) — геометрия слот+бамп
    валидна в обеих семантиках (зазор 4 нулевых байта ровно до
    page[0]), парсер контейнера не локализован → эмпирика E37; (2) не
    блокирует.
  - goto-сток-сигнал: 0/31+0/58 REF-целей без страницы → E37 основной
    путь, E36 — дешёвый дискриминатор fallback; goto-записи не нужны
    (25/31 стока без них); REF FormId@+13 — движковый emit_ref уже
    корректен.
  - Журнал: `docs/reports/2026-09-07-setup-page-recon.md`.
- **NP-P** — пре-чеки движка (TDD-план
  `docs/superpowers/plans/2026-09-07-setup-new-page-prechecks.md` —
  на ревью владельца): ref-вопрос в `hii question add` (refs-список в
  схеме, фиксапы ifr-офсетов без $SPF-аппендов), op `hii page add`
  (скелет-клон страницы родителя: marker/u18 от родителя, fid/title/
  seq=188/B=слот родителя, cnt=0, ровно 0x20; занятие нулевого слота
  @0x354 с проверкой зазора + count@0x60 188→189 + bump длины),
  real-image гейты np1 (E36) / np2 (E37) на LIVE + сборка кандидатов
  и pack-отчёт (прошивка — NP-E, владелец).
  - Закрыта (2026-09-09): гейты np1/np2 зелёные (коммит `7f1ac12`;
    живой прогон: $SPF count 188/слот 188 (E37: 188→189, скелет на
    конце контейнера, title-id 772), REF@+13→10101 без $SPF-записи,
    Δ=96 константный сдвиг 187/216 сток-файлов FV1 (слот Setup 7Б
    слэка — by-design), save→open→save стабилен).
  - Кандидаты собраны живым движком через CLI (команды = гейт):
    **E36** goto-only `df9c38f0…`
    (`~/E36/E36-candidate-df9c38f0.bin`) и **E37** + регистрация
    страницы `0c4d536e…` (`~/E37/E37-candidate-0c4d536e.bin`), оба
    16 МиБ на базе E5C88C6F; валидированы независимо (fv_audit
    216→219/+0x1BF14, файл-уровневый дифф: вне FV1 — 0 байт, порядок/
    GUID/тела стока сохранены). Pack-отчёт:
    `docs/reports/2026-09-07-setup-np-e36-e37-pack.md`.
  - Урок вердикта (2026-09-09): гейт переиспользовал хелперы s4c и
    взял glue v3 (E35-цепочка, никогда не прошивалась) вместо планной
    v2 — кандидаты первого пакета закрыты негативом (см. NP-E); гейт
    np и кандидаты возвращены на v2 (E32-цепочка), гейты зелёные.
- **NP-E** — лестница E36+, один семантический дифф на кандидат:
  E36 goto-only (дискриминатор goto-рендера, исходы a–d), E37
  регистрация страницы, E38 косметика. Флеш — только владелец,
  вердикт — аддендум спеки.
  - Раунд 1 (2026-09-09) — негатив: E36 `df9c38f0…` и E37
    `0c4d536e…` (v3-цепочка) оба валят Setup на входе с сигнатурой
    E34 (`SOFTWARE/DXE_BS_DRIVER/0x0D`; вердикт
    `docs/reports/2026-09-09-setup-np-e36-e37-verdict.md`,
    спека-аддендум §11) — первичная гипотеза H1: glue v3 множит
    TerminalDxe-чайлды (E34-класс токсичности), Setup-правки
    замаскированы.
  - Раунд 2 готов: кандидаты пересобраны на планной v2-цепочке —
    **E36v2** `b1e38008…` (`~/E36/E36v2-candidate-b1e38008.bin`) и
    **E37v2** `8e1b476c…` (`~/E37/E37v2-candidate-8e1b476c.bin`),
    приоритет прошивки E37v2 (pack §7); E35 `031df12f…` —
    опциональный форензик-дискриминатор H1. Откат — E32
    `8def2850…` / E5C88C6F.
  - Раунд 2 (2026-09-09) — тоже негатив, H1 отвергнута: E36v2
    `b1e38008…`/E37v2 `8e1b476c…` (v2-цепочка) виснут той же
    сигнатурой; впервые виден маркер SC-S1 (краш до ClearScreen —
    инициализация TSE). Корень найден статически (probe-метод,
    вердикт §7.2): сток-формсет объявляет varstore ids 1..20 — наш
    демо-varstore **id 2 дублировал стоковый** (у стока size 0x2,
    вопрос q0x7 @0x0) → формсет нёс две декларации id 2, все 4
    кандидата висят этим. Спека-аддендум §12.
  - Раунд 3 готов: демо-varstore → свободный **id 31**, гейты np1/np2
    зелёные, кандидаты **E36v3** `08f9b3df…` / **E37v3** `d5b60d7d…`
    (pack §8), приоритет E37v3. H2 (REF/страница) и H4 (сдвиг FV1)
    замаскированы — бисект при новом негативе.
  - Раунд 3 — негатив, но с прогрессом: краш сместился за ClearScreen
    (инициализация хранилищ пройдена — фикс id 31 действовал; падение
    теперь на первом рендере, вердикт §8). ATA-error мигрирует между
    кандидатами — шум. Непроверенные железом: новый varstore с
    несуществующей NVRAM-переменной (A), сдвиг FV1 (B), REF на новую
    форму (C).
  - Раунд 4 готов (состав-эксперимент, без правок движка): форма
    storage-free с принудительным сдвигом Δ=392 — **E36v4**
    `24adcd1b…` / **E37v4** `e097a186…` (pack §9, фикстура
    `np_form_v4.json`); дерево интерпретации — вердикт §8.3. Откат
    прежний.
  - Раунд 4 + контроль E32 (2026-09-09) — БЛОКЕР: ХОСТ: E36v4
    `24adcd1b…`/E37v4 `e097a186…` виснут как v3, но и E32 (рабочие
    биты, sha сверен) висит так же → текущий блокер вне диффов
    (CMOS/NVRAM-мусор эры E33+/ATA-железо; «ATA device failure»
    только в бутах 09-09). Протокол восстановления — вердикт §9
    (холодный сброс → без дисков → план C: NVRAM-reset образ). Дуга
    заморожена до выздоровления хоста; NP-диффы (v3/v4)
    предположительно невиновны (замаскированы). Контроль E32-рефлешем
    — теперь обязательный пункт при «висе на входе» любой дуги.
  - Раунд 5 (2026-09-10, вердикты §9–13) — расщепление B/C: полный
    лог перепрошивок владельца (вердикт §10; всего 12) вскрыл
    host-фактор в три составляющих — battery-очищаемый яд hung-бутов
    (H1, джампер НЕ чистит, чистится только розеткой; протокол с
    батарейкой перед каждым образом, батарейка вынута — вердикт
    §10.3), дисковый фактор H2 (ATA/GPT+EFI — шум;
    невидимость даже у оригинала — проверить SATA-режим/железо) и
    внутреннюю поломку кандидатов (чистый участок: E32×2 работает,
    E37v4/v3/v2 висят; storage-free v4 виснет → varstore
    реабилитирован как единственная причина). Остаток: сдвиг FV1 (B)
    vs REF на новую форму (C). Кандидаты **E16p** `e984efe0…`
    (форма+строки, без REF, Δ=176) и **E36v5** `d8b22e85…` (+REF,
    Δ=200). Уроки процесса: hung-буты ядовиты для следующего образа;
    контрольный рефлеш обязателен; джампер ≠ battery-out.
  - Раунд 5 вердикт (2026-09-10): сдвиг FV1 РЕАБИЛИТИРОВАН (E16p
    ботит с Δ=176), виновник — REF на форму без $SPF-страницы (E36v5
    виснет; сток-инвариант 0/89 нельзя нарушать). Раунд 6 готов:
    **E37v5** `a0a555cc…` (= E16p + page add + REF) — чинит ли
    страницу; при успехе — ре-флеш E37v3 `d5b60d7d…` (целевой состав)
    тем же протоколом (розетка перед каждым; батарейка на хосте
    вынута). Спека §16.
  - Раунд 6 вердикт + корень (2026-09-10): E37v5 (страница+REF)
    виснет → страница не лечит; побайтовое сравнение нашло корень:
    сток-REF'ы несут **voff=0xFFFF** (сентинел; все 31 сток-REF), наш
    эмиттер — 0x0000 (TSE крашится на обработке). Фикс в emit_ref
    (оба вызова; `aca67a9`), раунд 7: **E36v6** `bf87c7d0…` (фикс,
    без страницы) / **E37v6** `08bf10b7…` (+страница) — при успехе
    заодно дискриминация (a)/(b). Спека §17.
  - Раунд 7 вердикт: E36v6/E37v6 (voff-фикс) оба висают; E36v6 даёт
    `Operation: A` ещё на буте (REF на цель без $SPF), E37v6 — только
    `D` на входе → сентинел не единственный.
  - Раунд 8 (2026-09-10) — корень найден zero-flash (дельта-дамп
    E16p vs E36v6): `build_ref_ops` эмитил [REF + END], сплайн ставит
    ops перед сток form-END → **лишний END дисбалансирует скоуп-стек
    IFR-пакета** (краш TSE-парсера на Entering Setup; дефект из
    плана NP-P Task 2). Фиксы: план `f3c3f6e` (rule-11), код
    `c78e11e` (REF без END; инварианты роста 15 + scope-balance в
    юнит-тесте и np-гейте). Кандидаты **E36v7** `2d1e7305…` /
    **E37v7** `d4a0913e…` (pack §13), порядок прошивки E37v7 →
    E36v7, протокол розетки прежний. Побочные закрытия: qid 528
    дублирует EC87D643 — сток-норма (182 кросс-формсетных дублей);
    строки только в en-US — как у работающих E30–E32.
  - Раунд 8 — ВЕРДИКТ ПОЛОЖИТЕЛЬНЫЙ (2026-09-10, вердикт §14): оба
    ботят, пункт «UEFIPatcher Setup» + help рендерится, вход в форму
    10101 работает («пустая страница» — форма без вопросов, by
    design), серийный вывод жив; goto работает и БЕЗ $SPF-страницы
    (E36v7 ≡ E37v7 → сток-сигнал NP-R — конвенция билдера, не
    ограничение TSE).
  - Раунд 9 готов: E37v8 `a61909b6…` — полный состав np2 (varstore
    31 + q600/q601 + страница + REF), приёмка — вопросы, ввод, F10,
    живость NVRAM после ребута (pack §14).
  - Раунд 9 (2026-09-10, вердикт §15): вопросы рендерятся и
    переключаются, значения НЕ сохраняются — у q600/q601 нет
    $SPF-записей (`add_form` пишет только IFR; записи пишет
    `add_question` — так жили q512/q513).
  - Раунд 10 готов: **E37v9** `ba52655e…` — форма пустая с varstore
    31, q600/q601 через add_question ($SPF 393 записи), страница +
    REF (pack §15); приёмка та же + наблюдение ERROR 3000000/60000/A
    на буте (была и без varstore — источник не локализован).
  - Раунд 10 вердикт (2026-09-10, вердикт §16): значения всё ещё не
    живут + стена IntelRCSetup (выяснилось: уже на v7, в раунде 8
    проверяли только наш пункт). Корень несохранения (stage-track
    §16.2): `add_form` с varstore вставляет декларацию в НАЧАЛО
    формс-пакета без $SPF ifr-фиксапа → ~34 записи LIVE разсогласованы
    с IFR (TODO.md; на e16p-форме без varstore не проявляется).
  - Раунд 11 готов: обход бага — **E39** `a49b8723…` (e16p-форма +
    страница + q600/q601 через add_question на сток varstore 1 @95/96
    + REF), гейт np3 с инвариантом IFR-согласованности записей
    (коммит `d62c86d`; pack §16); протокол: контроль E32 (IntelRCSetup
    — дискриминация стены) → E39. Открытое: источник v7-стены (записи
    там согласованы — не varstore-баг).
  - Раунд 11 вердикт (2026-09-10, §17): E32 чист — стена наша; E39:
    значения СОХРАНЯЮТСЯ (несохранение закрыто), стена осталась.
    Дубли qid — не крашер-класс (сток q600/q601 заняты, наши
    дублируют и E39 работает).
  - Раунд 12 готов: бисект **E40** `03412ad9…` = E32 + форма 10101
    (короткий title, без REF/страницы; in-slot рост) — форма/строка
    vs REF (pack §17).
  - Раунд 12 вердикт: E40 стенит → виновник ФОРМА (структура вставки
    идеальна; REF/qid/страница реабилитированы; стена с E16p — не
    проверялась).
  - Раунд 13 готов: **E41** `204ce5cc…` = E32 + форма **10041**
    (сток-диапазон id; дискриминация form_id-лимита vs
    форма-как-класс; pack §18).
  - Раунд 13 вердикт (§19): E41 стенит → форма-как-класс (id ни при
    чём; стена строго IntelRC-специфична, все прочие пункты работают;
    дифф E32→E41 строго 8 Б IFR + строка).
  - Раунд 13-бис готов: **E42** `4810f11e…` = E32 + форма 10041 +
    $SPF-страница (слот 188) — H1 «форма требует страницу» vs H2
    «форма токсична» (pack §19).
  - Раунд 13-бис вердикт (§20): E42 стенит → H2: любая новая форма
    Setup-формсета ломает IntelRCSetup-вход (страница не лечит; в
    E40/E41 $SPF не тронут — стена чисто IFR; парсер $SPF AMITSE не
    локализован, разбор паркнут).
  - Раунд 14 готов — ПИВОТ: **E43** `7e5d4bba…` = E32 + q600/q601
    add_question на МЁРТВУЮ пустую сток-форму **10009** (vs 1 @95/96)
    + REF q0x210 → 10009; ноль новых форм/страниц (гейт np4; pack
    §20) — IntelRC не затрагивается по построению; приёмка:
    IntelRCSetup + пункт + вопросы + живость значений.
  - Раунд 14 вердикт (2026-09-10): ПРИЁМКА ПРОЙДЕНА — «стены нет,
    наша форма есть, значения сохраняются» (вердикт §21, pack §21,
    спека §26). **Дуга setup-new-page ЗАКРЫТА положительно на E43
    `7e5d4bba…`** (эталонный образец). Хвосты (по решению владельца):
    E44 косметика заголовка 10009, наполнение страницы реальными
    serial-настройками.
  - Пост-дуга: фикс add_form×varstore выполнен (`c5a5c09`) — фиксап
    $SPF ifr-offset-ов по плану резолвящихся записей
    (threshold=точка вставки varstore, delta=её длина), план до
    мутаций; отказ при заблокированном $SPF; юнит-гейты
    (resource/bare/refusal) + инвариант раунда 11 на реальном образе
    в гейте np1.
Зависимости: движок + $SPF-аппенд S3/E31 (железо) + карта каналов
hijack-v2.1 (E24–E29).

## Дуга TUI Revival + Forms View (2026-09-11)

Возвращение TUI к статусу полноценного рабочего инструмента + полноэкранный
Forms View для работы с HII-формами. Решение владельца 2026-09-11: два цикла.
Revival — «максимум всего» (метки узлов, FIND/GO, tab-completion + общий парсер
флагов, engine name-lift через обёртки, prune Remove-узлов после flush,
регионы flash-дескриптора: полная таблица FLREG + ME/FTPR-парсер + read-only
индикация, `ImageUpload` RPC + `:upload`, снапшоты образа — именованные точки
отката, аддендум 2026-09-11: журнал pending-операций отменён — все мутации
уже write-through, персистентность правок обеспечена). Forms View — дуга малых ступеней
V1 просмотр → V2 правки на месте → V3 schema-операции добавления; полноэкранные
вкладки Tab/Shift-Tab (AMIBCP-канон «отдельный экран», TODO:151).

- **Цикл A — TUI Revival**: Spec
  `docs/superpowers/specs/2026-09-11-tui-revival-design.md`. Статус:
  реализован (2026-09-11, коммиты `b0eecac..55181c0` + закрывающий
  real-image-гейт-коммит Task 14): name-lift через обёртки, метки узлов TUI,
  flush re-parse (чистое дерево после мутаций), `:goto`, tab-completion +
  `uefi-common::cli`, регионы flash-дескриптора (FLREG + ME/$FPT, read-only,
  `Node.region`), `ImageUpload` RPC + `:upload`, снапшоты образа
  (`:snapshot`/`:snapshots`/`:restore`). Real-image гейты Task 14: 42/42
  `#[ignore]`-тестов зелёные. Остаточные миноры — TODO.md (раздел цикла).
- **Цикл B — TUI Forms View**: Spec (живая, ступени V1–V3 с гейтами на реальном
  образе): `docs/superpowers/specs/2026-09-11-tui-forms-view-design.md`.
  Статус: спека согласована; план отдельный на ступень, закрытие — вердиктами
  (аддендумы к спеке, паттерн serial-ladder). Статус: V1 исполнена (план
  `2026-09-11-tui-forms-v1.md`, ветка слита 2026-09-11) — вкладка Forms,
  вопросы через `HiiListQuestions`, strings-браузер с фильтром; real-image
  гейт engine-автоматизирован; TUI-гейт пройден владельцем 2026-09-11
  (вердикт-аддендум в спеке §7 — позитивный). Статус: V2 исполнена (план
  `2026-09-11-tui-forms-v2.md`, ветка `feat/tui-forms-v2`) — REF-дерево форм
  (дизайн §3.5, `HiiFormTree` + `T`-переключатель) + правки на месте
  (visibility/unlock/set-value — `:hii`-глаголы и `v`/`u`/Enter);
  real-image гейт engine-автоматизирован; гейт V2 ПРОЙДЕН владельцем
  2026-09-11 — вердикт-аддендум в спеке §8 (позитивный; паритет sha256
  TUI/CLI `5722e79c…` подтверждён). Ступень V2 ЗАКРЫТА; следующий шаг —
  план V3 (операции добавления, решение D5). Статус: V3 — исполнена
  (`docs/superpowers/plans/2026-09-11-tui-forms-v3.md`, 8 задач,
  PR #14 смержен 2026-09-12); гейт §9 пройден владельцем 2026-09-12 —
  вердикт-аддендум в спеке §9 (ПОЗИТИВНЫЙ: prefill `a`, form add из
  TUI на живом образе, ref-вложенность под 10018, completion; формальный
  sha256-прогон отдельно не гонялся — вердикт по живому прогону
  TUI+CLI). Ступень V3 ЗАКРЫТА — дуга V1–V3 закрыта: полный редактор
  форм в TUI. Отложенные улучшения — TODO.md (ref-UX «форма под
  формой», completion-меню, `--ffs`-порядок, высота help, write-UX).

Зависимости: циклы 1–2 (RPC-контракт, CLI-паритет); engine-добавки —
`HiiListQuestions` (B/V1), `ImageUpload` (A7). HII-мутационная база — дуги
hijack/setup-new-page.

## Дуга formset-unlock (U1–U4) — кросс-формсетные гейты и перенос IIO-бифуркации (2026-09-12)

Спека `docs/superpowers/specs/2026-09-12-formset-unlock-design.md` +
план `docs/superpowers/plans/2026-09-12-formset-unlock.md` (9 задач,
TDD). Источник — live-сессия 450x (TODO.md, раздел 2026-09-12):
формсет IntelRCSetup (EC87D643) скрыт suppress-гейтом вокруг
кросс-формсетного GOTO (REF3/REF4) в корневом Setup (899407d7), который
unlock не сканировал («0 gates»). Ключевой факт-фикс: REF-варианты —
один опкод 0x0F, различаются length (REF=15/REF2=17/REF3=33/REF4=35/
REF5=13-динамический). Ступени: U1 парсер `ref_variant` + кросс-гейты
`Wraps::CrossFormsetRef` + кросс-рёбра `FormEdge.target_formset_guid`;
U2a unlock по донорским секциям (`cross_formset.rs`, GateInfo.source_target)
и U2b emit REF3 (fallback-инжект GOTO из видимой формы); U3
`QuestionAddList.varstores` — декларация IntelSetup (IfrVarStoreEfi) в
чужом Setup-пакете при question add + сдвиг $SPF-записей; U4 live-гейт
`real_amibcp_450x_formset_unlock` + карта NVRAM-эффекта (спека §6).
Статус: спланировано (2026-09-12), к исполнению после engine-p0-hotfix
(закрыт PR #15). Гейт владельца — живой прогон TUI/CLI на 450x,
вердикт-аддендум в спеке §7.

## Связи между циклами

```
Цикл 1 (движок + uefi-common)
├── Цикл 2 (CLI) ──────── Цикл 3 (TUI)
├── Цикл 4 (session mgr, опц.)
├── Цикл 5 (WebUI) ────── Цикл 7 (grpc-шлюз, опц.)
└── Цикл 6 (расш. Setup)
```

Циклы 2 и 3 можно делать параллельно после цикла 1. Цикл 6 независим от 2/3/5.
